<?xml version="1.0" encoding="utf-8"?><Benchmark xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:cpe="http://cpe.mitre.org/language/2.0" xmlns:xhtml="http://www.w3.org/1999/xhtml" xmlns:dsig="http://www.w3.org/2000/09/xmldsig#" xsi:schemaLocation="http://checklists.nist.gov/xccdf/1.1 http://nvd.nist.gov/schema/xccdf-1.1.4.xsd" id="CIS_Google_Kubernetes_Engine_GKE_Benchmark" xml:lang="en" xmlns="http://checklists.nist.gov/xccdf/1.1">
  <status date="2026-06-30">accepted</status>
  <title>CIS Google Kubernetes Engine (GKE) Benchmark</title>
  <description>All CIS Benchmarks™ (Benchmarks) focus on technical configuration settings used to maintain and/or increase the security of the addressed technology, and they should be used in conjunction with other essential cyber hygiene tasks like: • Monitoring the base operating system and applications for vulnerabilities and quickly updating with the latest security patches. • End-point protection (Antivirus software, Endpoint Detection and Response (EDR), etc.). • Logging and monitoring user and system activity. In the end, the Benchmarks are designed to be a key component of a comprehensive cybersecurity program. Important Usage Information All Benchmarks are available free for non-commercial use from the CIS Website. They can be used to manually assess and remediate systems and applications. In lieu of manual assessment and remediation, there are several tools available to assist with assessment: • CIS Configuration Assessment Tool (CIS-CAT ® Pro Assessor) • CIS Benchmarks™ Certified 3rd Party Tooling These tools make the hardening process much more scalable for large numbers of systems and applications. NOTE: Some tooling focuses only on the Benchmark Recommendations that can be fully automated (skipping ones marked Manual). It is important that ALL Recommendations ( Automated and Manual) be addressed since all are important for properly securing systems and are typically in scope for audits. Key Stakeholders Cybersecurity is a collaborative effort, and cross functional cooperation is imperative within an organization to discuss, test, and deploy Benchmarks in an effective and efficient way. The Benchmarks are developed to be best practice configuration guidelines applicable to a wide range of use cases. In some organizations, exceptions to specific Recommendations will be needed, and this team should work to prioritize the problematic Recommendations based on several factors like risk, time, cost, and labor. These exceptions should be properly categorized and documented for auditing purposes. Internal Only - General Apply the Correct Version of a Benchmark Benchmarks are developed and tested for a specific set of products and versions and applying an incorrect Benchmark to a system can cause the resulting pass/fail score to be incorrect. This is due to the assessment of settings that do not apply to the target systems. To assure the correct Benchmark is being assessed: • Deploy the Benchmark applicable to the way settings are managed in the environment: An example of this is the Microsoft Windows family of Benchmarks, which have separate Benchmarks for Group Policy, Intune, and Stand-alone systems based upon how system management is deployed. Applying the wrong Benchmark in this case will give invalid results. • Use the most recent version of a Benchmark: This is true for all Benchmarks, but especially true for cloud technologies. Cloud technologies change frequently and using an older version of a Benchmark may have invalid methods for auditing and remediation. Exceptions The guidance items in the Benchmarks are called recommendations and not requirements, and exceptions to some of them are expected and acceptable. The Benchmarks strive to be a secure baseline, or starting point, for a specific technology, with known issues identified during Benchmark development are documented in the Impact section of each Recommendation. In addition, organizational, system specific requirements, or local site policy may require changes as well, or an exception to a Recommendation or group of Recommendations (e.g. A Benchmark could Recommend that a Web server not be installed on the system, but if a system's primary purpose is to function as a Webserver, there should be a documented exception to this Recommendation for that specific server). In the end, exceptions to some Benchmark Recommendations are common and acceptable, and should be handled as follows: • The reasons for the exception should be reviewed cross-functionally and be well documented for audit purposes. • A plan should be developed for mitigating, or eliminating, the exception in the future, if applicable. • If the organization decides to accept the risk of this exception (not work toward mitigation or elimination), this should be documented for audit purposes. It is the responsibility of the organization to determine their overall security policy, and which settings are applicable to their unique needs based on the overall risk profile for the organization. Internal Only - General Remediation CIS has developed Build Kits for many technologies to assist in the automation of hardening systems. Build Kits are designed to correspond to Benchmark's “Remediation” section, which provides the manual remediation steps necessary to make that Recommendation compliant to the Benchmark. When remediating systems (changing configuration settings on deployed systems as per the Benchmark's Recommendations), please approach this with caution and test thoroughly. The following is a reasonable remediation approach to follow: • CIS Build Kits, or internally developed remediation methods should never be applied to production systems without proper testing. • Proper testing consists of the following: o Understand the configuration (including installed applications) of the targeted systems. Various parts of the organization may need different configurations (e.g., software developers vs standard office workers). o Read the Impact section of the given Recommendation to help determine if there might be an issue with the targeted systems. o Test the configuration changes with representative lab system(s). If issues arise during testing, they can be resolved prior to deploying to any production systems. o When testing is complete, initially deploy to a small sub-set of production systems and monitor closely for issues. If there are issues, they can be resolved prior to deploying more broadly. o When the initial deployment above is completed successfully, iteratively deploy to additional systems and monitor closely for issues. Repeat this process until the full deployment is complete. Summary Using the Benchmarks Certified tools, working as a team with key stakeholders, being selective with exceptions, and being careful with remediation deployment, it is possible to harden large numbers of deployed systems in a cost effective, efficient, and safe manner. NOTE: As previously stated, the PDF versions of the CIS Benchmarks™ are available for free, non-commercial use on the CIS Website. All other formats of the CIS Benchmarks™ (MS Word, Excel, and Build Kits) are available for CIS SecureSuite ® members. CIS-CAT ® Pro is also available to CIS SecureSuite ® members. Internal Only - General Target Technology Details This document provides prescriptive guidance for running Google Kubernetes Engine (GKE) v1.34 &amp; 1.35 following recommended security controls. This benchmark only includes controls which can be modified by an end user of GKE. For information on GKE's performance against the Kubernetes CIS benchmarks, for items which cannot be audited or modified, see the GKE documentation at https://cloud.google.com/kubernetes-engine/docs/concepts/cis-benchmarks. For the latest GKE hardening guide, see g.co/gke/hardening. To obtain the latest version of this guide, please visit www.cisecurity.org. If you have questions, comments, or have identified ways to improve this guide, please write us at support@cisecurity.org.</description>
  <notice id="terms-of-use" xml:lang="en"/>
  <front-matter xml:lang="en"/>
  <rear-matter xml:lang="en"/>
  <reference href="https://www.cisecurity.org/cis-benchmarks">
    <dc:publisher>Center for Internet Security</dc:publisher>
    <dc:source>CIS Benchmarks</dc:source>
  </reference>
  <plain-text id="release-info">Release: 2.0.0 Benchmark Date: 2026-06-30</plain-text>
  <version>2.0.0</version>
  <Profile id="L1_Public">
    <title>Level 1</title>
    <description>CIS profile: Level 1</description>
    <select idref="V-4AEE11A9" selected="true"/>
    <select idref="V-2236C7CB" selected="true"/>
    <select idref="V-775EA6BF" selected="true"/>
    <select idref="V-B22BABA0" selected="true"/>
    <select idref="V-90C36B7B" selected="true"/>
    <select idref="V-C08E6B79" selected="true"/>
    <select idref="V-FBBECC08" selected="true"/>
    <select idref="V-09DDE66E" selected="true"/>
    <select idref="V-954DB3E7" selected="true"/>
    <select idref="V-6EAF0CCA" selected="true"/>
    <select idref="V-96FB423B" selected="true"/>
    <select idref="V-B0144FD3" selected="true"/>
    <select idref="V-F6929848" selected="true"/>
    <select idref="V-C7D55BC9" selected="true"/>
    <select idref="V-9510B75F" selected="true"/>
    <select idref="V-BAAA6363" selected="true"/>
    <select idref="V-2A1A0469" selected="true"/>
    <select idref="V-F84F6F7B" selected="true"/>
    <select idref="V-AB2871AB" selected="true"/>
    <select idref="V-096C94A4" selected="true"/>
    <select idref="V-CA2A21F5" selected="true"/>
    <select idref="V-0C3AAA74" selected="true"/>
    <select idref="V-3BC20EE1" selected="true"/>
    <select idref="V-F5A482A8" selected="true"/>
    <select idref="V-E058CF7D" selected="true"/>
    <select idref="V-A4486CFC" selected="true"/>
  </Profile>
  <Profile id="L1_Classified">
    <title>Level 1</title>
    <description>CIS profile: Level 1</description>
    <select idref="V-4AEE11A9" selected="true"/>
    <select idref="V-2236C7CB" selected="true"/>
    <select idref="V-775EA6BF" selected="true"/>
    <select idref="V-B22BABA0" selected="true"/>
    <select idref="V-90C36B7B" selected="true"/>
    <select idref="V-C08E6B79" selected="true"/>
    <select idref="V-FBBECC08" selected="true"/>
    <select idref="V-09DDE66E" selected="true"/>
    <select idref="V-954DB3E7" selected="true"/>
    <select idref="V-6EAF0CCA" selected="true"/>
    <select idref="V-96FB423B" selected="true"/>
    <select idref="V-B0144FD3" selected="true"/>
    <select idref="V-F6929848" selected="true"/>
    <select idref="V-C7D55BC9" selected="true"/>
    <select idref="V-9510B75F" selected="true"/>
    <select idref="V-BAAA6363" selected="true"/>
    <select idref="V-2A1A0469" selected="true"/>
    <select idref="V-F84F6F7B" selected="true"/>
    <select idref="V-AB2871AB" selected="true"/>
    <select idref="V-096C94A4" selected="true"/>
    <select idref="V-CA2A21F5" selected="true"/>
    <select idref="V-0C3AAA74" selected="true"/>
    <select idref="V-3BC20EE1" selected="true"/>
    <select idref="V-F5A482A8" selected="true"/>
    <select idref="V-E058CF7D" selected="true"/>
    <select idref="V-A4486CFC" selected="true"/>
  </Profile>
  <Profile id="L1_Sensitive">
    <title>Level 1</title>
    <description>CIS profile: Level 1</description>
    <select idref="V-4AEE11A9" selected="true"/>
    <select idref="V-2236C7CB" selected="true"/>
    <select idref="V-775EA6BF" selected="true"/>
    <select idref="V-B22BABA0" selected="true"/>
    <select idref="V-90C36B7B" selected="true"/>
    <select idref="V-C08E6B79" selected="true"/>
    <select idref="V-FBBECC08" selected="true"/>
    <select idref="V-09DDE66E" selected="true"/>
    <select idref="V-954DB3E7" selected="true"/>
    <select idref="V-6EAF0CCA" selected="true"/>
    <select idref="V-96FB423B" selected="true"/>
    <select idref="V-B0144FD3" selected="true"/>
    <select idref="V-F6929848" selected="true"/>
    <select idref="V-C7D55BC9" selected="true"/>
    <select idref="V-9510B75F" selected="true"/>
    <select idref="V-BAAA6363" selected="true"/>
    <select idref="V-2A1A0469" selected="true"/>
    <select idref="V-F84F6F7B" selected="true"/>
    <select idref="V-AB2871AB" selected="true"/>
    <select idref="V-096C94A4" selected="true"/>
    <select idref="V-CA2A21F5" selected="true"/>
    <select idref="V-0C3AAA74" selected="true"/>
    <select idref="V-3BC20EE1" selected="true"/>
    <select idref="V-F5A482A8" selected="true"/>
    <select idref="V-E058CF7D" selected="true"/>
    <select idref="V-A4486CFC" selected="true"/>
  </Profile>
  <Profile id="L2_Public">
    <title>Level 2</title>
    <description>CIS profile: Level 2</description>
    <select idref="V-277795C1" selected="true"/>
    <select idref="V-656B4469" selected="true"/>
    <select idref="V-291329CE" selected="true"/>
    <select idref="V-4A500600" selected="true"/>
    <select idref="V-2E7A1A7F" selected="true"/>
    <select idref="V-0FECC586" selected="true"/>
    <select idref="V-187073DA" selected="true"/>
    <select idref="V-114A3A66" selected="true"/>
    <select idref="V-517DB500" selected="true"/>
    <select idref="V-A9720197" selected="true"/>
    <select idref="V-BB9220D2" selected="true"/>
    <select idref="V-648406C9" selected="true"/>
    <select idref="V-3EB1A480" selected="true"/>
    <select idref="V-0F47DD1D" selected="true"/>
    <select idref="V-5D4C2465" selected="true"/>
    <select idref="V-497B228C" selected="true"/>
    <select idref="V-F9F27032" selected="true"/>
    <select idref="V-F15935D7" selected="true"/>
    <select idref="V-28A5CD83" selected="true"/>
    <select idref="V-4BF398D2" selected="true"/>
    <select idref="V-7B76E18B" selected="true"/>
    <select idref="V-63FCE561" selected="true"/>
    <select idref="V-C674AD2E" selected="true"/>
    <select idref="V-0983AB29" selected="true"/>
    <select idref="V-A37D1A4F" selected="true"/>
    <select idref="V-B53292EC" selected="true"/>
    <select idref="V-6B05630D" selected="true"/>
    <select idref="V-7F2B9802" selected="true"/>
  </Profile>
  <Profile id="L2_Classified">
    <title>Level 2</title>
    <description>CIS profile: Level 2</description>
    <select idref="V-277795C1" selected="true"/>
    <select idref="V-656B4469" selected="true"/>
    <select idref="V-291329CE" selected="true"/>
    <select idref="V-4A500600" selected="true"/>
    <select idref="V-2E7A1A7F" selected="true"/>
    <select idref="V-0FECC586" selected="true"/>
    <select idref="V-187073DA" selected="true"/>
    <select idref="V-114A3A66" selected="true"/>
    <select idref="V-517DB500" selected="true"/>
    <select idref="V-A9720197" selected="true"/>
    <select idref="V-BB9220D2" selected="true"/>
    <select idref="V-648406C9" selected="true"/>
    <select idref="V-3EB1A480" selected="true"/>
    <select idref="V-0F47DD1D" selected="true"/>
    <select idref="V-5D4C2465" selected="true"/>
    <select idref="V-497B228C" selected="true"/>
    <select idref="V-F9F27032" selected="true"/>
    <select idref="V-F15935D7" selected="true"/>
    <select idref="V-28A5CD83" selected="true"/>
    <select idref="V-4BF398D2" selected="true"/>
    <select idref="V-7B76E18B" selected="true"/>
    <select idref="V-63FCE561" selected="true"/>
    <select idref="V-C674AD2E" selected="true"/>
    <select idref="V-0983AB29" selected="true"/>
    <select idref="V-A37D1A4F" selected="true"/>
    <select idref="V-B53292EC" selected="true"/>
    <select idref="V-6B05630D" selected="true"/>
    <select idref="V-7F2B9802" selected="true"/>
  </Profile>
  <Profile id="L2_Sensitive">
    <title>Level 2</title>
    <description>CIS profile: Level 2</description>
    <select idref="V-277795C1" selected="true"/>
    <select idref="V-656B4469" selected="true"/>
    <select idref="V-291329CE" selected="true"/>
    <select idref="V-4A500600" selected="true"/>
    <select idref="V-2E7A1A7F" selected="true"/>
    <select idref="V-0FECC586" selected="true"/>
    <select idref="V-187073DA" selected="true"/>
    <select idref="V-114A3A66" selected="true"/>
    <select idref="V-517DB500" selected="true"/>
    <select idref="V-A9720197" selected="true"/>
    <select idref="V-BB9220D2" selected="true"/>
    <select idref="V-648406C9" selected="true"/>
    <select idref="V-3EB1A480" selected="true"/>
    <select idref="V-0F47DD1D" selected="true"/>
    <select idref="V-5D4C2465" selected="true"/>
    <select idref="V-497B228C" selected="true"/>
    <select idref="V-F9F27032" selected="true"/>
    <select idref="V-F15935D7" selected="true"/>
    <select idref="V-28A5CD83" selected="true"/>
    <select idref="V-4BF398D2" selected="true"/>
    <select idref="V-7B76E18B" selected="true"/>
    <select idref="V-63FCE561" selected="true"/>
    <select idref="V-C674AD2E" selected="true"/>
    <select idref="V-0983AB29" selected="true"/>
    <select idref="V-A37D1A4F" selected="true"/>
    <select idref="V-B53292EC" selected="true"/>
    <select idref="V-6B05630D" selected="true"/>
    <select idref="V-7F2B9802" selected="true"/>
  </Profile>
  <Group id="V-4AEE11A9">
    <title>3.1 Worker Node Configuration Files</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-4AEE11A9r01_rule" weight="10.0" severity="medium">
      <version>3.1.1</version>
      <title>Ensure that the kubeconfig file permissions are set to 644 or more restrictive</title>
      <description>&lt;VulnDiscussion&gt;If kubelet is running, and if it is configured by a kubeconfig file, ensure that the proxy kubeconfig file has permissions of 644 or more restrictive.

Rationale:
The kubelet kubeconfig file controls various parameters of the kubelet service in the worker node. You should restrict its file permissions to maintain the integrity of the file.
The file should be writable by only the administrators on the system.
It is possible to run kubelet with the kubeconfig parameters configured as a Kubernetes ConfigMap instead of a file. In this case, there is no proxy kubeconfig file.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>3.1.1</dc:identifier>
      </reference>
      <fixtext fixref="F-4AEE11A9r01_fix">Run the below command (based on the file location on your system) on the each worker node. For example, Internal Only - General
chmod 644 &lt;kubeconfig file&gt;</fixtext>
      <fix id="F-4AEE11A9r01_fix"/>
      <check system="C-4AEE11A9_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Method 1 Optional: Run the following command to enter debug mode where NODE_NAME and IMAGE are defined variables:
kubectl debug node/$NODE_NAME -it --image=$IMAGE --profile=general
Run the following command on each node to find the appropriate kubeconfig file:
ps -efww | grep kubelet | grep -- '--kubeconfig'
The command line output should show a path for kubeconfig similar to -- kubeconfig=/var/lib/kubelet/kubeconfig which is the location of the kubeconfig file.
Run this command to obtain the kubeconfig file permissions:
Internal Only - General
stat -c %a /var/lib/kubelet/kubeconfig (or stat -c %a
/host/var/lib/kubelet/kubeconfig if in debub mode)
The output of the above command gives you the kubeconfig file's permissions.
Verify that if a file is specified and it exists, the permissions are 644 or more restrictive.
Method 2 Create and Run a Privileged Pod.
You will need to run a pod that is privileged enough to access the host's file system.
This can be achieved by deploying a pod that uses the hostPath volume to mount the node's file system into the pod.
Here's an example of a simple pod definition that mounts the root of the host to /host within the pod:
apiVersion: v1
kind: Pod
metadata:
name: file-check
spec:
volumes:
- name: host-root
hostPath:
path: /
type: Directory
containers:
- name: nsenter
image: busybox
command: ["sleep", "3600"]
volumeMounts:
- name: host-root
mountPath: /host
securityContext:
privileged: true
Save this to a file (e.g., file-check-pod.yaml) and create the pod:
kubectl apply -f file-check-pod.yaml
Once the pod is running, you can exec into it to check file permissions on the node:
kubectl exec -it file-check -- sh
Now you are in a shell inside the pod, but you can access the node's file system through the /host directory and check the permission level of the file:
ls -l /host/var/lib/kubelet/kubeconfig
Verify that if a file is specified and it exists, the permissions are 644 or more restrictive.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-2236C7CB">
    <title>3.1 Worker Node Configuration Files</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-2236C7CBr01_rule" weight="10.0" severity="medium">
      <version>3.1.2</version>
      <title>Ensure that the kubelet kubeconfig file ownership is set to root:root</title>
      <description>&lt;VulnDiscussion&gt;If kubelet is running, ensure that the file ownership of its kubeconfig file is set to root:root.

Rationale:
The kubeconfig file for kubelet controls various parameters for the kubelet service in the worker node. You should set its file ownership to maintain the integrity of the file.
The file should be owned by root:root.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>3.1.2</dc:identifier>
      </reference>
      <fixtext fixref="F-2236C7CBr01_fix">Run the below command (based on the file location on your system) on each worker node.
For example,
chown root:root &lt;proxy kubeconfig file&gt;</fixtext>
      <fix id="F-2236C7CBr01_fix"/>
      <check system="C-2236C7CB_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Method 1 Optional: Run the following command to enter debug mode where NODE_NAME and IMAGE are defined variables:
kubectl debug node/$NODE_NAME -it --image=$IMAGE --profile=general
Run the following command on each node to find the appropriate kubeconfig file:
ps -efww | grep kubelet | grep -- '--kubeconfig'
The command line output should show a path for kubeconfig similar to -- kubeconfig=/var/lib/kubelet/kubeconfig which is the location of the kubeconfig file.
Run this command to obtain the kubeconfig file permissions:
stat -c %U:%G /var/lib/kubelet/kubeconfig (or stat -c %U:%G
/host/var/lib/kubelet/kubeconfig if in debub mode)
The output of the above command gives you the kubeconfig file's ownership. Verify that the ownership is set to root:root.
Method 2 Create and Run a Privileged Pod.
Internal Only - General You will need to run a pod that is privileged enough to access the host's file system.
This can be achieved by deploying a pod that uses the hostPath volume to mount the node's file system into the pod.
Here's an example of a simple pod definition that mounts the root of the host to /host within the pod:
apiVersion: v1
kind: Pod
metadata:
name: file-check
spec:
volumes:
- name: host-root
hostPath:
path: /
type: Directory
containers:
- name: nsenter
image: busybox
command: ["sleep", "3600"]
volumeMounts:
- name: host-root
mountPath: /host
securityContext:
privileged: true
Save this to a file (e.g., file-check-pod.yaml) and create the pod:
kubectl apply -f file-check-pod.yaml
Once the pod is running, you can exec into it to check file ownership on the node:
kubectl exec -it file-check -- sh
Now you are in a shell inside the pod, but you can access the node's file system through the /host directory and check the ownership of the file:
ls -l /host/var/lib/kubelet/kubeconfig
The output of the above command gives you the kubeconfig file's ownership. Verify that the ownership is set to root:root.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-775EA6BF">
    <title>3.1 Worker Node Configuration Files</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-775EA6BFr01_rule" weight="10.0" severity="medium">
      <version>3.1.3</version>
      <title>Ensure that the kubelet configuration file has permissions set to 644</title>
      <description>&lt;VulnDiscussion&gt;Ensure that if the kubelet refers to a configuration file with the --config argument, that file has permissions of 644 or more restrictive.

Rationale:
The kubelet reads various parameters, including security settings, from a config file specified by the --config argument. If this file exists, you should restrict its file permissions to maintain the integrity of the file. The file should be writable by only the administrators on the system.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>3.1.3</dc:identifier>
      </reference>
      <fixtext fixref="F-775EA6BFr01_fix">Run the following command (using the kubelet config file location):
chmod 644 &lt;kubelet_config_file&gt;</fixtext>
      <fix id="F-775EA6BFr01_fix"/>
      <check system="C-775EA6BF_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Method 1 Optional: Run the following command to enter debug mode where NODE_NAME and IMAGE are defined variables:
kubectl debug node/$NODE_NAME -it --image=$IMAGE --profile=general
Run the following command on each node to find the appropriate config file:
ps -efww | grep kubelet | grep -- '--config'
The command line output should show a path for config similar to --config /etc/kubernetes/kubelet-config.yaml which is the location of the config file.
Run this command to obtain the config file permissions:
stat -c %a /etc/kubernetes/kubelet-config.yaml (or stat -c %a
/host/etc/kubernetes/kubelet-config.yaml if in debub mode)
The output of the above command is the Kubelet config file's permissions. Verify that the permissions are 644 or more restrictive.
Method 2 Create and Run a Privileged Pod.
Internal Only - General You will need to run a pod that is privileged enough to access the host's file system.
This can be achieved by deploying a pod that uses the hostPath volume to mount the node's file system into the pod.
Here's an example of a simple pod definition that mounts the root of the host to /host within the pod:
apiVersion: v1
kind: Pod
metadata:
name: file-check
spec:
volumes:
- name: host-root
hostPath:
path: /
type: Directory
containers:
- name: nsenter
image: busybox
command: ["sleep", "3600"]
volumeMounts:
- name: host-root
mountPath: /host
securityContext:
privileged: true
Save this to a file (e.g., file-check-pod.yaml) and create the pod:
kubectl apply -f file-check-pod.yaml
Once the pod is running, you can exec into it to check file permissions on the node:
kubectl exec -it file-check -- sh
Now you are in a shell inside the pod, but you can access the node's file system through the /host directory and check the permission level of the file:
ls -l /host/etc/kubernetes/kubelet-config.yaml
Verify that if a file is specified and it exists, the permissions are 644 or more restrictive.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-B22BABA0">
    <title>3.1 Worker Node Configuration Files</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-B22BABA0r01_rule" weight="10.0" severity="medium">
      <version>3.1.4</version>
      <title>Ensure that the kubelet configuration file ownership is set to root:root</title>
      <description>&lt;VulnDiscussion&gt;Ensure that if the kubelet refers to a configuration file with the --config argument, that file is owned by root:root.

Rationale:
The kubelet reads various parameters, including security settings, from a config file specified by the --config argument. If this file is specified you should restrict its file permissions to maintain the integrity of the file. The file should be owned by root:root.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>3.1.4</dc:identifier>
      </reference>
      <fixtext fixref="F-B22BABA0r01_fix">Run the following command (using the config file location identified in the Audit step):
chown root:root &lt;kubelet_config_file&gt;</fixtext>
      <fix id="F-B22BABA0r01_fix"/>
      <check system="C-B22BABA0_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Method 1 Optional: Run the following command to enter debug mode where NODE_NAME and IMAGE are defined variables:
kubectl debug node/$NODE_NAME -it --image=$IMAGE --profile=general
Run the following command on each node to find the appropriate config file:
ps -efww | grep kubelet | grep -- '--config'
The command line output should show a path for config similar to --config /etc/kubernetes/kubelet-config.yaml which is the location of the config file.
Run this command to obtain the config file permissions:
stat -c %U:%G /etc/kubernetes/kubelet-config.yaml (or stat -c %U:%G
/host/etc/kubernetes/kubelet-config.yaml if in debub mode)
The output of the above command is the Kubelet config file's ownership. Verify that the ownership is set to root:root Method 2 Create and Run a Privileged Pod.
You will need to run a pod that is privileged enough to access the host's file system.
This can be achieved by deploying a pod that uses the hostPath volume to mount the node's file system into the pod.
Internal Only - General Here's an example of a simple pod definition that mounts the root of the host to /host within the pod:
apiVersion: v1
kind: Pod
metadata:
name: file-check
spec:
volumes:
- name: host-root
hostPath:
path: /
type: Directory
containers:
- name: nsenter
image: busybox
command: ["sleep", "3600"]
volumeMounts:
- name: host-root
mountPath: /host
securityContext:
privileged: true
Save this to a file (e.g., file-check-pod.yaml) and create the pod:
kubectl apply -f file-check-pod.yaml
Once the pod is running, you can exec into it to check file ownership on the node:
kubectl exec -it file-check -- sh
Now you are in a shell inside the pod, but you can access the node's file system through the /host directory and check the ownership of the file:
ls -l /etc/kubernetes/kubelet-config.yaml
The output of the above command gives you the file's ownership. Verify that the ownership is set to root:root.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-90C36B7B">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-90C36B7Br01_rule" weight="10.0" severity="medium">
      <version>4.1.1</version>
      <title>Ensure the cluster-admin ClusterRole is only used when required</title>
      <description>&lt;VulnDiscussion&gt;The RBAC cluster-admin ClusterRole grants full administrative authority across cluster resources and should be assigned only to approved users, groups, or service accounts with a documented operational need. GKE RBAC is intended to provide fine grained control over what users and workloads can do in the cluster.

Rationale:
In RBAC, GKE uses Roles, ClusterRoles, RoleBindings, and ClusterRoleBindings to grant permissions to authenticated subjects, including users, groups, and Kubernetes service accounts. RBAC permissions are additive, so there are no deny rules to offset excessive access once a broad role is bound. GKE guidance emphasizes least privilege and recommends granting only the minimum permissions required for a task.
The cluster-admin ClusterRole grants permission to do anything on any resource in the cluster. When assigned through a ClusterRoleBinding, it provides cluster wide administrative access across all namespaces. When assigned through a RoleBinding, the ClusterRole is limited to that namespace, but it still grants the broad permissions of cluster-admin within that namespace. Where broad access is not explicitly required, create a narrower Role or ClusterRole that grants only the required API groups, resources, and verbs.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.1</dc:identifier>
      </reference>
      <fixtext fixref="F-90C36B7Br01_fix">For each cluster-admin binding, replace the broad access with a least privilege Role or ClusterRole that grants only the required API groups, resources, and verbs. Bind the replacement role to the approved user, group, or service account first, validate access, and then remove the cluster-admin binding and use least privilege and avoiding direct bindings to cluster-admin where a specific role can meet the requirement.
• Identify all ClusterRoleBinding and RoleBinding objects that assign the cluster- admin ClusterRole, including both cluster wide and namespace level bindings
• Review each bound user, group, or service account to confirm whether full administrative access is still required and documented
• Replace cluster-admin access where needed with a least privilege Role or ClusterRole that grants only the required API groups, resources, and verbs
• Bind and validate the replacement permissions before removing the existing cluster-admin binding to avoid disrupting administrative, platform, or automation workflows
• Remove only confirmed bindings, and avoid modifying RBAC resources with the system: prefix unless explicitly required and validated because they support cluster functionality</fixtext>
      <fix id="F-90C36B7Br01_fix"/>
      <check system="C-90C36B7B_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review all Kubernetes RBAC bindings that assign the cluster-admin ClusterRole.
This should include ClusterRoleBinding objects, which grant access across the cluster, and RoleBinding objects, which can bind the cluster-admin ClusterRole within a specific namespace. GKE RBAC supports both binding types, and cluster-admin grants permission to do anything on any resource in the cluster.
List all ClusterRoleBinding subjects bound to cluster-admin:
Internal Only - General
kubectl get clusterrolebindings -o json \
| jq -r '.items[]
| select(.roleRef.kind=="ClusterRole" and .roleRef.name=="cluster-admin")
| .metadata.name as $binding
| .subjects[]?
| [$binding, .kind, .name, (.namespace // "-")]
| @tsv'
List all namespaced RoleBinding subjects that bind the cluster-admin ClusterRole:
kubectl get rolebindings -A -o json \
| jq -r '.items[]
| select(.roleRef.kind=="ClusterRole" and .roleRef.name=="cluster-admin")
| .metadata.namespace as $ns
| .metadata.name as $binding
| .subjects[]?
| [$ns, $binding, .kind, .name, (.namespace // "-")]
| @tsv'
Review each listed user, group, service account, and namespace to confirm that cluster-admin access is explicitly required.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-C08E6B79">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-C08E6B79r01_rule" weight="10.0" severity="medium">
      <version>4.1.2</version>
      <title>Ensure access to Secrets is minimized and granted only where required</title>
      <description>&lt;VulnDiscussion&gt;Kubernetes Secrets can contain sensitive application credentials, configuration values, certificates, and legacy service account tokens. Access to these secrets should be restricted to the smallest practical set of users, groups, service accounts, and workloads to reduce the risk of credential exposure, privilege escalation, and unauthorized access to cluster or external resources. GKE RBAC supports fine grained permissions over Kubernetes resources, and Google’s RBAC guidance emphasizes least privilege for users and workloads.

Rationale:
Inappropriate access to Secrets stored in the cluster can allow an attacker to retrieve credentials used by applications, automation, or workloads, and then use those credentials to access Kubernetes resources or external systems. RBAC permissions for Secrets should be granted only through explicit Roles or ClusterRoles that define the required API group, resource, and verbs, such as get, list, watch, create, update, or delete. Wildcards should be avoided because they can unintentionally grant broader Secret access than intended.
Where possible, workloads should use dedicated Kubernetes service accounts and least privilege bindings rather than shared or default service accounts. For access to Google Cloud Secret Manager or other Google Cloud APIs, Workload Identity Federation for GKE is preferred over storing long lived service account keys as Kubernetes Secrets, because it reduces reliance on static credentials stored inside the cluster.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.2</dc:identifier>
      </reference>
      <fixtext fixref="F-C08E6B79r01_fix">Remediate Secret access by replacing broad or unnecessary RBAC permissions with narrowly defined access that supports only the required workload, user, or operational function. The remediation process should focus on reducing get, list, and watch permissions on the core API secrets resource, especially where access is inherited through broad roles, wildcard rules, shared service accounts, or default service accounts. Changes should be validated before removal to avoid disrupting applications, controllers, operators, or required platform services.
• Remove unnecessary Secret access by updating or replacing RBAC roles that grant get, list, or watch on secrets, especially where the access is inherited through wildcard resources or wildcard verbs
• Replace broad Secret permissions with least privilege Roles or ClusterRoles that grant only the required API group, resource, and verb. Avoid list and watch when a subject only needs to read a specific Secret
• Bind Secret access only to approved users, groups, or dedicated Kubernetes service accounts. Avoid binding Secret access to the default service account because Pods that do not explicitly specify a service account can inherit that access
• Move sensitive external credentials out of Kubernetes Secrets and use Secret Manager with Workload Identity Federation for GKE Internal Only - General
• Validate application, controller, operator, and platform workflows before removing access, and avoid modifying required system: RBAC resources because they support normal cluster functionality</fixtext>
      <fix id="F-C08E6B79r01_fix"/>
      <check system="C-C08E6B79_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review RBAC permissions that grant read access to Secrets in GKE Standard clusters.
Audit should identify where get, list, or watch permissions on the core API secrets resource are defined, how those permissions are assigned through bindings, and which users, groups, or service accounts receive the access. The review should also account for broad default roles, wildcard rules, default groups, and shared or default service accounts, because these can unintentionally expose Secrets beyond the intended operational need. GKE RBAC emphasizes least privilege, avoiding wildcards, limiting permissions to the namespace level where possible, and regularly reviewing RBAC access.
• Review Role and ClusterRole objects that grant get, list, or watch access to the core API secrets resource
• Include wildcard rules such as resources: ["*"] or verbs: ["*"], because they can unintentionally grant Secret access
• Map Secret permissions to RoleBinding and ClusterRoleBinding objects to identify the actual users, groups, and service accounts with access
• Validate that each subject has a documented need for Secret access and that access is limited to the required namespace, resource, and verb
• Review broad roles such as cluster-admin, admin, and edit, default groups such as system:authenticated, and shared or default service accounts, but avoid modifying required system: RBAC resources unless explicitly validated</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-FBBECC08">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-FBBECC08r01_rule" weight="10.0" severity="medium">
      <version>4.1.3</version>
      <title>Minimize wildcard use in Roles and ClusterRoles</title>
      <description>&lt;VulnDiscussion&gt;Kubernetes Roles and ClusterRoles define what actions users, groups, and service accounts can perform against Kubernetes API resources. Wildcard permissions should be minimized because the * character applies broadly across API groups, resources, or verbs and can grant more access than intended. Roles should explicitly define only the API groups, resources, and verbs required for the subject’s function. GKE RBAC specifically advises avoiding wildcards and using least privilege role design.

Rationale:
The principle of least privilege requires granting only the permissions needed to perform a specific task. Using * in RBAC rules can unintentionally grant broad access across current and future Kubernetes API capabilities, including new API groups, resources, verbs, or custom resources introduced into the cluster. For example, using * in the verbs field grants all actions, including patch, update, delete, and deletecollection, instead of only the specific actions required.
In GKE Standard clusters, RBAC rules should be written with explicit API groups, resources, and verbs, and split into separate rules when different resources require different permissions. This prevents subjects from inheriting access to resources or actions they do not need and supports namespace level access control where possible.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.3</dc:identifier>
      </reference>
      <fixtext fixref="F-FBBECC08r01_fix">Replace wildcard permissions with explicit RBAC rules that define only the required API groups, resources, and verbs. Remediation should focus on preserving required access while removing broad * grants that could expand privileges as APIs, resources, or CRDs are added. Validate affected users, workloads, controllers, and automation before removing wildcard access. GKE RBAC guidance recommends least privilege role design and avoiding wildcard permissions.
• Replace verbs: ["*"] with only the required actions, such as get, list, watch, create, update, patch, or delete
• Replace resources: ["*"] with only the required Kubernetes resources or custom resources
• Replace apiGroups: ["*"] with the specific API groups required by the subject’s function
• Review related RoleBinding and ClusterRoleBinding objects to confirm the revised role is assigned only to approved users, groups, or service accounts
• Avoid modifying required system: RBAC resources unless explicitly required and tested Internal Only - General</fixtext>
      <fix id="F-FBBECC08r01_fix"/>
      <check system="C-FBBECC08_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review Kubernetes RBAC roles in GKE Standard clusters to identify wildcard permissions in Roles and ClusterRoles. The audit should check for * in apiGroups, resources, or verbs, because wildcard permissions can unintentionally grant access to current or future Kubernetes APIs, resources, custom resources, or actions. After identifying wildcard rules, review the related RoleBinding and ClusterRoleBinding objects to determine which users, groups, or service accounts receive the permissions.
GKE RBAC always recommends least privilege, avoiding wildcard permissions, and regularly reviewing access.
Internal Only - General Retrieve Roles and ClusterRoles across all namespaces and review them for wildcard use:
kubectl get roles,clusterroles -A -o json | jq -r '
.items[]
| select(any(.rules[]?;
((.apiGroups // []) | index("*")) or
((.resources // []) | index("*")) or
((.verbs // []) | index("*"))
))
| "\(.kind)\t\(.metadata.namespace // "cluster-wide")\t\(.metadata.name)"
'
• Determine whether each wildcard permission is required and documented
• Review verbs: ["*"], resources: ["*"], and apiGroups: ["*"] because each can grant broad or unintended access
• Pay special attention to broad roles such as cluster-admi n, admin, and edit
• Review bindings to default groups, shared service accounts, and default service accounts that may inherit wildcard access
• Avoid modifying required system: RBAC resources unless explicitly required and validated GKE Policy Controller also includes K8sProhibitRoleWildcardAccess, which can help audit or enforce restrictions on wildcard access in Roles and ClusterRoles</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-09DDE66E">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-09DDE66Er01_rule" weight="10.0" severity="medium">
      <version>4.1.4</version>
      <title>Ensure that default service accounts are not actively used</title>
      <description>&lt;VulnDiscussion&gt;The Kubernetes default service account should not be actively used by application workloads. Instead, each workload should use a dedicated Kubernetes service account so permissions can be explicitly assigned, audited, and reviewed. Create a Kubernetes service account for each workload and not use the default service account.

Rationale:
Kubernetes creates a default service account in each namespace, and Pods that do not specify serviceAccountName are automatically assigned that default service account. If RBAC permissions are bound to the default service account, any Pod in that namespace that omits an explicit service account can unintentionally inherit those permissions.
Where Kubernetes API access is required, create a dedicated Kubernetes service account for the workload and grant only the required RBAC permissions through a Role or ClusterRole binding. If the Pod does not need Kubernetes API access, set automountServiceAccountToken=false so a service account token is not automatically mounted into the Pod. The automountServiceAccountToken field defaults to true, and disable token when Pods do not need to communicate with the API server.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.4</dc:identifier>
      </reference>
      <fixtext fixref="F-09DDE66Er01_fix">Each workload that requires Kubernetes API access should have its own service account with narrowly defined permissions. Workloads that do not need Kubernetes API access should disable automatic token mounting to reduce unnecessary credential exposure. For Google Cloud API access, use Workload Identity Federation for GKE instead of relying on static credentials in Pods.
• Create dedicated Kubernetes service accounts for workloads that require Kubernetes API access
• Update workload manifests to specify serviceAccountName instead of relying on the namespace default service account
• Remove RoleBinding and ClusterRoleBinding assignments from the default service account that are not needed
• Set automountServiceAccountToken=false for Pods that do not need to communicate with the Kubernetes API server
• Configure the namespace default service account with automountServiceAccountToken: false and validate affected workloads</fixtext>
      <fix id="F-09DDE66Er01_fix"/>
      <check system="C-09DDE66E_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each namespace in the GKE Standard cluster to confirm that the Kubernetes default service account is not actively used by application workloads and does not have explicit RBAC permissions assigned to it. The audit process should identify workloads that omit serviceAccountName, workloads that explicitly use serviceAccountName:
default, and any RoleBinding or ClusterRoleBinding objects that bind permissions to the default service account. In addition, verify whether service account token mounting is disabled where Pods do not need Kubernetes API access, because automountServiceAccountToken defaults to true.
Internal Only - General
kubectl get serviceaccounts/default -o yaml
Review the output and confirm the following:
• The default service account in each namespace has no explicit RoleBinding or ClusterRoleBinding assignments unless there is a documented exception
• Application workloads specify a dedicated serviceAccountName instead of omitting the field or using serviceAccountName: default
• Pods that do not need Kubernetes API access set automountServiceAccountToken=false in the Pod spec
• The default service account is configured with automountServiceAccountToken: false where necessary to reduce accidental token mounting</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-954DB3E7">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-954DB3E7r01_rule" weight="10.0" severity="medium">
      <version>4.1.5</version>
      <title>Ensure service account tokens are mounted only when Pods require Kubernetes API access</title>
      <description>&lt;VulnDiscussion&gt;Service account tokens should be mounted in Pods only when the workload explicitly requires authenticated access to the Kubernetes API server. For workloads that do not need API server access, disable token mounting to reduce unnecessary credential exposure inside the Pod. automountServiceAccountToken controls whether Kubernetes injects a service account credential token into the Pod, and its default value is true.

Rationale:
Mounting service account tokens into Pods increases the impact of a Pod compromise because the token can be used to make authenticated requests to the Kubernetes API server using the permissions assigned to the Pod’s Kubernetes service account. This risk is higher when workloads use shared service accounts, the namespace default service account, or service accounts with broad RBAC permissions.
Disabling automatic token mounting for Pods that do not need Kubernetes API access removes an unnecessary credential from the workload runtime environment and supports least privilege. Workloads that do need API access should use a dedicated Kubernetes service account with narrowly assigned RBAC permissions.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.5</dc:identifier>
      </reference>
      <fixtext fixref="F-954DB3E7r01_fix">Remediate by disabling service account token mounting and assigning API access only to workloads that require it. Pods that do not need to communicate with the Kubernetes API server should set automountServiceAccountToken=false and workloads that do need API access should use a dedicated Kubernetes service account with narrowly assigned RBAC permissions. In addition, avoiding the default service account and using separate Kubernetes service accounts per workload is recommended.
• Set automountServiceAccountToken=false in Pod or workload specifications that do not require Kubernetes API access
• Create dedicated Kubernetes service accounts for workloads that require API access instead of relying on the namespace default service account
• Grant only the required RBAC permissions to those service accounts through least privilege Roles, ClusterRoles, RoleBindings, or ClusterRoleBindings
• Set automountServiceAccountToken=false on service accounts where token mounting should be disabled by default</fixtext>
      <fix id="F-954DB3E7r01_fix"/>
      <check system="C-954DB3E7_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review Kubernetes service accounts and Pods in each namespace to confirm that service account tokens are mounted only where the workload requires authenticated access to the Kubernetes API server. The audit should verify both the service account configuration and the Pod specification, because token mounting can be controlled at either level. The automountServiceAccountToken field defaults to true, so workloads that do not need Kubernetes API access should explicitly set it to false.
Review the service account configuration:
Internal Only - General
kubectl get serviceaccount $SERVICE_ACCOUNT -n $NAMESPACE -o yaml
Review the Pod configuration:
kubectl get pod $POD_NAME -n $NAMESPACE -o yaml
Expected output:
automountServiceAccountToken: false
• Confirm that Pods without Kubernetes API access requirements have automountServiceAccountToken: false
• Confirm that workloads requiring API access use a dedicated Kubernetes service account with least privilege RBAC permissions
• Review Pods using the namespace default service account, especially where automountServiceAccountToken is unset or true
• Validate exceptions for controllers, operators, jobs, or applications that require Kubernetes API server access</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-6EAF0CCA">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-6EAF0CCAr01_rule" weight="10.0" severity="medium">
      <version>4.1.6</version>
      <title>Avoid granting cluster admin level access through the system:masters group</title>
      <description>&lt;VulnDiscussion&gt;The special Kubernetes group system:masters should not be used to grant cluster admin level access to users, groups, or service accounts. In GKE, administrative access should be granted through explicit RBAC bindings and limited to the minimum permissions required. The system:masters group is a default Kubernetes group, and adding custom subjects to it gives those subjects access to do anything to any resource in the cluster.

Rationale:
The system:masters group is bound to the cluster-admin ClusterRole by default to support system functionality. Adding users, groups, or service accounts to this group grants unrestricted cluster administrator access across all namespaces and resources.
This bypasses normal least privilege role design and can make access harder to review, reduce, or replace with narrower RBAC controls.
Do not use system:masters for routine administrative access. Where full administrative access is truly required, use an explicit ClusterRoleBinding to the built in cluster- admin ClusterRole and document the operational need. For most use cases, create a narrower Role or ClusterRole that defines only the required API groups, resources, and verbs. Google also recommends avoiding default roles and groups, avoiding broad cluster-admin bindings, and applying least privilege.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.6</dc:identifier>
      </reference>
      <fixtext fixref="F-6EAF0CCAr01_fix">Remediate custom system:masters usage by replacing it with explicit, auditable RBAC assignments. Do not remove the default cluster-admin ClusterRoleBinding to system:masters, because it supports Kubernetes system functionality. Instead, remove custom paths that add users, groups, service accounts, or client certificates to system:masters, and grant required access through least privilege Roles, ClusterRoles, RoleBindings, or ClusterRoleBindings.
• Remove any custom RoleBinding or ClusterRoleBinding that references system:masters, after confirming it is not a required system binding
• Remove system:masters from identity provider mappings, certificate subject groups, or other external authentication sources used for cluster access
• Replace required administrative access with explicit RBAC bindings to cluster- admin only where full cluster access is justified
• For most users, groups, service accounts, and automation, create narrower roles that define only the required API groups, resources, and verbs</fixtext>
      <fix id="F-6EAF0CCAr01_fix"/>
      <check system="C-6EAF0CCA_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review RBAC bindings and identity sources to confirm that system:masters is not used to grant cluster admin level access to custom users, groups, service accounts, or client certificates. The default cluster-admin ClusterRoleBinding to system:masters is expected and should not be treated as a failure by itself. Any additional or custom use of system:masters should be reviewed because Kubernetes assigns the cluster-admin ClusterRole to this group by default, and adding subjects to it grants full access to every resource in the cluster.
Internal Only - General Review ClusterRoleBindings that reference system:masters:
kubectl get clusterrolebinding -o json \
| jq -r '.items[]
| select(.subjects[]? | .kind=="Group" and .name=="system:masters")
| "\(.metadata.name)\t\(.roleRef.kind)/\(.roleRef.name)"'
Expected output:
cluster-admin ClusterRole/cluster-admin
• Confirm that the only expected ClusterRoleBinding involving system:masters is the default cluster-admin binding
• Review RoleBindings and ClusterRoleBindings for any custom references to system:masters
• Review certificate, identity provider, and group mapping processes to confirm no custom user or workload identity is issued with system:masters group membership
• Confirm administrative access is granted through explicit RBAC bindings, not by placing subjects in system:masters Note: Do not delete or modify required system: RBAC resources unless explicitly validated, because they support cluster functionality</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-96FB423B">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-96FB423Br01_rule" weight="10.0" severity="medium">
      <version>4.1.7</version>
      <title>Limit use of the Bind, Impersonate and Escalate permissions in the Kubernetes cluster</title>
      <description>&lt;VulnDiscussion&gt;Roles and ClusterRoles with bind, escalate, or impersonate permissions should be granted only to tightly controlled administrative or platform identities with a documented need. These permissions can allow a subject to expand effective access beyond normal RBAC assignments and should not be included in general user, workload, or automation roles. RBAC permissions are defined in Role and ClusterRole rules and assigned through RoleBinding and ClusterRoleBinding objects.

Rationale:
The bind and escalate permissions are high risk because they can allow a subject to create or update RBAC objects in ways that grant permissions they would not otherwise hold. escalate allows creation or update of roles with permissions beyond the caller’s current access, while bind allows creation or update of role bindings that reference roles with permissions beyond the caller’s current access. These permissions can allow a subject to grant any permission to any role and should be avoided unless explicitly required.
The impersonate permission is also sensitive because it can allow a subject to act as another user, group, or service account for Kubernetes API requests. When combined with broad target identities or privileged RBAC bindings, impersonation can provide an escalation path to cluster admin level access. These verbs should be limited to controlled administrative workflows, troubleshooting use cases, or approved automation with narrow scope. RBAC role design should follow least privilege and account for escalation risks such as bind and escalate.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.7</dc:identifier>
      </reference>
      <fixtext fixref="F-96FB423Br01_fix">Replace bind, escalate, and impersonate access with narrowly defined RBAC permissions that support only the approved administrative, platform, or automation function. Remove these verbs from general user, workload, and service account roles unless there is a valid requirement. Review changes before removal to avoid disrupting controllers, operators, CI/CD workflows, or access review processes.
• Remove bind, escalate, and impersonate from Roles and ClusterRoles where they are not explicitly required
• Replace broad access with least privilege rules that define the exact API groups, resources, verbs, and namespace scope required
• Avoid granting RBAC modification permissions such as bind, escalate, create, update, or patch on rbac.authorization.k8s.io resources to service accounts unless specifically justified
• Move required elevated access to tightly controlled administrative or platform identities, not workload or default service accounts
• Validate impacted workflows and avoid modifying required system: RBAC resources unless the change is explicitly required and tested Internal Only - General</fixtext>
      <fix id="F-96FB423Br01_fix"/>
      <check system="C-96FB423B_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review Role and ClusterRole objects that grant bind, escalate, or impersonate, then trace those roles through RoleBinding and ClusterRoleBinding objects to identify the users, groups, and service accounts that receive the permissions. These verbs should be limited to documented administrative or platform use cases because bind and escalate can grant permissions beyond the caller’s current access, while impersonate can allow API requests as another subject. RBAC permissions are additive, so unnecessary access should be identified and removed.
kubectl get roles,clusterroles -A -o json \
| jq -r '
.items[]
| select(any(.rules[]?.verbs[]?;
.=="bind" or .=="escalate" or .=="impersonate"
))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name]
| @tsv'
• Review each matching Role or ClusterRole to identify which sensitive verb is granted and on which API group and resource
• Map each matching role to its RoleBinding or ClusterRoleBinding to identify the bound users, groups, and service accounts
• Confirm that each subject has a documented need for bind, escalate, or impersonate
• Note service accounts, CI/CD identities, automation, and broad groups because these permissions can create privilege escalation paths</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-277795C1">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-277795C1r01_rule" weight="10.0" severity="high">
      <version>4.1.8</version>
      <title>Avoid binding RBAC roles to unauthenticated users and groups</title>
      <description>&lt;VulnDiscussion&gt;Avoid RoleBinding or ClusterRoleBinding objects that grant permissions to the system:anonymous user or the related system:unauthenticated group. These subjects represent unauthenticated Kubernetes API requests and should not receive RBAC access to cluster resources.

Rationale:
Kubernetes uses system:anonymous for requests that do not provide authentication credentials, and unauthenticated requests can also be associated with the system:unauthenticated group. Binding RBAC roles to either subject can grant unauthenticated callers access to Kubernetes API resources. This is especially risky if the binding includes broad roles, wildcard permissions, Secret access, workload access, or namespace wide permissions.
GKE can limit anonymous authentication so unauthenticated requests are rejected for non health check endpoints before RBAC evaluation. Even with that protection available, RBAC bindings to system:anonymous or system:unauthenticated should be avoided because they create unnecessary exposure and undermine least privilege access.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.8</dc:identifier>
      </reference>
      <fixtext fixref="F-277795C1r01_fix">Remediate any RoleBinding or ClusterRoleBinding returned by the audit by removing unauthenticated access and replacing it with authenticated, least privilege RBAC access. Bindings to system:anonymous or system:unauthenticated should not be used for normal cluster operations because they can grant Kubernetes API permissions to unauthenticated requests.
• Review each returned binding and identify the referenced Role or ClusterRole, including the API groups, resources, and verbs granted
• Delete unnecessary bindings to system:anonymous or system:unauthenticated after confirming they are not required for cluster operation.
kubectl delete clusterrolebinding &lt;clusterrolebinding-name&gt;
kubectl delete rolebinding &lt;rolebinding-name&gt; -n $NAMESPACE
• Replace any required access with authenticated users, groups, or dedicated Kubernetes service accounts using least privilege Role or ClusterRole permissions Internal Only - General
• Do not replace unauthenticated access with broad roles, wildcard permissions, Secret access, or namespace wide permissions unless explicitly required and documented</fixtext>
      <fix id="F-277795C1r01_fix"/>
      <check system="C-277795C1_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review all RoleBinding and ClusterRoleBinding objects to confirm that RBAC permissions are not assigned to the system:anonymous and system:unauthenticated. This subject represents unauthenticated Kubernetes API requests, and any binding to it can expose cluster resources without authenticated access. Include both cluster wide and namespace level bindings, then review any matching role reference to determine the exact permissions granted. Kubernetes RBAC is enabled by default in GKE and controls access through Role, ClusterRole, RoleBinding, and ClusterRoleBinding objects.
Check for bindings to system:anonymous:
Internal Only - General
kubectl get rolebindings,clusterrolebindings -A -o json \
| jq -r '.items[]
| select(any(.subjects[]?; .kind=="User" and .name=="system:anonymous"))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name,
.roleRef.name]
| @tsv'
Check for bindings to system:unauthenticated:
kubectl get rolebindings,clusterrolebindings -A -o json \
| jq -r '.items[]
| select(any(.subjects[]?; .kind=="Group" and
.name=="system:unauthenticated"))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name,
.roleRef.name]
| @tsv'
Expected output: No bindings should be returned.
• Review any result as a potential finding unless there is a documented and validated exception
• Review the referenced Role or ClusterRole to identify the API groups, resources, and verbs granted
• Confirm that no broad roles, wildcard permissions, Secret access, workload access, or namespace wide permissions are assigned to unauthenticated subjects
• Validate that required cluster operations use authenticated users, groups, or service accounts instead of unauthenticated access</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-B0144FD3">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-B0144FD3r01_rule" weight="10.0" severity="medium">
      <version>4.1.9</version>
      <title>Avoid binding RBAC roles to all authenticated users</title>
      <description>&lt;VulnDiscussion&gt;Avoid non-default RoleBinding and ClusterRoleBinding objects that grant RBAC permissions to the system:authenticated group. This group represents all authenticated Kubernetes API requests from users signed in with a Google Account, including Gmail accounts, so it should not be used for custom access grants. The default system ClusterRoleBindings system:basic-user, system:discovery, and system:public-info-viewer are expected and should not be treated as findings by themselves.

Rationale:
Binding a Role or ClusterRole to system:authenticated grants the referenced permissions to a very broad set of authenticated identities instead of a specific user, group, or service account. This can unintentionally expose cluster resources to any authenticated Google Account, depending on the role being bound. RBAC permissions are additive, and access should be granted through specific subjects with only the API groups, resources, and verbs required for the task.
Non-default bindings to system:authenticated are especially risky when they include broad roles, wildcard permissions, Secret access, workload access, or namespace wide access. Where access is required, bind permissions to a defined Google Group, IAM service account, Kubernetes service account, or named user instead of the global system:authenticated group.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.1.9</dc:identifier>
      </reference>
      <fixtext fixref="F-B0144FD3r01_fix">Remove any non-default binding to system:authenticated identified in the audit and replace it with explicit, least privilege access for specific authenticated subjects. Do not remove the expected default ClusterRoleBindings system:basic-user, system:discovery, or system:public-info-viewer. RBAC permissions should be granted through defined users, groups, or service accounts rather than the broad system:authenticated group.
• Review each non-default RoleBinding or ClusterRoleBinding returned by the audit and identify the referenced Role or ClusterRole
• Determine the specific API groups, resources, verbs, and namespace scope required for the affected users, tools, or automation
• Create or update a least privilege Role or ClusterRole that grants only the required access
• Bind the replacement role to a specific user, Google Group, IAM service account, or Kubernetes service account
• Delete the unsafe non-default binding to system:authenticated only after replacement access is validated
kubectl delete clusterrolebinding &lt;clusterrolebinding-name&gt;
kubectl delete rolebinding &lt;rolebinding-name&gt; -n $NAMESPACE</fixtext>
      <fix id="F-B0144FD3r01_fix"/>
      <check system="C-B0144FD3_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review all RoleBinding and ClusterRoleBinding objects to confirm that RBAC permissions are not assigned to the system:authenticated group outside of expected default system bindings. This group represents all authenticated Kubernetes API requests, so custom bindings can grant access to a broader audience than intended.
RBAC permissions should be assigned to specific users, groups, or service accounts using least privilege.
Check for bindings to system:authenticated:
kubectl get rolebindings,clusterrolebindings -A -o json \
| jq -r '
["KIND","NAMESPACE","BINDING","ROLE"],
(
.items[]
| select(any(.subjects[]?;
.kind=="Group" and .name=="system:authenticated"
))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name,
.roleRef.name]
)
| @tsv'
Expected output should be limited to the default ClusterRoleBindings below:
KIND NAMESPACE BINDING ROLE
ClusterRoleBinding cluster-wide system:basic-user system:basic-
user
ClusterRoleBinding cluster-wide system:discovery
system:discovery
ClusterRoleBinding cluster-wide system:public-info-viewer
system:public-info-viewer
• Confirm that no non-default ClusterRoleBinding objects reference system:authenticated
• Confirm that no namespace level RoleBinding objects reference system:authenticated
• Review any non-default result to identify the referenced Role or ClusterRole, including API groups, resources, and verbs granted
• Flag broad roles, wildcard permissions, Secret access, workload access, or namespace wide permissions as high risk Do not delete default system RBAC resources such as system:basic-user, system:discovery, or system:public-info-viewer, because they support normal cluster functionality Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-F6929848">
    <title>4.2 Pod Security Standards</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-F6929848r01_rule" weight="10.0" severity="medium">
      <version>4.2.1</version>
      <title>Enforce baseline or restricted Pod Security Standards on workload namespaces</title>
      <description>&lt;VulnDiscussion&gt;Pod Security Standards provide predefined pod security profiles that can be applied to GKE Standard workload namespaces through the built in PodSecurity admission controller. At minimum, workload namespaces should enforce the baseline profile, with restricted used where stronger pod hardening is required.

Rationale:
Without enforce mode applied to workload namespaces, GKE can allow pods that violate the intended Pod Security Standard to be admitted. The baseline profile helps prevent known privilege escalation patterns, while the restricted profile adds stronger controls such as requiring allowPrivilegeEscalation=false, dropping all Linux capabilities, running as non root, and using an allowed seccomp profile. warn and audit are useful for rollout and visibility, but they do not block noncompliant pods.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.2.1</dc:identifier>
      </reference>
      <fixtext fixref="F-F6929848r01_fix">Update workload namespaces so Pod Security enforcement is applied consistently through namespace labels. Use restricted where workloads can support it, and use baseline where compatibility requires a less restrictive profile.
• Identify all namespaces that host user workloads and confirm each one uses pod-security.kubernetes.io/enforce=baseline or pod- security.kubernetes.io/enforce=restricted.
• Test the selected profile before enforcement using server side dry run, especially when moving from no enforcement to restricted.
• Remediate violating workload specs by updating security contexts, privilege settings, host namespace usage, volume types, and Linux capability settings.
kubectl edit deployment/&lt;deployment-name&gt; -n $NAMESPACE
kubectl rollout restart deployment/&lt;deployment-name&gt; -n $NAMESPACE
• Apply the enforcement label only after violations are resolved or formally approved as exceptions.
kubectl label --overwrite namespace $NAMESPACE \
pod-security.kubernetes.io/enforce=baseline
Or, for stricter enforcement:
kubectl label --overwrite namespace $NAMESPACE \
pod-security.kubernetes.io/enforce=restricted
• Do not rely on warn or audit labels alone because they report violations but do not block noncompliant pod creation.
Internal Only - General After remediation, update namespace provisioning standards so new workload namespaces receive the appropriate Pod Security enforcement label at creation.
Periodically recheck namespace labels to confirm that enforcement remains in place as teams, applications, and environments change.</fixtext>
      <fix id="F-F6929848r01_fix"/>
      <check system="C-F6929848_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Run the following command to list workload namespaces that do not enforce either the baseline or restricted Pod Security Standard. The restricted profile satisfies this control because it is more restrictive than baseline. PodSecurity is applied to namespaces with labels, and enforce mode rejects violating pod creation while audit and warn do not block pods.
kubectl get namespace -o json | jq -r
Or to filter run:
Internal Only - General
kubectl get namespace -o json | jq -r '
["NAMESPACE","ENFORCE"],
(
.items[]
| select(.metadata.name | test("^(kube-system|kube-public|kube-node-
lease)$") | not)
| [
.metadata.name,
(.metadata.labels["pod-security.kubernetes.io/enforce"] // "missing")
]
| select(.[1] != "baseline" and .[1] != "restricted")
)
| @tsv
'
Review any namespace returned by the command and confirm whether it contains user workloads. For GKE versions where policies applied to kube-system are ignored, do not use kube-system as the enforcement target for this control.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-C7D55BC9">
    <title>4.3 Network Policies and NetworkPolicy Enforcement</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-C7D55BC9r01_rule" weight="10.0" severity="medium">
      <version>4.3.1</version>
      <title>Enable NetworkPolicy enforcement with GKE Dataplane V2 or Calico</title>
      <description>&lt;VulnDiscussion&gt;NetworkPolicy enforcement should be enabled for GKE clusters by using a supported enforcement mechanism, either GKE Dataplane V2 or the Calico network policy plugin.
Without a supported enforcement mechanism, Kubernetes NetworkPolicy objects cannot reliably restrict pod to pod, pod to service, ingress, or egress traffic.

Rationale:
NetworkPolicies define pod level traffic rules, but enforcement depends on the networking implementation used by the cluster. In GKE, the GKE Dataplane V2 is the recommended network plugin and uses Cilium and eBPF for routing, load balancing, and NetworkPolicy enforcement. Calico is also available for Standard clusters and enforces NetworkPolicy through the cluster network plugin.
By default, pods in a cluster can communicate freely. Enabling NetworkPolicy enforcement allows teams to implement defense in depth, namespace based segmentation, tenant isolation, and explicit ingress and egress controls for approved workload communication paths.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.3.1</dc:identifier>
      </reference>
      <fixtext fixref="F-C7D55BC9r01_fix">For noncompliant GKE Standard clusters, update the cluster build standard so a supported NetworkPolicy enforcement option is selected during cluster creation. Use GKE Dataplane V2 where possible, or Calico based NetworkPolicy enforcement where Dataplane V2 is not used. GKE Dataplane V2 is selected only during cluster creation, and existing clusters cannot be upgraded to use it.
• Select GKE Dataplane V2 when creating new or replacement GKE Standard clusters, because NetworkPolicy enforcement is built into Dataplane V2.
For existing legacy datapath clusters that cannot be recreated immediately, enable Calico based NetworkPolicy enforcement as an interim remediation path by enabling both the NetworkPolicy add-on and node enforcement. Enabling only the add-on does not enforce policies, and enabling node enforcement recreates node pools, so plan the change during an approved maintenance window.
Internal Only - General
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--update-addons=NetworkPolicy=ENABLED
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-network-policy
After remediation, validate that the cluster either reports ADVANCED_DATAPATH or reports networkPolicy.enabled=True, networkPolicy.provider=CALICO, and addonsConfig.networkPolicyConfig.disabled=False.</fixtext>
      <fix id="F-C7D55BC9r01_fix"/>
      <check system="C-C7D55BC9_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard cluster configuration and verify that NetworkPolicy enforcement is available through GKE Dataplane V2 or Calico. NetworkPolicy requires a network plugin to implement enforcement. GKE Dataplane V2 is the recommended option, and Calico is available for Standard clusters. If no managed or self managed plugin is configured, NetworkPolicy objects are not enforced.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME\
--location $LOCATION \
--project "$PROJECT_ID" \
--format="table(
name,
networkConfig.datapathProvider,
networkPolicy.enabled,
networkPolicy.provider,
addonsConfig.networkPolicyConfig.disabled
)"
• Expected output: The cluster satisfies this control if networkConfig.datapathProvider is ADVANCED_DATAPATH, because Kubernetes NetworkPolicy is always on for clusters that use GKE Dataplane V2.
NAME DATAPATH_PROVIDER ENABLED PROVIDER DISABLED
cluster-1 ADVANCED_DATAPATH True
If the cluster uses the legacy datapath, verify that networkPolicy.enabled is true, networkPolicy.provider is CALICO, and addonsConfig.networkPolicyConfig.disabled is false. Both the NetworkPolicy add-on and node enforcement are required for policies to function.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-656B4469">
    <title>4.3 Network Policies and NetworkPolicy Enforcement</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-656B4469r01_rule" weight="10.0" severity="high">
      <version>4.3.2</version>
      <title>Define default deny NetworkPolicies for each workload namespace</title>
      <description>&lt;VulnDiscussion&gt;Use NetworkPolicy objects to define default deny traffic controls for each GKE workload namespace, then add explicit allow rules for approved pod, service, namespace, ingress, and egress communication paths. GKE uses NetworkPolicy enforcement to create pod level firewall rules that control which Pods and Services can communicate inside the cluster.

Rationale:
Pods in a GKE cluster can communicate freely with each other, which increases the risk that a compromised workload can reach unrelated applications or services in the same cluster. Default deny NetworkPolicies help establish namespace level segmentation by denying traffic unless a policy explicitly allows it. This supports defense in depth, multi- tenancy, and stronger separation between applications, environments, and teams.
NetworkPolicy enforcement must also be enabled through a supported GKE mechanism, such as GKE Dataplane V2 or Calico for Standard clusters. If no enforcing network plugin is configured, NetworkPolicy objects are not enforced, which means defined policies might not restrict traffic.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.3.2</dc:identifier>
      </reference>
      <fixtext fixref="F-656B4469r01_fix">For noncompliant GKE Standard clusters, update the cluster build standard so a supported NetworkPolicy enforcement option is selected during cluster creation. Use GKE Dataplane V2 where possible, or Calico based NetworkPolicy enforcement where Dataplane V2 is not used. GKE Dataplane V2 is selected only during cluster creation, and existing clusters cannot be upgraded to use it.
• Select GKE Dataplane V2 when creating new or replacement GKE Standard clusters, because NetworkPolicy enforcement is built into Dataplane V2.
• For legacy datapath clusters, enable Calico based NetworkPolicy enforcement by enabling both the NetworkPolicy add-on and node enforcement.
Internal Only - General
• Plan remediation during an approved maintenance window because enabling Calico based enforcement on existing legacy datapath clusters recreates nodes and can disrupt running workloads.
• Confirm that workload NetworkPolicy objects are defined after enforcement is enabled, because enabling enforcement alone does not create traffic restrictions.
• Validate the final configuration by confirming either ADVANCED_DATAPATH or networkPolicy.enabled=True, networkPolicy.provider=CALICO, and addonsConfig.networkPolicyConfig.disabled=False.
For legacy datapath clusters that use Calico, enable both required components:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--update-addons=NetworkPolicy=ENABLED
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-network-policy
Both the NetworkPolicy add-on and node enforcement are required for Calico based NetworkPolicy enforcement to function. For clusters already using ADVANCED_DATAPATH, do not run the Calico enablement commands because NetworkPolicy enforcement is built into GKE Dataplane V2.</fixtext>
      <fix id="F-656B4469r01_fix"/>
      <check system="C-656B4469_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard cluster configuration from both supported enforcement paths, GKE Dataplane V2 and Calico on legacy datapath. NetworkPolicy objects are enforced only when the cluster has a supported network plugin. If no managed or self managed plugin is configured, NetworkPolicy objects are not enforced.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format="table(
name,
networkConfig.datapathProvider,
networkPolicy.enabled,
networkPolicy.provider,
addonsConfig.networkPolicyConfig.disabled
)"
Dataplane V2 audit path:
The cluster satisfies the enforcement requirement through GKE Dataplane V2 when networkConfig.datapathProvider is ADVANCED_DATAPATH. In this configuration, NetworkPolicy enforcement is built in, and the separate Kubernetes NetworkPolicy add- on is not enabled or managed separately. A value of addonsConfig.networkPolicyConfig.disabled=True is acceptable for this path.
NAME DATAPATH_PROVIDER ENABLED PROVIDER DISABLED
cluster-1 ADVANCED_DATAPATH True
Calico audit path:
If the cluster uses the legacy datapath, confirm that Calico based NetworkPolicy enforcement is enabled. The cluster satisfies the enforcement requirement when networkConfig.datapathProvider is LEGACY_DATAPATH, networkPolicy.enabled is True, networkPolicy.provider is CALICO, and addonsConfig.networkPolicyConfig.disabled is False. Both the NetworkPolicy add-on and node enforcement are required for NetworkPolicies to function.
NAME DATAPATH_PROVIDER ENABLED PROVIDER DISABLED
cluster-1 LEGACY_DATAPATH True CALICO False
• Any other combination should be reviewed as noncompliant or incomplete, especially legacy datapath clusters with missing or false NetworkPolicy settings.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-291329CE">
    <title>4.4 Secrets Management and Workload Secret Access</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-291329CEr01_rule" weight="10.0" severity="high">
      <version>4.4.1</version>
      <title>Prefer using secrets as files over secrets as environment variables</title>
      <description>&lt;VulnDiscussion&gt;GKE workloads should avoid consuming secrets as environment variables. Prefer accessing secrets directly from Secret Manager by using Workload Identity Federation for GKE and client libraries, or mount secrets as files by using the Secret Manager add- on when file based access is required.

Rationale:
Secrets exposed as environment variables can be unintentionally disclosed through application logs, debug output, crash reports, process inspection, or overly broad troubleshooting access. Direct Secret Manager API access reduces secret exposure inside the cluster and avoids long lived service account keys when paired with Workload Identity Federation for GKE. Mounted files are still preferable to environment variables when the application cannot call the Secret Manager API directly, but file permissions, pod access, and IAM bindings must be tightly controlled.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.4.1</dc:identifier>
      </reference>
      <fixtext fixref="F-291329CEr01_fix">Update workloads to avoid passing secrets through environment variables. Prefer direct Secret Manager API access through Workload Identity Federation for GKE and Google Cloud client libraries. This keeps sensitive data outside cluster storage, avoids long lived service account keys, and uses IAM rather than in cluster RBAC for Secret Manager access.
• Modify application code to read secrets from Secret Manager API where possible.
• Use a dedicated Kubernetes ServiceAccount for each workload identity that needs Secret Manager access.
• Grant only the required Secret Manager IAM permissions to that workload identity.
• When code cannot call the API directly, use the Secret Manager add-on to mount secrets as files in the Pod file system.
• Remove secretKeyRef and secretRef environment variable usage from workload manifests after the new access pattern is validated.
For mounted file access, enable the Secret Manager add-on and define a SecretProviderClass that maps specific Secret Manager secrets to Pod volumes.
The add-on lets applications access Secret Manager secrets as mounted volumes and supports centralized Secret Manager capabilities such as fine grained access control, rotation, lifecycle management, and audit logs.</fixtext>
      <fix id="F-291329CEr01_fix"/>
      <check system="C-291329CE_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review workload manifests and running Pods to identify containers that consume Kubernetes Secrets through environment variables. The audit should check both env.valueFrom.secretKeyRef and envFrom.secretRef, including regular containers and init containers. Secrets should be accessed through Secret Manager API with Workload Identity Federation for GKE where possible, or through mounted files using the Secret Manager add-on when file based access is required.
Internal Only - General
echo -e "KIND\tNAMESPACE\tNAME\tCONTAINER\tSECRET_ENV_TYPE"
kubectl get deploy,sts,ds,job,cronjob,pod -A -o json | jq -r '
.items[] as $i
| ($i.spec.template.spec // $i.spec.jobTemplate.spec.template.spec //
$i.spec) as $s
| (($s.containers // []) + ($s.initContainers // []))[]
| select(any(.env[]?; .valueFrom.secretKeyRef) or any(.envFrom[]?;
.secretRef))
| [
$i.kind,
$i.metadata.namespace,
$i.metadata.name,
.name,
if any(.env[]?; .valueFrom.secretKeyRef) then "secretKeyRef" else
"secretRef" end
]
| @tsv
'
Sample output would look like this:
KIND NAMESPACE NAME CONTAINER
SECRET_ENV_TYPE
Deployment default my-app my-app-container
secretKeyRef
Pod default my-app-f5966f8b4-p2h my-app-container
secretKeyRef
StatefulSet database postgres postgres
secretKeyRef
DaemonSet monitoring log-agent fluent-bit
secretRef
Job batch nightly-import importer
secretKeyRef
CronJob reporting report-generator generator
secretRef
Pod default test-secret-env app
secretKeyRef
Interpretation:
• secretKeyRef indicates the workload references a specific key from a Secret in an environment variable.
• secretRef indicates the workload imports all keys from a Secret into environment variables using envFrom.
Any result indicates a workload that should be reviewed for remediation. Prioritize workloads that expose high value credentials, run in shared namespaces, have broad log access, or can be changed to use Secret Manager API access through a Kubernetes ServiceAccount and IAM permissions.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-4A500600">
    <title>4.4 Secrets Management and Workload Secret Access</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-4A500600r01_rule" weight="10.0" severity="high">
      <version>4.4.2</version>
      <title>Use Secret Manager for external workload secret storage</title>
      <description>&lt;VulnDiscussion&gt;Use Secret Manager as the preferred external storage and management service for workload secrets used by GKE Standard clusters. Workloads should access secrets through Secret Manager API calls using Workload Identity Federation for GKE, or through mounted volumes using the Secret Manager add-on when file based access is required.

Rationale:
Kubernetes Secrets are supported, but they require careful control through Kubernetes RBAC, namespace boundaries, encryption, and workload configuration. Storing sensitive values in Secret Manager centralizes secret lifecycle management outside the cluster and supports IAM based access control, auditability, rotation, and version management. Workload Identity Federation for GKE lets workloads access Secret Manager without static service account keys and allows access to be granted to specific Kubernetes ServiceAccounts.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.4.2</dc:identifier>
      </reference>
      <fixtext fixref="F-4A500600r01_fix">Move externally managed workload secrets to Secret Manager and update GKE workloads to access them through Workload Identity Federation for GKE. Prefer direct Secret Manager API access through Google Cloud client libraries where the application supports it. For applications that require file based secret access, enable the Secret Manager add-on and mount secrets into Pods using SecretProviderClass and CSI volumes.
• Enable Workload Identity Federation for GKE on the Standard cluster and required node pools.
• Use dedicated Kubernetes ServiceAccounts for workloads that need Secret Manager access.
• Grant least privilege Secret Manager IAM permissions only to the required workload identities.
• Enable the Secret Manager add-on when workloads need secrets mounted as files.
Internal Only - General
• Migrate sensitive values out of Kubernetes Secrets where practical, then remove unused in-cluster Secret objects.
Secret Manager provides centralized secret management features such as fine grained access control, managed rotation, lifecycle management, and audit logs.</fixtext>
      <fix id="F-4A500600r01_fix"/>
      <check system="C-4A500600_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard secrets implementation and verify that sensitive workload secrets are stored in Secret Manager where external secret storage is required, rather than relying only on native Kubernetes Secrets. Confirm whether workloads access Secret Manager through direct API calls with Workload Identity Federation for GKE, or through mounted files using the Secret Manager add-on and SecretProviderClass resources.
Secret Manager access from GKE workloads can use Workload Identity Federation for GKE without static service account keys, and the Secret Manager add-on supports mounting Secret Manager secrets into Pods as files.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format="yaml(workloadIdentityConfig.workloadPool,secretManagerConfig)"
Sample output should look like this:
1. Workload Identity Federation for GKE for Secret Manager API access 2. Secret Manager add-on with rotation enabled for mounted file access
secretManagerConfig:
enabled: true
rotationConfig:
enabled: true
workloadIdentityConfig:
workloadPool: gke-for-cis.svc.id.goog
Interpretation:
• workloadIdentityConfig.workloadPool populated means Workload Identity Federation for GKE is enabled.
• secretManagerConfig.enabled: true means the Secret Manager add-on is enabled for mounting Secret Manager secrets as files.
• rotationConfig.enabled: true means mounted secrets can be refreshed through the add-on rotation configuration.
For any workload still using Kubernetes Secrets for sensitive values, confirm whether there is a documented reason, whether RBAC access is limited, and whether migration to Secret Manager is planned. Also validate that Secret Manager IAM permissions are granted only to the required workload identities, because Workload Identity Federation for GKE does not grant Google Cloud API permissions by itself.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-2E7A1A7F">
    <title>4.5 Admission Control and Policy Enforcement</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-2E7A1A7Fr01_rule" weight="10.0" severity="high">
      <version>4.5.1</version>
      <title>Enforce image provenance using Binary Authorization for GKE</title>
      <description>&lt;VulnDiscussion&gt;Use Binary Authorization for GKE to enforce image provenance and deploy time trust policies before container images are admitted to the cluster. Policies should allow only approved images, trusted registries, or images with required attestations from approved build, security, or release processes.

Rationale:
Binary Authorization provides centralized policy enforcement for GKE deployments by evaluating container images at deploy time. When attestation based rules are used, the policy verifies that an image digest has been signed by a trusted attestor before the workload is allowed to run. This helps prevent unapproved, unsigned, unverified, or noncompliant images from being deployed, even if a user or pipeline has permission to create Kubernetes workloads.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.5.1</dc:identifier>
      </reference>
      <fixtext fixref="F-2E7A1A7Fr01_fix">Enable Binary Authorization enforcement for the GKE Standard cluster and configure the policy to block unapproved images at deploy time. Use approved image rules, trusted registries, or attestation based requirements aligned to the build, security scan, and release approval process. Validate the policy in dry run or a lower environment before enforcing broadly in production.</fixtext>
      <fix id="F-2E7A1A7Fr01_fix"/>
      <check system="C-2E7A1A7F_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard cluster and confirm that Binary Authorization enforcement is enabled and confirm it does not simply allow all images and that it requires approved images, trusted registries, or attestations from trusted attestors where appropriate.
Binary Authorization works with Standard clusters and evaluates the configured policy at deploy time.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-9510B75F">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-9510B75Fr01_rule" weight="10.0" severity="medium">
      <version>4.6.1</version>
      <title>Use namespaces to separate workload administration and resource governance</title>
      <description>&lt;VulnDiscussion&gt;Use dedicated namespaces to separate GKE Standard workloads by application, team, environment, or tenant. Namespaces provide a logical boundary for organizing resources and applying namespace level governance, but they should be paired with access, quota, network, and pod security controls.

Rationale:
Namespaces help reduce administrative risk by limiting where users and workloads operate inside the cluster. GKE supports namespace restricted access patterns where IAM and Kubernetes RBAC work together, and RBAC should grant permissions in as few namespaces as possible using Roles and RoleBindings where practical. This reduces the blast radius of mistakes, unauthorized changes, and overly broad operational access.
Dedicated namespaces also make it easier to apply workload governance consistently, including ResourceQuotas, NetworkPolicies, Pod Security labels, ownership labels, and environment specific deployment rules. Workloads should not be placed in the default namespace for production because shared use can increase naming collisions, resource contention, weak access separation, and cleanup complexity.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.6.1</dc:identifier>
      </reference>
      <fixtext fixref="F-9510B75Fr01_fix">Create dedicated namespaces for GKE workloads based on application, environment, or tenant boundaries. Move production workloads out of the default namespace and apply consistent namespace level governance so administration, access, and resource controls are easier to manage.
• Create a dedicated namespace before deploying new application resources.
• Move production workloads out of the default namespace where applicable.
• Apply namespace labels for owner, application, environment, tenant, and cost allocation.
• Configure least privilege RBAC using namespace level Roles and RoleBindings where possible.
• Apply ResourceQuotas, NetworkPolicies, and Pod Security labels based on workload risk and governance requirements.
Review namespaces, owners, workload placement, and access bindings to confirm that the namespace model still matches current application and environment boundaries.</fixtext>
      <fix id="F-9510B75Fr01_fix"/>
      <check system="C-9510B75F_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Run the following command and review the namespaces created in the cluster:
kubectl get namespaces --show-labels
Verify that user workloads are placed in dedicated namespaces based on application, environment, or tenant, rather than being deployed into the default namespace. In addition, confirm that each workload namespace has both ownership and appropriate namespace level controls, such as RBAC RoleBindings, ResourceQuotas, NetworkPolicies, and Pod Security labels where required.
Internal Only - General Review namespace access to confirm that users and groups are granted only the namespace level permissions they need. Prefer namespace level Roles and RoleBindings, rather than broad cluster wide access.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-0FECC586">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-0FECC586r01_rule" weight="10.0" severity="high">
      <version>4.6.2</version>
      <title>Require RuntimeDefault seccomp or approved custom profiles for workload pods</title>
      <description>&lt;VulnDiscussion&gt;Require GKE workload pods to use the RuntimeDefault seccomp profile unless an approved custom Localhost profile is required. Seccomp should be configured in the pod or container security context to reduce the system calls available to containers.

Rationale:
Seccomp limits the Linux system calls that a container can make, reducing the kernel attack surface available to a compromised workload. In GKE clusters, seccomp profiles must be applied manually because GKE does not apply a profile for you. Using RuntimeDefault applies the container runtime’s default seccomp profile, while approved Localhost profiles can be used when workloads require more specific syscall controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.6.2</dc:identifier>
      </reference>
      <fixtext fixref="F-0FECC586r01_fix">Update the workload controller templates for any Pods returned by the audit so seccomp is defined in the source manifest, not patched directly on running Pods. Apply the setting through the owning Deployment, StatefulSet, DaemonSet, Job, CronJob, Helm chart, Kustomize overlay, or GitOps source.
• Set securityContext.seccompProfile.type to RuntimeDefault at the Pod level where possible so all containers inherit the runtime default seccomp profile.
• Use container level seccomp settings only when a specific container requires a different approved profile than the rest of the Pod.
• Replace Unconfined with RuntimeDefault unless there is a documented and approved exception.
• Allow Localhost only for approved custom seccomp profiles, and confirm the profile is documented, tested, and available on every node where the workload can run.
• Redeploy the workload and rerun the audit to confirm no unset or Unconfined findings remain.
After remediation, update workload deployment standards so new manifests explicitly define RuntimeDefault or an approved custom seccomp profile before release.</fixtext>
      <fix id="F-0FECC586r01_fix"/>
      <check system="C-0FECC586_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review running Pods across all namespaces and identify containers that do not explicitly use RuntimeDefault or an approved Localhost seccomp profile. In GKE Standard clusters, seccomp must be applied manually because GKE does not apply a profile for you.
echo -e "NAMESPACE\tPOD\tCONTAINER\tEFFECTIVE_SECCOMP"
kubectl get pods -A -o json | jq -r '
.items[] as $p
| ($p.spec.securityContext.seccompProfile.type // "unset") as $podProfile
| (($p.spec.containers // []) + ($p.spec.initContainers // []))[]
| (.securityContext.seccompProfile.type // $podProfile) as $effective
| select($effective != "RuntimeDefault" and $effective != "Localhost")
| [$p.metadata.namespace, $p.metadata.name, .name, $effective]
| @tsv
'
Any returned result should be reviewed.
Internal Only - General
• unset means neither the Pod nor the container explicitly sets a seccomp profile.
• Unconfined means seccomp is explicitly disabled.
• Localhost is treated as compliant by the command, but should still be approved, documented, and available on the nodes where the workload runs.
• No returned rows means all checked containers use either RuntimeDefault or Localhost.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-187073DA">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-187073DAr01_rule" weight="10.0" severity="high">
      <version>4.6.3</version>
      <title>Require hardened security contexts for all workload Pods and containers</title>
      <description>&lt;VulnDiscussion&gt;Require hardened security contexts for GKE workload Pods and containers. Security contexts should define workload level and container level controls that reduce privilege, limit host exposure, and enforce safer runtime behavior.

Rationale:
Security contexts control important runtime security settings for Pods and containers, including user and group identity, privilege escalation, Linux capabilities, privileged mode, seccomp profile, and related container isolation settings. GKE settings help reduce the workload attack surface and should be explicitly defined in workload manifests rather than relying on broad runtime defaults.
A hardened configuration should normally include settings such as runAsNonRoot=true, allowPrivilegeEscalation=false, privileged=false, dropped Linux capabilities, and seccompProfile.type=RuntimeDefault or an approved custom profile. These align with restricted Pod security expectations and can also be enforced or audited with admission controls such as Pod Security Admission and Policy Controller.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.6.3</dc:identifier>
      </reference>
      <fixtext fixref="F-187073DAr01_fix">Update workload manifests, Helm charts, overlays, or GitOps sources so hardened security contexts are defined before deployment. Apply settings at the Pod level when they apply to all containers, and use container level settings where a specific container requires a different configuration.
• Set workloads to run as non root where compatible.
• Set allowPrivilegeEscalation=false for workload containers.
• Set privileged=false and remove host level access unless explicitly required.
• Drop unnecessary Linux capabilities, and use drop: ["ALL"] where the application supports it.
• Set seccompProfile.type=RuntimeDefault or use an approved custom Localhost profile.
Redeploy the workload and validate application behavior in a lower environment before broad rollout. Use admission controls such as Pod Security Admission or Policy Controller to prevent future workloads from being admitted without the required security context settings.</fixtext>
      <fix id="F-187073DAr01_fix"/>
      <check system="C-187073DA_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review workload Pods and controller templates to confirm hardened security contexts are explicitly defined at the Pod and container level. Focus the audit on workload namespaces and verify settings such as runAsNonRoot=true, allowPrivilegeEscalation=false, privileged=false, dropped Linux capabilities where practical, and seccompProfile.type=RuntimeDefault or an approved custom profile. GKE Standard supports using Pod Security Admission, Policy Controller, and security posture scanning to identify and enforce workload configuration risks.
Internal Only - General Review any workload that has no security context, runs as root, allows privilege escalation, runs privileged, uses broad Linux capabilities, or lacks an approved seccomp profile. These findings should be documented as remediation items or approved exceptions based on workload requirements.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-114A3A66">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-114A3A66r01_rule" weight="10.0" severity="high">
      <version>4.6.4</version>
      <title>Avoid deploying workloads in the default namespace</title>
      <description>&lt;VulnDiscussion&gt;Avoid deploying GKE workloads in the default namespace. Use dedicated namespaces to separate applications, environments, or tenants so namespace level controls can be applied consistently.

Rationale:
The default namespace exists for objects created without an explicit namespace, but using it for normal workloads weakens ownership, access separation, and resource governance. Dedicated namespaces make it easier to apply namespace based RBAC, ResourceQuotas, NetworkPolicies, Pod Security labels, observability controls, and cost attribution.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>4.6.4</dc:identifier>
      </reference>
      <fixtext fixref="F-114A3A66r01_fix">Create dedicated namespaces for workloads based on application, environment, or tenant boundaries, then migrate application resources out of the default namespace.
Make updates so all workload resources explicitly specify the new target namespace.
After migration, apply appropriate namespace level governance, including RBAC, ResourceQuotas, NetworkPolicies, Pod Security labels, and ownership labels.</fixtext>
      <fix id="F-114A3A66r01_fix"/>
      <check system="C-114A3A66_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Run the following command to identify resources currently deployed in the default namespace:
kubectl get
all,configmaps,secrets,pvc,ingress,serviceaccounts,networkpolicies -n default
Review any returned resources and determine whether they are application, environment, or tenant workloads that should be moved to a dedicated namespace.
GKE supports identifying workloads running in the default namespace and migrating them to dedicated namespaces to improve reliability and security.
In addition, confirm that each target namespace has appropriate ownership and namespace level governance, including RBAC RoleBindings, ResourceQuotas, NetworkPolicies, and Pod Security labels where required.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-517DB500">
    <title>5.1 Container Image Registry and Vulnerability Scanning</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-517DB500r01_rule" weight="10.0" severity="high">
      <version>5.1.1</version>
      <title>Enable Artifact Analysis scanning for Artifact Registry container images</title>
      <description>&lt;VulnDiscussion&gt;Enable Artifact Analysis vulnerability scanning for container images stored in Artifact Registry and used by GKE Standard workloads. This verifies that container images are scanned at the registry layer before deployment to identify known vulnerabilities in image packages, dependencies, and software components. For GKE clusters, this should be treated as an Artifact Registry and Artifact Analysis, not as a cluster level workload vulnerability scanning setting, because GKE security posture dashboard vulnerability scanning capabilities are being removed.

Rationale:
Container images can include vulnerable operating system packages, language packages, dependencies, or other software components that increase the risk of compromise when deployed to GKE workloads. Artifact Analysis scans container images in Artifact Registry through the Container Scanning API and records vulnerability metadata through the Container Analysis API, giving platform and application teams visibility into known vulnerabilities before images are promoted or deployed.
Automatic scanning evaluates new images when they are pushed to Artifact Registry and continuously monitors scanned image metadata for newly disclosed vulnerabilities.
Scans are based on image digest, so retagging an image does not trigger a new scan, and findings are limited to packages that are publicly monitored for security vulnerabilities.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.1.1</dc:identifier>
      </reference>
      <fixtext fixref="F-517DB500r01_fix">Enable Artifact Analysis automatic vulnerability scanning for container images stored in Artifact Registry and used by GKE workloads. Enabling the Container Scanning API activates automatic scanning for images pushed to standard and remote Docker repositories in Artifact Registry, and it also enables the Container Analysis API for vulnerability metadata storage and retrieval.
Enable the required project APIs:
gcloud services enable artifactregistry.googleapis.com
containerscanning.googleapis.com \
--project=$PROJECT_ID
If scanning was disabled on a specific Artifact Registry repository, update the repository to allow vulnerability scanning:
Internal Only - General
gcloud artifacts repositories update $REPOSITORY \
--project=$PROJECT_ID \
--location=$LOCATION \
--allow-vulnerability-scanning
Note: Repeat the repository update command for each Docker repository that stores images deployed to GKE Standard workloads. Repository level scanning controls are supported in Artifact Registry, and --allow-vulnerability-scanning permits automatic vulnerability scanning for the repository.</fixtext>
      <fix id="F-517DB500r01_fix"/>
      <check system="C-517DB500_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the Google Cloud project that hosts container images for GKE Standard workloads and verify that Artifact Registry and Artifact Analysis automatic vulnerability scanning are enabled. Artifact Analysis provides automated vulnerability scanning for container images in Artifact Registry through the Container Scanning API, and enabling that API also enables the Container Analysis API for vulnerability metadata storage and retrieval.
Internal Only - General Verify that the required APIs are enabled:
gcloud services list --enabled \
--project=$PROJECT_ID \
--filter='name:artifactregistry.googleapis.com OR
name:containerscanning.googleapis.com OR
name:containeranalysis.googleapis.com' \
--format='table(config.name)'
Expected output: artifactregistry.googleapis.com and containerscanning.googleapis.com should be listed.
containeranalysis.googleapis.com may also be listed because the Container Analysis API is used for metadata storage and retrieval.
Review Artifact Registry repository scanning settings for Docker repositories used by GKE workloads:
gcloud artifacts repositories list \
--project=$PROJECT_ID \
--location=$LOCATION \
--
format='table(name,format,mode,vulnerabilityScanningConfig.enablementConfig,v
ulnerabilityScanningConfig.enablementState,vulnerabilityScanningConfig.enable
mentStateReason)'
Expected output: Docker repositories that store GKE workload images should not show repository level scanning disabled. Enabling the Container Scanning API activates scanning for images pushed to standard and remote Docker repositories in Artifact Registry, but scanning can be disabled on individual repositories, so repository level settings should also be reviewed.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-A9720197">
    <title>5.1 Container Image Registry and Vulnerability Scanning</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-A9720197r01_rule" weight="10.0" severity="high">
      <version>5.1.2</version>
      <title>Grant least privilege IAM access to Artifact Registry repositories</title>
      <description>&lt;VulnDiscussion&gt;Restrict access to Artifact Registry repositories that store GKE Standard workload images. Grant users, groups, CI/CD identities, and GKE node service accounts only the IAM roles needed to pull, push, administer, or manage container images.

Rationale:
Weak IAM access to Artifact Registry can allow unauthorized users to pull sensitive images, push unapproved images, overwrite trusted image tags, or delete repository content. Artifact Registry supports repository level IAM, so access should be limited to the minimum required role, such as Artifact Registry Reader for image pulls, Artifact Registry Writer for image publishing, and repository administrator roles only for approved platform owners. GKE nodes require pull access to repositories that host workload images, especially when images are stored in a different project or when using user provided node service accounts.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.1.2</dc:identifier>
      </reference>
      <fixtext fixref="F-A9720197r01_fix">Apply least privilege IAM access to Artifact Registry Docker repositories that store images deployed to GKE Standard clusters. Repository access should be limited to approved users, groups, service accounts, CI/CD identities, and GKE node service accounts based on their required function. Use repository level IAM where access should differ by application, team, environment, or repository, and avoid broad project level grants unless the same access is intentionally required across all repositories.
• Grant GKE node service accounts read only access to the specific Artifact Registry repositories they need to pull workload images from by assigning roles/artifactregistry.reader at the repository level.
• Grant CI/CD publishing identities access only to repositories where they need to publish images by assigning roles/artifactregistry.writer at the repository level.
• Remove excessive repository level access from unauthorized users, groups, service accounts, or public principals, including unnecessary roles/artifactregistry.admin, roles/artifactregistry.repoAdmin, roles/artifactregistry.writer, or public roles/artifactregistry.reader grants.
• Remove unnecessary project level Artifact Registry roles or broad basic roles such as Owner, Editor, and Viewer when repository level access is sufficient for the user, group, service account, or automation identity.
Confirm that GKE node service accounts can still pull approved images and that CI/CD identities can only publish to the repositories they are authorized to manage.</fixtext>
      <fix id="F-A9720197r01_fix"/>
      <check system="C-A9720197_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review IAM access for Artifact Registry Docker repositories that store images deployed to GKE Standard clusters. Verify that repository permissions are limited to approved users, groups, service accounts, CI/CD identities, and GKE node service accounts based on their required function. Artifact Registry access can be granted at the project level or repository level, but repository level IAM should be used when access needs to differ by application, team, environment, or repository.
List Artifact Registry Docker repositories used by GKE workloads:
gcloud artifacts repositories list \
--project=$PROJECT_ID \
--location=$LOCATION \
--format='table(name,format,mode)'
• Review each Artifact Registry repository IAM policy and verify that only approved principals have access, with roles limited to the minimum required level such as Internal Only - General Reader for image pulls, Writer for image publishing, and repository administrator roles only for approved platform or repository administrators.
Analyze project level IAM bindings that may grant access across Artifact Registry repositories in the project. Verify that Artifact Registry roles, Owner, Editor, and Viewer are granted only to principals that require the same level of access across all repositories. Use repository level IAM where access should be limited by application, team, environment, or repository.
Examine the GKE node service account assigned to each node pool and verify that it has only the Artifact Registry permissions required to pull approved workload images, typically read only access to the required repositories.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-BB9220D2">
    <title>5.1 Container Image Registry and Vulnerability Scanning</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-BB9220D2r01_rule" weight="10.0" severity="high">
      <version>5.1.3</version>
      <title>Enforce Binary Authorization for trusted GKE container image deployments</title>
      <description>&lt;VulnDiscussion&gt;Use Binary Authorization to enforce trusted image deployment policies for GKE Standard clusters. The policy should allow only approved container images to be deployed, based on defined admission rules such as approved image locations, trusted build or promotion processes, and required attestations where appropriate.

Rationale:
Unrestricted image deployment can allow users or automation pipelines to run unapproved, vulnerable, tampered, or externally sourced images in the cluster. Binary Authorization integrates with GKE admission control to evaluate container images before deployment and enforce centrally managed policy decisions, such as requiring an image to come from an approved registry path or requiring a signed approval that verifies the image passed required build, scan, approval, or release controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.1.3</dc:identifier>
      </reference>
      <fixtext fixref="F-BB9220D2r01_fix">Enable Binary Authorization enforcement on the GKE Standard cluster when the audit shows that the cluster is not using an enforcing evaluation mode. Use PROJECT_SINGLETON_POLICY_ENFORCE to enforce the project Binary Authorization policy. If the cluster also uses continuous validation with policy bindings, use POLICY_BINDINGS_AND_PROJECT_SINGLETON_POLICY_ENFORCE instead. Monitoring only modes should not be used as the remediation target for this control.
gcloud container clusters update $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--binauthz-evaluation-mode=PROJECT_SINGLETON_POLICY_ENFORCE
Export the current Binary Authorization policy to a local YAML file. The trusted image deployment rules are edited in this policy file, not directly in the GKE cluster configuration.
gcloud container binauthz policy export \
--project=$PROJECT_ID &gt; policy.yaml
• Edit policy.yaml and update the defaultAdmissionRule or the applicable rule under clusterAdmissionRules so that trusted image controls are enforced. For signed approval based enforcement, set evaluationMode to REQUIRE_ATTESTATION, set enforcementMode to ENFORCED_BLOCK_AND_AUDIT_LOG, and set requireAttestationsBy to the approved attestor resource.
Internal Only - General
defaultAdmissionRule:
evaluationMode: REQUIRE_ATTESTATION
enforcementMode: ENFORCED_BLOCK_AND_AUDIT_LOG
requireAttestationsBy:
- projects/$PROJECT_ID/attestors/$ATTESTOR_NAME
globalPolicyEvaluationMode: ENABLE
• Import the updated Binary Authorization policy after review and approval.
gcloud container binauthz policy import policy.yaml \
--project=$PROJECT_ID
After remediation, test deployment behavior with both an approved image and an unapproved image. Approved images should be admitted, while images that do not meet the Binary Authorization policy should be blocked and logged.</fixtext>
      <fix id="F-BB9220D2r01_fix"/>
      <check system="C-BB9220D2_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard cluster configuration and verify that Binary Authorization enforcement is enabled for trusted container image deployments. Define the cluster name, location, and project, then check the cluster Binary Authorization evaluation mode.
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='value(binaryAuthorization.evaluationMode)'
• Expected output: the output should be PROJECT_SINGLETON_POLICY_ENFORCE or POLICY_BINDINGS_AND_PROJECT_SINGLETON_POLICY_ENFORCE. A monitoring only mode does not enforce trusted image deployment and should not be considered compliant for this control.
Export the Binary Authorization policy and review the default admission rule and any cluster specific admission rules that apply to the GKE cluster.
Internal Only - General
gcloud container binauthz policy export \
--project=$PROJECT_ID &gt; current-policy.yaml
Review the policy file and verify that the default rule is not configured to allow all images. The default Binary Authorization policy allows all images, so defaultAdmissionRule.evaluationMode: ALWAYS_ALLOW should be changed for production enforcement.
defaultAdmissionRule:
evaluationMode: ALWAYS_ALLOW
• Expected output: production policies should use an enforcing rule such as REQUIRE_ATTESTATION with required signed approvals, or another approved rule design that blocks untrusted images. The policy should use ENFORCED_BLOCK_AND_AUDIT_LOG for enforcement rather than DRYRUN_AUDIT_LOG_ONLY, which allows noncompliant images to deploy while logging violations.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-BAAA6363">
    <title>5.2 Identity and Access Management</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-BAAA6363r01_rule" weight="10.0" severity="medium">
      <version>5.2.1</version>
      <title>Use custom least privilege service accounts for GKE node pools</title>
      <description>&lt;VulnDiscussion&gt;Use a custom, least privilege IAM service account for GKE Standard node pools instead of the Compute Engine default service account. The node service account provides the default Google Cloud identity for nodes and should be limited to the permissions required for node level system tasks such as logging, monitoring, autoscaling metrics, and approved image pulls.

Rationale:
GKE nodes use an attached IAM service account to perform Google Cloud operations on behalf of node level system components. If a custom service account is not specified, GKE uses the Compute Engine default service account, which might also be used by other resources in the project and could have more permissions than GKE nodes require. Using a dedicated custom node service account reduces the blast radius if a node is compromised and makes node permissions easier to review and govern.
For GKE the node service account should have the roles/container.defaultNodeServiceAccount role, which is the least privilege role intended for GKE nodes. Additional permissions should be granted only when required, such as roles/artifactregistry.reader for pulling private images from Artifact Registry repositories. Workloads that need Google Cloud API access should use Workload Identity Federation for GKE rather than relying on the node service account.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.2.1</dc:identifier>
      </reference>
      <fixtext fixref="F-BAAA6363r01_fix">Create a dedicated custom IAM service account for GKE Standard node pools that are using the Compute Engine default service account or a service account with excessive permissions. The custom node service account should be used for node level system tasks and should be granted only the permissions required for GKE node operation.
gcloud iam service-accounts create $NODE_SA_ACCOUNT \
--project=$PROJECT_ID \
--display-name="GKE Node Service Account"
Grant the custom node service account the current baseline GKE node role, roles/container.defaultNodeServiceAccount. This role is intended to provide the required permissions for GKE node operation, replacing the older approach of separately assigning logging and monitoring roles.
gcloud projects add-iam-policy-binding $PROJECT_ID \
--
member=serviceAccount:$NODE_SA_ACCOUNT@$PROJECT_ID.iam.gserviceaccount.com \
--role=roles/container.defaultNodeServiceAccount
Grant roles only when required.
• Example: If the node pool must pull private images from Artifact Registry, grant roles/artifactregistry.reader at the repository level when possible.
Internal Only - General
gcloud artifacts repositories add-iam-policy-binding $REPOSITORY \
--project=$PROJECT_ID \
--location=$LOCATION \
--
member=serviceAccount:$NODE_SA_ACCOUNT@$PROJECT_ID.iam.gserviceaccount.com \
--role=roles/artifactregistry.reader
Create a new node pool that uses the custom node service account. The service account should be specified when the node pool is created, so node pools that use the Compute Engine default service account should be replaced with new node pools using the custom service account.
gcloud container node-pools create $NODE_POOL \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--service-account=$NODE_SA_ACCOUNT@$PROJECT_ID.iam.gserviceaccount.com
After the new node pool is healthy, migrate workloads to the new node pool and validate node registration, logging, monitoring, autoscaling metrics, and private image pulls.
Then drain and delete old node pools that use the Compute Engine default service account or an overly privileged service account.</fixtext>
      <fix id="F-BAAA6363r01_fix"/>
      <check system="C-BAAA6363_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE Standard node pool and verify that it uses a custom least privilege IAM service account instead of the Compute Engine default service account. GKE uses the node service account for node level system tasks, and the recommended approach is to use a dedicated custom service account with only the permissions required for GKE node operation.
Internal Only - General List the service account assigned to each node pool:
gcloud container node-pools list \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='table(name,config.serviceAccount)'
• Expected output: no node pool should show default or the Compute Engine default service account in the format $PROJECT_NUMBER- compute@developer.gserviceaccount.com. If the value is default, the node pool is using the Compute Engine default service account.
Review the IAM roles assigned to the custom node service account and verify that it has the required GKE node role and no unnecessary broad permissions.
gcloud projects get-iam-policy $PROJECT_ID \
--flatten='bindings[].members' \
--format='table(bindings.role,bindings.members)' \
--filter="bindings.members:serviceAccount:$NODE_SA_EMAIL"
• Expected output: the custom node service account should have roles/container.defaultNodeServiceAccount. Additional roles should be limited to documented operational requirements, such as roles/artifactregistry.reader when the node pool must pull private images from Artifact Registry.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-648406C9">
    <title>5.2 Identity and Access Management</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-648406C9r01_rule" weight="10.0" severity="high">
      <version>5.2.2</version>
      <title>Use dedicated Kubernetes service accounts with Workload Identity Federation for GKE</title>
      <description>&lt;VulnDiscussion&gt;Use Workload Identity Federation for GKE to give GKE workloads distinct, least privilege identities when they need access to Google Cloud APIs. Workloads should use dedicated Kubernetes service accounts and IAM allow policies instead of relying on node service account permissions or long lived IAM service account keys.

Rationale:
Workloads that authenticate through node service account permissions or static IAM service account keys create unnecessary risk. Node service account permissions are shared across workloads running on the node, which weakens least privilege, while static keys introduce credential storage, rotation, and leakage concerns. Workload Identity Federation for GKE lets IAM trust Kubernetes identities and issue federated tokens without distributing Google Cloud credentials to workloads.
For GKE clusters, this federation model must be enabled at the cluster level and on the node pools that run the workloads. IAM access is then granted on the target Google Cloud resource to a principal identifier that represents the workload, namespace, or Kubernetes service account. When a target API does not support direct federated access, a Kubernetes service account can be configured to impersonate a dedicated IAM service account.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.2.2</dc:identifier>
      </reference>
      <fixtext fixref="F-648406C9r01_fix">This remediation is important when GKE Standard workloads rely on node service account permissions, mounted IAM service account keys, or shared Kubernetes service accounts to access Google Cloud APIs. Enabling Workload Identity Federation for GKE and using dedicated Kubernetes service accounts allows each workload to receive only the Google Cloud permissions it needs, reducing reliance on broad node level permissions and improving workload level identity separation.
Internal Only - General Enable Workload Identity Federation for GKE on the GKE Standard cluster if the workload pool is not configured.
gcloud container clusters update $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--workload-pool=$PROJECT_ID.svc.id.goog
Enable the GKE metadata server on existing node pools that run workloads requiring Google Cloud API access. Existing node pools are not automatically updated when the cluster setting is enabled, so they must be updated or replaced.
gcloud container node-pools update $NODE_POOL \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--workload-metadata=GKE_METADATA
Create a dedicated Kubernetes service account for each workload or application that requires Google Cloud API access instead of using the namespace default service account.
• Grant the required IAM role on the target Google Cloud resource to the specific Kubernetes principal identifier for the namespace and Kubernetes service account. Grant only the permissions required by the workload.
• Update the workload manifest so the Pod runs as the dedicated Kubernetes service account. For GKE Standard clusters, ensure the workload is scheduled onto a node pool that uses the GKE metadata server.
Redeploy the workload and test Google Cloud API access to confirm the federated identity configuration works as expected. Update any workloads that previously depended on node service account permissions or mounted IAM service account keys to use the dedicated Kubernetes service account and IAM allow policy model.</fixtext>
      <fix id="F-648406C9r01_fix"/>
      <check system="C-648406C9_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard cluster and verify that Workload Identity Federation for GKE is enabled at the cluster level. The cluster should have a workload pool configured in the format $PROJECT_ID.svc.id.goog.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='value(workloadIdentityConfig.workloadPool)'
• Expected ouput: $PROJECT_ID.svc.id.goog Review each node pool and verify that it is configured to use the GKE metadata server.
For Standard clusters, Workload Identity Federation for GKE must be enabled on the cluster before it can be used by node pools. Existing node pools are not automatically updated when the cluster setting is enabled.
gcloud container node-pools list \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='table(name,config.workloadMetadataConfig.mode)'
• Expected result: node pools that run workloads requiring Google Cloud API access should show GKE_METADATA. A node pool using GCE_METADATA is not using the GKE metadata server for this authentication model.
Review workloads that require Google Cloud API access and verify that they use dedicated Kubernetes service accounts instead of the namespace default service account.
kubectl get pods -A \
-o custom-
columns='NAMESPACE:.metadata.namespace,POD:.metadata.name,SERVICE_ACCOUNT:.sp
ec.serviceAccountName'
• Expected result: workloads that require Google Cloud API access should use an application specific Kubernetes service account. They should not rely on the node service account, mounted IAM service account keys, or the namespace default service account.
Review IAM allow policies for the target Google Cloud resources and verify that access is granted to the specific Kubernetes principal identifier for the namespace and Kubernetes service account.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-3EB1A480">
    <title>5.3 Cloud Key Management Service</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-3EB1A480r01_rule" weight="10.0" severity="high">
      <version>5.3.1</version>
      <title>Enable application-layer Secrets encryption with Cloud KMS keys</title>
      <description>&lt;VulnDiscussion&gt;Enable application layer Secrets encryption for GKE Standard clusters using a Cloud KMS key. This control adds customer managed encryption for Kubernetes Secret resources stored in the cluster state database, providing an additional protection layer beyond Google managed encryption at rest.

Rationale:
By default, GKE encrypts customer content at rest, including Kubernetes Secrets, using encryption that Google manages automatically. Application layer Secrets encryption adds another layer by encrypting Secret data before it is written to the cluster state database, using a key that is managed in Cloud KMS.
This improves protection for sensitive Kubernetes Secret data by allowing the organization to manage the encryption key lifecycle, including key access control, rotation, and disablement. It is important for clusters that store sensitive application credentials, tokens, certificates, or other Secret values that require stronger key management controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.3.1</dc:identifier>
      </reference>
      <fixtext fixref="F-3EB1A480r01_fix">Enable application layer encryption on the GKE Standard cluster when the audit shows that databaseEncryption.state is not ALL_OBJECTS_ENCRYPTION_ENABLED, databaseEncryption.currentState is not CURRENT_STATE_ALL_OBJECTS_ENCRYPTION_ENABLED, or databaseEncryption.keyName does not reference the approved Cloud KMS CryptoKey.
In the Google Cloud console:
• Open the GKE cluster
• Select Edit and then go to the Security section
• Edit Application-layer Secrets Encryption
• Check Encrypt secrets at the application layer
• Choose the approved Cloud KMS key. (If prompted, grant the Kubernetes Engine Service Agent access to use the selected key)
• Save the change After the update completes, rerun the audit command and verify that databaseEncryption.state returns ALL_OBJECTS_ENCRYPTION_ENABLED, databaseEncryption.currentState returns CURRENT_STATE_ALL_OBJECTS_ENCRYPTION_ENABLED, and databaseEncryption.keyName references the approved Cloud KMS CryptoKey.
Updating an existing cluster can restart the control plane and re-encrypt existing data, so apply the change during an approved maintenance window, especially for zonal clusters.</fixtext>
      <fix id="F-3EB1A480r01_fix"/>
      <check system="C-3EB1A480_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Standard cluster and verify that application layer encryption is enabled with an approved Cloud KMS key. This confirms that all Kubernetes API objects in the cluster state database are encrypted using a customer managed Cloud KMS key in addition to Google managed encryption at rest.
Check the database encryption configuration for the cluster:
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='json(databaseEncryption)'
• Expected result: databaseEncryption.state should return ALL_OBJECTS_ENCRYPTION_ENABLED, databaseEncryption.currentState should return CURRENT_STATE_ALL_OBJECTS_ENCRYPTION_ENABLED, and databaseEncryption.keyName should reference the approved Cloud KMS CryptoKey. If state or currentState shows a disabled, decrypted, or transitional value, or if keyName does not match the approved Cloud KMS key, application layer encryption is not correctly configured.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-0F47DD1D">
    <title>5.4 Node Metadata</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-0F47DD1Dr01_rule" weight="10.0" severity="high">
      <version>5.4.1</version>
      <title>Enable GKE metadata server on Standard node pools</title>
      <description>&lt;VulnDiscussion&gt;Enable the GKE metadata server on GKE node pools to support Workload Identity Federation for GKE and reduce workload access to the full Compute Engine metadata server. This setting helps workloads use Kubernetes based identity federation instead of relying on broad node service account credentials.

Rationale:
The Compute Engine metadata server exposes VM metadata and credentials associated with the node service account. If workloads can access those endpoints directly, they might obtain credentials or metadata that were intended for the node rather than the workload. This is especially risky in shared or multi-tenant clusters where different workloads run on the same node.
When the GKE metadata server is enabled on a node pool, it provides the subset of metadata endpoints required by Kubernetes workloads and intercepts requests to the metadata endpoint for normal pods. This supports Workload Identity Federation for GKE by allowing workloads to receive federated credentials based on their Kubernetes service account identity instead of using node level IAM permissions.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.4.1</dc:identifier>
      </reference>
      <fixtext fixref="F-0F47DD1Dr01_fix">Enable Workload Identity Federation for GKE on the cluster if the audit shows that no workload pool is configured. This is required before the GKE metadata server can be enabled on Standard node pools.
gcloud container clusters update $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--workload-pool=$PROJECT_ID.svc.id.goog
Enable the GKE metadata server on existing node pools that require workload access to Google Cloud APIs. Existing node pools are not automatically updated when the cluster setting is enabled, so each existing node pool must be updated or replaced.
gcloud container node-pools update $NODE_POOL \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--workload-metadata=GKE_METADATA
After remediation, rerun the audit command and confirm that each applicable node pool reports GKE_METADATA. Review workloads that previously relied on direct Compute Engine metadata server access or node service account credentials, because enabling the GKE metadata server can require workload identity and IAM permission updates.
Internal Only - General</fixtext>
      <fix id="F-0F47DD1Dr01_fix"/>
      <check system="C-0F47DD1D_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that Workload Identity Federation for GKE is enabled at the cluster level. The GKE metadata server can only be enabled on node pools when the cluster has a workload pool configured.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='value(workloadIdentityConfig.workloadPool)'
• Expected output: the output should be $PROJECT_ID.svc.id.goog. If the value is blank, Workload Identity Federation for GKE is not enabled at the cluster level, and the GKE metadata server cannot be enabled for node pools.
Review each node pool and verify that the GKE metadata server is enabled.
gcloud container node-pools list \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='table(name,config.workloadMetadataConfig.mode)'
• Expected output: each applicable node pool should show GKE_METADATA. A value of GCE_METADATA, a blank value, or a missing configuration indicates that the node pool is not using the GKE metadata server.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-2A1A0469">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-2A1A0469r01_rule" weight="10.0" severity="medium">
      <version>5.5.1</version>
      <title>Use COS_CONTAINERD node images for GKE node pools</title>
      <description>&lt;VulnDiscussion&gt;Use COS_CONTAINERD node images for GKE node pools unless there is a specific workload requirement for Ubuntu or Windows. COS_CONTAINERD provides Container- Optimized OS with containerd, giving GKE nodes a Google maintained operating system image that is built, optimized, and hardened for running containers.

Rationale:
Container-Optimized OS is recommended for GKE nodes because it is designed specifically for container workloads, has a smaller operating system footprint, and is maintained by Google for GKE node security, stability, and support. Using COS_CONTAINERD helps reduce the node host attack surface and avoids unnecessary host level packages or services that are not required to run Kubernetes workloads.
COS_CONTAINERD uses containerd as the container runtime directly integrated with Kubernetes. Containerd is used by all current GKE node images and is considered more resource efficient and secure than the older Docker runtime. Workloads and operational tooling should not depend on Docker Engine access on the node.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.1</dc:identifier>
      </reference>
      <fixtext fixref="F-2A1A0469r01_fix">Update noncompliant GKE node pools to use COS_CONTAINERD unless there is a documented requirement for Ubuntu or Windows node images. This remediation changes the node image for the selected node pool and should be planned as a node pool upgrade activity.
gcloud container clusters upgrade $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--node-pool=$NODE_POOL \
--image-type=COS_CONTAINERD
After the upgrade completes, rerun the audit command and verify that the node pool reports COS_CONTAINERD. Validate workloads before and after the change, especially anything that depends on host level packages, Ubuntu specific behavior, Windows nodes, or Docker Engine access.</fixtext>
      <fix id="F-2A1A0469r01_fix"/>
      <check system="C-2A1A0469_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE node pool and verify that the node image type is set to COS_CONTAINERD. This confirms that node pools are using Container Optimized OS with containerd as the container runtime.
Internal Only - General
gcloud container node-pools list \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='table(name,config.imageType)'
• Expected output: each Linux node pool should show COS_CONTAINERD in the config.imageType column. Any node pool showing UBUNTU_CONTAINERD, a Windows image type, or another image type should be reviewed to confirm there is a documented workload requirement for that exception.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-5D4C2465">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-5D4C2465r01_rule" weight="10.0" severity="high">
      <version>5.5.2</version>
      <title>Verify node auto-repair is enabled for GKE node pools</title>
      <description>&lt;VulnDiscussion&gt;Verify node auto-repair is enabled for GKE node pools so unhealthy nodes are automatically repaired and returned to a healthy running state. Node auto-repair is enabled by default for new node pools, but for older node pools should still be reviewed for compliance.

Rationale:
Node auto-repair helps maintain cluster reliability by periodically checking node health and initiating repair when a node remains unhealthy for an extended period. When repair is triggered, GKE drains and re-creates the affected node, helping reduce the risk of workloads remaining scheduled on degraded or nonfunctional infrastructure.
Keeping auto-repair enabled reduces the need for manual node recovery and helps ensure that node level failures are remediated consistently across node pools. This is especially important for production clusters where unhealthy nodes can affect workload availability, scheduling capacity, and operational response time.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.2</dc:identifier>
      </reference>
      <fixtext fixref="F-5D4C2465r01_fix">Enable node auto-repair for any GKE node pool where the audit result is not true. This allows GKE to monitor nodes in the node pool and automatically repair nodes that fail health checks for an extended period.
gcloud container node-pools update $POOL_NAME \
--cluster=$CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--enable-autorepair
After remediation, rerun the audit command and confirm that management.autoRepair returns true. If multiple node pools are present, repeat the audit and remediation for each applicable node pool.</fixtext>
      <fix id="F-5D4C2465r01_fix"/>
      <check system="C-5D4C2465_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE node pool and verify that node auto-repair is enabled. Node auto- repair is configured per node pool and automatically monitors nodes for failed health checks. If a node remains unhealthy for an extended period, GKE initiates a repair action.
gcloud container node-pools describe $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.management'
• Expected output: the output should include autoRepair set to true.
Internal Only - General
{
"autoRepair": true
}
If autoRepair is false, blank, or missing, node auto-repair is not enabled for the node pool and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-497B228C">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-497B228Cr01_rule" weight="10.0" severity="high">
      <version>5.5.3</version>
      <title>Verify node auto-upgrade is enabled for GKE node pools</title>
      <description>&lt;VulnDiscussion&gt;Verify node auto-upgrade is enabled for GKE node pools so nodes receive supported Kubernetes versions, node image updates, and security fixes through the managed upgrade process. Node auto-upgrade is enabled by default for new GKE node pools, but it should still be reviewed for older node pools.

Rationale:
Node auto-upgrade helps keep node pools aligned with supported GKE versions and current node image updates without requiring manual upgrade coordination for every patch or security release. When enabled, GKE automatically schedules eligible node pools for upgrade as new auto-upgrade targets become available, while considering release channel behavior, maintenance policies, and other upgrade constraints.
From a security and reliability perspective, keeping node auto-upgrade enabled reduces the risk of running outdated node software, unsupported Kubernetes versions, or node images that are missing important fixes. It also helps keep nodes compatible with the upgraded control plane, because GKE nodes must run the same or an earlier version than the control plane.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.3</dc:identifier>
      </reference>
      <fixtext fixref="F-497B228Cr01_fix">Enable node auto-upgrade for any GKE node pool where the audit result is not true.
This allows GKE to automatically schedule eligible node pools for upgrades when upgrade targets become available, subject to maintenance windows, maintenance exclusions, release channel behavior, and upgrade rollout constraints.
gcloud container node-pools update $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-autoupgrade
After remediation, rerun the audit command and confirm that management.autoUpgrade returns true. If multiple node pools are present, repeat the audit and remediation for each applicable node pool.</fixtext>
      <fix id="F-497B228Cr01_fix"/>
      <check system="C-497B228C_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE node pool and verify that node auto-upgrade is enabled. Node auto- upgrade is configured per node pool and helps keep nodes updated with supported Kubernetes versions, node image updates, and security fixes through the managed GKE upgrade process.
Internal Only - General
gcloud container node-pools describe $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.management'
• Expected output: the output should include autoUpgrade set to true.
{
"autoUpgrade": true
}
If autoUpgrade is false, blank, or missing, node auto-upgrade is not enabled for the node pool and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-F84F6F7B">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-F84F6F7Br01_rule" weight="10.0" severity="medium">
      <version>5.5.4</version>
      <title>Enroll GKE clusters in release channels for automated version management</title>
      <description>&lt;VulnDiscussion&gt;Enroll GKE clusters in an appropriate release channel to automate version management and align cluster upgrades with the organization’s required balance of feature availability, upgrade predictability, and production stability. Release channels let teams choose how quickly clusters receive supported Kubernetes versions, patches, security fixes, and GKE updates without manually selecting every version.

Rationale:
Release channels provide a managed approach for keeping GKE clusters on supported versions. The selected channel determines which GKE versions the cluster can run and how quickly the cluster receives new Kubernetes versions, patches, security fixes, and GKE platform updates. This reduces version management complexity and helps avoid clusters remaining on outdated or unsupported versions.
The Regular channel is the default and is appropriate for many production clusters that need a balance of stability and timely feature availability. The Stable channel prioritizes production stability with slower version adoption, while the Rapid channel provides faster access to newer GKE features. The Extended channel can be used when longer support for a minor version is required. Critical security patches are delivered to all release channels.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.4</dc:identifier>
      </reference>
      <fixtext fixref="F-F84F6F7Br01_fix">Enroll the existing GKE cluster in an approved release channel rather than recreating the cluster. Use regular or stable unless the organization has approved another channel for a documented use case. Before changing the release channel, confirm that the cluster control plane version is available in the target channel and configure maintenance windows or exclusions if upgrade timing needs to be controlled.
gcloud container clusters update $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--release-channel=$RELEASE_CHANNEL
After remediation, rerun the audit command and verify that.releaseChannel.channel returns the approved channel. For clusters enrolled in a release channel, node auto- upgrade is managed as part of the GKE release channel upgrade process, so maintenance policies should be reviewed to reduce workload disruption during automatic upgrades.</fixtext>
      <fix id="F-F84F6F7Br01_fix"/>
      <check system="C-F84F6F7B_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that it is enrolled in an approved release channel for automated version management. Release channels help manage GKE control plane and node upgrades by aligning the cluster to an approved channel such as REGULAR or STABLE, based on the organization’s required balance of stability and update frequency.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format=json | jq .releaseChannel.channel
• Expected output: the output should return an approved release channel, typically REGULAR or STABLE for production clusters. If the output is empty, null, or not an approved channel, the cluster is not aligned with this control. RAPID or EXTENDED should only be used when there is a documented operational requirement.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-AB2871AB">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-AB2871ABr01_rule" weight="10.0" severity="medium">
      <version>5.5.5</version>
      <title>Verify Shielded GKE Nodes are enabled for GKE clusters</title>
      <description>&lt;VulnDiscussion&gt;Verify Shielded GKE Nodes are enabled for GKE clusters to provide stronger node identity and integrity protections for worker nodes. Shielded GKE Nodes help ensure that worker nodes are based on trusted boot components and can be cryptographically verified by the GKE control plane before joining the cluster.

Rationale:
Shielded GKE Nodes help protect clusters from node impersonation and boot level compromise by using Shielded VM capabilities to provide verifiable node identity and integrity. Without this protection, an attacker who compromises a workload or node could attempt to access bootstrap credentials and impersonate a node in the cluster.
For stronger node hardening, Shielded GKE Nodes should be used with Secure Boot and Integrity Monitoring where supported by the workload and node pool design.
Secure Boot validates boot components during startup, while Integrity Monitoring validates the boot sequence against a known baseline and records pass or fail results in Cloud Logging.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.5</dc:identifier>
      </reference>
      <fixtext fixref="F-AB2871ABr01_fix">Enable Shielded GKE Nodes for any GKE cluster where the audit result is not true. This updates the cluster so worker nodes are recreated as Shielded VMs and can be cryptographically verified by the GKE control plane before joining the cluster.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-shielded-nodes
Rerun the audit command and confirm that shieldedNodes.enabled returns true.
Enabling Shielded GKE Nodes on an existing cluster immediately recreates the control plane and recreates worker nodes in a rolling manner while respecting maintenance availability, so plan the change during an approved maintenance window.</fixtext>
      <fix id="F-AB2871ABr01_fix"/>
      <check system="C-AB2871AB_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that Shielded GKE Nodes are enabled. Shielded GKE Nodes are a cluster level setting that affects worker nodes and helps provide verifiable node identity and integrity protections.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.shieldedNodes'
• Expected output: the output should show enabled set to true.
{
"enabled": true
}
If enabled is false, blank, or missing, Shielded GKE Nodes are not enabled for the cluster and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-096C94A4">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-096C94A4r01_rule" weight="10.0" severity="medium">
      <version>5.5.6</version>
      <title>Verify Shielded GKE Node integrity monitoring is enabled</title>
      <description>&lt;VulnDiscussion&gt;Verify Shielded GKE Node integrity monitoring is enabled for GKE node pools. Integrity monitoring validates each Shielded VM boot sequence against a known good baseline and records pass or fail results so administrators can detect potential boot level or kernel level integrity issues on worker nodes.

Rationale:
Integrity monitoring strengthens node security by measuring the Shielded VM boot sequence and comparing it to an expected baseline. This helps identify unexpected changes in boot components, firmware, kernel, or other early boot measurements that could indicate tampering, rootkit activity, or unauthorized low level modification.
This control complements Shielded GKE Nodes by improving visibility into node integrity events. Integrity monitoring does not replace workload security, image controls, or runtime detection, but it provides an important signal in Cloud Logging that can be reviewed, monitored, or used for alerting when boot integrity checks fail.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.6</dc:identifier>
      </reference>
      <fixtext fixref="F-096C94A4r01_fix">Create a replacement node pool with Shielded GKE Node integrity monitoring enabled.
Integrity monitoring is enabled by default for new GKE node pools, but it can also be explicitly enabled during node pool creation by using the --shielded-integrity- monitoring option.
gcloud container node-pools create $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--shielded-integrity-monitoring
Migrate workloads from the noncompliant node pool to the new node pool, validate workload health, and then drain and delete the old node pool. After remediation, rerun the audit command and confirm that enableIntegrityMonitoring returns true.</fixtext>
      <fix id="F-096C94A4r01_fix"/>
      <check system="C-096C94A4_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE node pool and verify that Shielded GKE Node integrity monitoring is enabled. Integrity monitoring is a node pool setting that validates the Shielded VM boot sequence against a known good baseline and records pass or fail results in Cloud Logging.
gcloud container node-pools describe $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq .config.shieldedInstanceConfig
• Expected output: the output should include enableIntegrityMonitoring set to true.
Internal Only - General
{
"enableIntegrityMonitoring": true
}
If enableIntegrityMonitoring is false, blank, or missing, integrity monitoring is not enabled for the node pool and should be remediated. Secure Boot is a separate Shielded node setting and should not be used as the determining value for this specific control.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-F9F27032">
    <title>5.5 Node Configuration and Maintenance</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-F9F27032r01_rule" weight="10.0" severity="high">
      <version>5.5.7</version>
      <title>Ensure Secure Boot is enabled for Shielded GKE node pools</title>
      <description>&lt;VulnDiscussion&gt;Enable Secure Boot for Shielded GKE node pools to verify the digital signature of node VM boot components, including the kernel and bootloader, during the boot process.
Secure Boot helps ensure that only trusted boot components are allowed to run when the node starts.

Rationale:
Secure Boot helps reduce the risk of boot level compromise by verifying the authenticity of node boot components before the operating system starts. If signature verification fails, the boot process is halted, which helps prevent nodes from starting with tampered bootloaders, kernels, or other unauthorized low level components.
This control complements Shielded GKE Nodes and integrity monitoring by strengthening node startup integrity. Shielded GKE Nodes, Secure Boot, and integrity monitoring are independent features, so Secure Boot should be explicitly reviewed for each applicable node pool rather than assumed from the cluster level Shielded GKE Nodes setting.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.5.7</dc:identifier>
      </reference>
      <fixtext fixref="F-F9F27032r01_fix">Create a replacement node pool with Secure Boot enabled for any node pool where the audit result is not true. Secure Boot is disabled by default for node pools and should be explicitly enabled when creating the replacement node pool.
gcloud container node-pools create $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--shielded-secure-boot
• Before migrating workloads, validate that Secure Boot will not block required boot components or node level dependencies. On Ubuntu nodes, Secure Boot blocks unsigned third party kernel modules from loading.
Migrate workloads from the noncompliant node pool to the new Secure Boot enabled node pool, validate workload health, and then drain and delete the old node pool. After remediation, rerun the audit command and confirm that enableSecureBoot returns true.</fixtext>
      <fix id="F-F9F27032r01_fix"/>
      <check system="C-F9F27032_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE node pool and verify that Secure Boot is enabled for Shielded GKE node pools. Secure Boot is a node pool setting that authenticates node VM boot components, such as the kernel and bootloader, during the boot process.
gcloud container node-pools describe $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq .config.shieldedInstanceConfig
• Expected output: The output should include enableSecureBoot set to true.
Internal Only - General
{
"enableSecureBoot": true
}
If enableSecureBoot is false, blank, or missing, Secure Boot is not enabled for the node pool and should be remediated. Secure Boot is separate from Shielded GKE Nodes and integrity monitoring, so those settings should not be used as the determining value for this specific control.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-F15935D7">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-F15935D7r01_rule" weight="10.0" severity="high">
      <version>5.6.1</version>
      <title>Enable VPC Flow Logs and Intranode Visibility</title>
      <description>&lt;VulnDiscussion&gt;Enable VPC Flow Logs on the subnet used by GKE nodes and enable intranode visibility on the GKE cluster to improve visibility into Pod traffic. This combination allows network flow logging to include traffic between Pods, including traffic between Pods running on the same worker node.

Rationale:
VPC Flow Logs capture network flow records for subnet traffic, but Pod to Pod traffic on the same node requires GKE intranode visibility so that the traffic is visible to the VPC network. Enabling both controls improves traffic observability for security monitoring, network troubleshooting, forensics, and validation of expected workload communication patterns.
Without intranode visibility, traffic between Pods on the same node can bypass VPC level visibility, reducing the usefulness of VPC Flow Logs for Kubernetes east west traffic analysis. Intranode visibility sends Pod to Pod traffic on the same node through the VPC networking path, allowing VPC Flow Logs and other VPC features to observe that traffic.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.1</dc:identifier>
      </reference>
      <fixtext fixref="F-F15935D7r01_fix">Enable intranode visibility on the GKE cluster when the audit shows that networkConfig.enableIntraNodeVisibility is not true. Enabling intranode visibility makes Pod to Pod traffic on the same node visible to the VPC network path so it can be captured by VPC Flow Logs and other VPC features. Plan the change during an approved maintenance window because enabling intranode visibility on an existing cluster can restart components in the control plane and worker nodes.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-intra-node-visibility
After intranode visibility is enabled, review the subnet used by the GKE cluster and confirm that VPC Flow Logs are enabled for that subnet. VPC Flow Logs are configured at the subnet level, while intranode visibility is configured at the cluster level, so both settings must be in place for complete network flow visibility.
• If VPC Flow Logs are not enabled for the subnet used by the GKE cluster, enable flow logging on that subnet.
• Configure VPC Flow Logs settings such as aggregation interval, sampling, metadata inclusion, and log filters based on security monitoring, troubleshooting and retention.
• Rerun the audit check and confirm that intranode visibility returns true and that the GKE subnet has an effective VPC Flow Logs configuration.
Internal Only - General</fixtext>
      <fix id="F-F15935D7r01_fix"/>
      <check system="C-F15935D7_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that intranode visibility is enabled. Intranode visibility is enabled at the cluster level and makes Pod to Pod traffic on the same node visible to the VPC network path so VPC Flow Logs can capture that traffic.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.networkConfig.enableIntraNodeVisibility'
• Expected output: true. If the result is false, blank, or missing, intranode visibility is not enabled for the cluster and should be remediated.
Internal Only - General Identify the subnet used by the GKE cluster so the VPC Flow Logs configuration can be reviewed for the correct network path. GKE clusters use a VPC subnet for node and Pod networking, and VPC Flow Logs must apply to that subnet to provide network flow visibility for the cluster.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format='value(subnetwork)'
Use the subnet returned by the command as $SUBNET_NAME, and confirm the subnet region is set as $REGION.
Review the subnet used by the GKE cluster and verify that VPC Flow Logs are enabled.
gcloud compute networks subnets describe $SUBNET_NAME \
--region $REGION \
--project $PROJECT_ID \
--format='value(enableFlowLogs)'
• Expected output: true. If the result is false, blank, or missing, VPC Flow Logs are not enabled for the subnet and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-CA2A21F5">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-CA2A21F5r01_rule" weight="10.0" severity="medium">
      <version>5.6.2</version>
      <title>Use VPC native GKE clusters with alias IP address ranges</title>
      <description>&lt;VulnDiscussion&gt;Use VPC native GKE clusters with alias IP address ranges so Pod and Service IP addresses are allocated from subnet secondary IP address ranges in the cluster’s VPC network. This provides native VPC routing for GKE workloads and avoids relying on custom static routes for Pod routing.

Rationale:
VPC native clusters improve network planning and control because Pod IP addresses are reserved in the VPC network before Pods are created, which helps prevent IP conflicts with other VPC resources. Pod IP ranges also do not depend on custom static routes, reducing route scale concerns and allowing automatically generated subnet routes to handle cluster routing.
Using alias IP address ranges also allows firewall rules to target Pod IP address ranges separately from node IP addresses. This improves network segmentation options and supports better visibility and control for GKE workload traffic across the VPC network and connected networks.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.2</dc:identifier>
      </reference>
      <fixtext fixref="F-CA2A21F5r01_fix">VPC native networking is selected at cluster creation and cannot be enabled on an existing routes based cluster. To remediate a noncompliant cluster, create a new GKE cluster with alias IP address ranges enabled, migrate workloads to the new cluster, validate application connectivity, and decommission the routes based cluster after migration is complete.
gcloud container clusters create $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-ip-alias
For production clusters, define or select appropriate subnet secondary IP address ranges for Pods and Services before cluster creation. Proper IP range planning helps avoid Pod or Service IP exhaustion as the cluster scales.
• Note: VPC native networking is selected at cluster creation and cannot be enabled on an existing routes based cluster. To remediate a noncompliant cluster, create a replacement GKE cluster with alias IP address ranges enabled, migrate workloads to the new cluster, validate workload and network connectivity, and decommission the routes based cluster after migration is complete.</fixtext>
      <fix id="F-CA2A21F5r01_fix"/>
      <check system="C-CA2A21F5_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that it uses VPC native networking with alias IP address ranges. VPC native clusters allocate Pod and Service IP addresses from subnet secondary IP address ranges, while routes based clusters use custom static routes for Pod routing.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.ipAllocationPolicy.useIpAliases'
• Expected output: true. If the result is false, null, blank, or missing, the cluster is not using VPC native networking with alias IP address ranges and should be remediated by creating a replacement VPC native cluster.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-28A5CD83">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-28A5CD83r01_rule" weight="10.0" severity="high">
      <version>5.6.3</version>
      <title>Restrict GKE control plane access with authorized networks</title>
      <description>&lt;VulnDiscussion&gt;Restrict GKE control plane access by enabling authorized networks for IP based control plane endpoints and allowing only approved CIDR ranges. This reduces exposure of the Kubernetes API server by limiting which source IP ranges can reach the control plane over the configured IP endpoint.

Rationale:
GKE control plane access can use DNS based endpoints and IP based endpoints.
When IP based endpoints are enabled, authorized networks provide an IP based access control layer that limits control plane reachability to approved CIDR ranges.
Without authorized networks, the control plane IP endpoint can be reachable from any external IP address, including public internet addresses and Google Cloud external IP addresses, subject to authentication and authorization.
Restricting access to approved CIDR ranges reduces the control plane attack surface and helps protect the Kubernetes API server if credentials, certificates, or automation tokens are exposed. For stronger control plane isolation, authorized networks should be combined with private endpoint configuration where appropriate, denial of broad Google Cloud external IP access, and IAM based authentication controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.3</dc:identifier>
      </reference>
      <fixtext fixref="F-28A5CD83r01_fix">Enable control plane authorized networks when the audit shows that masterAuthorizedNetworksConfig is missing, does not show authorized networks as enabled, does not include approved cidrBlocks, or includes broad ranges such as 0.0.0.0/0. Authorized networks should include only trusted administrative, bastion, VPN, CI/CD, or automation source ranges that require access to the GKE control plane IP endpoint, and use authorized networks when IP based control plane endpoints are enabled.
Internal Only - General
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-master-authorized-networks \
--master-authorized-networks &lt;authorized-cidrs&gt;
If broad access from Google Cloud external IP addresses is not explicitly approved, disable that access so external IP addresses from Google Cloud are not automatically allowed to reach the control plane.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--no-enable-google-cloud-access
If the cluster uses the control plane internal endpoint and the same CIDR restrictions should apply to private endpoint access, enable authorized network enforcement on the private endpoint.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-authorized-networks-on-private-endpoint
• Note: Rerun the audit command and confirm that masterAuthorizedNetworksConfig shows authorized networks enabled, cidrBlocks contains only approved trusted source ranges, and gcpPublicCidrsAccessEnabled is false unless explicitly approved.</fixtext>
      <fix id="F-28A5CD83r01_fix"/>
      <check system="C-28A5CD83_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that control plane authorized networks are configured to restrict access to approved CIDR ranges.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.masterAuthorizedNetworksConfig'
• Expected output should show the following:
{
"enabled": true,
"cidrBlocks": [
{
"cidrBlock": "203.0.113.0/24",
"displayName": "approved-admin-range"
}
],
"gcpPublicCidrsAccessEnabled": false
}
Review the command line statement for the following:
• enabled should be true, indicating authorized networks are enabled for the control plane.
• cidrBlocks should list only approved trusted source ranges, such as administrative, bastion, VPN, CI/CD, or automation networks.
• gcpPublicCidrsAccessEnabled should be false unless access from Google Cloud external IP ranges is explicitly approved.
• If the output only shows gcpPublicCidrsAccessEnabled: false, authorized networks are not fully configured for this control because no approved CIDR ranges are listed.
• If the output is null, {}, missing enabled: true, missing cidrBlocks, or includes broad ranges such as 0.0.0.0/0, the cluster should be reviewed and remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-4BF398D2">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-4BF398D2r01_rule" weight="10.0" severity="high">
      <version>5.6.4</version>
      <title>Use DNS based access or private GKE control plane endpoints</title>
      <description>&lt;VulnDiscussion&gt;Use DNS based access or private GKE control plane endpoints to reduce Kubernetes API server exposure. Prefer DNS based control plane access where possible, and if IP based access is required, disable the external IP based endpoint or restrict it to trusted networks only.

Rationale:
The GKE control plane supports DNS based endpoints and IP based endpoints. The DNS based endpoint provides a stable fully qualified domain name for the control plane and can be protected using IAM, authentication tokens, and VPC Service Controls. This approach avoids exposing the control plane through a public IP address and provides a more flexible access model for administrators, automation, and private clients.
When IP based endpoints are required, disabling the external IP based endpoint or using authorized networks limits who can reach the Kubernetes API server. This reduces the control plane attack surface and helps protect the cluster if credentials, certificates, or automation tokens are exposed. If an external IP based endpoint must remain enabled, access should be restricted to approved CIDR ranges only.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.4</dc:identifier>
      </reference>
      <fixtext fixref="F-4BF398D2r01_fix">Remediate existing GKE clusters by updating the control plane access configuration.
Recreating the cluster is not required for this control. Choose the access model that matches the organization’s approved management path.
Method 1: Use DNS based control plane access and disable IP based endpoint access where appropriate:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-dns-access \
--no-enable-ip-access
Method 2: Use IP based access with the external endpoint disabled and the private endpoint enabled:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-ip-access \
--enable-private-endpoint
If the public IP based endpoint must remain enabled, restrict it with authorized networks and use only approved CIDR ranges. Rerun the audit command and confirm that the selected access model is reflected in controlPlaneEndpointsConfig. If using DNS based access, update kubeconfig to use the DNS endpoint as needed.</fixtext>
      <fix id="F-4BF398D2r01_fix"/>
      <check system="C-4BF398D2_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that control plane access is configured through either DNS based access or private IP based access with the external endpoint disabled. The preferred current approach is to use DNS based access where possible. If IP based access is required, the public endpoint should be disabled or tightly restricted.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.controlPlaneEndpointsConfig'
Review the command line output for the following:
Internal Only - General
• For DNS based access, allowExternalTraffic should be true, enableK8sTokensViaDns should be true, enableK8sCertsViaDns should be true, and dnsEndpointConfig.endpoint should be populated.
• For private IP based access, ipEndpointsConfig.enabled should be true, ipEndpointsConfig.enablePublicEndpoint should be false, and ipEndpointsConfig.privateEndpoint should be populated.
• If the external IP based endpoint remains enabled, authorized networks should restrict access to approved CIDR ranges only.
• If neither DNS based access nor private IP based access is configured, or if the public IP based endpoint is broadly reachable, the cluster should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-0C3AAA74">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-0C3AAA74r01_rule" weight="10.0" severity="medium">
      <version>5.6.5</version>
      <title>Ensure private GKE nodes are configured without external IP addresses</title>
      <description>&lt;VulnDiscussion&gt;Configure GKE nodes without external IP addresses so worker nodes are reachable only through internal network paths. Private nodes reduce direct exposure of the node network interface to external clients while still allowing the cluster to run workloads and communicate with the control plane through internal connectivity.

Rationale:
Nodes with external IP addresses increase the externally reachable surface of the cluster infrastructure. Configuring private nodes limits direct inbound access to the node VMs from the internet and helps ensure node administration, workload traffic, and supporting services use approved private connectivity patterns, firewall rules, and controlled egress paths.
Private nodes also support stronger network isolation because workloads run on nodes with only internal IP addresses. If internet or Google API access is required, it should be provided through controlled paths such as Cloud NAT, Private Google Access, or approved private connectivity rather than direct external IP assignment on the nodes.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.5</dc:identifier>
      </reference>
      <fixtext fixref="F-0C3AAA74r01_fix">Remediate by enabling private nodes at the cluster level for new node pools and at the node pool level for existing node pools. Private nodes provision GKE nodes with internal IP addresses only. GKE does not automatically modify existing node pools when the cluster level setting is changed, so existing node pools must be updated separately.
Enable private nodes as the cluster level default for new node pools:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-private-nodes \
--enable-ip-alias
Enable private nodes on an existing node pool:
gcloud container node-pools update $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-private-nodes \
--enable-ip-alias
• Note: After remediation, rerun the audit commands and confirm that the cluster level default and each applicable node pool return true. Validate that private nodes still have required outbound access through approved paths such as Cloud NAT, Private Google Access, or private connectivity for image pulls, package downloads, Google APIs, and third party services.
Internal Only - General</fixtext>
      <fix id="F-0C3AAA74r01_fix"/>
      <check system="C-0C3AAA74_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and node pools to verify that private nodes are configured without external IP addresses. Private nodes can be configured at the cluster level as the default for new node pools, or directly at the node pool level. For this control, each node pool should be reviewed because node pool level settings can override the cluster level default.
Check the cluster level private node default:
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.privateClusterConfig.enablePrivateNodes'
• Expected output: true. If the result is false, blank, or missing, the cluster level default does not require new node pools to use private nodes.
Check each node pool for private node configuration:
gcloud container node-pools describe $POOL_NAME \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.networkConfig.enablePrivateNodes'
• Expected output: true. If the result is false, blank, or missing, the node pool should be reviewed and remediated because it might provision nodes with external IP addresses.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-7B76E18B">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-7B76E18Br01_rule" weight="10.0" severity="high">
      <version>5.6.6</version>
      <title>Apply least privilege firewall rules to GKE worker nodes</title>
      <description>&lt;VulnDiscussion&gt;Apply least privilege VPC firewall rules and firewall policies to GKE worker nodes to restrict ingress and egress traffic to approved sources, destinations, ports, and protocols. Firewall rules should target only the intended GKE nodes by using appropriate network tags, service accounts, or other approved targeting methods.

Rationale:
GKE worker nodes are Compute Engine VMs in a VPC network, so VPC firewall rules and firewall policies apply at the node level. Limiting node ingress reduces exposure to unnecessary management ports, NodePort ranges, workload ports, and unapproved source networks. Limiting egress helps reduce the ability of a compromised workload or node to reach unauthorized internal systems, internet destinations, or unmanaged services.
GKE creates default firewall rules for required cluster functionality, and additional rules can be created for Services such as load balancers. Custom firewall rules should not be overly permissive or override required GKE rules in a way that increases exposure or breaks cluster operations. Node level firewalling should also be used with Kubernetes Network Policies where Pod level traffic control is required.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.6</dc:identifier>
      </reference>
      <fixtext fixref="F-7B76E18Br01_fix">Apply least privilege firewall controls to GKE worker nodes by reducing broad ingress and egress access while preserving required cluster functionality. Firewall rules should be targeted carefully so they protect the intended node pools without unintentionally affecting unrelated VM instances in the same VPC.
• Update GKE worker node firewall rules so only approved sources, destinations, ports, and protocols are allowed.
Internal Only - General
• Target firewall rules to the specific GKE node tags or node service accounts used by the cluster. Avoid rules that apply broadly to the entire VPC unless there is a documented requirement.
• Remove or replace overly permissive rules, especially rules that allow broad ingress from 0.0.0.0/0, unnecessary management ports, broad NodePort ranges, or unrestricted egress.
• Create or update firewall rules to allow only required traffic, using approved source ranges, destination ranges, ports, protocols, node tags, or node service accounts.
• Preserve required GKE created firewall rules for cluster operations, load balancers, health checks, control plane to node communication, DNS, logging, monitoring, and image pulls unless replacement rules have been fully tested.</fixtext>
      <fix id="F-7B76E18Br01_fix"/>
      <check system="C-7B76E18B_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and identify the VPC network, subnet, node pool network tags, and node service accounts used by the cluster. These values are used to determine which VPC firewall rules or firewall policies apply to the GKE worker nodes.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '{
network: .network,
subnetwork: .subnetwork,
nodePools: [
.nodePools[] | {
name: .name,
tags: (.config.tags // []),
serviceAccount: .config.serviceAccount
}
]
}'
• Expected output: Firewall rules that apply to the cluster VPC network and verify that ingress and egress access is limited to approved sources, destinations, ports, and protocols.
gcloud compute firewall-rules list \
--project $PROJECT_ID \
--format="table(
name,
network,
direction,
priority,
sourceRanges.list():label=SRC_RANGES,
destinationRanges.list():label=DEST_RANGES,
allowed[].map().firewall_rule().list():label=ALLOW,
denied[].map().firewall_rule().list():label=DENY,
targetTags.list():label=TARGET_TAGS,
targetServiceAccounts.list():label=TARGET_SVC_ACCT,
disabled
)"
• Expected output: Firewall rules that apply to GKE worker nodes should be least privilege and should not expose unnecessary management ports, broad NodePort ranges, workload ports, or unrestricted egress. Rules should not allow broad ranges such as 0.0.0.0/0 unless there is a documented requirement, and rules without target tags or target service accounts should be reviewed because they can apply broadly across the VPC network.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-63FCE561">
    <title>5.6 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-63FCE561r01_rule" weight="10.0" severity="high">
      <version>5.6.7</version>
      <title>Use Google managed SSL certificates for GKE Ingress TLS</title>
      <description>&lt;VulnDiscussion&gt;Use Google managed SSL certificates for GKE Ingress TLS to encrypt client traffic between external users and the Google Cloud load balancer. Google provisions and renews these certificates for the configured domains, reducing manual certificate lifecycle management for supported GKE Ingress use cases.

Rationale:
TLS encryption for GKE Ingress helps protect application traffic between clients and the load balancer by preventing plaintext exposure over external networks. Google managed SSL certificates reduce operational risk by having Google provision, manage, and automatically renew domain validated certificates for supported load balancer and Ingress configurations.
This control is most appropriate for HTTPS traffic exposed through GKE Ingress. It does not apply to TCP or UDP load balancers created through a Service of type LoadBalancer, and GKE Ingress does not support certificates managed by Certificate Manager. Use Gateway API if Certificate Manager managed certificates are required.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.6.7</dc:identifier>
      </reference>
      <fixtext fixref="F-63FCE561r01_fix">Remediate public HTTPS workloads by using GKE Ingress with Google managed SSL certificates where this pattern is appropriate. This applies to HTTPS traffic terminated by the external Application Load Balancer through GKE Ingress and does not apply to TCP or UDP workloads exposed through Service objects of type LoadBalancer.
• For public HTTP or HTTPS workloads exposed directly through Service objects of type LoadBalancer, evaluate whether they should be exposed through GKE Ingress so Google managed SSL certificates can be used.
• Create a ManagedCertificate object in the same namespace as the Ingress and define the approved non wildcard domain names for the certificate.
• Update the Ingress metadata to include the networking.gke.io/managed- certificates annotation with the ManagedCertificate resource name.
Internal Only - General Multiple managed certificates can be listed as comma separated values when needed.
• Confirm that DNS records for the certificate domains point to the load balancer IP address, because Google must validate domain ownership before the certificate becomes active.
• After the certificate is active, verify that the Ingress serves HTTPS traffic using the Google managed certificate and remove any obsolete self managed certificate references that are no longer required.</fixtext>
      <fix id="F-63FCE561r01_fix"/>
      <check system="C-63FCE561_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review public application exposure and identify workloads using Service objects of type LoadBalancer. Google managed SSL certificates for GKE Ingress apply to HTTPS load balancers created through Ingress, not TCP or UDP load balancers created directly from Service objects. Public HTTP or HTTPS workloads exposed through LoadBalancer services should be reviewed to determine whether GKE Ingress is the better TLS termination pattern.
Internal Only - General
kubectl get svc -A -o json | jq '.items[] |
select(.spec.type=="LoadBalancer")'
• Expected output: Check for GKE Ingress resources and confirm that HTTPS Ingresses reference one or more Google managed certificates through the networking.gke.io/managed-certificates annotation.
kubectl get ingress -A -o json | jq '.items[] | {
namespace: .metadata.namespace,
name: .metadata.name,
managedCertificates: .metadata.annotations."networking.gke.io/managed-
certificates",
status: .status
}'
• Expected output: Each applicable HTTPS Ingress should include the networking.gke.io/managed-certificates annotation with one or more ManagedCertificate resource names. The annotation value is a comma separated list of ManagedCertificate objects that must exist in the same namespace as the Ingress.
{
"networking.gke.io/managed-certificates": "example-certificate"
}
Verify that the referenced ManagedCertificate objects exist and are active.
kubectl get managedcertificates -A
• Expected output: Each certificate referenced by an Ingress should exist in the same namespace as that Ingress, cover the expected domain names, and show an active or successfully provisioned status. If the certificate is missing, pending, failed, or not referenced by the Ingress annotation, Google managed SSL certificates are not correctly configured for that Ingress.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-3BC20EE1">
    <title>5.7 Logging</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-3BC20EE1r01_rule" weight="10.0" severity="medium">
      <version>5.7.1</version>
      <title>Enable Cloud Logging and Cloud Monitoring for GKE clusters</title>
      <description>&lt;VulnDiscussion&gt;Enable Cloud Logging and Cloud Monitoring for GKE clusters so cluster, system, and workload telemetry is sent to centralized Google Cloud observability services. This helps preserve logs and metrics outside the cluster for troubleshooting, security, performance analysis, and alerts.

Rationale:
GKE logs stored only on nodes or within the cluster are not durable because local container logs, system logs, and cluster events can be removed as Pods are deleted, disks fill, or logs age out. Cloud Logging provides a dedicated persistent datastore where events can be searched, analyzed, retained, routed, and correlated after a security event.
Cloud Monitoring collects GKE metrics that can be used for dashboards, alerting, trend analysis, and integration with other monitoring workflows. Enabling both services improves visibility across clusters and helps detect failures, capacity, workload changes, and security.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.7.1</dc:identifier>
      </reference>
      <fixtext fixref="F-3BC20EE1r01_fix">Enable Cloud Logging and Cloud Monitoring for the required GKE components. At minimum, enable system logging and system monitoring so cluster operations, troubleshooting, and security investigations have centralized telemetry. Enable workload logging when application logs should be collected in Cloud Logging.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--logging=SYSTEM,WORKLOAD \
--monitoring=SYSTEM
• Note: If additional observability is required, enable the appropriate monitoring components such as API server, scheduler, controller manager, kube state, cAdvisor, kubelet, storage, or GPU metrics based on the cluster’s operational requirements.</fixtext>
      <fix id="F-3BC20EE1r01_fix"/>
      <check system="C-3BC20EE1_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that Cloud Logging and Cloud Monitoring are enabled for required cluster components. GKE component based configuration shows which log and metric sources are collected, while empty component lists can indicate that logging or monitoring collection has been disabled.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '{
loggingService: .loggingService,
monitoringService: .monitoringService,
loggingComponents: .loggingConfig.componentConfig.enableComponents,
monitoringComponents: .monitoringConfig.componentConfig.enableComponents,
managedPrometheus: .monitoringConfig.managedPrometheusConfig.enabled
}'
• Expected output: loggingService should not be none, monitoringService should not be none, loggingComponents should include SYSTEM_COMPONENTS and WORKLOADS unless workload logging is intentionally excluded, and monitoringComponents should include at least SYSTEM_COMPONENTS. If logging or monitoring components are empty, missing, or set to NONE, Cloud Logging or Cloud Monitoring is not enabled as expected and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-C674AD2E">
    <title>5.7 Logging</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-C674AD2Er01_rule" weight="10.0" severity="high">
      <version>5.7.2</version>
      <title>Enable Linux auditd logging for Container Optimized OS GKE nodes</title>
      <description>&lt;VulnDiscussion&gt;Enable Linux auditd logging for GKE nodes running Container Optimized OS to collect detailed operating system audit events from the node. These logs provide host level visibility into activity such as login attempts, error messages, and binary executions, and can be forwarded to Cloud Logging using the supported logging agent configuration.

Rationale:
Linux auditd logging improves node level security visibility by capturing operating system events that are not fully represented by Kubernetes workload logs, Cloud Audit Logs, or standard cluster telemetry. This information can help support troubleshooting, incident investigation, and detection of suspicious host level activity on GKE nodes running Container Optimized OS.
Because GKE nodes run workloads that may interact with the underlying host, detailed operating system audit logs can provide additional evidence during a security investigation, including process execution, authentication activity, and other node level events. This control is especially useful when host level visibility is required beyond normal GKE logging and monitoring.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.7.2</dc:identifier>
      </reference>
      <fixtext fixref="F-C674AD2Er01_fix">Deploy an approved Linux auditd logging DaemonSet when the audit returns no matching auditd or cos-auditd DaemonSets or Pods. The DaemonSet should run on eligible Container Optimized OS nodes and forward Linux audit events to Cloud Logging or another approved log destination.
• Download the current GKE example manifest for Linux auditd logging on Container Optimized OS nodes.
curl https://raw.githubusercontent.com/GoogleCloudPlatform/k8s-node-
tools/master/troubleshooting/os-audit/cos-auditd-logging.yaml &gt; cos-auditd-
logging.yaml
• Review the manifest before deployment, including the namespace, DaemonSet name, node selectors, ConfigMap settings, and logging destination.
• Replace any sample logging agent image with an approved image from a trusted source and pin it by digest where required.
• Tune the auditd and fluent-bit configuration to control log volume, filtering, retention, routing, and cost.
• Deploy the approved manifest to the cluster.
kubectl apply -f cos-auditd-logging.yaml
• After deployment, rerun the audit commands and confirm that the approved DaemonSet exists and that the audit logging Pods are running on eligible Container Optimized OS nodes.</fixtext>
      <fix id="F-C674AD2Er01_fix"/>
      <check system="C-C674AD2E_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the cluster and verify that Linux auditd logging is deployed for Container Optimized OS nodes using an approved DaemonSet.
Search for an audit logging DaemonSet across all namespaces:
kubectl get daemonsets -A | grep -Ei 'auditd|cos-auditd'
• Expected output: An approved audit logging DaemonSet, such as cos-auditd- logging, should be present and show the desired Pods as available. If no matching DaemonSet is returned, Linux auditd logging is not deployed.
Verify that the audit logging Pods are running:
Internal Only - General
kubectl get pods -A | grep -Ei 'auditd|cos-auditd'
• Expected output: Sudit logging Pods should be in a Running state on eligible Container Optimized OS nodes. If no matching Pods are returned, or if Pods are missing, pending, failed, or not scheduled to eligible nodes, Linux auditd logging is not operating as expected.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-F5A482A8">
    <title>5.8 Authentication and Authorization</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-F5A482A8r01_rule" weight="10.0" severity="medium">
      <version>5.8.1</version>
      <title>Disable legacy client certificate authentication for the GKE API server</title>
      <description>&lt;VulnDiscussion&gt;Disable legacy client certificate authentication for the GKE API server by ensuring GKE does not issue a pre provisioned client certificate for cluster access. Use supported identity based authentication methods, such as OAuth tokens through gcloud, service account bearer tokens, or approved external identity provider authentication instead of long lived static client certificates.

Rationale:
Legacy client certificates are harder to govern than identity based authentication because they are long lived credentials and are not managed through normal IAM user lifecycle controls. If a client certificate is exposed, access can persist until the certificate expires or the cluster certificate authority is rotated, which makes containment and revocation more operationally disruptive than disabling a user, group, or service account permission.
GKE supports multiple Kubernetes API server authentication methods, including OAuth tokens, service account bearer tokens, and X.509 client certificates. For administrator access, the gcloud CLI uses OAuth tokens, which reduces the need for legacy static credentials. Disabling GKE client certificate issuance prevents GKE from providing the legacy client certificate, but it does not disable the Kubernetes certificates API, which is used by the cluster and cannot be disabled.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.8.1</dc:identifier>
      </reference>
      <fixtext fixref="F-F5A482A8r01_fix">For new clusters, create the cluster without issuing a legacy client certificate by using the --no-issue-client-certificate flag.
gcloud container clusters create $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--no-issue-client-certificate
For existing clusters where a legacy client certificate was already issued, GKE does not provide a direct method to remove that client certificate from the cluster. To fully remediate the setting, create a replacement cluster with --no-issue-client- certificate, migrate workloads, validate access, and decommission the old cluster after migration.
Note: If immediate replacement is not practical, reduce risk on the existing cluster by ensuring RBAC is enabled and that the legacy client certificate has no Kubernetes RBAC permissions. Also review RBAC permissions for the Kubernetes CertificateSigningRequest API so only approved administrative identities can create or approve certificate requests. The --no-issue-client-certificate flag disables legacy GKE client certificate issuance, but it does not disable the Kubernetes certificates API, which is required for cluster operation.</fixtext>
      <fix id="F-F5A482A8r01_fix"/>
      <check system="C-F5A482A8_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that legacy client certificate credentials were not issued for the API server. GKE client certificate issuance should be disabled so users and automation rely on supported identity based authentication methods such as OAuth tokens through gcloud, service account bearer tokens, or approved external identity provider authentication.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '{
clientCertificate: .masterAuth.clientCertificate,
clientKey: .masterAuth.clientKey
}'
• Expected output: Both clientCertificate and clientKey should return null. If either field contains a value, legacy client certificate credentials were issued for the cluster and the cluster should be reviewed for remediation.
{
"clientCertificate": null,
"clientKey": null
}</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-0983AB29">
    <title>5.8 Authentication and Authorization</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-0983AB29r01_rule" weight="10.0" severity="high">
      <version>5.8.2</version>
      <title>Use Google Groups to centrally manage Kubernetes RBAC access for GKE clusters</title>
      <description>&lt;VulnDiscussion&gt;Use Google Groups for RBAC to centrally manage Kubernetes RBAC access for GKE clusters. Cluster administrators should assign Kubernetes RoleBinding and ClusterRoleBinding permissions to approved Google Groups rather than directly binding access to individual user email addresses.

Rationale:
Group based RBAC improves access governance by allowing user onboarding, role changes, and offboarding to be managed through centralized group membership instead of editing Kubernetes RBAC bindings for each individual user. This reduces inconsistent one off permissions, simplifies access reviews, and helps ensure that access is removed when a user leaves the organization or changes responsibilities.
For GKE, the required gke-security-groups group acts as the security group used to evaluate Google Group membership for Kubernetes RBAC. Approved access groups should be nested under that security group and then referenced in Kubernetes RoleBinding or ClusterRoleBinding subjects.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.8.2</dc:identifier>
      </reference>
      <fixtext fixref="F-0983AB29r01_fix">Enable Google Groups for RBAC when the audit shows that authenticatorGroupsConfig.enabled is not true, or when securityGroup is missing or incorrect. The required security group must be named gke-security- groups@DOMAIN, and approved access groups should be nested under that security group before they are referenced in Kubernetes RBAC bindings.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--security-group=gke-security-groups@$DOMAIN
After Google Groups for RBAC is enabled, update Kubernetes RoleBinding and ClusterRoleBinding objects to reference approved Google Groups as kind: Group subjects. Replace direct User bindings with group based bindings where practical, while preserving required ServiceAccount bindings for workloads, controllers, and automation.
Internal Only - General Review the RBAC table from the audit and remediate findings as follows:
• Replace unnecessary SUBJECT_KIND values of User with approved Group subjects.
• Confirm each Google Group referenced in RBAC is nested under gke- security-groups@DOMAIN.
• Confirm group names match exactly because Google Group references in RBAC are case sensitive.
• Remove direct user bindings only after validating that equivalent group based access is working.
• Test access for representative users with kubectl auth can-i before removing existing access paths.
Rerun the audit commands and confirm that Google Groups for RBAC is enabled, the configured security group is correct, and Kubernetes RBAC bindings use approved Google Groups instead of direct individual user binding.</fixtext>
      <fix id="F-0983AB29r01_fix"/>
      <check system="C-0983AB29_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that Google Groups for RBAC is enabled and configured with the required security group. Google Groups for RBAC lets Kubernetes RBAC permissions be assigned to Google Groups managed in Google Workspace or Cloud Identity, rather than binding access directly to individual user email addresses.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '{
enabled: .authenticatorGroupsConfig.enabled,
securityGroup: .authenticatorGroupsConfig.securityGroup
}'
• Expected output: enabled should return true, and securityGroup should be set to gke-security-groups@DOMAIN. The local part must be gke-security- groups, and the domain should match the Google Workspace or Cloud Identity domain used for group management.
Review Kubernetes RBAC bindings and verify that access is assigned to approved Google Groups instead of individual users where practical.
kubectl get rolebindings,clusterrolebindings -A -o json | jq -r '
["BINDING_KIND","NAMESPACE","BINDING_NAME","SUBJECT_KIND","SUBJECT_NAME","ROL
E_KIND","ROLE_NAME"],
(.items[] as $b |
$b.subjects[]? |
select(.kind=="User" or .kind=="Group") |
[$b.kind, ($b.metadata.namespace // "cluster-wide"), $b.metadata.name,
.kind, .name, $b.roleRef.kind, $b.roleRef.name]
)
| @tsv' | column -t -s $'\t'
• Expected output: RBAC subjects should primarily use SUBJECT_KIND of Group with approved Google Group email addresses that are nested under gke- security-groups@DOMAIN. Direct User bindings should be reviewed and removed unless there is a documented exception. Google Group names used in RBAC bindings are case sensitive and must match exactly.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-E058CF7D">
    <title>5.8 Authentication and Authorization</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-E058CF7Dr01_rule" weight="10.0" severity="medium">
      <version>5.8.3</version>
      <title>Ensure Legacy Authorization (ABAC) is Disabled</title>
      <description>&lt;VulnDiscussion&gt;Keep legacy ABAC authorization disabled for GKE clusters. ABAC is a legacy Kubernetes authorization method that has been superseded by Kubernetes RBAC, which provides fine grained authorization through Role, ClusterRole, RoleBinding, and ClusterRoleBinding resources.

Rationale:
ABAC uses broad, static authorization policies that are less flexible and harder to manage than Kubernetes RBAC. RBAC supports more precise permissions at the namespace and cluster level, allowing administrators to grant only the actions required for users, groups, service accounts, workloads, and automation.
Leaving ABAC disabled reduces the risk of overly broad legacy permissions bypassing the intended RBAC model. GKE hardening guidance recommends using IAM and RBAC for access control and not enabling ABAC which is disabled by default in all GKE clusters.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.8.3</dc:identifier>
      </reference>
      <fixtext fixref="F-E058CF7Dr01_fix">Disable legacy ABAC authorization for any GKE cluster where the audit result returns true. Before disabling ABAC, confirm that required users, groups, service accounts, workloads, and automation have the necessary Kubernetes RBAC permissions through Role, ClusterRole, RoleBinding, and ClusterRoleBinding resources.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--no-enable-legacy-authorization
Rerun the audit command and confirm that.legacyAbac.enabled returns false or null. Validate representative access with kubectl auth can-i to confirm legitimate users and automation still have the required RBAC access.</fixtext>
      <fix id="F-E058CF7Dr01_fix"/>
      <check system="C-E058CF7D_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that legacy ABAC authorization is disabled. ABAC is a legacy authorization method, and GKE recommends using IAM and Kubernetes RBAC for access control instead. ABAC is disabled by default in all GKE clusters.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.legacyAbac.enabled'
• Expected output: The output should return false or null. If the result returns true, legacy ABAC authorization is enabled and should be remediated.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-A37D1A4F">
    <title>5.9 Storage</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-A37D1A4Fr01_rule" weight="10.0" severity="high">
      <version>5.9.1</version>
      <title>Use CMEK protected StorageClasses for GKE Persistent Disk volumes</title>
      <description>&lt;VulnDiscussion&gt;Use CMEK protected StorageClass objects for GKE Persistent Disk volumes so newly provisioned Persistent Disks are encrypted with an approved Cloud KMS key. This applies to dynamically provisioned Persistent Disk volumes that use the Compute Engine Persistent Disk CSI driver and the disk-encryption-kms-key parameter.

Rationale:
Persistent Disks are encrypted at rest by default, but CMEK gives the organization direct control over the Cloud KMS key used to protect the disk encryption keys. This supports stronger key governance, including key access control, rotation, separation of duties, and the ability to disable key use when required by security or compliance processes.
Using a CMEK protected StorageClass helps ensure that new Persistent Disk volumes created by Kubernetes are encrypted consistently with approved key material. The disk-encryption-kms-key value must reference the fully qualified Cloud KMS key resource, and the key ring location must align with the cluster location requirements.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.9.1</dc:identifier>
      </reference>
      <fixtext fixref="F-A37D1A4Fr01_fix">Remediate any StorageClass or Persistent Disk that does not use an approved Cloud KMS key. CMEK for GKE Persistent Disk volumes is applied through a CMEK protected StorageClass for new dynamically provisioned volumes. Existing Persistent Disks cannot be converted to CMEK in place, so noncompliant volumes must be replaced or migrated.
• Grant the Compute Engine service agent for the cluster project roles/cloudkms.cryptoKeyEncrypterDecrypter on the approved Cloud KMS key.
Internal Only - General
• Create a new CMEK protected StorageClass using the Persistent Disk CSI driver, pd.csi.storage.gke.io, and the approved disk-encryption-kms-key value.
• Update new PersistentVolumeClaims to use the CMEK protected StorageClass. If this should be the default storage option, mark the CMEK protected StorageClass as the default only after validating the key, location, and access permissions.
• For existing PersistentVolumes that do not return an approved diskEncryptionKey.kmsKeyName, provision a new CMEK protected Persistent Disk through the approved StorageClass, migrate the data, update the workload to use the new PersistentVolumeClaim, and retire the old volume after validation.
• Rerun the audit commands and confirm that the StorageClass includes the approved disk-encryption-kms-key, the PersistentVolumes reference the expected StorageClass, and the underlying Compute Engine disks return the approved Cloud KMS key.</fixtext>
      <fix id="F-A37D1A4Fr01_fix"/>
      <check system="C-A37D1A4F_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review GKE StorageClass objects and verify that Persistent Disk classes use the Compute Engine Persistent Disk CSI driver with an approved Cloud KMS key. The disk-encryption-kms-key parameter should contain the fully qualified Cloud KMS key resource path used to encrypt newly provisioned Persistent Disk volumes.
Internal Only - General
kubectl get storageclass -o json | jq '.items[] |
select(.provisioner=="pd.csi.storage.gke.io") | {
name: .metadata.name,
provisioner: .provisioner,
kmsKey: .parameters."disk-encryption-kms-key"
}'
• Expected output: Each approved Persistent Disk StorageClass used for sensitive workloads should show pd.csi.storage.gke.io as the provisioner and a non null kmsKey value that matches an approved Cloud KMS key. If kmsKey is null, blank, missing, or does not reference an approved key, newly provisioned Persistent Disk volumes using that StorageClass are not configured for CMEK.
Identify existing PersistentVolumes and review the associated StorageClass and underlying disk reference.
kubectl get pv -o json | jq -r '.items[] | [.metadata.name,
.spec.storageClassName, .spec.csi.volumeHandle] | @tsv'
• Expected output: PersistentVolumes backed by GKE Persistent Disk should reference an approved CMEK protected StorageClass. The volumeHandle value should identify the underlying Compute Engine disk used for the PersistentVolume.
For each Persistent Disk identified, verify that the disk is encrypted with the approved Cloud KMS key.
gcloud compute disks describe $DISK_NAME \
--zone $ZONE \
--project $PROJECT_ID \
--format='value(diskEncryptionKey.kmsKeyName)'
• Expected output: the command should return the approved Cloud KMS key name. If the result is empty or null, the Persistent Disk is not encrypted with CMEK.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-B53292EC">
    <title>5.9 Storage</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-B53292ECr01_rule" weight="10.0" severity="high">
      <version>5.9.2</version>
      <title>Use CMEK protected boot disks for GKE node pools</title>
      <description>&lt;VulnDiscussion&gt;Use CMEK protected boot disks for GKE node pools so node boot disks are encrypted with approved Cloud KMS keys managed by the organization. This applies when creating a new cluster or creating a new node pool with the --boot-disk-kms-key setting.

Rationale:
GKE node boot disks are encrypted at rest by default using Google managed encryption. CMEK provides additional control by allowing the organization to manage the Cloud KMS key used for node boot disk encryption, including key access control, rotation, separation of duties, and the ability to disable key use when required by security or compliance processes.
Using CMEK protected boot disks helps align node infrastructure encryption with enterprise key management requirements. The Cloud KMS key must be created before use, and the node boot disk key should be specified when creating the cluster or node pool.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.9.2</dc:identifier>
      </reference>
      <fixtext fixref="F-B53292ECr01_fix">CMEK for node boot disks cannot be enabled, changed, or disabled in place for an existing node pool. To remediate a noncompliant node pool, create a replacement node pool with the approved Cloud KMS key, migrate workloads to the new node pool, validate workload health, and delete the old node pool after migration. Before creating the replacement node pool, grant the required Cloud KMS key access to the Compute Engine service agent for the cluster project.
Create the replacement node pool with the approved boot disk type and Cloud KMS key. Current GKE CMEK boot disk guidance uses pd-standard or pd-ssd for this configuration.
gcloud container node-pools create $NEW_NODE_POOL \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--disk-type $DISK_TYPE \
--boot-disk-kms-key $BOOT_DISK_KMS_KEY
After the replacement node pool is available, migrate workloads, validate scheduling and application health, and then drain and delete the noncompliant node pool. Rerun the audit command and confirm that bootDiskKmsKey returns the approved Cloud KMS key.</fixtext>
      <fix id="F-B53292ECr01_fix"/>
      <check system="C-B53292EC_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review each GKE node pool and verify that node boot disks are encrypted with an approved Cloud KMS key. The node pool configuration should show both the boot disk type and the bootDiskKmsKey value used to encrypt the boot disk attached to each node.
Internal Only - General
gcloud container node-pools describe $NODE_POOL \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '{
name: .name,
diskType: .config.diskType,
bootDiskKmsKey: .config.bootDiskKmsKey
}'
• Expected output: bootDiskKmsKey should contain the approved Cloud KMS key resource path, and diskType should be a supported boot disk type for the approved node pool design. If bootDiskKmsKey is null, blank, missing, or does not match the approved key, the node pool is not using CMEK protected boot disks and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-A4486CFC">
    <title>5.10 Additional Cluster Configurations</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-A4486CFCr01_rule" weight="10.0" severity="medium">
      <version>5.10.1</version>
      <title>Do not use GKE alpha clusters for production workloads</title>
      <description>&lt;VulnDiscussion&gt;Do not use GKE alpha clusters for production workloads. Alpha clusters are intended only for short lived testing of Kubernetes alpha APIs and features, and they are not covered by the GKE SLA.

Rationale:
Alpha clusters enable Kubernetes alpha APIs and feature gates so teams can experiment with functionality that is not production ready. They are not appropriate for production because they cannot be upgraded, cannot be enrolled in a release channel, do not receive security updates, and are automatically deleted after 30 days. GKE also does not automatically save data stored on alpha clusters before deletion.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.10.1</dc:identifier>
      </reference>
      <fixtext fixref="F-A4486CFCr01_fix">Alpha cluster behavior is selected at cluster creation and should not be used for production workloads. To remediate a production workload running on an alpha cluster, create a supported non alpha GKE cluster, migrate workloads and required data, validate application health, and decommission the alpha cluster. Alpha clusters expire after 30 days, do not receive security updates, cannot be upgraded, and are not covered by the GKE SLA.
gcloud container clusters create $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID
Do not include the --enable-kubernetes-alpha flag when creating the replacement production cluster. If alpha features are needed for testing, use a separate nonproduction alpha cluster and migrate any required data before the 30 day expiration window.</fixtext>
      <fix id="F-A4486CFCr01_fix"/>
      <check system="C-A4486CFC_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that Kubernetes alpha features are not enabled.
Alpha clusters are short lived test clusters for Kubernetes alpha APIs and features, and they are not intended for production workloads.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.enableKubernetesAlpha'
• Expected output: The output should return false or null. If the result is true, the cluster is an alpha cluster and should not be used for production workloads.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-6B05630D">
    <title>5.10 Additional Cluster Configurations</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-6B05630Dr01_rule" weight="10.0" severity="high">
      <version>5.10.2</version>
      <title>Use GKE Sandbox for untrusted or high risk workloads</title>
      <description>&lt;VulnDiscussion&gt;Use GKE Sandbox for untrusted, third party, multi tenant, or high risk workloads that need stronger isolation from the node host. GKE Sandbox uses gVisor to add an isolation layer between the container and the host kernel, reducing the risk that untrusted code can affect the node.

Rationale:
GKE Sandbox helps protect the host kernel when Pods run unknown or untrusted code.
gVisor provides a user space kernel that handles most system calls on behalf of the host kernel, which limits direct access to the host kernel and helps reduce the impact of container escape vulnerabilities.
This control is especially useful for multi-tenant clusters, SaaS platforms, web hosting environments, services that process untrusted user input, workloads that run third party code, and high value workloads that need additional security. In GKE Standard, sandboxed workloads should use the gvisor RuntimeClass and run on node pools enabled for GKE Sandbox.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.10.2</dc:identifier>
      </reference>
      <fixtext fixref="F-6B05630Dr01_fix">Create a new GKE Sandbox enabled node pool for untrusted or high risk workloads.
GKE Sandbox cannot be enabled on the default node pool when it is the only node pool, and GKE Sandbox node pools must use the cos_containerd node image.
gcloud container node-pools create $NODE_POOL \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--image-type=cos_containerd \
--sandbox type=gvisor
Update workload manifests for untrusted or high risk workloads to request the gvisor RuntimeClass.
Internal Only - General
spec:
runtimeClassName: gvisor
Migrate the applicable workloads to the sandbox enabled node pool, validate application behavior, and confirm the Pods are running with runtimeClassName: gvisor. Keep trusted system workloads and regular workloads on separate node pools unless there is an approved reason to run them in the sandbox.</fixtext>
      <fix id="F-6B05630Dr01_fix"/>
      <check system="C-6B05630D_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Review the GKE node pool and verify that GKE Sandbox is enabled for node pools that run untrusted or high risk workloads. GKE Sandbox uses gVisor to provide additional isolation between the container and the host kernel.
Internal Only - General
gcloud container node-pools describe $NODE_POOL \
--cluster $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.config.sandboxConfig'
• Expected result: the output should show the sandbox type as GVISOR. The older sandboxType field can appear in some output, but the current field is type.
{
"type": "GVISOR"
}
• Expected output: If the result is null, blank, missing, or does not show GVISOR, GKE Sandbox is not enabled for that node pool. Repeat this check for each node pool that is intended to run untrusted, third party, multi tenant, or high risk workloads.
For workloads that should run in the sandbox, verify that the Pod or workload manifest uses runtimeClassName: gvisor. In GKE Standard, enabling GKE Sandbox on the node pool is not enough by itself because the Pod must request the gvisor RuntimeClass.
kubectl get pods -A -o json | jq -r '.items[] |
select(.spec.runtimeClassName=="gvisor") | [.metadata.namespace,
.metadata.name, .spec.runtimeClassName] | @tsv'
• Expected output: Untrusted or high risk workloads should show gvisor as the runtime class. If those workloads do not use runtimeClassName: gvisor, they are not running in the sandbox.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-7F2B9802">
    <title>5.10 Additional Cluster Configurations</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-7F2B9802r01_rule" weight="10.0" severity="high">
      <version>5.10.3</version>
      <title>Enable the GKE security posture dashboard for cluster security insights</title>
      <description>&lt;VulnDiscussion&gt;Enable the GKE security posture dashboard to surface cluster and workload security insights, including workload configuration auditing, actionable security concerns, and security bulletin related findings. The dashboard provides a centralized view of security posture findings so teams can identify, prioritize, and remediate issues across eligible GKE clusters and workloads.

Rationale:
The GKE security posture dashboard helps identify workload and cluster configuration risks during runtime by scanning Kubernetes workloads for security concerns and presenting actionable findings in the Google Cloud console. This improves visibility into misconfigurations, risky workload settings, and security concerns that might otherwise require manual review across namespaces, manifests, and clusters.
Security posture findings can support operational review, remediation tracking, and security governance across individual clusters or fleets.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Benchmark</dc:subject>
        <dc:identifier>5.10.3</dc:identifier>
      </reference>
      <fixtext fixref="F-7F2B9802r01_fix">Enable the GKE security posture dashboard for workload configuration auditing when the audit shows that securityPostureConfig.mode is not BASIC. GKE supports enabling configuration auditing on existing clusters by setting the security posture mode to standard, which maps to BASIC in the cluster configuration.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--security-posture=standard
For new clusters, include --security-posture=standard during cluster creation when this setting must be explicitly enforced. Rerun the audit command and confirm that securityPostureConfig.mode returns BASIC. Initial configuration audit results can take several minutes to appear in the security posture dashboard and related logs.</fixtext>
      <fix id="F-7F2B9802r01_fix"/>
      <check system="C-7F2B9802_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Benchmark.xml" name="M"/>
        <check-content>Define three variables for Cluster Name, Location, and Project, then review the cluster security posture configuration.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.securityPostureConfig'
• Expected output: mode should return BASIC. The vulnerabilityMode value might return VULNERABILITY_DISABLED, which is acceptable for this control because the control is focused on the GKE security posture dashboard and workload configuration auditing, not deprecated workload vulnerability scanning.
Internal Only - General
{
"mode": "BASIC",
"vulnerabilityMode": "VULNERABILITY_DISABLED"
}
If mode is DISABLED, MODE_UNSPECIFIED, null, blank, or missing, the GKE security posture dashboard is not enabled for workload configuration auditing and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
</Benchmark>

