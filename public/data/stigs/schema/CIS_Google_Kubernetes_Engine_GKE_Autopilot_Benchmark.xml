<?xml version="1.0" encoding="utf-8"?><Benchmark xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:cpe="http://cpe.mitre.org/language/2.0" xmlns:xhtml="http://www.w3.org/1999/xhtml" xmlns:dsig="http://www.w3.org/2000/09/xmldsig#" xsi:schemaLocation="http://checklists.nist.gov/xccdf/1.1 http://nvd.nist.gov/schema/xccdf-1.1.4.xsd" id="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark" xml:lang="en" xmlns="http://checklists.nist.gov/xccdf/1.1">
  <status date="2026-06-30">accepted</status>
  <title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</title>
  <description>All CIS Benchmarks™ (Benchmarks) focus on technical configuration settings used to maintain and/or increase the security of the addressed technology, and they should be used in conjunction with other essential cyber hygiene tasks like: • Monitoring the base operating system and applications for vulnerabilities and quickly updating with the latest security patches. • End-point protection (Antivirus software, Endpoint Detection and Response (EDR), etc.). • Logging and monitoring user and system activity. In the end, the Benchmarks are designed to be a key component of a comprehensive cybersecurity program. Important Usage Information All Benchmarks are available free for non-commercial use from the CIS Website. They can be used to manually assess and remediate systems and applications. In lieu of manual assessment and remediation, there are several tools available to assist with assessment: • CIS Configuration Assessment Tool (CIS-CAT ® Pro Assessor) • CIS Benchmarks™ Certified 3rd Party Tooling These tools make the hardening process much more scalable for large numbers of systems and applications. NOTE: Some tooling focuses only on the Benchmark Recommendations that can be fully automated (skipping ones marked Manual). It is important that ALL Recommendations ( Automated and Manual) be addressed since all are important for properly securing systems and are typically in scope for audits. Key Stakeholders Cybersecurity is a collaborative effort, and cross functional cooperation is imperative within an organization to discuss, test, and deploy Benchmarks in an effective and efficient way. The Benchmarks are developed to be best practice configuration guidelines applicable to a wide range of use cases. In some organizations, exceptions to specific Recommendations will be needed, and this team should work to prioritize the problematic Recommendations based on several factors like risk, time, cost, and labor. These exceptions should be properly categorized and documented for auditing purposes. Internal Only - General Apply the Correct Version of a Benchmark Benchmarks are developed and tested for a specific set of products and versions and applying an incorrect Benchmark to a system can cause the resulting pass/fail score to be incorrect. This is due to the assessment of settings that do not apply to the target systems. To assure the correct Benchmark is being assessed: • Deploy the Benchmark applicable to the way settings are managed in the environment: An example of this is the Microsoft Windows family of Benchmarks, which have separate Benchmarks for Group Policy, Intune, and Stand-alone systems based upon how system management is deployed. Applying the wrong Benchmark in this case will give invalid results. • Use the most recent version of a Benchmark: This is true for all Benchmarks, but especially true for cloud technologies. Cloud technologies change frequently and using an older version of a Benchmark may have invalid methods for auditing and remediation. Exceptions The guidance items in the Benchmarks are called recommendations and not requirements, and exceptions to some of them are expected and acceptable. The Benchmarks strive to be a secure baseline, or starting point, for a specific technology, with known issues identified during Benchmark development are documented in the Impact section of each Recommendation. In addition, organizational, system specific requirements, or local site policy may require changes as well, or an exception to a Recommendation or group of Recommendations (e.g. A Benchmark could Recommend that a Web server not be installed on the system, but if a system's primary purpose is to function as a Webserver, there should be a documented exception to this Recommendation for that specific server). In the end, exceptions to some Benchmark Recommendations are common and acceptable, and should be handled as follows: • The reasons for the exception should be reviewed cross-functionally and be well documented for audit purposes. • A plan should be developed for mitigating, or eliminating, the exception in the future, if applicable. • If the organization decides to accept the risk of this exception (not work toward mitigation or elimination), this should be documented for audit purposes. It is the responsibility of the organization to determine their overall security policy, and which settings are applicable to their unique needs based on the overall risk profile for the organization. Internal Only - General Remediation CIS has developed Build Kits for many technologies to assist in the automation of hardening systems. Build Kits are designed to correspond to Benchmark's “Remediation” section, which provides the manual remediation steps necessary to make that Recommendation compliant to the Benchmark. When remediating systems (changing configuration settings on deployed systems as per the Benchmark's Recommendations), please approach this with caution and test thoroughly. The following is a reasonable remediation approach to follow: • CIS Build Kits, or internally developed remediation methods should never be applied to production systems without proper testing. • Proper testing consists of the following: o Understand the configuration (including installed applications) of the targeted systems. Various parts of the organization may need different configurations (e.g., software developers vs standard office workers). o Read the Impact section of the given Recommendation to help determine if there might be an issue with the targeted systems. o Test the configuration changes with representative lab system(s). If issues arise during testing, they can be resolved prior to deploying to any production systems. o When testing is complete, initially deploy to a small sub-set of production systems and monitor closely for issues. If there are issues, they can be resolved prior to deploying more broadly. o When the initial deployment above is completed successfully, iteratively deploy to additional systems and monitor closely for issues. Repeat this process until the full deployment is complete. Summary Using the Benchmarks Certified tools, working as a team with key stakeholders, being selective with exceptions, and being careful with remediation deployment, it is possible to harden large numbers of deployed systems in a cost effective, efficient, and safe manner. NOTE: As previously stated, the PDF versions of the CIS Benchmarks™ are available for free, non-commercial use on the CIS Website. All other formats of the CIS Benchmarks™ (MS Word, Excel, and Build Kits) are available for CIS SecureSuite ® members. CIS-CAT ® Pro is also available to CIS SecureSuite ® members. Internal Only - General Target Technology Details This document provides prescriptive guidance for running Google Kubernetes Engine (GKE) AutoPilot following recommended security controls. This benchmark only includes controls which can be modified by an end user of GKE AutoPilot. To obtain the latest version of this guide, please visit www.cisecurity.org. If you have questions, comments, or have identified ways to improve this guide, please write us at support@cisecurity.org.</description>
  <notice id="terms-of-use" xml:lang="en"/>
  <front-matter xml:lang="en"/>
  <rear-matter xml:lang="en"/>
  <reference href="https://www.cisecurity.org/cis-benchmarks">
    <dc:publisher>Center for Internet Security</dc:publisher>
    <dc:source>CIS Benchmarks</dc:source>
  </reference>
  <plain-text id="release-info">Release: 2.0.0 Benchmark Date: 2026-06-30</plain-text>
  <version>2.0.0</version>
  <Profile id="L1_Public">
    <title>Level 1</title>
    <description>CIS profile: Level 1</description>
    <select idref="V-D37AB7DB" selected="true"/>
    <select idref="V-B2AFE70A" selected="true"/>
    <select idref="V-6ADA1939" selected="true"/>
    <select idref="V-F47BEB32" selected="true"/>
    <select idref="V-6261F9E6" selected="true"/>
    <select idref="V-BA186A9A" selected="true"/>
    <select idref="V-467F2CCC" selected="true"/>
    <select idref="V-38DDACE1" selected="true"/>
    <select idref="V-60E9BEE7" selected="true"/>
    <select idref="V-0C57289F" selected="true"/>
  </Profile>
  <Profile id="L1_Classified">
    <title>Level 1</title>
    <description>CIS profile: Level 1</description>
    <select idref="V-D37AB7DB" selected="true"/>
    <select idref="V-B2AFE70A" selected="true"/>
    <select idref="V-6ADA1939" selected="true"/>
    <select idref="V-F47BEB32" selected="true"/>
    <select idref="V-6261F9E6" selected="true"/>
    <select idref="V-BA186A9A" selected="true"/>
    <select idref="V-467F2CCC" selected="true"/>
    <select idref="V-38DDACE1" selected="true"/>
    <select idref="V-60E9BEE7" selected="true"/>
    <select idref="V-0C57289F" selected="true"/>
  </Profile>
  <Profile id="L1_Sensitive">
    <title>Level 1</title>
    <description>CIS profile: Level 1</description>
    <select idref="V-D37AB7DB" selected="true"/>
    <select idref="V-B2AFE70A" selected="true"/>
    <select idref="V-6ADA1939" selected="true"/>
    <select idref="V-F47BEB32" selected="true"/>
    <select idref="V-6261F9E6" selected="true"/>
    <select idref="V-BA186A9A" selected="true"/>
    <select idref="V-467F2CCC" selected="true"/>
    <select idref="V-38DDACE1" selected="true"/>
    <select idref="V-60E9BEE7" selected="true"/>
    <select idref="V-0C57289F" selected="true"/>
  </Profile>
  <Profile id="L2_Public">
    <title>Level 2</title>
    <description>CIS profile: Level 2</description>
    <select idref="V-CC75C113" selected="true"/>
    <select idref="V-1DE1623A" selected="true"/>
    <select idref="V-DBE55120" selected="true"/>
    <select idref="V-CFA67159" selected="true"/>
    <select idref="V-58195E0C" selected="true"/>
    <select idref="V-EA6A9410" selected="true"/>
    <select idref="V-30A4558B" selected="true"/>
    <select idref="V-24351AE5" selected="true"/>
    <select idref="V-809F1CF6" selected="true"/>
    <select idref="V-74304A97" selected="true"/>
    <select idref="V-C3DF1D8E" selected="true"/>
    <select idref="V-892E3A91" selected="true"/>
    <select idref="V-0711AE63" selected="true"/>
    <select idref="V-B456A198" selected="true"/>
    <select idref="V-86A40B0D" selected="true"/>
    <select idref="V-05623EA5" selected="true"/>
    <select idref="V-8E8AE20A" selected="true"/>
    <select idref="V-26185D15" selected="true"/>
    <select idref="V-54B06A01" selected="true"/>
  </Profile>
  <Profile id="L2_Classified">
    <title>Level 2</title>
    <description>CIS profile: Level 2</description>
    <select idref="V-CC75C113" selected="true"/>
    <select idref="V-1DE1623A" selected="true"/>
    <select idref="V-DBE55120" selected="true"/>
    <select idref="V-CFA67159" selected="true"/>
    <select idref="V-58195E0C" selected="true"/>
    <select idref="V-EA6A9410" selected="true"/>
    <select idref="V-30A4558B" selected="true"/>
    <select idref="V-24351AE5" selected="true"/>
    <select idref="V-809F1CF6" selected="true"/>
    <select idref="V-74304A97" selected="true"/>
    <select idref="V-C3DF1D8E" selected="true"/>
    <select idref="V-892E3A91" selected="true"/>
    <select idref="V-0711AE63" selected="true"/>
    <select idref="V-B456A198" selected="true"/>
    <select idref="V-86A40B0D" selected="true"/>
    <select idref="V-05623EA5" selected="true"/>
    <select idref="V-8E8AE20A" selected="true"/>
    <select idref="V-26185D15" selected="true"/>
    <select idref="V-54B06A01" selected="true"/>
  </Profile>
  <Profile id="L2_Sensitive">
    <title>Level 2</title>
    <description>CIS profile: Level 2</description>
    <select idref="V-CC75C113" selected="true"/>
    <select idref="V-1DE1623A" selected="true"/>
    <select idref="V-DBE55120" selected="true"/>
    <select idref="V-CFA67159" selected="true"/>
    <select idref="V-58195E0C" selected="true"/>
    <select idref="V-EA6A9410" selected="true"/>
    <select idref="V-30A4558B" selected="true"/>
    <select idref="V-24351AE5" selected="true"/>
    <select idref="V-809F1CF6" selected="true"/>
    <select idref="V-74304A97" selected="true"/>
    <select idref="V-C3DF1D8E" selected="true"/>
    <select idref="V-892E3A91" selected="true"/>
    <select idref="V-0711AE63" selected="true"/>
    <select idref="V-B456A198" selected="true"/>
    <select idref="V-86A40B0D" selected="true"/>
    <select idref="V-05623EA5" selected="true"/>
    <select idref="V-8E8AE20A" selected="true"/>
    <select idref="V-26185D15" selected="true"/>
    <select idref="V-54B06A01" selected="true"/>
  </Profile>
  <Group id="V-D37AB7DB">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-D37AB7DBr01_rule" weight="10.0" severity="medium">
      <version>4.1.1</version>
      <title>Ensure the cluster-admin ClusterRole is only used when required</title>
      <description>&lt;VulnDiscussion&gt;The RBAC cluster-admin ClusterRole grants full administrative authority across cluster resources and should be assigned only to approved users, groups, or service accounts with a documented operational need. For GKE Autopilot clusters, the control should focus on limiting RoleBinding and ClusterRoleBinding assignments to cluster- admin, not on removing or modifying the built in ClusterRole itself. GKE RBAC provides fine grained control over what users, groups, IAM service accounts, and Kubernetes ServiceAccounts can do inside the cluster.

Rationale:
In GKE, Kubernetes RBAC uses Role, ClusterRole, RoleBinding, and ClusterRoleBinding objects to grant permissions to authenticated subjects, including users, groups, IAM service accounts, and Kubernetes ServiceAccounts. RBAC works alongside IAM in GKE, but Kubernetes RBAC is the appropriate mechanism for fine grained permissions on Kubernetes objects within a cluster or namespace. RBAC permissions are additive, so there are no deny rules to offset excessive access once a broad role is bound.
The cluster-admin ClusterRole grants permission to do anything on any resource in the cluster. When assigned through a ClusterRoleBinding, it provides cluster wide administrative access across all namespaces. When assigned through a RoleBinding, the ClusterRole is limited to the namespace specified in the binding, but it still grants the broad permissions of cluster-admin within that namespace. Where full administrative access is not explicitly required, define a narrower Role or ClusterRole that grants only the required API groups, resources, and verbs.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.1</dc:identifier>
      </reference>
      <fixtext fixref="F-D37AB7DBr01_fix">For each cluster-admin binding, replace broad access with a least privilege Role or ClusterRole that grants only the required API groups, resources, and verbs. Bind the replacement role to the approved user, group, IAM service account, or Kubernetes ServiceAccount first, validate access, and then remove the unnecessary cluster- admin binding.
Internal Only - General
• Identify all ClusterRoleBinding and RoleBinding objects that assign the cluster-admin ClusterRole, including both cluster wide and namespace level bindings.
• Review each bound user, group, IAM service account, or Kubernetes ServiceAccount to confirm whether full administrative access is still required and documented.
• Replace cluster-admin access where possible with a least privilege Role or ClusterRole that grants only the required API groups, resources, and verbs.
• Bind and validate the replacement permissions before removing the existing cluster-admin binding to avoid disrupting administrative, platform, or automation workflows.
• Remove only confirmed unnecessary bindings, and avoid modifying RBAC resources with the system: prefix unless explicitly required and validated because they support cluster functionality.</fixtext>
      <fix id="F-D37AB7DBr01_fix"/>
      <check system="C-D37AB7DB_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review all Kubernetes RBAC bindings that assign the cluster-admin ClusterRole.
This review should include ClusterRoleBinding objects, which grant access across the cluster, and namespaced RoleBinding objects, which can bind the cluster-admin ClusterRole within a specific namespace.
List all ClusterRoleBinding subjects bound to cluster-admin:
kubectl get clusterrolebindings -o json \
| jq -r '
["BINDING", "SUBJECT_KIND", "SUBJECT_NAME", "SUBJECT_NAMESPACE"],
(
.items[]
| select(.roleRef.kind=="ClusterRole" and .roleRef.name=="cluster-admin")
| .metadata.name as $binding
| .subjects[]?
| [$binding, .kind, .name, (.namespace // "-")]
)
| @tsv
' \
| column -t -s $'\t'
List all namespaced RoleBinding subjects that bind the cluster-admin ClusterRole:
kubectl get rolebindings -A -o json \
| jq -r '
["NAMESPACE", "BINDING", "SUBJECT_KIND", "SUBJECT_NAME",
"SUBJECT_NAMESPACE"],
(
.items[]
| select(.roleRef.kind=="ClusterRole" and .roleRef.name=="cluster-admin")
| .metadata.namespace as $ns
| .metadata.name as $binding
| .subjects[]?
| [$ns, $binding, .kind, .name, (.namespace // "-")]
)
| @tsv
' \
| column -t -s $'\t'
Review each listed user, group, IAM service account, Kubernetes ServiceAccount, and namespace to confirm that cluster-admin access is explicitly required and documented.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-B2AFE70A">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-B2AFE70Ar01_rule" weight="10.0" severity="medium">
      <version>4.1.2</version>
      <title>Ensure access to Secrets is minimized and granted only where required</title>
      <description>&lt;VulnDiscussion&gt;Kubernetes Secrets can contain sensitive application credentials, configuration values, certificates, and legacy service account tokens. Access to these Secrets should be restricted to the smallest practical set of users, groups, service accounts, and workloads to reduce the risk of credential exposure, privilege escalation, and unauthorized access to cluster or external resources. GKE RBAC supports fine grained permissions over Kubernetes resources, and GKE Autopilot clusters still rely on Kubernetes RBAC to authorize access to Kubernetes API resources such as Secrets.

Rationale:
Inappropriate access to Secrets stored in the cluster can allow an attacker to retrieve credentials used by applications, automation, or workloads, and then use those credentials to access Kubernetes resources or external systems. RBAC permissions for Secrets should be granted only through explicit Roles or ClusterRoles that define the required API group, resource, and verbs, such as get, list, watch, create, update, or delete. Wildcards should be avoided because they can unintentionally grant broader Secret access than intended.
Where possible, workloads should use dedicated Kubernetes ServiceAccounts and least privilege bindings rather than shared or default service accounts. For access to Secret Manager or other Google Cloud APIs, Workload Identity Federation for GKE is preferred over storing long lived service account keys as Kubernetes Secrets, because it gives workloads fine grained access to Google Cloud APIs without relying on static credentials stored inside the cluster.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.2</dc:identifier>
      </reference>
      <fixtext fixref="F-B2AFE70Ar01_fix">Remediate Secret access by replacing broad or unnecessary RBAC permissions with narrowly defined access that supports only the required workload, user, or operational function. The remediation process should focus on reducing get, list, and watch permissions on the core API secrets resource, especially where access is inherited through broad roles, wildcard rules, shared service accounts, or default service accounts. Changes should be validated before removal to avoid disrupting applications, controllers, operators, or required platform services.
• Remove unnecessary Secret access by updating or replacing RBAC roles that grant get, list, or watch on secrets, especially where the access is inherited through wildcard resources or wildcard verbs.
• Replace broad Secret permissions with least privilege Roles or ClusterRoles that grant only the required API group, resource, and verb. Avoid list and watch when a subject only needs to read a specific Secret.
• Bind Secret access only to approved users, groups, or dedicated Kubernetes ServiceAccounts. Avoid binding Secret access to the default service account because Pods that do not explicitly specify a service account can inherit that access.
• Move sensitive external credentials out of Kubernetes Secrets and use Secret Manager with Workload Identity Federation for GKE where the workload needs access to Google Cloud secrets or APIs.
Internal Only - General
• Validate application, controller, operator, and platform workflows before removing access, and avoid modifying required ystem: RBAC resources because they support normal cluster functionality.</fixtext>
      <fix id="F-B2AFE70Ar01_fix"/>
      <check system="C-B2AFE70A_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review RBAC permissions that grant read access to Secrets in GKE Autopilot clusters.
The audit should identify where get, list, or watch permissions on the core API secrets resource are defined, how those permissions are assigned through bindings, and which users, groups, or service accounts receive the access. The review should also account for broad default roles, wildcard rules, default groups, and shared or default service accounts, because these can unintentionally expose Secrets beyond the intended operational need. GKE Autopilot supports Kubernetes RBAC for cluster resource authorization, so Secret access should be reviewed through Role, ClusterRole, RoleBinding, and ClusterRoleBinding objects.
• Review Role and ClusterRole objects that grant get, list, or watch access to the core API secrets resource.
• Include wildcard rules such as resources: ["*"] or verbs: ["*"], because they can unintentionally grant Secret access.
• Map Secret permissions to RoleBinding and ClusterRoleBinding objects to identify the actual users, groups, and service accounts with access.
• Validate that each subject has a documented need for Secret access and that access is limited to the required namespace, resource, and verb.
• Review broad roles such as cluster-admin, admin, and edit, default groups such as system:authenticated, and shared or default service accounts, but avoid modifying required system: RBAC resources unless explicitly validated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-6ADA1939">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-6ADA1939r01_rule" weight="10.0" severity="medium">
      <version>4.1.3</version>
      <title>Minimize wildcard use in Roles and ClusterRoles</title>
      <description>&lt;VulnDiscussion&gt;Kubernetes Roles and ClusterRoles define what actions users, groups, and Kubernetes service accounts can perform against Kubernetes API resources in GKE Autopilot clusters. Wildcard permissions should be minimized because the * character applies broadly across API groups, resources, or verbs and can grant more access than intended. Roles should explicitly define only the API groups, resources, and verbs required for the subject’s function. RBAC best practices advise avoiding wildcards and using least privilege role design.

Rationale:
The principle of least privilege requires granting only the permissions needed to perform a specific task. Using * in RBAC rules can unintentionally grant broad access across current and future Kubernetes API capabilities, including new API groups, resources, verbs, or custom resources introduced into the cluster. For example, using * in the verbs field grants all actions, including patch, update, delete, and deletecollection, instead of only the specific actions required.
In GKE Autopilot clusters, RBAC rules should be written with explicit API groups, resources, and verbs, and split into separate rules when different resources require different permissions. This prevents subjects from inheriting access to resources or actions they do not need and supports namespace level access control where possible.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.3</dc:identifier>
      </reference>
      <fixtext fixref="F-6ADA1939r01_fix">Replace wildcard permissions with explicit RBAC rules that define only the required API groups, resources, and verbs. Remediation should focus on preserving required access while removing broad * grants that could expand privileges as APIs, resources, or CRDs are added. Validate affected users, workloads, controllers, and automation before removing wildcard access. For GKE Autopilot clusters, remediation remains focused on Kubernetes RBAC objects, including Roles, ClusterRoles, RoleBindings, and ClusterRoleBindings.
• Replace verbs: ["*"] with only the required actions, such as get, list, watch, create, update, patch, or delete.
• Replace resources: ["*"] with only the required Kubernetes resources or custom resources.
• Replace apiGroups: ["*"] with the specific API groups required by the subject’s function.
Internal Only - General
• Review related RoleBinding and ClusterRoleBinding objects to confirm the revised role is assigned only to approved users, groups, or Kubernetes service accounts.
• Avoid modifying required system: RBAC resources unless explicitly required and tested.
Where centralized enforcement is required, use Policy Controller with K8sProhibitRoleWildcardAccess to audit or enforce restrictions on wildcard access in Roles and ClusterRoles.</fixtext>
      <fix id="F-6ADA1939r01_fix"/>
      <check system="C-6ADA1939_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review Kubernetes RBAC roles in GKE Autopilot clusters to identify wildcard permissions in Roles and ClusterRoles. The audit should check for * in apiGroups, resources, or verbs, because wildcard permissions can unintentionally grant access to current or future Kubernetes APIs, resources, custom resources, or actions. After identifying wildcard rules, review the related RoleBinding and ClusterRoleBinding objects to determine which users, groups, or Kubernetes service accounts receive the permissions. RBAC access should be reviewed regularly and aligned to least privilege.
Internal Only - General Retrieve Roles and ClusterRoles across all namespaces and review them for wildcard use:
kubectl get roles,clusterroles -A -o json \
| jq -r '
["KIND", "NAMESPACE", "NAME"],
(
.items[]
| select(any(.rules[]?;
((.apiGroups // []) | index("*")) or
((.resources // []) | index("*")) or
((.verbs // []) | index("*"))
))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name]
)
| @tsv
' \
| column -t -s $'\t'
Determine whether each wildcard permission is required and documented.
• Review verbs: ["*"], resources: ["*"], and apiGroups: ["*"] because each can grant broad or unintended access.
• Pay special attention to broad roles such as cluster-admin, admin, and edit.
• Review bindings to default groups, shared service accounts, and default service accounts that may inherit wildcard access.
• Avoid modifying required system: RBAC resources unless explicitly required and validated.
Note: If Policy Controller is enabled, K8sProhibitRoleWildcardAccess can help audit or enforce restrictions on wildcard access in Roles and ClusterRoles.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-F47BEB32">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-F47BEB32r01_rule" weight="10.0" severity="medium">
      <version>4.1.4</version>
      <title>Ensure that default service accounts are not actively used</title>
      <description>&lt;VulnDiscussion&gt;The Kubernetes default service account should not be actively used by application workloads. Instead, each workload should use a dedicated Kubernetes service account so permissions can be explicitly assigned, audited, and reviewed. Create a dedicated Kubernetes service account for each workload that requires Kubernetes API access, and avoid relying on the namespace default service account.

Rationale:
Kubernetes creates a default service account in each namespace, and Pods that do not specify serviceAccountName are automatically assigned that default service account. If RBAC permissions are bound to the default service account, any Pod in that namespace that omits an explicit service account can unintentionally inherit those permissions.
Where Kubernetes API access is required, create a dedicated Kubernetes service account for the workload and grant only the required RBAC permissions through a Role or ClusterRole binding. If the Pod does not need Kubernetes API access, set automountServiceAccountToken=false so a service account token is not automatically mounted into the Pod. The automountServiceAccountToken field defaults to true, and should be disabled when Pods do not need to communicate with the Kubernetes API server.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.4</dc:identifier>
      </reference>
      <fixtext fixref="F-F47BEB32r01_fix">Each workload that requires Kubernetes API access should have its own service account with narrowly defined permissions. Workloads that do not need Kubernetes API access should disable automatic token mounting to reduce unnecessary credential exposure. For Google Cloud API access, use Workload Identity Federation for GKE instead of relying on static credentials in Pods.
• Create dedicated Kubernetes service accounts for workloads that require Kubernetes API access
• Update workload manifests to specify serviceAccountName instead of relying on the namespace default service account
• Remove RoleBinding and ClusterRoleBinding assignments from the default service account that are not needed
• Set automountServiceAccountToken=false for Pods that do not need to communicate with the Kubernetes API server
• Configure the namespace default service account with automountServiceAccountToken: false and validate affected workloads</fixtext>
      <fix id="F-F47BEB32r01_fix"/>
      <check system="C-F47BEB32_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review each namespace in the GKE Autopilot cluster to confirm that the Kubernetes default service account is not actively used by application workloads and does not have explicit RBAC permissions assigned to it. The audit process should identify workloads that omit serviceAccountName, workloads that explicitly use serviceAccountName:
default, and any RoleBinding or ClusterRoleBinding objects that bind permissions to the default service account. In addition, verify whether service account token mounting is disabled where Pods do not need Kubernetes API access, because automountServiceAccountToken defaults to true.
kubectl get serviceaccounts/default -o yaml
Review the output and confirm the following:
• The default service account in each namespace has no explicit RoleBinding or ClusterRoleBinding assignments unless there is a documented exception.
• Application workloads specify a dedicated serviceAccountName instead of omitting the field or using serviceAccountName: default.
• Pods that do not need Kubernetes API access set automountServiceAccountToken=false in the Pod spec.
• The default service account is configured with automountServiceAccountToken: false where necessary to reduce accidental token mounting.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-6261F9E6">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-6261F9E6r01_rule" weight="10.0" severity="medium">
      <version>4.1.5</version>
      <title>Ensure service account tokens are mounted only when Pods require Kubernetes API access</title>
      <description>&lt;VulnDiscussion&gt;Kubernetes service account tokens should be mounted in Pods only when the workload explicitly requires authenticated access to the Kubernetes API server. For workloads that do not need API server access, disable token mounting to reduce unnecessary credential exposure inside the Pod. automountServiceAccountToken controls whether Kubernetes injects a service account credential token into the Pod, and its default value is true.

Rationale:
Mounting service account tokens into Pods increases the impact of a Pod compromise because the token can be used to make authenticated requests to the Kubernetes API server using the permissions assigned to the Pod’s Kubernetes service account. This risk is higher when workloads use shared service accounts, the namespace default service account, or service accounts with broad RBAC permissions.
Disabling automatic token mounting for Pods that do not need Kubernetes API access removes an unnecessary credential from the workload runtime environment and supports least privilege. Workloads that do need API access should use a dedicated Kubernetes service account with narrowly assigned RBAC permissions.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.5</dc:identifier>
      </reference>
      <fixtext fixref="F-6261F9E6r01_fix">Remediate by disabling service account token mounting and assigning API access only to workloads that require it. Pods that do not need to communicate with the Kubernetes API server should set automountServiceAccountToken=false, and workloads that do need API access should use a dedicated Kubernetes service account with narrowly assigned RBAC permissions. In addition, avoiding the default service account and using separate Kubernetes service accounts per workload is recommended.
• Set automountServiceAccountToken=false in Pod or workload specifications that do not require Kubernetes API access.
• Create dedicated Kubernetes service accounts for workloads that require API access instead of relying on the namespace default service account.
• Grant only the required RBAC permissions to those service accounts through least privilege Roles, ClusterRoles, RoleBindings, or ClusterRoleBindings.
• Set automountServiceAccountToken=false on service accounts where token mounting should be disabled by default.</fixtext>
      <fix id="F-6261F9E6r01_fix"/>
      <check system="C-6261F9E6_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review Kubernetes service accounts and Pods in each namespace in the GKE Autopilot cluster to confirm that service account tokens are mounted only where the workload requires authenticated access to the Kubernetes API server. The audit should verify both the service account configuration and the Pod specification, because token mounting can be controlled at either level. The automountServiceAccountToken field defaults to true, so workloads that do not need Kubernetes API access should explicitly set it to false.
Review the service account configuration:
Internal Only - General
kubectl get serviceaccount $SERVICE_ACCOUNT -n $NAMESPACE -o yaml
Review the Pod configuration:
kubectl get pod $POD_NAME -n $NAMESPACE -o yaml
Expected output: automountServiceAccountToken: false
• Confirm that Pods without Kubernetes API access requirements have automountServiceAccountToken: false.
• Confirm that workloads requiring API access use a dedicated Kubernetes service account with least privilege RBAC permissions.
• Review Pods using the namespace default service account, especially where automountServiceAccountToken is unset or true.
• Validate exceptions for controllers, operators, jobs, or applications that require Kubernetes API server access.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-BA186A9A">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-BA186A9Ar01_rule" weight="10.0" severity="medium">
      <version>4.1.6</version>
      <title>Avoid granting cluster admin level access through the system:masters group</title>
      <description>&lt;VulnDiscussion&gt;The special Kubernetes group system:masters should not be used to grant cluster- admin level access to users, groups, or service accounts. In GKE Autopilot, administrative access should be granted through explicit RBAC bindings and limited to the minimum permissions required. The system:masters group is a default Kubernetes group, and adding custom subjects to it gives those subjects access to do anything to any resource in the cluster.

Rationale:
The system:masters group is bound to the cluster-admin ClusterRole by default to support system functionality. Adding users, groups, or service accounts to this group grants unrestricted cluster administrator access across all namespaces and resources.
This bypasses normal least privilege role design and can make access harder to review, reduce, or replace with narrower RBAC controls.
Do not use system:masters for routine administrative access. Where full administrative access is truly required, use an explicit ClusterRoleBinding to the built in cluster- admin ClusterRole and document the operational need. For most use cases, create a narrower Role or ClusterRole that defines only the required API groups, resources, and verbs. Google also recommends avoiding default roles and groups, avoiding broad cluster-admin bindings, and applying least privilege.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.6</dc:identifier>
      </reference>
      <fixtext fixref="F-BA186A9Ar01_fix">Remediate custom system:masters usage by replacing it with explicit, auditable RBAC assignments. Do not remove the default cluster-admin ClusterRoleBinding to system:masters, because it supports Kubernetes system functionality. Instead, remove custom paths that add users, groups, service accounts, or client certificates to system:masters, and grant required access through least privilege Roles, ClusterRoles, RoleBindings, or ClusterRoleBindings.
• Remove any custom RoleBinding or ClusterRoleBinding that references system:masters, after confirming it is not a required system binding.
• Remove system:masters from identity provider mappings, certificate subject groups, or other external authentication sources used for cluster access.
• Replace required administrative access with explicit RBAC bindings to cluster- admin only where full cluster access is justified.
• For most users, groups, service accounts, and automation, create narrower roles that define only the required API groups, resources, and verbs.</fixtext>
      <fix id="F-BA186A9Ar01_fix"/>
      <check system="C-BA186A9A_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review RBAC bindings and identity sources to confirm that system:masters is not used to grant cluster-admin level access to custom users, groups, service accounts, or client certificates. The default cluster-admin ClusterRoleBinding to system:masters is expected and should not be treated as a failure by itself. Any additional or custom use of system:masters should be reviewed because Kubernetes assigns the cluster-admin ClusterRole to this group by default, and adding subjects to it grants full access to every resource in the cluster.
Internal Only - General Review ClusterRoleBindings that reference system:masters:
kubectl get clusterrolebinding -o json \
| jq -r '.items[]
| select(.subjects[]? | .kind=="Group" and .name=="system:masters")
| "\(.metadata.name)\t\(.roleRef.kind)/\(.roleRef.name)"'
Expected output: cluster-admin ClusterRole/cluster-admin
• Confirm that the only expected ClusterRoleBinding involving system:masters is the default cluster-admin binding.
• Review RoleBindings and ClusterRoleBindings for any custom references to system:masters.
• Review certificate, identity provider, and group mapping processes to confirm no custom user or workload identity is issued with system:masters group membership.
• Confirm administrative access is granted through explicit RBAC bindings, not by placing subjects in system:masters.
Note: Do not delete or modify required system: RBAC resources unless explicitly validated, because they support cluster functionality.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-467F2CCC">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-467F2CCCr01_rule" weight="10.0" severity="medium">
      <version>4.1.7</version>
      <title>Limit use of the Bind, Impersonate and Escalate permissions in the Kubernetes cluster</title>
      <description>&lt;VulnDiscussion&gt;Roles and ClusterRoles with bind, escalate, or impersonate permissions should be granted only to tightly controlled administrative or platform identities with a documented need. These permissions can allow a subject to expand effective access beyond normal RBAC assignments and should not be included in general user, workload, or automation roles. RBAC permissions are defined in Role and ClusterRole rules and assigned through RoleBinding and ClusterRoleBinding objects.

Rationale:
The bind and escalate permissions are high risk because they can allow a subject to create or update RBAC objects in ways that grant permissions they would not otherwise hold. escalate allows creation or update of roles with permissions beyond the caller’s current access, while bind allows creation or update of role bindings that reference roles with permissions beyond the caller’s current access. These permissions can allow a subject to grant any permission to any role and should be avoided unless explicitly required.
The impersonate permission is also sensitive because it can allow a subject to act as another user, group, or service account for Kubernetes API requests. When combined with broad target identities or privileged RBAC bindings, impersonation can provide an escalation path to cluster admin level access. These verbs should be limited to controlled administrative workflows, troubleshooting use cases, or approved automation with narrow scope. In GKE Autopilot clusters, user impersonation is supported for user- defined users and groups, but system users and groups such as system:masters cannot be impersonated. RBAC role design should follow least privilege and account for escalation risks such as bind and escalate.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.7</dc:identifier>
      </reference>
      <fixtext fixref="F-467F2CCCr01_fix">Replace bind, escalate, and impersonate access with narrowly defined RBAC permissions that support only the approved administrative, platform, or automation function. Remove these verbs from general user, workload, and service account roles unless there is a valid requirement. Review changes before removal to avoid disrupting controllers, operators, CI/CD workflows, or access review processes.
• Remove bind, escalate, and impersonate from Roles and ClusterRoles where they are not explicitly required.
• Replace broad access with least privilege rules that define the exact API groups, resources, verbs, and namespace scope required.
• Avoid granting RBAC modification permissions such as bind, escalate, create, update, or patch on rbac.authorization.k8s.io resources to service accounts unless specifically justified.
• Move required elevated access to tightly controlled administrative or platform identities, not workload or default service accounts.
• Validate impacted workflows and avoid modifying required system: RBAC resources unless the change is explicitly required and tested.
Internal Only - General</fixtext>
      <fix id="F-467F2CCCr01_fix"/>
      <check system="C-467F2CCC_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review Role and ClusterRole objects that grant bind, escalate, or impersonate, then trace those roles through RoleBinding and ClusterRoleBinding objects to identify the users, groups, and service accounts that receive the permissions. These verbs should be limited to documented administrative or platform use cases because bind and escalate can grant permissions beyond the caller’s current access, while impersonate can allow API requests as another subject. RBAC permissions are additive, so unnecessary access should be identified and removed.
kubectl get roles,clusterroles -A -o json \
| jq -r '
.items[]
| select(any(.rules[]?.verbs[]?;
.=="bind" or .=="escalate" or .=="impersonate"
))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name]
| @tsv'
• Review each matching Role or ClusterRole to identify which sensitive verb is granted and on which API group and resource.
• Map each matching role to its RoleBinding or ClusterRoleBinding to identify the bound users, groups, and service accounts.
• Confirm that each subject has a documented need for bind, escalate, or impersonate.
• Note service accounts, CI/CD identities, automation, and broad groups because these permissions can create privilege escalation paths.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-CC75C113">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-CC75C113r01_rule" weight="10.0" severity="high">
      <version>4.1.8</version>
      <title>Avoid binding RBAC roles to unauthenticated users and groups</title>
      <description>&lt;VulnDiscussion&gt;Avoid non-default RoleBinding or ClusterRoleBinding objects that grant permissions to the system:anonymous user or the related system:unauthenticated group. These subjects represent unauthenticated Kubernetes API requests and should not receive additional RBAC access to cluster resources.

Rationale:
Kubernetes uses system:anonymous for requests that do not provide authentication credentials, and unauthenticated requests can also be associated with the system:unauthenticated group. Binding RBAC roles to either subject can grant unauthenticated callers access to Kubernetes API resources. This is especially risky if the binding includes broad roles, wildcard permissions, Secret access, workload access, or namespace wide permissions.
GKE can limit anonymous authentication so unauthenticated requests are rejected for non health check endpoints before RBAC evaluation. In supported GKE versions, this control can restrict anonymous requests to Kubernetes API server health check endpoints such as /healthz, /livez, and /readyz. Even with that protection available, RBAC bindings to system:anonymous or system:unauthenticated should be avoided because they create unnecessary exposure and undermine least privilege access.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.8</dc:identifier>
      </reference>
      <fixtext fixref="F-CC75C113r01_fix">Remediate any non-default RoleBinding or ClusterRoleBinding returned by the audit by removing unauthenticated access and replacing it with authenticated, least privilege RBAC access. Bindings to system:anonymous or system:unauthenticated should not be used for normal cluster operations because they can grant Kubernetes API permissions to unauthenticated requests.
• Review each returned binding and identify the referenced Role or ClusterRole, including the API groups, resources, and verbs granted.
• Delete unnecessary non-default bindings to system:anonymous or system:unauthenticated after confirming they are not required for cluster operation.
Internal Only - General
kubectl delete clusterrolebinding &lt;clusterrolebinding-name&gt;
kubectl delete rolebinding &lt;rolebinding-name&gt; -n $NAMESPACE
• Replace any required access with authenticated users, groups, or dedicated Kubernetes service accounts using least privilege Role or ClusterRole permissions.
• Do not replace unauthenticated access with broad roles, wildcard permissions, Secret access, or namespace wide permissions unless explicitly required and documented.</fixtext>
      <fix id="F-CC75C113r01_fix"/>
      <check system="C-CC75C113_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review all RoleBinding and ClusterRoleBinding objects to confirm that RBAC permissions are not assigned to the system:anonymous user and system:unauthenticated group through non-default bindings. These subjects represent unauthenticated Kubernetes API requests, and any non-default binding to them can expose cluster resources without authenticated access. Include both cluster wide and namespace level bindings, then review any matching role reference to determine the exact permissions granted. Kubernetes RBAC is enabled by default in GKE and controls access through Role, ClusterRole, RoleBinding, and ClusterRoleBinding objects.
Internal Only - General Check for bindings to system:anonymous:
kubectl get rolebindings,clusterrolebindings -A -o json \
| jq -r '.items[]
| select(any(.subjects[]?; .kind=="User" and .name=="system:anonymous"))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name,
.roleRef.name]
| @tsv'
Check for bindings to system:unauthenticated:
kubectl get rolebindings,clusterrolebindings -A -o json \
| jq -r '.items[]
| select(any(.subjects[]?; .kind=="Group" and
.name=="system:unauthenticated"))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name,
.roleRef.name]
| @tsv'
Expected output: No non-default bindings should be returned.
• Review any non-default result as a potential finding unless there is a documented and validated exception.
• Review the referenced Role or ClusterRole to identify the API groups, resources, and verbs granted.
• Confirm that no broad roles, wildcard permissions, Secret access, workload access, or namespace wide permissions are assigned to unauthenticated subjects.
• Validate that required cluster operations use authenticated users, groups, or service accounts instead of unauthenticated access.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-38DDACE1">
    <title>4.1 RBAC and Service Accounts</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-38DDACE1r01_rule" weight="10.0" severity="medium">
      <version>4.1.9</version>
      <title>Avoid binding RBAC roles to all authenticated users</title>
      <description>&lt;VulnDiscussion&gt;Avoid non-default RoleBinding and ClusterRoleBinding objects that grant RBAC permissions to the system:authenticated group. This group represents all authenticated Kubernetes API requests from users signed in with a Google Account, including Gmail accounts, so it should not be used for custom access grants. The default system ClusterRoleBindings system:basic-user, system:discovery, and system:public-info-viewer are expected and should not be treated as findings by themselves.

Rationale:
Binding a Role or ClusterRole to system:authenticated grants the referenced permissions to a very broad set of authenticated identities instead of a specific user, group, or service account. This can unintentionally expose cluster resources to any authenticated Google Account, depending on the role being bound. RBAC permissions are additive, and access should be granted through specific subjects with only the API groups, resources, and verbs required for the task.
Non-default bindings to system:authenticated are especially risky when they include broad roles, wildcard permissions, Secret access, workload access, or namespace wide access. Where access is required, bind permissions to a defined Google Group, IAM service account, Kubernetes service account, or named user instead of the global system:authenticated group.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.1.9</dc:identifier>
      </reference>
      <fixtext fixref="F-38DDACE1r01_fix">Remove any non-default binding tosystem:authenticate identified in the audit and replace it with explicit, least privilege access for specific authenticated subjects. Do not remove the expected default ClusterRoleBindings system:basic-user, system:discovery, or system:public-info-viewer. RBAC permissions should be granted through defined users, groups, or service accounts rather than the broad system:authenticated group.
• Review each non-default RoleBinding or ClusterRoleBinding returned by the audit and identify the referenced Role or ClusterRole.
• Determine the specific API groups, resources, verbs, and namespace scope required for the affected users, tools, or automation.
• Create or update a least privilege Role or ClusterRole that grants only the required access.
• Bind the replacement role to a specific user, Google Group, IAM service account, or Kubernetes service account.
• Delete the unsafe non-default binding to system:authenticated only after replacement access is validated.
kubectl delete clusterrolebinding &lt;clusterrolebinding-name&gt;
kubectl delete rolebinding &lt;rolebinding-name&gt; -n $NAMESPACE</fixtext>
      <fix id="F-38DDACE1r01_fix"/>
      <check system="C-38DDACE1_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review all RoleBinding and ClusterRoleBinding objects to confirm that RBAC permissions are not assigned to the system:authenticated group outside of expected default system bindings. This group represents all authenticated Kubernetes API requests, so custom bindings can grant access to a broader audience than intended.
RBAC permissions should be assigned to specific users, groups, or service accounts using least privilege.
Check for bindings to system:authenticated:
kubectl get rolebindings,clusterrolebindings -A -o json \
| jq -r '
["KIND","NAMESPACE","BINDING","ROLE"],
(
.items[]
| select(any(.subjects[]?;
.kind=="Group" and .name=="system:authenticated"
))
| [.kind, (.metadata.namespace // "cluster-wide"), .metadata.name,
.roleRef.name]
)
| @tsv'
Expected output: Should be limited to the default ClusterRoleBindings below:
KIND NAMESPACE BINDING ROLE
ClusterRoleBinding cluster-wide system:basic-user system:basic-
user
ClusterRoleBinding cluster-wide system:discovery
system:discovery
ClusterRoleBinding cluster-wide system:public-info-viewer
system:public-info-viewer
• Confirm that no non-default ClusterRoleBinding objects reference system:authenticated.
• Confirm that no namespace level RoleBinding objects reference system:authenticated.
• Review any non-default result to identify the referenced Role or ClusterRole, including API groups, resources, and verbs granted.
• Flag broad roles, wildcard permissions, Secret access, workload access, or namespace wide permissions as high risk.
Do not delete default system RBAC resources such as system:basic-user, system:discovery, or system:public-info-viewer, because they support normal cluster functionality.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-1DE1623A">
    <title>4.3 Network Policies and NetworkPolicy Enforcement</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-1DE1623Ar01_rule" weight="10.0" severity="high">
      <version>4.3.1</version>
      <title>Use NetworkPolicy with built in GKE Dataplane V2 enforcement</title>
      <description>&lt;VulnDiscussion&gt;NetworkPolicy enforcement is built in for GKE Autopilot clusters through GKE Dataplane V2. Kubernetes NetworkPolicy objects can restrict Pod to Pod, Pod to Service, ingress, and egress traffic without enabling Calico or a separate NetworkPolicy add-on.

Rationale:
NetworkPolicies define pod level traffic rules, but enforcement depends on the networking implementation used by the cluster. In GKE Autopilot, GKE Dataplane V2 is enabled by default and provides built in Kubernetes NetworkPolicy enforcement. GKE Dataplane V2 uses Cilium and eBPF for routing, load balancing, and NetworkPolicy enforcement.
By default, pods in a cluster can communicate freely. Using NetworkPolicy in GKE Autopilot allows teams to implement defense in depth, namespace based segmentation, tenant isolation, and explicit ingress and egress controls for approved workload communication paths.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.3.1</dc:identifier>
      </reference>
      <fixtext fixref="F-1DE1623Ar01_fix">For GKE Autopilot clusters, no remediation is required to enable NetworkPolicy enforcement because GKE Dataplane V2 is enabled by default and Kubernetes NetworkPolicy enforcement is built in. Remediation should focus on creating and maintaining NetworkPolicy resources that define the intended ingress and egress traffic rules for workloads.
• Apply NetworkPolicy resources in the required namespaces to define explicit allowed traffic paths for approved workload communication.
• Use default deny policies where appropriate, followed by explicit allow policies for required ingress and egress traffic.
• Validate application connectivity after applying NetworkPolicy resources.</fixtext>
      <fix id="F-1DE1623Ar01_fix"/>
      <check system="C-1DE1623A_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster configuration and verify that the cluster uses GKE Dataplane V2 for built in NetworkPolicy enforcement. NetworkPolicy requires a network plugin to implement enforcement. In Autopilot clusters, GKE Dataplane V2 is enabled by default and Kubernetes NetworkPolicy is always on.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format="table(
name,
networkConfig.datapathProvider
)"
• Expected output: The cluster satisfies this control if networkConfig.datapathProvider is ADVANCED_DATAPATH, because Kubernetes NetworkPolicy is always on for clusters that use GKE Dataplane V2.
NAME DATAPATH_PROVIDER
cluster-1 ADVANCED_DATAPATH</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-DBE55120">
    <title>4.4 Secrets Management and Workload Secret Access</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-DBE55120r01_rule" weight="10.0" severity="high">
      <version>4.4.1</version>
      <title>Use Secret Manager for external workload secret storage</title>
      <description>&lt;VulnDiscussion&gt;Use Secret Manager as the preferred external storage and management service for workload secrets used by GKE Autopilot clusters. Workloads should access secrets through Secret Manager API calls using Workload Identity Federation for GKE, or through mounted volumes using the Secret Manager add-on when file based access is required.

Rationale:
Kubernetes Secrets are supported, but they require careful control through Kubernetes RBAC, namespace boundaries, encryption, and workload configuration. Storing sensitive values in Secret Manager centralizes secret lifecycle management outside the cluster and supports IAM based access control, auditability, rotation, and version management. Workload Identity Federation for GKE lets workloads access Secret Manager without static service account keys and allows access to be granted to specific Kubernetes ServiceAccounts.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.4.1</dc:identifier>
      </reference>
      <fixtext fixref="F-DBE55120r01_fix">Move externally managed workload secrets to Secret Manager and update GKE workloads to access them through Workload Identity Federation for GKE. Prefer direct Secret Manager API access through Google Cloud client libraries where the application supports it. For applications that require file based secret access, enable the Secret Manager add-on and mount secrets into Pods using SecretProviderClass and CSI volumes.
• Configure applications to use Workload Identity Federation for GKE for Secret Manager access.
• Use dedicated Kubernetes ServiceAccounts for workloads that need Secret Manager access.
• Grant least privilege Secret Manager IAM permissions only to the required workload identities.
• Enable the Secret Manager add-on when workloads need secrets mounted as files.
Internal Only - General
• Migrate sensitive values out of Kubernetes Secrets where practical, then remove unused in-cluster Secret objects.
Secret Manager provides centralized secret management features such as fine grained access control, managed rotation, lifecycle management, and audit logs.</fixtext>
      <fix id="F-DBE55120r01_fix"/>
      <check system="C-DBE55120_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot secrets implementation and verify that sensitive workload secrets are stored in Secret Manager where external secret storage is required, rather than relying only on native Kubernetes Secrets. Confirm whether workloads access Secret Manager through direct API calls with Workload Identity Federation for GKE, or through mounted files using the Secret Manager add-on and SecretProviderClass resources.
Secret Manager access from GKE workloads can use Workload Identity Federation for GKE without static service account keys, and the Secret Manager add-on supports mounting Secret Manager secrets into Pods as files.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format="yaml(workloadIdentityConfig.workloadPool,secretManagerConfig)"
Sample output should look like this:
• Workload Identity Federation for GKE for Secret Manager API access
• Secret Manager add-on with rotation enabled for mounted file access
secretManagerConfig:
enabled: true
rotationConfig:
enabled: true
workloadIdentityConfig:
workloadPool: gke-for-cis.svc.id.goog
Interpretation:
• workloadIdentityConfig.workloadPool populated means Workload Identity Federation for GKE is enabled.
• secretManagerConfig.enabled: true means the Secret Manager add-on is enabled for mounting Secret Manager secrets as files.
• rotationConfig.enabled: true means mounted secrets can be refreshed through the add-on rotation configuration.
For any workload still using Kubernetes Secrets for sensitive values, confirm whether there is a documented reason, whether RBAC access is limited, and whether migration to Secret Manager is planned. Also validate that Secret Manager IAM permissions are granted only to the required workload identities, because Workload Identity Federation for GKE does not grant Google Cloud API permissions by itself.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-CFA67159">
    <title>4.5 Admission Control and Policy Enforcement</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-CFA67159r01_rule" weight="10.0" severity="high">
      <version>4.5.1</version>
      <title>Enforce image provenance using Binary Authorization for GKE</title>
      <description>&lt;VulnDiscussion&gt;Use Binary Authorization for GKE to enforce image provenance and deploy time trust policies before container images are admitted to the GKE Autopilot cluster. Policies should allow only approved images, trusted registries, or images with required attestations from approved build, security, or release processes.

Rationale:
Binary Authorization provides centralized policy enforcement for GKE deployments by evaluating container images at deploy time. When attestation based rules are used, the policy verifies that an image digest has been signed by a trusted attestor before the workload is allowed to run. This helps prevent unapproved, unsigned, unverified, or noncompliant images from being deployed, even if a user or pipeline has permission to create Kubernetes workloads.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.5.1</dc:identifier>
      </reference>
      <fixtext fixref="F-CFA67159r01_fix">Enable Binary Authorization enforcement for the GKE Autopilot cluster and configure the policy to block unapproved images at deploy time. Use approved image rules, trusted registries, or attestation based requirements aligned to the build, security scan, and release approval process. Validate the policy in dry run or a lower environment before enforcing broadly in production.</fixtext>
      <fix id="F-CFA67159r01_fix"/>
      <check system="C-CFA67159_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and confirm that Binary Authorization enforcement is enabled and confirm it does not simply allow all images and that it requires approved images, trusted registries, or attestations from trusted attestors where appropriate.
Binary Authorization works with GKE clusters, including Autopilot clusters, and evaluates the configured policy at deploy time.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-60E9BEE7">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-60E9BEE7r01_rule" weight="10.0" severity="medium">
      <version>4.6.1</version>
      <title>Use namespaces to separate workload administration and resource governance</title>
      <description>&lt;VulnDiscussion&gt;Use dedicated namespaces to separate GKE Autopilot workloads by application, team, environment, or tenant. Namespaces provide a logical boundary for organizing resources and applying namespace level governance, but they should be paired with access, quota, network, and pod security controls.

Rationale:
Namespaces help reduce administrative risk by limiting where users and workloads operate inside the cluster. GKE supports namespace restricted access patterns where IAM and Kubernetes RBAC work together, and RBAC should grant permissions in as few namespaces as possible using Roles and RoleBindings where practical. This reduces the blast radius of mistakes, unauthorized changes, and overly broad operational access.
Dedicated namespaces also make it easier to apply workload governance consistently, including ResourceQuotas, NetworkPolicies, Pod Security labels, ownership labels, and environment specific deployment rules. Workloads should not be placed in the default namespace for production because shared use can increase naming collisions, resource contention, weak access separation, and cleanup complexity.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.6.1</dc:identifier>
      </reference>
      <fixtext fixref="F-60E9BEE7r01_fix">Create dedicated namespaces for GKE Autopilot workloads based on application, environment, or tenant boundaries. Move production workloads out of the default namespace and apply consistent namespace level governance so administration, access, and resource controls are easier to manage.
• Create a dedicated namespace before deploying new application resources.
• Move production workloads out of the default namespace where applicable.
• Apply namespace labels for owner, application, environment, tenant, and cost allocation.
• Configure least privilege RBAC using namespace level Roles and RoleBindings where possible.
• Apply ResourceQuotas, NetworkPolicies, and Pod Security labels based on workload risk and governance requirements.
Review namespaces, owners, workload placement, and access bindings to confirm that the namespace model still matches current application and environment boundaries.</fixtext>
      <fix id="F-60E9BEE7r01_fix"/>
      <check system="C-60E9BEE7_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Run the following command and review the namespaces created in the cluster:
kubectl get namespaces --show-labels
Verify that user workloads are placed in dedicated namespaces based on application, environment, or tenant, rather than being deployed into the default namespace. In addition, confirm that each workload namespace has both ownership and appropriate namespace level controls, such as RBAC RoleBindings, ResourceQuotas, NetworkPolicies, and Pod Security labels where required.
Internal Only - General Review namespace access to confirm that users and groups are granted only the namespace level permissions they need. Prefer namespace level Roles and RoleBindings, rather than broad cluster wide access.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-58195E0C">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-58195E0Cr01_rule" weight="10.0" severity="high">
      <version>4.6.2</version>
      <title>Rely on Autopilot RuntimeDefault seccomp; custom profiles are unsupported</title>
      <description>&lt;VulnDiscussion&gt;GKE Autopilot automatically applies the RuntimeDefault seccomp profile to workload Pods, so custom seccomp profile configuration is not required for normal workloads.
Custom Localhost seccomp profiles are not supported in Autopilot because GKE manages the nodes, and GKE Sandbox Pods use sandbox isolation as the security boundary instead of Pod specified seccomp rules.

Rationale:
Seccomp reduces container risk by limiting the Linux system calls that container processes can make to the node kernel. In Autopilot, GKE applies the containerd default seccomp profile automatically, which provides baseline hardening while maintaining compatibility with most workloads. The default profile blocks selected syscalls that can be used to bypass container isolation or gain privileged access to the node.
Because Autopilot nodes are managed by GKE, custom node level seccomp profiles are not a supported hardening path. Workloads should rely on the Autopilot applied RuntimeDefault profile unless there is a documented exception, such as a workload that explicitly requires Unconfined or a GKE Sandbox workload where seccomp rules in the Pod manifest are ignored.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.6.2</dc:identifier>
      </reference>
      <fixtext fixref="F-58195E0Cr01_fix">For GKE Autopilot workloads, remove unsupported custom Localhost seccomp profile settings and rely on the automatically applied RuntimeDefault profile. Do not attempt to install or manage custom seccomp profiles on Autopilot nodes because GKE manages the node environment.
If a workload specifies Unconfined, validate and document the requirement, then remove the override where possible so the workload uses the Autopilot default seccomp protection. For GKE Sandbox workloads, treat the sandbox as the security boundary because seccomp rules specified in the Pod manifest are ignored.</fixtext>
      <fix id="F-58195E0Cr01_fix"/>
      <check system="C-58195E0C_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review workload Pods to confirm that they do not specify unsupported custom seccomp profiles and do not use Unconfined unless there is a documented exception. Missing seccompProfile fields are not a finding in Autopilot because GKE applies the RuntimeDefault seccomp profile automatically.
Internal Only - General
kubectl get pods -A -o json \
| jq -r '
["NAMESPACE","POD","POD_SECCOMP","CONTAINER","CONTAINER_SECCOMP"],
(
.items[] as $pod
| ($pod.spec.securityContext.seccompProfile.type // "not-set") as
$podSeccomp
| $pod.spec.containers[]
| [
$pod.metadata.namespace,
$pod.metadata.name,
$podSeccomp,
.name,
(.securityContext.seccompProfile.type // "not-set")
]
)
| @tsv
' \
| column -t -s $'\t'
Expected output:
NAMESPACE POD POD_SECCOMP CONTAINER
CONTAINER_SECCOMP
default nginx-6c569569-7mjgj RuntimeDefault nginx
not-set
default nginx-6c564569-twlq4 RuntimeDefault nginx
not-set
gke-gmp-system collector-m6rd RuntimeDefault prometheus
not-set
gke-gmp-system collector-m6rd not-set config-reloader
RuntimeDefault
Any Localhost value should be treated as unsupported for Autopilot. Any Unconfined value should be reviewed as an exception because it overrides the default seccomp restriction for that workload.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-EA6A9410">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-EA6A9410r01_rule" weight="10.0" severity="high">
      <version>4.6.3</version>
      <title>Require hardened security contexts for all workload Pods and containers</title>
      <description>&lt;VulnDiscussion&gt;Require hardened security contexts for GKE Autopilot workload Pods and containers.
Security contexts should define workload level and container level controls that reduce privilege, limit host exposure, and enforce safer runtime behavior.

Rationale:
Security contexts control important runtime security settings for Pods and containers, including user and group identity, privilege escalation, Linux capabilities, privileged mode, seccomp profile, and related container isolation settings. GKE Autopilot applies multiple Pod security constraints by default, but workload manifests should still define applicable security context settings for controls that remain workload configurable.
A hardened configuration should normally include settings such as runAsNonRoot=true, allowPrivilegeEscalation=false, privileged=false, dropped Linux capabilities, and the RuntimeDefault seccomp profile where explicitly specified. Custom Localhost seccomp profiles are not supported in GKE Autopilot, and Unconfined should be limited to documented exceptions. These align with restricted Pod security expectations and can also be enforced or audited with admission controls such as Pod Security Admission and Policy Controller.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.6.3</dc:identifier>
      </reference>
      <fixtext fixref="F-EA6A9410r01_fix">Update workload manifests, Helm charts, overlays, or GitOps sources so hardened security contexts are defined before deployment. Apply settings at the Pod level when they apply to all containers, and use container level settings where a specific container requires a different configuration.
• Set workloads to run as non root where compatible.
• Set allowPrivilegeEscalation=false for workload containers.
• Set privileged=false and remove host level access unless explicitly required.
• Drop unnecessary Linux capabilities, and use drop: ["ALL"] where the application supports it.
• Set seccompProfile.type=RuntimeDefault where seccomp is explicitly defined, and remove unsupported custom Localhost seccomp profiles from GKE Autopilot workloads.
Redeploy the workload and validate application behavior in a lower environment before broad rollout. Use admission controls such as Pod Security Admission or Policy Controller to prevent future workloads from being admitted without the required security context settings.</fixtext>
      <fix id="F-EA6A9410r01_fix"/>
      <check system="C-EA6A9410_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review workload Pods and controller templates to confirm hardened security contexts are explicitly defined at the Pod and container level. Focus the audit on workload namespaces and verify settings such as runAsNonRoot=true, allowPrivilegeEscalation=false, privileged=false, dropped Linux capabilities where practical, and no explicit seccompProfile.type=Unconfined unless documented as an approved exception. Missing seccompProfile fields are not a finding in GKE Autopilot because Autopilot applies the RuntimeDefault seccomp profile to workload Pods by default. GKE Autopilot supports using Pod Security Admission, Policy Controller, and security posture scanning to identify and enforce workload configuration risks.
Review any workload that has no security context, runs as root, allows privilege escalation, runs privileged, uses broad Linux capabilities, or explicitly uses seccompProfile.type=Unconfined without an approved exception. These findings should be documented as remediation items or approved exceptions based on workload requirements.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-30A4558B">
    <title>4.6 General Policies</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-30A4558Br01_rule" weight="10.0" severity="high">
      <version>4.6.4</version>
      <title>Avoid deploying workloads in the default namespace</title>
      <description>&lt;VulnDiscussion&gt;Avoid deploying GKE Autopilot workloads in the default namespace. Use dedicated namespaces to separate applications, environments, or tenants so namespace level controls can be applied consistently.

Rationale:
The default namespace exists for objects created without an explicit namespace, but using it for normal workloads weakens ownership, access separation, and resource governance. Dedicated namespaces make it easier to apply namespace based RBAC, ResourceQuotas, NetworkPolicies, Pod Security labels, observability controls, and cost attribution.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>4.6.4</dc:identifier>
      </reference>
      <fixtext fixref="F-30A4558Br01_fix">Create dedicated namespaces for GKE Autopilot workloads based on application, environment, or tenant boundaries, then migrate application resources out of the default namespace. Make updates so all workload resources explicitly specify the new target namespace. After migration, apply appropriate namespace level governance, including RBAC, ResourceQuotas, NetworkPolicies, Pod Security labels, and ownership labels.</fixtext>
      <fix id="F-30A4558Br01_fix"/>
      <check system="C-30A4558B_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Run the following command to identify resources currently deployed in the default namespace:
kubectl get
all,configmaps,secrets,pvc,ingress,serviceaccounts,networkpolicies -n default
Review any returned resources and determine whether they are application, environment, or tenant workloads that should be moved to a dedicated namespace.
GKE supports identifying workloads running in the default namespace and migrating them to dedicated namespaces to improve reliability and security.
In addition, confirm that each target namespace has appropriate ownership and namespace level governance, including RBAC RoleBindings, ResourceQuotas, NetworkPolicies, and Pod Security labels where required.
Internal Only - General</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-24351AE5">
    <title>5.1 Container Image Registry and Vulnerability Scanning</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-24351AE5r01_rule" weight="10.0" severity="high">
      <version>5.1.1</version>
      <title>Enable Artifact Analysis scanning for Artifact Registry container images</title>
      <description>&lt;VulnDiscussion&gt;Enable Artifact Analysis vulnerability scanning for container images stored in Artifact Registry and used by GKE Autopilot workloads. This verifies that container images are scanned at the registry layer before deployment to identify known vulnerabilities in image packages, dependencies, and software components. For GKE clusters, this should be treated as an Artifact Registry and Artifact Analysis control, not as a cluster level workload vulnerability scanning setting, because GKE security posture dashboard vulnerability scanning capabilities are being removed.

Rationale:
Container images can include vulnerable operating system packages, language packages, dependencies, or other software components that increase the risk of compromise when deployed to GKE workloads. Artifact Analysis scans container images in Artifact Registry through the Container Scanning API and records vulnerability metadata through the Container Analysis API, giving platform and application teams visibility into known vulnerabilities before images are promoted or deployed.
Automatic scanning evaluates new images when they are pushed to Artifact Registry and continuously monitors scanned image metadata for newly disclosed vulnerabilities.
Scans are based on image digest, so retagging an image does not trigger a new scan, and findings are limited to packages that are publicly monitored for security vulnerabilities.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.1.1</dc:identifier>
      </reference>
      <fixtext fixref="F-24351AE5r01_fix">Enable Artifact Analysis automatic vulnerability scanning for container images stored in Artifact Registry and used by GKE Autopilot workloads. Enabling the Container Scanning API activates automatic scanning for images pushed to standard and remote Docker repositories in Artifact Registry, and it also enables the Container Analysis API for vulnerability metadata storage and retrieval.
Enable the required project APIs:
gcloud services enable artifactregistry.googleapis.com
containerscanning.googleapis.com \
--project=$PROJECT_ID
If scanning was disabled on a specific Artifact Registry repository, update the repository to allow vulnerability scanning:
Internal Only - General
gcloud artifacts repositories update $REPOSITORY \
--project=$PROJECT_ID \
--location=$LOCATION \
--allow-vulnerability-scanning
Note: Repeat the repository update command for each Docker repository that stores images deployed to GKE Autopilot workloads. Repository level scanning controls are supported in Artifact Registry, and --allow-vulnerability-scanning permits automatic vulnerability scanning for the repository.</fixtext>
      <fix id="F-24351AE5r01_fix"/>
      <check system="C-24351AE5_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the Google Cloud project that hosts container images for GKE Autopilot workloads and verify that Artifact Registry and Artifact Analysis automatic vulnerability scanning are enabled. Artifact Analysis provides automated vulnerability scanning for container images in Artifact Registry through the Container Scanning API, and enabling that API also enables the Container Analysis API for vulnerability metadata storage and retrieval.
Internal Only - General Verify that the required APIs are enabled:
gcloud services list --enabled \
--project=$PROJECT_ID \
--filter='name:artifactregistry.googleapis.com OR
name:containerscanning.googleapis.com OR
name:containeranalysis.googleapis.com' \
--format='table(config.name)'
• Expected output: artifactregistry.googleapis.com and containerscanning.googleapis.com should be listed.
containeranalysis.googleapis.com may also be listed because the Container Analysis API is used for metadata storage and retrieval.
Review Artifact Registry repository scanning settings for Docker repositories used by GKE workloads:
gcloud artifacts repositories list \
--project=$PROJECT_ID \
--location=$LOCATION \
--
format='table(name,format,mode,vulnerabilityScanningConfig.enablementConfig,v
ulnerabilityScanningConfig.enablementState,vulnerabilityScanningConfig.enable
mentStateReason)'
• Expected output: Docker repositories that store GKE Autopilot workload images should not show repository level scanning disabled. Enabling the Container Scanning API activates scanning for images pushed to standard and remote Docker repositories in Artifact Registry, but scanning can be disabled on individual repositories, so repository level settings should also be reviewed.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-809F1CF6">
    <title>5.1 Container Image Registry and Vulnerability Scanning</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-809F1CF6r01_rule" weight="10.0" severity="high">
      <version>5.1.2</version>
      <title>Grant least privilege IAM access to Artifact Registry repositories</title>
      <description>&lt;VulnDiscussion&gt;Restrict access to Artifact Registry repositories that store GKE Autopilot workload images. Grant users, groups, CI/CD identities, and GKE node service accounts only the IAM roles needed to pull, push, administer, or manage container images.

Rationale:
Weak IAM access to Artifact Registry can allow unauthorized users to pull sensitive images, push unapproved images, overwrite trusted image tags, or delete repository content. Artifact Registry supports repository level IAM, so access should be limited to the minimum required role, such as Artifact Registry Reader for image pulls, Artifact Registry Writer for image publishing, and repository administrator roles only for approved platform owners. GKE nodes require pull access to repositories that host workload images, especially when images are stored in a different project or when using user provided node service accounts.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.1.2</dc:identifier>
      </reference>
      <fixtext fixref="F-809F1CF6r01_fix">Apply least privilege IAM access to Artifact Registry Docker repositories that store images deployed to GKE Autopilot clusters. Repository access should be limited to approved users, groups, service accounts, CI/CD identities, and GKE node service accounts based on their required function. Use repository level IAM where access should differ by application, team, environment, or repository, and avoid broad project level grants unless the same access is intentionally required across all repositories.
• Grant GKE node service accounts read only access to the specific Artifact Registry repositories they need to pull workload images from by assigning roles/artifactregistry.reader at the repository level.
• Grant CI/CD publishing identities access only to repositories where they need to publish images by assigning roles/artifactregistry.writer at the repository level.
• Remove excessive repository level access from unauthorized users, groups, service accounts, or public principals, including unnecessary roles/artifactregistry.admin, roles/artifactregistry.repoAdmin, roles/artifactregistry.writer, or public roles/artifactregistry.reader grants.
• Remove unnecessary project level Artifact Registry roles or broad basic roles such as Owner, Editor, and Viewer when repository level access is sufficient for the user, group, service account, or automation identity.
Confirm that GKE node service accounts can still pull approved images and that CI/CD identities can only publish to the repositories they are authorized to manage.</fixtext>
      <fix id="F-809F1CF6r01_fix"/>
      <check system="C-809F1CF6_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review IAM access for Artifact Registry Docker repositories that store images deployed to GKE Autopilot clusters. Verify that repository permissions are limited to approved users, groups, service accounts, CI/CD identities, and GKE node service accounts based on their required function. Artifact Registry access can be granted at the project level or repository level, but repository level IAM should be used when access needs to differ by application, team, environment, or repository.
List Artifact Registry Docker repositories used by GKE workloads:
gcloud artifacts repositories list \
--project=$PROJECT_ID \
--location=$LOCATION \
--format='table(name,format,mode)'
• Review each Artifact Registry repository IAM policy and verify that only approved principals have access, with roles limited to the minimum required level such as Internal Only - General Reader for image pulls, Writer for image publishing, and repository administrator roles only for approved platform or repository administrators.
Analyze project level IAM bindings that may grant access across Artifact Registry repositories in the project. Verify that Artifact Registry roles, Owner, Editor, and Viewer are granted only to principals that require the same level of access across all repositories. Use repository level IAM where access should be limited by application, team, environment, or repository.
Examine the GKE Autopilot cluster node service account and verify that it has only the Artifact Registry permissions required to pull approved workload images, typically read only access to the required repositories.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-74304A97">
    <title>5.1 Container Image Registry and Vulnerability Scanning</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-74304A97r01_rule" weight="10.0" severity="high">
      <version>5.1.3</version>
      <title>Enforce Binary Authorization for trusted GKE container image deployments</title>
      <description>&lt;VulnDiscussion&gt;Use Binary Authorization to enforce trusted image deployment policies for GKE Autopilot clusters. The policy should allow only approved container images to be deployed, based on defined admission rules such as approved image locations, trusted build or promotion processes, and required signed approvals where appropriate. Binary Authorization works with both Autopilot and Standard clusters.

Rationale:
Unrestricted image deployment can allow users or automation pipelines to run unapproved, vulnerable, tampered, or externally sourced images in the cluster. Binary Authorization integrates with GKE admission control to evaluate container images before deployment and enforce centrally managed policy decisions, such as requiring an image to come from an approved registry path or requiring a signed approval that verifies the image passed required build, scan, approval, or release controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.1.3</dc:identifier>
      </reference>
      <fixtext fixref="F-74304A97r01_fix">Enable Binary Authorization enforcement on the GKE Autopilot cluster when the audit shows that the cluster is not using an enforcing evaluation mode. Use PROJECT_SINGLETON_POLICY_ENFORCE to enforce the project Binary Authorization policy. If the cluster also uses continuous validation with policy bindings, use POLICY_BINDINGS_AND_PROJECT_SINGLETON_POLICY_ENFORCE instead. Monitoring only modes should not be used as the remediation target for this control.
gcloud container clusters update $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--binauthz-evaluation-mode=PROJECT_SINGLETON_POLICY_ENFORCE
Export the current Binary Authorization policy to a local YAML file. The trusted image deployment rules are edited in this policy file, not directly in the GKE cluster configuration.
gcloud container binauthz policy export \
--project=$PROJECT_ID &gt; policy.yaml
• Edit policy.yaml and update the defaultAdmissionRule or the applicable rule under clusterAdmissionRules so that trusted image controls are enforced. For signed approval based enforcement, set evaluationMode to REQUIRE_ATTESTATION set enforcementMode to ENFORCED_BLOCK_AND_AUDIT_LOG, and set requireAttestationsBy to the approved attestor resource.
Internal Only - General
defaultAdmissionRule:
evaluationMode: REQUIRE_ATTESTATION
enforcementMode: ENFORCED_BLOCK_AND_AUDIT_LOG
requireAttestationsBy:
- projects/$PROJECT_ID/attestors/$ATTESTOR_NAME
globalPolicyEvaluationMode: ENABLE
• Import the updated Binary Authorization policy after review and approval.
gcloud container binauthz policy import policy.yaml \
--project=$PROJECT_ID
After remediation, test deployment behavior with both an approved image and an unapproved image. Approved images should be admitted, while images that do not meet the Binary Authorization policy should be blocked and logged.</fixtext>
      <fix id="F-74304A97r01_fix"/>
      <check system="C-74304A97_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster configuration and verify that Binary Authorization enforcement is enabled for trusted container image deployments. Define the cluster name, location, and project, then check the cluster Binary Authorization evaluation mode.
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='value(binaryAuthorization.evaluationMode)'
• Expected output: The output should be PROJECT_SINGLETON_POLICY_ENFORCE or POLICY_BINDINGS_AND_PROJECT_SINGLETON_POLICY_ENFORCE. A monitoring only mode does not enforce trusted image deployment and should not be considered compliant for this control.
Export the Binary Authorization policy and review the default admission rule and any cluster specific admission rules that apply to the GKE cluster.
Internal Only - General
gcloud container binauthz policy export \
--project=$PROJECT_ID &gt; current-policy.yaml
Review the policy file and verify that the default rule is not configured to allow all images. The default Binary Authorization policy allows all images, so defaultAdmissionRule.evaluationMode: ALWAYS_ALLOW should be changed for production enforcement.
defaultAdmissionRule:
evaluationMode: ALWAYS_ALLOW
• Expected output: Production policies should use an enforcing rule such as REQUIRE_ATTESTATION with required signed approvals, or another approved rule design that blocks untrusted images. The policy should use ENFORCED_BLOCK_AND_AUDIT_LOG for enforcement rather than DRYRUN_AUDIT_LOG_ONLY, which allows noncompliant images to deploy while logging violations.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-C3DF1D8E">
    <title>5.2 Identity and Access Management</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-C3DF1D8Er01_rule" weight="10.0" severity="high">
      <version>5.2.1</version>
      <title>Use a custom least privilege node service account for GKE Autopilot</title>
      <description>&lt;VulnDiscussion&gt;Use a custom least privilege IAM service account for GKE Autopilot nodes instead of the Compute Engine default service account. The node service account should have only the IAM permissions required for GKE system tasks, logging, monitoring, and approved image pulls. GKE recommends using a custom node service account because the Compute Engine default service account might have broader permissions than GKE requires.

Rationale:
GKE uses IAM service accounts attached to nodes to perform system tasks such as logging and monitoring. At minimum, the node service account must have the Kubernetes Engine Default Node Service Account role, roles/container.defaultNodeServiceAccount, on the project. If the Compute Engine default service account is reused for other functions, it might accumulate broad IAM permissions that increase the risk of excessive access from the node identity.
For GKE Autopilot, the node service account is configured at the cluster level instead of through user managed node pools. A custom node service account can be specified when the Autopilot cluster is created, but it cannot be changed on an existing Autopilot cluster after creation.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.2.1</dc:identifier>
      </reference>
      <fixtext fixref="F-C3DF1D8Er01_fix">For new GKE Autopilot clusters, create a custom IAM service account for nodes and grant only the permissions required for GKE node operation. At minimum, grant roles/container.defaultNodeServiceAccount on the project.
gcloud iam service-accounts create $NODE_SERVICE_ACCOUNT_NAME \
--project $PROJECT_ID
gcloud projects add-iam-policy-binding $PROJECT_ID \
--member=serviceAccount:$NODE_SERVICE_ACCOUNT \
--role=roles/container.defaultNodeServiceAccount
Create the Autopilot cluster with the custom node service account.
gcloud container clusters create-auto $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--service-account=$NODE_SERVICE_ACCOUNT
If the existing Autopilot cluster was created with the Compute Engine default service account or an overly privileged node service account, create a replacement Autopilot cluster with the correct custom service account and migrate workloads. The node service account for an existing Autopilot cluster cannot be changed after cluster creation.
Internal Only - General</fixtext>
      <fix id="F-C3DF1D8Er01_fix"/>
      <check system="C-C3DF1D8E_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and verify that the cluster uses a custom node service account instead of the Compute Engine default service account.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format='table(name,nodeConfig.serviceAccount)'
• Expected output: nodeConfig.serviceAccount should show a custom IAM service account, not the Compute Engine default service account. If the field is empty or shows the project’s Compute Engine default service account, the cluster does not meet this control.
Review the IAM roles assigned to the node service account and verify that they are limited to the permissions required for GKE node operation and approved image pulls.
gcloud projects get-iam-policy $PROJECT_ID \
--flatten='bindings[].members' \
--filter='bindings.members:serviceAccount:$NODE_SERVICE_ACCOUNT' \
--format='table(bindings.role)'
• Expected output: the node service account should have roles/container.defaultNodeServiceAccount and only additional roles that are required for the cluster’s operating model, such as read access to approved Artifact Registry repositories when needed. Avoid broad roles such as Owner, Editor, or unnecessary administrator roles.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-892E3A91">
    <title>5.3 Cloud Key Management Service</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-892E3A91r01_rule" weight="10.0" severity="high">
      <version>5.3.1</version>
      <title>Enable application-layer Secrets encryption with Cloud KMS keys</title>
      <description>&lt;VulnDiscussion&gt;Enable application layer Secrets encryption for GKE Autopilot clusters using a Cloud KMS key. This control adds customer managed encryption for Kubernetes Secret resources stored in the cluster state database, providing an additional protection layer beyond Google managed encryption at rest.

Rationale:
By default, GKE encrypts customer content at rest, including Kubernetes Secrets, using encryption that Google manages automatically. Application layer Secrets encryption adds another layer by encrypting Secret data before it is written to the cluster state database, using a key that is managed in Cloud KMS.
This improves protection for sensitive Kubernetes Secret data by allowing the organization to manage the encryption key lifecycle, including key access control, rotation, and disablement. It is important for clusters that store sensitive application credentials, tokens, certificates, or other Secret values that require stronger key management controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.3.1</dc:identifier>
      </reference>
      <fixtext fixref="F-892E3A91r01_fix">Enable application layer encryption on the GKE Autopilot cluster when the audit shows that databaseEncryption.state is not ALL_OBJECTS_ENCRYPTION_ENABLED or ENCRYPTED, databaseEncryption.currentState is not CURRENT_STATE_ALL_OBJECTS_ENCRYPTION_ENABLED or CURRENT_STATE_ENCRYPTED, or databaseEncryption.keyName does not reference the approved Cloud KMS CryptoKey. Before enabling encryption, grant the Kubernetes Engine Service Agent permission to use the Cloud KMS key. The service agent needs the Cloud KMS CryptoKey Encrypter/Decrypter role so GKE can encrypt and decrypt Kubernetes Secret data in the cluster state database.
Define the approved Cloud KMS key resource:
KMS_KEY=projects/$KMS_PROJECT_ID/locations/$KMS_LOCATION/keyRings/$KMS_KEYRIN
G/cryptoKeys/$KMS_KEY_NAME
Grant the Kubernetes Engine Service Agent access to use the key:
gcloud kms keys add-iam-policy-binding $KMS_KEY \
--member serviceAccount:service-$PROJECT_NUMBER@container-engine-
robot.iam.gserviceaccount.com \
--role roles/cloudkms.cryptoKeyEncrypterDecrypter
Enable application layer Secrets encryption on the GKE Autopilot cluster:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--database-encryption-key $KMS_KEY
After the update completes, rerun the audit command:
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='json(databaseEncryption)'
Expected values:
Internal Only - General
{
"databaseEncryption": {
"currentState": "CURRENT_STATE_ALL_OBJECTS_ENCRYPTION_ENABLED",
"keyName":
"projects/$KMS_PROJECT_ID/locations/$KMS_LOCATION/keyRings/$KMS_KEYRING/crypt
oKeys/$KMS_KEY_NAME",
"state": "ALL_OBJECTS_ENCRYPTION_ENABLED"
}
}
Depending on the cluster API response, the expected values might also appear as state: ENCRYPTED and currentState: CURRENT_STATE_ENCRYPTED. The key finding is that encryption is enabled and databaseEncryption.keyName references the approved Cloud KMS CryptoKey. Updating an existing cluster can restart the control plane and re-encrypt existing data, so apply the change during an approved maintenance window, especially for zonal clusters.</fixtext>
      <fix id="F-892E3A91r01_fix"/>
      <check system="C-892E3A91_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and verify that application layer encryption is enabled with an approved Cloud KMS key. This confirms that eligible Kubernetes resources in the cluster state database are encrypted using a customer managed Cloud KMS key in addition to Google managed encryption at rest.
Check the database encryption configuration for the cluster:
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location=$LOCATION \
--project=$PROJECT_ID \
--format='json(databaseEncryption)'
• Expected output: databaseEncryption.state should return ALL_OBJECTS_ENCRYPTION_ENABLED or ENCRYPTED, databaseEncryption.currentState should return CURRENT_STATE_ALL_OBJECTS_ENCRYPTION_ENABLED or CURRENT_STATE_ENCRYPTED, and databaseEncryption.keyName should reference the approved Cloud KMS CryptoKey. If state or currentState shows a disabled, decrypted, or transitional value, or if keyName does not match the approved Cloud KMS key, application layer encryption is not correctly configured.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-0C57289F">
    <title>5.4 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-0C57289Fr01_rule" weight="10.0" severity="medium">
      <version>5.4.1</version>
      <title>Enable VPC Flow Logs and Intranode Visibility</title>
      <description>&lt;VulnDiscussion&gt;Enable VPC Flow Logs on the subnet used by GKE Autopilot nodes and enable intranode visibility on the GKE cluster to improve visibility into Pod traffic. This combination allows network flow logging to include traffic between Pods, including traffic between Pods running on the same worker node.

Rationale:
VPC Flow Logs capture network flow records for subnet traffic, but Pod to Pod traffic on the same node requires GKE intranode visibility so that the traffic is visible to the VPC network. Enabling both controls improves traffic observability for security monitoring, network troubleshooting, forensics, and validation of expected workload communication patterns.
Without intranode visibility, traffic between Pods on the same node can bypass VPC level visibility, reducing the usefulness of VPC Flow Logs for Kubernetes east west traffic analysis. Intranode visibility sends Pod to Pod traffic on the same node through the VPC networking path, allowing VPC Flow Logs and other VPC features to observe that traffic.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.4.1</dc:identifier>
      </reference>
      <fixtext fixref="F-0C57289Fr01_fix">Enable intranode visibility on the GKE Autopilot cluster when the audit shows that networkConfig.enableIntraNodeVisibility is not true. Enabling intranode visibility makes Pod to Pod traffic on the same node visible to the VPC network path so it can be captured by VPC Flow Logs and other VPC features. Plan the change during an approved maintenance window because enabling intranode visibility on an existing cluster can restart components in the control plane and worker nodes.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-intra-node-visibility
After intranode visibility is enabled, review the subnet used by the GKE Autopilot cluster and confirm that VPC Flow Logs are enabled for that subnet. VPC Flow Logs are configured at the subnet level, while intranode visibility is configured at the cluster level, so both settings must be in place for complete network flow visibility.
• If VPC Flow Logs are not enabled for the subnet used by the GKE Autopilot cluster, enable flow logging on that subnet.
• Configure VPC Flow Logs settings such as aggregation interval, sampling, metadata inclusion, and log filters based on security monitoring, troubleshooting and retention.
• Rerun the audit check and confirm that intranode visibility returns true and that the GKE subnet has an effective VPC Flow Logs configuration.
Internal Only - General</fixtext>
      <fix id="F-0C57289Fr01_fix"/>
      <check system="C-0C57289F_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and verify that intranode visibility is enabled.
Intranode visibility is enabled at the cluster level and makes Pod to Pod traffic on the same node visible to the VPC network path so VPC Flow Logs can capture that traffic.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.networkConfig.enableIntraNodeVisibility'
• Expected output: true. If the result is false, blank, or missing, intranode visibility is not enabled for the cluster and should be remediated.
Internal Only - General Identify the subnet used by the GKE Autopilot cluster so the VPC Flow Logs configuration can be reviewed for the correct network path. GKE clusters use a VPC subnet for node and Pod networking, and VPC Flow Logs must apply to that subnet to provide network flow visibility for the cluster.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format='value(subnetwork)'
Use the subnet returned by the command as $SUBNET_NAME, and confirm the subnet region is set as $REGION.
Review the subnet used by the GKE Autopilot cluster and verify that VPC Flow Logs are enabled.
gcloud compute networks subnets describe $SUBNET_NAME \
--region $REGION \
--project $PROJECT_ID \
--format='value(enableFlowLogs)'
• Expected output: true. If the result is false, blank, or missing, VPC Flow Logs are not enabled for the subnet and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-0711AE63">
    <title>5.4 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-0711AE63r01_rule" weight="10.0" severity="high">
      <version>5.4.2</version>
      <title>Restrict GKE control plane access with authorized networks</title>
      <description>&lt;VulnDiscussion&gt;Restrict GKE Autopilot control plane access by enabling authorized networks for IP based control plane endpoints and allowing only approved CIDR ranges. This reduces exposure of the Kubernetes API server by limiting which source IP ranges can reach the control plane over the configured IP endpoint.

Rationale:
GKE control plane access can use DNS based endpoints and IP based endpoints.
When IP based endpoints are enabled, authorized networks provide an IP based access control layer that limits control plane reachability to approved CIDR ranges.
Without authorized networks, the control plane IP endpoint can be reachable from any external IP address, including public internet addresses and Google Cloud external IP addresses, subject to authentication and authorization.
Restricting access to approved CIDR ranges reduces the control plane attack surface and helps protect the Kubernetes API server if credentials, certificates, or automation tokens are exposed. For stronger control plane isolation, authorized networks should be combined with private endpoint configuration where appropriate, denial of broad Google Cloud external IP access, and IAM based authentication controls.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.4.2</dc:identifier>
      </reference>
      <fixtext fixref="F-0711AE63r01_fix">Enable control plane authorized networks when the audit shows that masterAuthorizedNetworksConfig is missing, does not show authorized networks as enabled, does not include approved cidrBlocks, or includes broad ranges such as 0.0.0.0/0. Authorized networks should include only trusted administrative, bastion, VPN, CI/CD, or automation source ranges that require access to the GKE Autopilot control plane IP endpoint, and use authorized networks when IP based control plane endpoints are enabled.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-master-authorized-networks \
--master-authorized-networks &lt;authorized-cidrs&gt;
If broad access from Google Cloud external IP addresses is not explicitly approved, disable that access so external IP addresses from Google Cloud are not automatically allowed to reach the control plane.
Internal Only - General
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--no-enable-google-cloud-access
If the cluster uses the control plane internal endpoint and the same CIDR restrictions should apply to private endpoint access, enable authorized network enforcement on the private endpoint.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-authorized-networks-on-private-endpoint
Note: Rerun the audit command and confirm that masterAuthorizedNetworksConfig shows authorized networks enabled, cidrBlocks contains only approved trusted source ranges, and gcpPublicCidrsAccessEnabled is false unless explicitly approved.</fixtext>
      <fix id="F-0711AE63r01_fix"/>
      <check system="C-0711AE63_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and verify that control plane authorized networks are configured to restrict access to approved CIDR ranges.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.masterAuthorizedNetworksConfig'
Expected output should show the following:
Internal Only - General
{
"enabled": true,
"cidrBlocks": [
{
"cidrBlock": "203.0.113.0/24",
"displayName": "approved-admin-range"
}
],
"gcpPublicCidrsAccessEnabled": false
}
Review the command line statement for the following:
• enabled should be true, indicating authorized networks are enabled for the control plane.
• cidrBlocks should list only approved trusted source ranges, such as administrative, bastion, VPN, CI/CD, or automation networks.
• gcpPublicCidrsAccessEnabled should be false unless access from Google Cloud external IP ranges is explicitly approved.
• If the output only shows gcpPublicCidrsAccessEnabled: false, authorized networks are not fully configured for this control because no approved CIDR ranges are listed.
• If the output is null, {}, missing enabled: true, missing cidrBlocks, or includes broad ranges such as 0.0.0.0/0, the cluster should be reviewed and remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-B456A198">
    <title>5.4 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-B456A198r01_rule" weight="10.0" severity="high">
      <version>5.4.3</version>
      <title>Use DNS based access or private GKE control plane endpoints</title>
      <description>&lt;VulnDiscussion&gt;Use DNS based access or private GKE control plane endpoints to reduce Kubernetes API server exposure. Prefer DNS based control plane access where possible, and if IP based access is required, disable the external IP based endpoint or restrict it to trusted networks only.

Rationale:
The GKE control plane supports DNS based endpoints and IP based endpoints. The DNS based endpoint provides a stable fully qualified domain name for the control plane and can be protected using IAM, VPC Service Controls, and approved authentication methods. Kubernetes ServiceAccount tokens and client certificates should be enabled for DNS based access only when required. This approach avoids exposing the control plane through a public IP address and provides a more flexible access model for administrators, automation, and private clients.
When IP based endpoints are required, disabling the external IP based endpoint or using authorized networks limits who can reach the Kubernetes API server. This reduces the control plane attack surface and helps protect the cluster if credentials, certificates, or automation tokens are exposed. If an external IP based endpoint must remain enabled, access should be restricted to approved CIDR ranges only.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.4.3</dc:identifier>
      </reference>
      <fixtext fixref="F-B456A198r01_fix">Remediate existing GKE clusters by updating the control plane access configuration.
Recreating the cluster is not required for this control. Choose the access model that matches the organization’s approved management path.
Method 1: Use DNS based control plane access and disable IP based endpoint access where appropriate:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-dns-access \
--no-enable-ip-access
Method 2: Use IP based access with the external endpoint disabled and the private endpoint enabled:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-ip-access \
--enable-private-endpoint
If the public IP based endpoint must remain enabled, restrict it with authorized networks and use only approved CIDR ranges. Rerun the audit command and confirm that the selected access model is reflected in controlPlaneEndpointsConfig. If using DNS based access, update kubeconfig to use the DNS endpoint as needed.
Internal Only - General</fixtext>
      <fix id="F-B456A198r01_fix"/>
      <check system="C-B456A198_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE cluster and verify that control plane access is configured through either DNS based access or private IP based access with the external endpoint disabled. The preferred current approach is to use DNS based access where possible. If IP based access is required, the public endpoint should be disabled or tightly restricted.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.controlPlaneEndpointsConfig'
Review the command line output for the following:
• For DNS based access, dnsEndpointConfig.endpoint should be populated and allowExternalTraffic should reflect the approved access path.
enableK8sTokensViaDns and enableK8sCertsViaDns should be true only when Kubernetes ServiceAccount token or client certificate authentication is explicitly required.
• For private IP based access, ipEndpointsConfig.enabled should be true, ipEndpointsConfig.enablePublicEndpoint should be false, and ipEndpointsConfig.privateEndpoint should be populated.
• If the external IP based endpoint remains enabled, authorized networks should restrict access to approved CIDR ranges only.
• If neither DNS based access nor private IP based access is configured, or if the public IP based endpoint is broadly reachable, the cluster should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-86A40B0D">
    <title>5.4 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-86A40B0Dr01_rule" weight="10.0" severity="high">
      <version>5.4.4</version>
      <title>Ensure private GKE nodes are configured without external IP addresses</title>
      <description>&lt;VulnDiscussion&gt;Configure GKE Autopilot nodes without external IP addresses so worker nodes are reachable only through internal network paths. Private nodes reduce direct exposure of the node network interface to external clients while still allowing the cluster to run workloads and communicate with the control plane through internal connectivity.

Rationale:
Nodes with external IP addresses increase the externally reachable surface of the cluster infrastructure. Configuring private nodes limits direct inbound access to the node VMs from the internet and helps ensure node administration, workload traffic, and supporting services use approved private connectivity patterns, firewall rules, and controlled egress paths.
Private nodes also support stronger network isolation because workloads run on nodes with only internal IP addresses. If internet or Google API access is required, it should be provided through controlled paths such as Cloud NAT, Private Google Access, or approved private connectivity rather than direct external IP assignment on the nodes.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.4.4</dc:identifier>
      </reference>
      <fixtext fixref="F-86A40B0Dr01_fix">Remediate by enabling private nodes at the cluster level for GKE Autopilot workloads.
Private nodes provision GKE nodes with internal IP addresses only. For Autopilot clusters, update the cluster level private node configuration instead of updating individual node pools. The cluster update takes effect after GKE reschedules workloads onto nodes that match the configuration.
Enable private nodes as the cluster level default for GKE Autopilot workloads:
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--enable-private-nodes \
--enable-ip-alias
If workload level placement is required, configure the workload to schedule on private nodes by using the cloud.google.com/private-node=true node selector.
Note: After remediation, rerun the audit command and confirm that the cluster level default returns true. Validate that private nodes still have required outbound access through approved paths such as Cloud NAT, Private Google Access, or private connectivity for image pulls, package downloads, Google APIs, and third party services.</fixtext>
      <fix id="F-86A40B0Dr01_fix"/>
      <check system="C-86A40B0D_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and verify that private nodes are configured without external IP addresses. For Autopilot clusters, private node configuration is evaluated at the cluster level instead of through user managed node pools.
Check the cluster level private node default:
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.privateClusterConfig.enablePrivateNodes'
• Expected output: true. If the result is false, blank, or missing, the cluster level default does not require Autopilot workloads to use private nodes.
Review any workload level node selection that explicitly controls private or public node placement. In Autopilot, workloads can use the cloud.google.com/private- node=true selector to schedule on private nodes when workload level configuration is required.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-05623EA5">
    <title>5.4 Cluster Networking</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-05623EA5r01_rule" weight="10.0" severity="high">
      <version>5.4.5</version>
      <title>Use Google managed SSL certificates for GKE Ingress TLS</title>
      <description>&lt;VulnDiscussion&gt;Use Google managed SSL certificates for GKE Ingress TLS to encrypt client traffic between external users and the Google Cloud load balancer. Google provisions and renews these certificates for the configured domains, reducing manual certificate lifecycle management for supported GKE Ingress use cases.

Rationale:
TLS encryption for GKE Ingress helps protect application traffic between clients and the load balancer by preventing plaintext exposure over external networks. Google managed SSL certificates reduce operational risk by having Google provision, manage, and automatically renew domain validated certificates for supported load balancer and Ingress configurations.
This control is most appropriate for HTTPS traffic exposed through GKE Ingress. It does not apply to TCP or UDP load balancers created through a Service of type LoadBalancer, and GKE Ingress does not support certificates managed by Certificate Manager. Use Gateway API if Certificate Manager managed certificates are required.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.4.5</dc:identifier>
      </reference>
      <fixtext fixref="F-05623EA5r01_fix">Remediate public HTTPS workloads by using GKE Ingress with Google managed SSL certificates where this pattern is appropriate. This applies to HTTPS traffic terminated by the external Application Load Balancer through GKE Ingress and does not apply to TCP or UDP workloads exposed through Service objects of type LoadBalancer.
• For public HTTP or HTTPS workloads exposed directly through Service objects of type LoadBalancer, evaluate whether they should be exposed through GKE Ingress so Google managed SSL certificates can be used.
• Create a ManagedCertificate object in the same namespace as the Ingress and define the approved non wildcard domain names for the certificate.
• Update the Ingress metadata to include the networking.gke.io/managed- certificates annotation with the ManagedCertificate resource name.
Internal Only - General Multiple managed certificates can be listed as comma separated values when needed.
• Confirm that DNS records for the certificate domains point to the load balancer IP address, because Google must validate domain ownership before the certificate becomes active.
• After the certificate is active, verify that the Ingress serves HTTPS traffic using the Google managed certificate and remove any obsolete self managed certificate references that are no longer required.</fixtext>
      <fix id="F-05623EA5r01_fix"/>
      <check system="C-05623EA5_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review public application exposure and identify workloads using Service objects of type LoadBalancer. Google managed SSL certificates for GKE Ingress apply to HTTPS load balancers created through Ingress, not TCP or UDP load balancers created directly from Service objects. Public HTTP or HTTPS workloads exposed through LoadBalancer services should be reviewed to determine whether GKE Ingress is the better TLS termination pattern.
Internal Only - General
kubectl get svc -A -o json | jq '.items[] |
select(.spec.type=="LoadBalancer")'
• Expected output: Check for GKE Ingress resources and confirm that HTTPS Ingresses reference one or more Google managed certificates through the networking.gke.io/managed-certificates annotation.
kubectl get ingress -A -o json | jq '.items[] | {
namespace: .metadata.namespace,
name: .metadata.name,
managedCertificates: .metadata.annotations."networking.gke.io/managed-
certificates",
status: .status
}'
• Expected output: Each applicable HTTPS Ingress should include the etworking.gke.io/managed-certificates annotation with one or more ManagedCertificate resource names. The annotation value is a comma separated list of ManagedCertificate objects that must exist in the same namespace as the Ingress.
{
"networking.gke.io/managed-certificates": "example-certificate"
}
Verify that the referenced ManagedCertificate objects exist and are active.
kubectl get managedcertificates -A
• Expected output: Each certificate referenced by an Ingress should exist in the same namespace as that Ingress, cover the expected domain names, and show an active or successfully provisioned status. If the certificate is missing, pending, failed, or not referenced by the Ingress annotation, Google managed SSL certificates are not correctly configured for that Ingress.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-8E8AE20A">
    <title>5.5 Authentication and Authorization</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-8E8AE20Ar01_rule" weight="10.0" severity="high">
      <version>5.5.1</version>
      <title>Use Google Groups to centrally manage Kubernetes RBAC access for GKE clusters</title>
      <description>&lt;VulnDiscussion&gt;Use Google Groups for RBAC to centrally manage Kubernetes RBAC access for GKE Autopilot clusters. Cluster administrators should assign Kubernetes RoleBinding and ClusterRoleBinding permissions to approved Google Groups rather than directly binding access to individual user email addresses.

Rationale:
Group based RBAC improves access governance by allowing user onboarding, role changes, and offboarding to be managed through centralized group membership instead of editing Kubernetes RBAC bindings for each individual user. This reduces inconsistent one off permissions, simplifies access reviews, and helps ensure that access is removed when a user leaves the organization or changes responsibilities.
For GKE, the required gke-security-groups group acts as the security group used to evaluate Google Group membership for Kubernetes RBAC. Approved access groups should be nested under that security group and then referenced in Kubernetes RoleBinding or ClusterRoleBinding subjects.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.5.1</dc:identifier>
      </reference>
      <fixtext fixref="F-8E8AE20Ar01_fix">Enable Google Groups for RBAC when the audit shows that authenticatorGroupsConfig.enabled is not true, or when securityGroup is missing or incorrect. The required security group must be named gke-security- groups@DOMAIN, and approved access groups should be nested under that security group before they are referenced in Kubernetes RBAC bindings.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--security-group=gke-security-groups@$DOMAIN
After Google Groups for RBAC is enabled, update Kubernetes RoleBinding and ClusterRoleBinding objects to reference approved Google Groups as kind: Group subjects. Replace direct User bindings with group based bindings where practical, while preserving required ServiceAccount bindings for workloads, controllers, and automation.
Internal Only - General Review the RBAC table from the audit and remediate findings as follows:
• Replace unnecessary SUBJECT_KIND values of User with approved Group subjects.
• Confirm each Google Group referenced in RBAC is nested under gke- security-groups@DOMAIN.
• Confirm group names match exactly because Google Group references in RBAC are case sensitive.
• Remove direct user bindings only after validating that equivalent group based access is working.
• Test access for representative users with kubectl auth can-i before removing existing access paths.
Rerun the audit commands and confirm that Google Groups for RBAC is enabled, the configured security group is correct, and Kubernetes RBAC bindings use approved Google Groups instead of direct individual user binding.</fixtext>
      <fix id="F-8E8AE20Ar01_fix"/>
      <check system="C-8E8AE20A_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review the GKE Autopilot cluster and verify that Google Groups for RBAC is enabled and configured with the required security group. Google Groups for RBAC lets Kubernetes RBAC permissions be assigned to Google Groups managed in Google Workspace or Cloud Identity, rather than binding access directly to individual user email addresses.
Internal Only - General
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '{
enabled: .authenticatorGroupsConfig.enabled,
securityGroup: .authenticatorGroupsConfig.securityGroup
}'
• Expected output: enabled should return true, and securityGroup should be set to gke-security-groups@DOMAIN. The local part must be gke-security- groups, and the domain should match the Google Workspace or Cloud Identity domain used for group management.
Review Kubernetes RBAC bindings and verify that access is assigned to approved Google Groups instead of individual users where practical.
kubectl get rolebindings,clusterrolebindings -A -o json | jq -r '
["BINDING_KIND","NAMESPACE","BINDING_NAME","SUBJECT_KIND","SUBJECT_NAME","ROL
E_KIND","ROLE_NAME"],
(.items[] as $b |
$b.subjects[]? |
select(.kind=="User" or .kind=="Group") |
[$b.kind, ($b.metadata.namespace // "cluster-wide"), $b.metadata.name,
.kind, .name, $b.roleRef.kind, $b.roleRef.name]
)
| @tsv' | column -t -s $'\t'
• Expected output: RBAC subjects should primarily use SUBJECT_KIND of Group with approved Google Group email addresses that are nested under gke- security-groups@DOMAIN. Direct User bindings should be reviewed and removed unless there is a documented exception. Google Group names used in RBAC bindings are case sensitive and must match exactly.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-26185D15">
    <title>5.6 Storage</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-26185D15r01_rule" weight="10.0" severity="high">
      <version>5.6.1</version>
      <title>Use CMEK protected StorageClasses for GKE Persistent Disk volumes</title>
      <description>&lt;VulnDiscussion&gt;Use CMEK protected StorageClass objects for GKE Autopilot Persistent Disk volumes so newly provisioned Persistent Disks are encrypted with an approved Cloud KMS key.
This applies to dynamically provisioned Persistent Disk volumes that use the Compute Engine Persistent Disk CSI driver and the disk-encryption-kms-key parameter.

Rationale:
Persistent Disks are encrypted at rest by default, but CMEK gives the organization direct control over the Cloud KMS key used to protect the disk encryption keys. This supports stronger key governance, including key access control, rotation, separation of duties, and the ability to disable key use when required by security or compliance processes.
Using a CMEK protected StorageClass helps ensure that new Persistent Disk volumes created by Kubernetes are encrypted consistently with approved key material. The disk-encryption-kms-key value must reference the fully qualified Cloud KMS key resource, and the key ring location must align with the cluster location requirements.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.6.1</dc:identifier>
      </reference>
      <fixtext fixref="F-26185D15r01_fix">Remediate any StorageClass or Persistent Disk that does not use an approved Cloud KMS key. CMEK for GKE Autopilot Persistent Disk volumes is applied through a CMEK protected StorageClass for new dynamically provisioned volumes. Existing Persistent Disks cannot be converted to CMEK in place, so noncompliant volumes must be replaced or migrated.
• Grant the Compute Engine service agent for the cluster project roles/cloudkms.cryptoKeyEncrypterDecrypter on the approved Cloud KMS key.
Internal Only - General
• Create a new CMEK protected StorageClass using the Persistent Disk CSI driver, pd.csi.storage.gke.io, and the approved disk-encryption-kms-key value.
• Update new PersistentVolumeClaims to use the CMEK protected StorageClass. If this should be the default storage option, mark the CMEK protected StorageClass as the default only after validating the key, location, and access permissions.
• For existing PersistentVolumes that do not return an approved diskEncryptionKey.kmsKeyName, provision a new CMEK protected Persistent Disk through the approved StorageClass, migrate the data, update the workload to use the new PersistentVolumeClaim, and retire the old volume after validation.
• Rerun the audit commands and confirm that the StorageClass includes the approved disk-encryption-kms-key, the PersistentVolumes reference the expected StorageClass, and the underlying Compute Engine disks return the approved Cloud KMS key.</fixtext>
      <fix id="F-26185D15r01_fix"/>
      <check system="C-26185D15_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Review GKE Autopilot StorageClass objects and verify that Persistent Disk classes use the Compute Engine Persistent Disk CSI driver with an approved Cloud KMS key.
The disk-encryption-kms-key parameter should contain the fully qualified Cloud KMS key resource path used to encrypt newly provisioned Persistent Disk volumes.
Internal Only - General
kubectl get storageclass -o json | jq '.items[] |
select(.provisioner=="pd.csi.storage.gke.io") | {
name: .metadata.name,
provisioner: .provisioner,
kmsKey: .parameters."disk-encryption-kms-key"
}'
• Expected output: Each approved Persistent Disk StorageClass used for sensitive workloads should show pd.csi.storage.gke.io as the provisioner and a non null kmsKey value that matches an approved Cloud KMS key. If kmsKey is null, blank, missing, or does not reference an approved key, newly provisioned Persistent Disk volumes using that StorageClass are not configured for CMEK.
Identify existing PersistentVolumes and review the associated StorageClass and underlying disk reference.
kubectl get pv -o json | jq -r '.items[] | [.metadata.name,
.spec.storageClassName, .spec.csi.volumeHandle] | @tsv'
• Expected output: PersistentVolumes backed by GKE Persistent Disk should reference an approved CMEK protected StorageClass. The volumeHandle value should identify the underlying Compute Engine disk used for the PersistentVolume.
For each Persistent Disk identified, verify that the disk is encrypted with the approved Cloud KMS key.
gcloud compute disks describe $DISK_NAME \
--zone $ZONE \
--project $PROJECT_ID \
--format='value(diskEncryptionKey.kmsKeyName)'
• Expected output: the command should return the approved Cloud KMS key name. If the result is empty or null, the Persistent Disk is not encrypted with CMEK.</check-content>
      </check>
    </Rule>
  </Group>
  <Group id="V-54B06A01">
    <title>5.7 Additional Cluster Configurations</title>
    <description>&lt;GroupDescription&gt;&lt;/GroupDescription&gt;</description>
    <Rule id="SV-54B06A01r01_rule" weight="10.0" severity="high">
      <version>5.7.1</version>
      <title>Enable the GKE security posture dashboard for cluster security insights</title>
      <description>&lt;VulnDiscussion&gt;Enable the GKE security posture dashboard to surface cluster and workload security insights, including workload configuration auditing, actionable security concerns, and security bulletin related findings. The dashboard provides a centralized view of security posture findings so teams can identify, prioritize, and remediate issues across eligible GKE clusters and workloads.

Rationale:
The GKE security posture dashboard helps identify workload and cluster configuration risks during runtime by scanning Kubernetes workloads for security concerns and presenting actionable findings in the Google Cloud console. This improves visibility into misconfigurations, risky workload settings, and security concerns that might otherwise require manual review across namespaces, manifests, and clusters.
Security posture findings can support operational review, remediation tracking, and security governance across individual clusters or fleets.&lt;/VulnDiscussion&gt;</description>
      <reference>
        <dc:title>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:title>
        <dc:publisher>CIS</dc:publisher>
        <dc:type>Benchmark</dc:type>
        <dc:subject>CIS Google Kubernetes Engine (GKE) Autopilot Benchmark</dc:subject>
        <dc:identifier>5.7.1</dc:identifier>
      </reference>
      <fixtext fixref="F-54B06A01r01_fix">Enable the GKE security posture dashboard for workload configuration auditing when the audit shows that securityPostureConfig.mode is not BASIC. GKE supports enabling configuration auditing on existing clusters by setting the security posture mode to standard, which maps to BASIC in the cluster configuration.
gcloud container clusters update $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--security-posture=standard
For new clusters, include --security-posture=standard during cluster creation when this setting must be explicitly enforced. Rerun the audit command and confirm that securityPostureConfig.mode returns BASIC. Initial configuration audit results can take up to 30 minutes to appear in the security posture dashboard and related logs.</fixtext>
      <fix id="F-54B06A01r01_fix"/>
      <check system="C-54B06A01_chk">
        <check-content-ref href="CIS_Google_Kubernetes_Engine_GKE_Autopilot_Benchmark.xml" name="M"/>
        <check-content>Define three variables for Cluster Name, Location, and Project, then review the cluster security posture configuration.
gcloud container clusters describe $CLUSTER_NAME \
--location $LOCATION \
--project $PROJECT_ID \
--format json | jq '.securityPostureConfig'
• Expected output: mode should return BASIC. The vulnerabilityMode value might return VULNERABILITY_DISABLED, which is acceptable for this control because the control is focused on the GKE security posture dashboard and workload configuration auditing, not deprecated workload vulnerability scanning.
Internal Only - General
{
"mode": "BASIC",
"vulnerabilityMode": "VULNERABILITY_DISABLED"
}
If mode is DISABLED, MODE_UNSPECIFIED, null, blank, or missing, the GKE security posture dashboard is not enabled for workload configuration auditing and should be remediated.</check-content>
      </check>
    </Rule>
  </Group>
</Benchmark>

