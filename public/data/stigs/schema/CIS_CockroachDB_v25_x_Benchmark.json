{
  "+p_xml": "version=\"1.0\" encoding=\"utf-8\"",
  "Benchmark": {
    "+@xmlns:dc": "http://purl.org/dc/elements/1.1/",
    "+@xmlns:xsi": "http://www.w3.org/2001/XMLSchema-instance",
    "+@xmlns:cpe": "http://cpe.mitre.org/language/2.0",
    "+@xmlns:xhtml": "http://www.w3.org/1999/xhtml",
    "+@xmlns:dsig": "http://www.w3.org/2000/09/xmldsig#",
    "+@xsi:schemaLocation": "http://checklists.nist.gov/xccdf/1.1 http://nvd.nist.gov/schema/xccdf-1.1.4.xsd",
    "+@id": "CIS_CockroachDB_v25_x_Benchmark",
    "+@xml:lang": "en",
    "+@xmlns": "http://checklists.nist.gov/xccdf/1.1",
    "status": {
      "+content": "accepted",
      "+@date": "2026-05-26"
    },
    "title": "CIS CockroachDB v25.x Benchmark",
    "description": "All CIS Benchmarks ™ (Benchmarks) focus on technical configuration settings used to maintain and/or increase the security of the addressed technology, and they should be used in conjunction with other essential cyber hygiene tasks like: • Monitoring the base operating system and applications for vulnerabilities and quickly updating with the latest security patches. • End-point protection (Antivirus software, Endpoint Detection and Response (EDR), etc.). • Logging and monitoring user and system activity. In the end, the Benchmarks are designed to be a key component of a comprehensive cybersecurity program. Important Usage Information All Benchmarks are available free for non-commercial use from the CIS Website. They can be used to manually assess and remediate systems and applications. In lieu of manual assessment and remediation, there are several tools available to assist with assessment: • CIS Configuration Assessment Tool (CIS-CAT ® Pro Assessor) • CIS Benchmarks ™ Certified 3rd Party Tooling These tools make the hardening process much more scalable for large numbers of systems and applications. NOTE: Some tooling focuses only on the Benchmark Recommendations that can be fully automated (skipping ones marked Manual). It is important that ALL Recommendations ( Automated and Manual) be addressed since all are important for properly securing systems and are typically in scope for audits. Key Stakeholders Cybersecurity is a collaborative effort, and cross functional cooperation is imperative within an organization to discuss, test, and deploy Benchmarks in an effective and efficient way. The Benchmarks are developed to be best practice configuration guidelines applicable to a wide range of use cases. In some organizations, exceptions to specific Recommendations will be needed, and this team should work to prioritize the problematic Recommendations based on several factors like risk, time, cost, and labor. These exceptions should be properly categorized and documented for auditing purposes. Apply the Correct Version of a Benchmark Benchmarks are developed and tested for a specific set of products and versions and applying an incorrect Benchmark to a system can cause the resulting pass/fail score to be incorrect. This is due to the assessment of settings that do not apply to the target systems. To assure the correct Benchmark is being assessed: • Deploy the Benchmark applicable to the way settings are managed in the environment: An example of this is the Microsoft Windows family of Benchmarks, which have separate Benchmarks for Group Policy, Intune, and Stand-alone systems based upon how system management is deployed. Applying the wrong Benchmark in this case will give invalid results. • Use the most recent version of a Benchmark: This is true for all Benchmarks, but especially true for cloud technologies. Cloud technologies change frequently and using an older version of a Benchmark may have invalid methods for auditing and remediation. Exceptions The guidance items in the Benchmarks are called recommendations and not requirements, and exceptions to some of them are expected and acceptable. The Benchmarks strive to be a secure baseline, or starting point, for a specific technology, with known issues identified during Benchmark development are documented in the Impact section of each Recommendation. In addition, organizational, system specific requirements, or local site policy may require changes as well, or an exception to a Recommendation or group of Recommendations (e.g. A Benchmark could Recommend that a Web server not be installed on the system, but if a system's primary purpose is to function as a Webserver, there should be a documented exception to this Recommendation for that specific server). In the end, exceptions to some Benchmark Recommendations are common and acceptable, and should be handled as follows: • The reasons for the exception should be reviewed cross-functionally and be well documented for audit purposes. • A plan should be developed for mitigating, or eliminating, the exception in the future, if applicable. • If the organization decides to accept the risk of this exception (not work toward mitigation or elimination), this should be documented for audit purposes. It is the responsibility of the organization to determine their overall security policy, and which settings are applicable to their unique needs based on the overall risk profile for the organization. Remediation CIS has developed Build Kits for many technologies to assist in the automation of hardening systems. Build Kits are designed to correspond to Benchmark's “Remediation” section, which provides the manual remediation steps necessary to make that Recommendation compliant to the Benchmark. When remediating systems (changing configuration settings on deployed systems as per the Benchmark's Recommendations), please approach this with caution and test thoroughly. The following is a reasonable remediation approach to follow: • CIS Build Kits, or internally developed remediation methods should never be applied to production systems without proper testing. • Proper testing consists of the following: o Understand the configuration (including installed applications) of the targeted systems. Various parts of the organization may need different configurations (e.g., software developers vs standard office workers). o Read the Impact section of the given Recommendation to help determine if there might be an issue with the targeted systems. o Test the configuration changes with representative lab system(s). If issues arise during testing, they can be resolved prior to deploying to any production systems. o When testing is complete, initially deploy to a small sub-set of production systems and monitor closely for issues. If there are issues, they can be resolved prior to deploying more broadly. o When the initial deployment above is completed successfully, iteratively deploy to additional systems and monitor closely for issues. Repeat this process until the full deployment is complete. Summary Using the Benchmarks Certified tools, working as a team with key stakeholders, being selective with exceptions, and being careful with remediation deployment, it is possible to harden large numbers of deployed systems in a cost effective, efficient, and safe manner. NOTE: As previously stated, the PDF versions of the CIS Benchmarks ™ are available for free, non-commercial use on the CIS Website. All other formats of the CIS Benchmarks ™ (MS Word, Excel, and Build Kits) are available for CIS SecureSuite ® members. CIS-CAT ® Pro is also available to CIS SecureSuite ® members. Target Technology Details This document, CockroachDB Benchmark, provides prescriptive guidance for establishing a secure configuration posture for CockroachDB. This guide was tested against CockroachDB version 25.x running on Ubuntu Linux, but applies to other Linux distributions as well. To obtain the latest version of this guide, please visit http://benchmarks.cisecurity.org. If you have questions, comments, or have identified ways to improve this guide, please write us at feedback@cisecurity.org.",
    "notice": {
      "+@id": "terms-of-use",
      "+@xml:lang": "en"
    },
    "front-matter": {
      "+@xml:lang": "en"
    },
    "rear-matter": {
      "+@xml:lang": "en"
    },
    "reference": {
      "+@href": "https://www.cisecurity.org/cis-benchmarks",
      "dc:publisher": "Center for Internet Security",
      "dc:source": "CIS Benchmarks"
    },
    "plain-text": {
      "+content": "Release: 1.0.0 Benchmark Date: 2026-05-26",
      "+@id": "release-info"
    },
    "version": "1.0.0",
    "Profile": [
      {
        "+@id": "L1_Public",
        "title": "Level 1",
        "description": "CIS profile: Level 1",
        "select": [
          {
            "+@idref": "V-CB3C0752",
            "+@selected": "true"
          },
          {
            "+@idref": "V-C963B3C6",
            "+@selected": "true"
          },
          {
            "+@idref": "V-D3C15FB1",
            "+@selected": "true"
          },
          {
            "+@idref": "V-358778DF",
            "+@selected": "true"
          },
          {
            "+@idref": "V-1D143DC8",
            "+@selected": "true"
          },
          {
            "+@idref": "V-88C0E9D7",
            "+@selected": "true"
          },
          {
            "+@idref": "V-2491B9D8",
            "+@selected": "true"
          },
          {
            "+@idref": "V-88A90A77",
            "+@selected": "true"
          },
          {
            "+@idref": "V-3754BB96",
            "+@selected": "true"
          },
          {
            "+@idref": "V-D21566C7",
            "+@selected": "true"
          },
          {
            "+@idref": "V-18E80386",
            "+@selected": "true"
          },
          {
            "+@idref": "V-70254B74",
            "+@selected": "true"
          },
          {
            "+@idref": "V-B6D25E5A",
            "+@selected": "true"
          },
          {
            "+@idref": "V-F19B5ADA",
            "+@selected": "true"
          },
          {
            "+@idref": "V-39E3263D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-BCB5D945",
            "+@selected": "true"
          },
          {
            "+@idref": "V-C5BFC1A3",
            "+@selected": "true"
          },
          {
            "+@idref": "V-16FB32BE",
            "+@selected": "true"
          },
          {
            "+@idref": "V-EF5BB7CA",
            "+@selected": "true"
          },
          {
            "+@idref": "V-3C6F5940",
            "+@selected": "true"
          },
          {
            "+@idref": "V-92CC4944",
            "+@selected": "true"
          },
          {
            "+@idref": "V-18B4D97D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-1C11CAE6",
            "+@selected": "true"
          },
          {
            "+@idref": "V-102A9449",
            "+@selected": "true"
          },
          {
            "+@idref": "V-15AAE31E",
            "+@selected": "true"
          },
          {
            "+@idref": "V-789AFD9B",
            "+@selected": "true"
          },
          {
            "+@idref": "V-FAC2F08B",
            "+@selected": "true"
          },
          {
            "+@idref": "V-FCB459A3",
            "+@selected": "true"
          },
          {
            "+@idref": "V-685B7C9D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-411A9867",
            "+@selected": "true"
          },
          {
            "+@idref": "V-09EC9BED",
            "+@selected": "true"
          }
        ]
      },
      {
        "+@id": "L1_Classified",
        "title": "Level 1",
        "description": "CIS profile: Level 1",
        "select": [
          {
            "+@idref": "V-CB3C0752",
            "+@selected": "true"
          },
          {
            "+@idref": "V-C963B3C6",
            "+@selected": "true"
          },
          {
            "+@idref": "V-D3C15FB1",
            "+@selected": "true"
          },
          {
            "+@idref": "V-358778DF",
            "+@selected": "true"
          },
          {
            "+@idref": "V-1D143DC8",
            "+@selected": "true"
          },
          {
            "+@idref": "V-88C0E9D7",
            "+@selected": "true"
          },
          {
            "+@idref": "V-2491B9D8",
            "+@selected": "true"
          },
          {
            "+@idref": "V-88A90A77",
            "+@selected": "true"
          },
          {
            "+@idref": "V-3754BB96",
            "+@selected": "true"
          },
          {
            "+@idref": "V-D21566C7",
            "+@selected": "true"
          },
          {
            "+@idref": "V-18E80386",
            "+@selected": "true"
          },
          {
            "+@idref": "V-70254B74",
            "+@selected": "true"
          },
          {
            "+@idref": "V-B6D25E5A",
            "+@selected": "true"
          },
          {
            "+@idref": "V-F19B5ADA",
            "+@selected": "true"
          },
          {
            "+@idref": "V-39E3263D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-BCB5D945",
            "+@selected": "true"
          },
          {
            "+@idref": "V-C5BFC1A3",
            "+@selected": "true"
          },
          {
            "+@idref": "V-16FB32BE",
            "+@selected": "true"
          },
          {
            "+@idref": "V-EF5BB7CA",
            "+@selected": "true"
          },
          {
            "+@idref": "V-3C6F5940",
            "+@selected": "true"
          },
          {
            "+@idref": "V-92CC4944",
            "+@selected": "true"
          },
          {
            "+@idref": "V-18B4D97D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-1C11CAE6",
            "+@selected": "true"
          },
          {
            "+@idref": "V-102A9449",
            "+@selected": "true"
          },
          {
            "+@idref": "V-15AAE31E",
            "+@selected": "true"
          },
          {
            "+@idref": "V-789AFD9B",
            "+@selected": "true"
          },
          {
            "+@idref": "V-FAC2F08B",
            "+@selected": "true"
          },
          {
            "+@idref": "V-FCB459A3",
            "+@selected": "true"
          },
          {
            "+@idref": "V-685B7C9D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-411A9867",
            "+@selected": "true"
          },
          {
            "+@idref": "V-09EC9BED",
            "+@selected": "true"
          }
        ]
      },
      {
        "+@id": "L1_Sensitive",
        "title": "Level 1",
        "description": "CIS profile: Level 1",
        "select": [
          {
            "+@idref": "V-CB3C0752",
            "+@selected": "true"
          },
          {
            "+@idref": "V-C963B3C6",
            "+@selected": "true"
          },
          {
            "+@idref": "V-D3C15FB1",
            "+@selected": "true"
          },
          {
            "+@idref": "V-358778DF",
            "+@selected": "true"
          },
          {
            "+@idref": "V-1D143DC8",
            "+@selected": "true"
          },
          {
            "+@idref": "V-88C0E9D7",
            "+@selected": "true"
          },
          {
            "+@idref": "V-2491B9D8",
            "+@selected": "true"
          },
          {
            "+@idref": "V-88A90A77",
            "+@selected": "true"
          },
          {
            "+@idref": "V-3754BB96",
            "+@selected": "true"
          },
          {
            "+@idref": "V-D21566C7",
            "+@selected": "true"
          },
          {
            "+@idref": "V-18E80386",
            "+@selected": "true"
          },
          {
            "+@idref": "V-70254B74",
            "+@selected": "true"
          },
          {
            "+@idref": "V-B6D25E5A",
            "+@selected": "true"
          },
          {
            "+@idref": "V-F19B5ADA",
            "+@selected": "true"
          },
          {
            "+@idref": "V-39E3263D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-BCB5D945",
            "+@selected": "true"
          },
          {
            "+@idref": "V-C5BFC1A3",
            "+@selected": "true"
          },
          {
            "+@idref": "V-16FB32BE",
            "+@selected": "true"
          },
          {
            "+@idref": "V-EF5BB7CA",
            "+@selected": "true"
          },
          {
            "+@idref": "V-3C6F5940",
            "+@selected": "true"
          },
          {
            "+@idref": "V-92CC4944",
            "+@selected": "true"
          },
          {
            "+@idref": "V-18B4D97D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-1C11CAE6",
            "+@selected": "true"
          },
          {
            "+@idref": "V-102A9449",
            "+@selected": "true"
          },
          {
            "+@idref": "V-15AAE31E",
            "+@selected": "true"
          },
          {
            "+@idref": "V-789AFD9B",
            "+@selected": "true"
          },
          {
            "+@idref": "V-FAC2F08B",
            "+@selected": "true"
          },
          {
            "+@idref": "V-FCB459A3",
            "+@selected": "true"
          },
          {
            "+@idref": "V-685B7C9D",
            "+@selected": "true"
          },
          {
            "+@idref": "V-411A9867",
            "+@selected": "true"
          },
          {
            "+@idref": "V-09EC9BED",
            "+@selected": "true"
          }
        ]
      }
    ],
    "Group": [
      {
        "+@id": "V-CB3C0752",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-CB3C0752r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.1",
          "title": "Obtain the binary from an official and trusted source and verify the SHA-256 sum after downloading.",
          "description": "<VulnDiscussion>CockroachDB server binaries and supporting tools must be obtained only from official Cockroach Labs release sources and verified using the published SHA-256 checksum before installation.\nThe canonical sources for CockroachDB self-hosted binaries are:\n• The CockroachDB Releases pages, which link to official versioned binaries.\n• https://binaries.cockroachdb.com/ (binary distribution site), which hosts release artifacts such as full binaries and associated checksum files.\nFor FIPS-ready runtimes, Cockroach Labs publishes per-version download links and corresponding SHA-256 checksum files (e.g., cockroach-v25.4.0.linux-amd64- fips.tgz and cockroach-v25.4.0.linux-amd64-fips.tgz.sha256sum) on the official documentation site and serves them from binaries.cockroachdb.com.\nAdministrators must verify that the locally computed SHA-256 checksum of any downloaded CockroachDB binary matches the corresponding checksum value published by Cockroach Labs before using it for installation or upgrade.\n\nRationale:\nObtaining CockroachDB binaries exclusively from official Cockroach Labs distribution endpoints and verifying them against the published SHA-256 checksums helps ensure:\n• Integrity - The downloaded binary has not been corrupted or altered in transit.\n• Authenticity - The binary corresponds to an official Cockroach Labs release and is traceable to a documented version.\n• Consistency - All CockroachDB nodes and environments run binaries that match known, documented release builds.\nRelying on unofficial sources or skipping checksum verification increases the risk of:\n• Malicious or trojanized CockroachDB binaries.\n• Outdated builds with known vulnerabilities.\n• Inconsistent behavior between environments due to untracked or modified binaries.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.1"
          },
          "fixtext": {
            "+content": "1. Reconfigure package pipelines Update all package managers, automation scripts, and manual runbooks to download CockroachDB binaries and tools only from:\n• https://binaries.cockroachdb.com/, or\n• A vetted internal mirror that pulls from this official source.\n2. Download CockroachDB from official source and verify checksum\n#Example: Linux ARM64; Adjust VERSION as needed\nVERSION=\"v25.3.4\"\nwget \"https://binaries.cockroachdb.com/cockroach-${VERSION}.linux-amd64.tgz\"\n# for Linux x86_64 use \"https://binaries.cockroachdb.com/cockroach-\n${VERSION}.linux-amd64.tgz\"\nwget \"https://binaries.cockroachdb.com/cockroach-${VERSION}.linux-\namd64.tgz.sha256sum\" # for Linux x86_64 use\n\"https://binaries.cockroachdb.com/cockroach-${VERSION}.linux-\namd64.tgz.sha256sum\"\n# Verify checksum\nsha256sum \"cockroach-${VERSION}.linux-amd64.tgz\"\ncat \"cockroach-${VERSION}.linux-amd64.tgz.sha256sum\"",
            "+@fixref": "F-CB3C0752r01_fix"
          },
          "fix": {
            "+@id": "F-CB3C0752r01_fix"
          },
          "check": {
            "+@system": "C-CB3C0752_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Inspect package pipelines / automation Review CI/CD pipelines, configuration-management code, and installation scripts that fetch CockroachDB binaries.\nConfirm they download CockroachDB artifacts only from:\n• https://binaries.cockroachdb.com/, or\n• An internal mirror that itself synchronizes exclusively from Cockroach Labs official distribution.\nInspect your package pipelines to ensure they only pull from binaries.cockroachdb.com.\n2. Verify checksum for a downloaded artifact On an administrator workstation or build node:\n#Example: Linux ARM64; adjust FILE_NAME/VERSION as needed\nVERSION=\"v25.3.4\"\nFILE_NAME=\"cockroach-${VERSION}.linux-arm64.tgz\" # for Linux x86_64 use\n\"cockroach-${VERSION}.linux-amd64.tgz\"\nFILE_URL=\"https://binaries.cockroachdb.com/${FILE_NAME}\"\nSHA_URL=\"${FILE_URL}.sha256sum\"\n# Ensure file exists locally (or download it)\nif [ ! -f \"$FILE_NAME\" ]; then\necho \"Missing $FILE_NAME. Download it from official source first:\n$FILE_URL\"\nexit 1\nfi\n# Verify SHA-256 checksum against Cockroach Labs-published value\nif [ \"$(wget -qO- \"$SHA_URL\" | awk '{print $1}')\" == \"$(sha256sum\n\"$FILE_NAME\" | awk '{print $1}')\" ]; then\necho \"OK: SHA-256 matches Cockroach Labs' published checksum.\"\nelse\necho \"FAIL: SHA-256 mismatch. File may be corrupted or tampered.\"\nexit 2\nfi\nIf the checksum does not match the published SHA-256 from Cockroach Labs, this is a finding.\nExpected Results:\n• All automation and manual procedures that obtain CockroachDB binaries:\no Pull artifacts from https://binaries.cockroachdb.com/ or from an internal mirror that itself only syncs from Cockroach Labs official sources.\n• For each CockroachDB binary used in production or non-production environments:\no The SHA-256 checksum is verified against the corresponding Cockroach Labs-published.sha256sum before installation or upgrade.\n• Any mismatch between the computed checksum and the published checksum is treated as a security finding, and the affected artifact is not used."
          }
        }
      },
      {
        "+@id": "V-C963B3C6",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-C963B3C6r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.2",
          "title": "Ensure CockroachDB systemd Service Files Are Enabled",
          "description": "<VulnDiscussion>Confirm, and correct if necessary, that the CockroachDB systemd service is enabled on Linux-based systems.\nThis ensures CockroachDB starts automatically at system boot, recovers gracefully from crashes, and is consistently managed under a dedicated non-root service account.\n\nRationale:\nRunning CockroachDB as a foreground/background process without a service manager increases the risk of:\n• Nodes not starting after system reboot or crash.\n• Inconsistent privilege separation (e.g., accidentally running as root).\n• Logs not being integrated into the system audit/logging pipeline.\nBy enabling CockroachDB as a systemd service:\n• The service is automatically started at boot and restarted on failure.\n• CockroachDB runs as a dedicated non-root account (cockroach), enforcing least privilege.\n• Service lifecycle is auditable via systemctl.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.2"
          },
          "fixtext": {
            "+content": "For each initial node of your cluster, complete the following steps:\n1. Create the Cockroach directory:\nmkdir /var/lib/cockroach\n2. Create a Unix user named cockroach:\nuseradd cockroach\n3. Move the certs directory to the cockroach directory:\nmv certs /var/lib/cockroach/\n4. Change the ownership of the cockroach directory to the user cockroach:\nchown -R cockroach /var/lib/cockroach\n5. Download the sample configuration template and save the file in the /etc/systemd/system/ directory:\ncurl -o cockroach.service\nhttps://raw.githubusercontent.com/cockroachdb/docs/main/src/current/_includes\n/v25.4/prod-deployment/securecockroachdb.service\nOR\n[Unit]\nDescription=Cockroach Database cluster node\nRequires=network.target\n[Service]\nType=notify\nWorkingDirectory=/var/lib/cockroach\nExecStart=/usr/local/bin/cockroach start --certs-dir=certs --advertise-\naddr=<node-address> --listen-addr=<node-address>:26357\n--sql-adr=<node-address>:26257 --http-addr=<node-address>:8080 --\njoin=<node1 address>,<node2 address>,<node3 address> --cache=.25 --max-sql-\nmemory=.25\nExecStop=/usr/local/bin/cockroach node drain --self --certs-dir=certs\nTimeoutStopSec=300\nRestart=always\nRestartSec=10\nStandardOutput=syslog\nStandardError=syslog\nSyslogIdentifier=cockroach\nUser=cockroach\n[Install]\nWantedBy=default.target\nSave the file in the /etc/systemd/system/directory.\n6. Customize the sample configuration template for your deployment:\nSpecify values for the following flags in the sample configuration template:\n• --advertise-addr:\no Specifies the IP address/hostname and port to tell other nodes to use. The port number can be omitted, in which case it defaults to 26257.\no This value must route to an IP address the node is listening on (with -- listen-addr unspecified, the node listens on all IP addresses).\no In some networking scenarios, you may need to use --advertise-addr and/or --listen-addr differently.\nFor enhanced security, it is a best practice to separate the listen address for inter-node communication from the listen address used for client SQL connections.\n• --join:\nIdentifies the address of 3-5 of the initial nodes of the cluster. These addresses should match the addresses that the target nodes are advertising..\n• Optionally include --locality:\nif using multi-region/latency-aware features.\n7. Reload systemd and enable the service:\nsystemctl daemon-reload\nsystemctl enable cockroach.service\nsystemctl start cockroach.service\nRepeat these steps for each additional node that you want in your cluster.\nNote: systemd handles node restarts in case of node failure. To stop a node without systemd restarting it, run :\nsystemctl stop cockroach.service",
            "+@fixref": "F-C963B3C6r01_fix"
          },
          "fix": {
            "+@id": "F-C963B3C6r01_fix"
          },
          "check": {
            "+@system": "C-C963B3C6_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Run the following to confirm that CockroachDB is registered as a systemd service and enabled:\n## Identify CockroachDB service unit if it exists\nsystemctl list-unit-files | grep cockroach | awk '{print $1}'\n# Check if the service is enabled at boot\nsystemctl is-enabled cockroach.service\n(or as identified from previous command)\n# Confirm it is active and running\nsystemctl status cockroach.service\n# Verify it runs under the dedicated 'cockroach' user\nps -u cockroach -f | grep cockroach\nExpected Results:\n• A CockroachDB service unit (for example, cockroach.service or securecockroachdb.service) exists under systemd.\n• The unit is enabled and active (running).\n• The CockroachDB process runs as the cockroach user (non-root).\nIf the service is absent, disabled, not running, or running as root, this is a finding."
          }
        }
      },
      {
        "+@id": "V-D3C15FB1",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-D3C15FB1r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.3",
          "title": "Ensure CockroachDB Cluster is Initialized Securely with TLS",
          "description": "<VulnDiscussion>CockroachDB clusters must be initialized and operated in secure mode so that all inter- node and client-node communication uses TLS with certificates signed by a trusted CA.\nCockroachDB's production documentation strongly recommends running secure clusters (with --certs-dir and valid certificates) instead of using --insecure, which disables all encryption and authentication.\nNodes must:\n• Start with valid TLS certificates in a --certs-dir directory: ca.crt, node.crt, node.key.\n• Use mutual TLS between nodes and TLS between clients and nodes.\n• Avoid --insecure in all production deployments.\nThe controls in this section establish the baseline secure configuration for CockroachDB using TLS-encrypted TCP/IP communication. Additional hardening options, such as using UNIX domain sockets for local connections, may be introduced in later controls. These options do not replace or invalidate the secure configuration defined here, instead they provide complementary, defense-in- depth enhancements where applicable.\n\nRationale:\nInitializing the cluster securely ensures:\n• TLS is required by default for CockroachDB communication.\n• A node certificate (TLS 1.3) is required on each node, with hostnames/IPs in CN/SAN and signed by a trusted CA.\n• Clients and nodes authenticate via certificates or passwords, but node identity is always enforced via TLS certificates in secure mode.\nThe --insecure flag disables all encryption and authentication. This configuration is strictly intended for non-production testing environments and must never be used in a production deployment.\nSecure initialization ensures:\n• Encryption in transit for all internode and client-node traffic.\n• Authenticated membership: only nodes with valid, CA-signed node.crt can join.\n• Strong access control via cryptographic identities for nodes and (optionally) clients.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.3"
          },
          "fixtext": {
            "+content": "1. Generate the cluster Certificate Authority (CA):\nThe documentation suggests creating two directories on your local machine: certs (for public files) and my-safe-directory (for the private CA key).\nOn a secure workstation:\n# Create directories for certificates\n# `certs` : You'll generate your CA certificate and all node and client\ncertificates and keys in this directory.\n# `my-safe-directory` : You'll generate your CA key in this directory and\nthen reference the key when generating node and client certificates.\nmkdir certs my-safe-directory\n# Generate the cluster Certificate Authority (CA)\ncockroach cert create-ca \\\n--certs-dir=certs \\\n--ca-key=my-safe-directory/ca.key\n2. Generate node certificates for each node (Repeat this block for each node):\nFor each node, issue a multi-purpose node.crt with all relevant addresses and DNS names (node IPs/hostnames, load-balancer addresses, localhost).\nExample:\n# NOTE: Replace <node-ip>, <node-hostname>, and <load-balancer> addresses.\ncockroach cert create-node \\\n<node-internal-ip> \\\n<node-external-ip> \\\n<node-hostname> \\\nlocalhost \\\n127.0.0.1 \\\n<load-balancer-ip> \\\n<load-balancer-hostname> \\\n--certs-dir=certs \\\n--ca-key=my-safe-directory/ca.key\nAfter uploading each node's cert/key, remove or overwrite node.crt/node.key locally before generating the next node's pair, as recommended.\n3. Generate client certificate for the root user:\ncockroach cert create-client \\\nroot \\\n--certs-dir=certs \\\n--ca-key=my-safe-directory/ca.key\nThis creates client.root.crt and client.root.key, used for secure administrative access.\n4. Distribute and Secure Certificates (on each node):\nOn each node, create a certs directory under the Cockroach data directory and upload the appropriate files. The docs commonly use ~/certs or /var/lib/cockroach/certs.\nssh <username>@<node-address> \"mkdir -p /var/lib/cockroach/certs\"\nscp certs/ca.crt certs/node.crt certs/node.key \\\n<username>@<node-address>:/var/lib/cockroach/certs\nSet ownership so the CockroachDB Unix user (e.g., cockroach) owns the directory:\nchown -R cockroach:cockroach /var/lib/cockroach\nOptionally copy client.root.* to a secure admin host where you run cockroach sql.\nAction Command (Run on Local Machine) scp certs/ca.crt certs/node.crt Upload CA and Node Certificates certs/node.key <username>@<node- address>:~/certs rm certs/node.crt certs/node.key (To (Local) Cleanup prepare for the next node's cert generation) scp certs/client.root.crt Upload Client Certificate (for local certs/client.root.key <username>@<node- admin/troubleshooting) address>:~/certs 5. Start and Initialize the Cluster Securely (on each node):\n5.1. Start each node with --certs-dir (no --insecure):\nThe service file ( cockroach.service from the previous control) contains the --certs- dir=certs flag and is configured to run from the /var/lib/cockroach working directory:\nEnsure the ExecStart line includes --certs-dir and excludes --insecure:\n[Unit]\nDescription=CockroachDB\nDocumentation=https://www.cockroachlabs.com/docs/\nAfter=network.target\n[Service]\nType=notify\nUser=cockroach\nGroup=cockroach\nWorkingDirectory=/var/lib/cockroach\nExecStart=/usr/local/bin/cockroach start \\\n--certs-dir=/var/lib/cockroach/certs \\\n--advertise-addr=<node-address> \\\n--join=<node1-address>,<node2-address>,<node3-address> \\\n--cache=.25 \\\n--max-sql-memory=.25\nTimeoutStopSec=60\nRestart=always\nRestartSec=10\nStandardOutput=syslog\nStandardError=syslog\n[Install]\nWantedBy=multi-user.target\n5.2. Enable and Start the Service Reload the systemd daemon to recognize the configuration, then enable and start the service:\nsudo systemctl daemon-reload\nsudo systemctl enable cockroach.service\nsudo systemctl start cockroach.service\nVerify the service is running:\nsystemctl status cockroach.service\n5.3. Initialize the cluster securely (Run only once from the local machine) Once all nodes are running, initialize the cluster using the root client certificate.\nAfter all nodes are started in secure mode:\n# Run once from the local machine:\ncockroach init \\\n--certs-dir=certs \\\n--host=<first-node-host>\n6. Verify Secure Operation\n• Use secure client connection (this already proves certs are working):\ncockroach sql \\\n--certs-dir=certs \\\n--host=<node-host> \\\n-e \"SHOW DATABASES;\"\n• Check node status and liveness with --certs-dir:\ncockroach node status --certs-dir=/var/lib/cockroach/certs --\nhost=<node-host>\no Cluster ID consistency:\ncockroach sql --certs-dir=certs --host=<node-host>\nAll nodes should report secure status and the same cluster ID.",
            "+@fixref": "F-D3C15FB1r01_fix"
          },
          "fix": {
            "+@id": "F-D3C15FB1r01_fix"
          },
          "check": {
            "+@system": "C-D3C15FB1_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Confirm the cluster is not running with --insecure Run the following command on any node:\nps -ef | grep cockroach\nExpected Results:\n• If --insecure appears, the node is definitely operating insecurely.\n• If --accept-sql-without-tls appears,\no TLS encryption is still used for all traffic.\no SQL clients may connect with sslmode=off/cockroach sql --insecure, skipping TLS authentication but not encryption 2. Verify presence and ownership of certificate directory Examples generally use a certs directory under a CockroachDB data directory (e.g., /var/lib/cockroach/certs), owned by the cockroach user.\nls -ld /var/lib/cockroach/certs\nls -l /var/lib/cockroach/certs\nExpected Results:\n• Directory is owned by a dedicated CockroachDB account (e.g., cockroach:cockroach).\n• Contains at least: ca.crt, node.crt, node.key. For client certs on that host, client.<user>.crt and client.<user>.key (e.g., client.root.*).\n3. Validate node certificate against the CA Use OpenSSL to verify that node.crt is signed by the cluster CA:\nopenssl verify -CAfile /var/lib/cockroach/certs/ca.crt\n/var/lib/cockroach/certs/node.crt\nExpected Results:\n/var/lib/cockroach/certs/node.crt: OK\n• Output shows the node certificate as OK.\nThis aligns with the requirement that node.crt must be signed by ca.crt.\n4. Confirm cluster identity and secure connectivity:\nUse the secure CLI to query node status:\ncockroach node status --certs-dir=/var/lib/cockroach/certs --host=<node-host>\nIf you connect this way to each node (or to different addresses that might represent different clusters), you can compare the Cluster ID line in the welcome message. All nodes that belong to the same cluster must report the same cluster Expected Results:\n• Command succeeds using certificates (no --insecure).\n• All nodes you query report the same Cluster ID and nodes are available. The docs note that nodes in the same cluster share a cluster ID; CLI usage with -- certs-dir is the standard secure pattern.\ncockroach sql \\\n--certs-dir=/var/lib/cockroach/certs \\\n--host=<node-host> \\\n-e \"SHOW DATABASES;\"\nA successful connection without --insecure confirms secure SQL connectivity."
          }
        }
      },
      {
        "+@id": "V-358778DF",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-358778DFr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.4",
          "title": "Do Not Use the --insecure Flag in Production Environments Under Any Circumstances",
          "description": "<VulnDiscussion>CockroachDB supports an --insecure startup option that disables all node-to-node and client-to-node encryption, authentication, and certificate validation. When started in this mode, the cluster operates entirely without Transport Layer Security (TLS), allowing unauthenticated and unencrypted connections.\nThis option exists only for initial testing or ephemeral local development. It must never be used in production or multi-node environments, where it exposes the cluster to interception, tampering, or unauthorized node participation.\n\nRationale:\nStarting CockroachDB with -insecure removes fundamental security controls and nullifies all certificate-based trust relationships. In insecure mode:\n• Network traffic is transmitted in plaintext.\n• Nodes can impersonate other nodes and join clusters without verification.\n• Client authentication is disabled.\n• Sensitive data (SQL queries, credentials, session tokens) can be read or modified in transit.\nRunning CockroachDB insecurely in production constitutes a critical finding and can result in data compromise, regulatory non-compliance, or full cluster takeover.\nThis reduces the risk of privilege escalation, unauthorized data access, and tampering with sensitive files such as logs, data stores, and TLS certificates.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.4"
          },
          "fixtext": {
            "+content": "1. Stop all insecure nodes:\nsudo systemctl stop cockroach.service\n2. Generate a certificate authority and signed certificates:\ncockroach cert create-ca \\\n--certs-dir=certs \\\n--ca-key=my-safe-directory/ca.key\ncockroach cert create-node \\\n<addresses/hostnames...> \\\n--certs-dir=certs \\\n--ca-key=my-safe-directory/ca.key\ncockroach cert create-client \\\nroot \\\n--certs-dir=certs \\\n--ca-key=my-safe-directory/ca.key\n3. Remove insecure configuration from systemd:\nEdit the systemd unit file (usually /etc/systemd/system/cockroach.service) to remove --insecure and add --certs-dir.\nAutomated cleanup (use with caution):\nsudo grep -Rl \"--insecure\" /etc/systemd/system | xargs sudo sed -i '/--\ninsecure/d'\n4. Manual Configuration (Recommended):\nEnsure the ExecStart line looks like this:\nExecStart=/usr/local/bin/cockroach start \\\n--certs-dir=/var/lib/cockroach/certs \\\n--advertise-addr=<node-address> \\\n--listen-addr=<node-address>:26357 \\\n--sql-adr=<node-address>:26257 \\\n--http-addr=<node-address>:8080 \\\n--join=<peer-nodes> \\\n--cache=.25 \\\n--max-sql-memory=.25\n5. Reload and Restart Securely Reload the systemd daemon to apply changes and restart the service:\nsudo systemctl daemon-reload\nsudo systemctl start cockroach.service\n6. Initialize the cluster securely (if new) If this is a fresh cluster being converted to secure mode before initialization:\ncockroach init \\\n--certs-dir=/var/lib/cockroach/certs \\\n--host=<first-node-host>\n7. Remove any insecure startup configuration:\nsudo grep -Rl \"--insecure\" /etc/systemd/system | xargs sudo sed -i '/--\ninsecure/d'\nsudo systemctl daemon-reload\nsudo systemctl restart cockroach.service\n8. Verify secure operation:\ncockroach sql --certs-dir=/car/lib/cockroach/certs --host=<node> -e \\\n\"select node_id, field as property, substr(value,1,36) as value \\\nfrom crdb_internal.node_build_info \\\nwhere field='ClusterID' \\\norder by 1,2;\"\nExpected Results:\nAll nodes should share the same Cluster ID and report secure status.",
            "+@fixref": "F-358778DFr01_fix"
          },
          "fix": {
            "+@id": "F-358778DFr01_fix"
          },
          "check": {
            "+@system": "C-358778DF_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Check for --insecure usage:\nps -ef | grep cockroach | grep -- '--insecure'\nFindings if:\nNo output. If --insecure appears in any process command line, this is a finding.\n2. Review logs for insecure startup warnings:\ngrep -i \"insecure mode\" /var/lib/cockroach/logs/cockroach.log\nExpected Results:\nNo entries indicating \"* WARNING: RUNNING IN INSECURE MODE!\"."
          }
        }
      },
      {
        "+@id": "V-1D143DC8",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-1D143DC8r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.5",
          "title": "Ensure All Cluster Nodes Are Running the Exact Same Version, Including the Patch Release Number",
          "description": "<VulnDiscussion>All CockroachDB nodes in a self-hosted cluster must run the same major, minor, and patch version of the CockroachDB binary. The docs explicitly recommend that \"all cluster nodes are running exactly the same CockroachDB version, including the patch release version number,\" and call out version mismatch as an essential alert condition.\nMixed versions usually occur when:\n• A rolling upgrade was started but not completed.\n• Some nodes were missed during upgrade or restarted with an outdated binary.\n• Manual differences in binaries were introduced.\nCockroachDB supports rolling upgrades by updating one node at a time, but the expectation is that after the upgrade is complete, all nodes are on the same patch version.\n\nRationale:\nMaintaining version consistency across all cluster nodes ensures that:\n• Consensus protocols (Raft) and replication mechanisms function correctly.\n• All nodes interpret metadata, schema changes, and internal APIs consistently.\n• Security patches and bug fixes are uniformly applied across the cluster.\n• Operational procedures such as backups, restores, and upgrades behave predictably.\nRunning nodes on mixed patch versions introduces operational risks such as:\n• Split-brain conditions or inconsistent data state across replicas.\n• Feature mismatches (newer nodes using RPCs unsupported by older peers).\n• Failed schema migrations or upgrade finalization errors.\nVersion uniformity is a key production readiness requirement defined by Cockroach Labs for cluster stability and compliance with enterprise-grade support contracts</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.5"
          },
          "fixtext": {
            "+content": "1. Identify out-of-date nodes:\ncockroach node status --certs-dir=/var/lib/cockroach/certs --host=<node> --\nformat=table\nReview build or version fields to locate mismatched nodes.\n2. Download the latest consistent binary Please refer to Control 1.1 3. Perform a rolling restart (Upgrade one node at a time.):\n• Use cockroach node drain with --shutdown to gracefully drain and then terminate the node's process.\n• Replace the binary, then restart via your process manager (systemd, etc.).\n3.1. Drain the node and terminate the process:\ncockroach node drain <node-id> \\\n--shutdown \\\n--host= {address of any live node} \\\n--certs-dir=/var/lib/cockroach/certs\nThis uses cockroach node drain to:\n• Drain the node.\n• Then shut down the cockroach process automatically because of --shutdown.\n3.2. Replace the binary On that node:\nInstall or copy the new cockroach binary in place of the old one (e.g., /var/lib/cockroach) 3.3. Restart the node via your process manager For a systemd-managed service:\nsystemctl start cockroach.service\n(Use your actual service name, e.g., cockroach as configured in your unit file.) 3.4. Verify the node has rejoined and is ready After a short wait, confirm the node is serving SQL:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<upgraded-node> \\\n-e \"SELECT 1;\"\nRepeat for the next node until all nodes have been drained, shut down, upgraded, and restarted with the new binary.\n4. Verify all nodes now run the same version:\nAfter completing the rolling procedure:\nUse the DB Console Node List to confirm that every node now reports the same CockroachDB version and build.\nIf your monitoring is configured per the Version mismatch alert guidance, it should no longer fire once all build timestamps are aligned.\n5. Finalize the cluster upgrade (if applicable):\nIf this remediation is part of a major-version upgrade and you have disabled auto-finalization (for example, via cluster.auto_upgrade.enabled = false), then once all nodes are on the new binary and healthy, you must finalize:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> -e \"SET\nCLUSTER SETTING version = '{VERSION}';\"\nReplace {VERSION} with the new major version, such as 25.4 Finalization completes when:\n• SHOW CLUSTER SETTING version returns the new version, and\n• All migration and schema change jobs have reached terminal states.",
            "+@fixref": "F-1D143DC8r01_fix"
          },
          "fix": {
            "+@id": "F-1D143DC8r01_fix"
          },
          "check": {
            "+@system": "C-1D143DC8_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Check the CockroachDB version running on each node:\nFrom any node host you can run:\ncockroach version\nOr The DB Console ( Cluster Overview → Node List) shows each node's version.\nYou should verify that all nodes are on the same version before starting an upgrade and to use the DB Console Node List to confirm uniformity.\nExpected Results:\nAll nodes should report the same CockroachDB version before and during an upgrade.\nIf any node is on a different version, that indicates an incomplete or inconsistent upgrade and must be corrected before proceeding.\n3. Check upgrade status:\n• For major-version upgrades:\no The upgrade is not complete until finalized (or auto-finalization completes).\no Finalization is done by setting the version cluster setting to the new major version, or by allowing auto-finalization to run.\nTo check the cluster's logical version:\ncockroach sql --certs-dir=//var/lib/cockroach/certs --host=<node> -e \\\n\"SHOW CLUSTER SETTING version;\"\nExpected Results:\n• After a completed major upgrade, this shows the new major version (e.g., 24.3 or 25.4 depending on docs/release)\n• If it still shows the prior major version, finalization has not completed, and some migration jobs may still be running."
          }
        }
      },
      {
        "+@id": "V-88C0E9D7",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-88C0E9D7r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.6",
          "title": "Establish a Process for Rolling Upgrades to Apply Security Patches Without Downtime",
          "description": "<VulnDiscussion>CockroachDB supports rolling upgrades, allowing administrators to apply security patches, minor version updates, and configuration changes without cluster downtime. A rolling upgrade updates one node at a time, while the remaining nodes continue serving traffic and maintaining quorum.\nEstablishing a formal, documented rolling upgrade process ensures consistent patch application, reduces service disruption, and maintains the cluster's integrity and availability. This process must include pre-upgrade validation, stepwise node restarts, health verification, and post-upgrade finalization.\n\nRationale:\nA defined rolling upgrade process:\n• Ensures security patches and bug fixes are applied consistently across nodes.\n• Maintains continuous availability by preserving quorum during upgrades.\n• Prevents version drift and configuration inconsistencies.\n• Enables safe rollback or recovery in case of unexpected upgrade failures.\n• Reduces operational risk while maintaining compliance with patch management policies.\nWithout a controlled rolling upgrade process:\n• Clusters may suffer unplanned downtime, split-brain events, or data unavailability.\n• Nodes could run outdated, unpatched binaries, exposing vulnerabilities.\n• Partial upgrades could lead to inconsistent Raft or schema states.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.6"
          },
          "fixtext": {
            "+content": "1. Prepare for rolling upgrade:\n• Confirm cluster health and full replication:\ncockroach node status --certs-dir=/var/lib/cockroach/certs --host=<node>\ncockroach node status --certs-dir=/var/lib/cockroach/certs --all\nBackups are strongly recommended before upgrades.\n2. Upgrade CockroachDB binary on each node (one at a time):\n• Use cockroach node drain with --shutdown to gracefully drain and then terminate the node's process.\n• Replace the binary, then restart via your process manager (systemd, etc.).\n2.1. Drain the node and terminate the process:\ncockroach node drain <node-id> \\\n--shutdown \\\n--host= {address of any live node} \\\n--certs-dir=/var/lib/cockroach/certs\nThis uses cockroach node drain to:\n• Drain the node.\n• Then shut down the cockroach process automatically because of --shutdown.\n2.2. Replace the binary On that node (Follow Control 1.1):\nInstall or copy the new cockroach binary in place of the old one (e.g., /var/lib/cockroach) 2.3. Restart the node via your process manager For a systemd-managed service:\nsystemctl start cockroach.service\n(Use your actual service name, e.g., cockroach as configured in your unit file.) 2.4. Verify the node has rejoined and is ready After a short wait, confirm the node is serving SQL:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<upgraded-node> \\\n-e \"SELECT 1;\"\nIterate through the remaining nodes, ensuring that each node is drained, shut down, upgraded, and restarted. To ensure data availability, strictly enforce a wait period after each restart until the cluster reports no under-replicated ranges before attempting to upgrade the subsequent node.\nExpected Results:\nNode shows is_available : true and matches the new build tag.\n3. Finalize the cluster version after all nodes upgraded:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node>\nSET CLUSTER SETTING version = '{VERSION}';\"\nReplace {VERSION} with the new major version, such as 25.4 4. Verify upgrade success:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> \\\n-e \"SHOW CLUSTER SETTING version;\"",
            "+@fixref": "F-88C0E9D7r01_fix"
          },
          "fix": {
            "+@id": "F-88C0E9D7r01_fix"
          },
          "check": {
            "+@system": "C-88C0E9D7_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Run the following checks:\n1. Verify presence of a documented upgrade procedure Confirm your organization has a written or automated process for performing rolling upgrades, including:\n• Stepwise node restarts (one node at a time).\n• Verification commands and pre/post checks.\n• Defined rollback plan.\n• Reference to Cockroach Labs' upgrade documentation.\nReview operational runbooks or CI/CD pipelines.\n2. Check CockroachDB cluster health before upgrade:\ncockroach node status --certs-dir=\"certs\" --all\nExpected Results:\nAll nodes report the same version, is_available: true, and the cluster is fully replicated.\n3. Verify rolling upgrade sequence logs exist:\ngrep -i \"upgrade\" /var/lib/cockroach/logs/cockroach.log\nExpected Results:\nLog entries should show sequential node restarts, not simultaneous restarts across all nodes.\n4. Confirm upgrade finalization post-process:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> -e \"SHOW\nCLUSTER SETTING version;\"\nExpected Results:\nCluster version matches the upgraded binary version."
          }
        }
      },
      {
        "+@id": "V-2491B9D8",
        "title": "1 1 Installation and Patches",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-2491B9D8r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "1.7",
          "title": "Ensure Encryption at Rest Keys Are Managed by an External KMS and Retrieved at Startup",
          "description": "<VulnDiscussion>CockroachDB supports the use of external key management systems (KMS), such as HashiCorp Vault, to securely manage and deliver encryption keys used for encryption at rest. External key managers provide a centralized and auditable way to store and rotate keys and supply them dynamically to CockroachDB at startup.\nBy retrieving keys securely at runtime instead of storing them on local disks or in configuration files, administrators can reduce the risk of key exposure and strengthen compliance with data protection standards.\n\nRationale:\nManaging encryption keys within the same host that stores the encrypted data increases risk in the event of a host or filesystem compromise. Integrating CockroachDB with an external KMS ensures that:\n• Encryption keys are not stored in plaintext on local systems.\n• Keys can be rotated and revoked centrally.\n• Only authorized CockroachDB nodes can access the keys at runtime.\n• Audit trails record every key access for accountability and compliance.\nWithout a secure external key manager, any compromise of a database node could reveal local key material and allow attackers to decrypt sensitive data.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "1.7"
          },
          "fixtext": {
            "+content": "1. Deploy or connect to a supported key management system:\nSuch as HashiCorp Vault, AWS KMS, or another trusted external provider.\n2. Configure the Systemd Service to enable encryption:\nEdit the service file at /etc/systemd/system/cockroach.service. Update the ExecStart command to include the --enterprise-encryption flag:\nExample Configuration (File-based key on separate volume):\nExecStart=/usr/local/bin/cockroach start \\\n--certs-dir=/var/lib/cockroach/certs \\\n--store=/var/lib/cockroach \\\n--enterprise-encryption=path=/var/lib/cockroach,key=/secure-keys/crdb-\nstore.key,old-key=plain \\\n--advertise-addr=<node-address> \\\n--listen-addr=<node-address>:26357 \\\n--http-addr=<node-address>:8080 \\\n--sql-adr=<node-address>:26257 \\\n--join=<peer-nodes>\nold-key=plain is used only when first enabling encryption on an existing plaintext cluster to allow rotation. Once encrypted, remove old-key=plain.\n3. Secure the key files If using local key files (instead of a direct KMS integration), ensure they are strictly protected:\n# Create a secure directory separate from data\nsudo mkdir -p /secure-keys\n# Move/Generate key\nsudo mv /path/to/generated.key /secure-keys/crdb-store.key\n# Restrict permissions\nsudo chown -R cockroach:cockroach /secure-keys\nsudo chmod 700 /secure-keys\nsudo chmod 400 /secure-keys/crdb-store.key\n4. Reload and Restart Reload systemd and restart the service to apply encryption:\nsudo systemctl daemon-reload\nsudo systemctl restart cockroach.service\n5. Verify encryption status:\nUse the CockroachDB debug tooling to validate that encryption is active and that a store key is in use.\ncockroach debug encryption-active-key /var/lib/cockroach\nExpected Results:\nEncryption is active and keys are sourced from the external KMS.",
            "+@fixref": "F-2491B9D8r01_fix"
          },
          "fix": {
            "+@id": "F-2491B9D8r01_fix"
          },
          "check": {
            "+@system": "C-2491B9D8_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Check for enterprise encryption configuration:\nps -ef | grep cockroach | grep -- '--enterprise-encryption'\nExpected Results:\nEach CockroachDB process for encrypted stores includes a --enterprise- encryption=path =...,key =...,old-key =...,rotation-period=... argument.\nNote: Note: Once --enterprise-encryption has been configured for a store, it must always be present on that node.\n2. Inspect configuration for external key source:\n# Look for Vault, Transit, or other secret loader references\ngrep -Ri \"vault\" /etc/cockroach /usr/lib/systemd/system\ngrep -Ri \"encryption-key\" /etc/cockroach /usr/lib/systemd/system\nExpected Results:\nYou may find references to Vault (or another secret delivery mechanism) in:\n• systemd unit files (e.g., ExecStartPre scripts fetching keys),\n• wrapper scripts under /etc/cockroach,\n• environment variables that point to Vault or a secret loader.\n3. Verify that local key files are not present:\n# Look for key files under Cockroach-related paths\nfind /etc/cockroach /var/lib/cockroach -type f -name \"*.key\" -ls\nExpected Results:\n• Store key files do exist somewhere (since self-hosted Enterprise Encryption at Rest requires them).\n• They are:\no Not on the same partition/drive as the data directory (e.g., /var/lib/cockroach).\no Protected by file permissions: owned by the CockroachDB user, not world- readable.\n• If you find key files inside the data directory or on the same volume, that is a finding to remediate."
          }
        }
      },
      {
        "+@id": "V-88A90A77",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-88A90A77r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.1",
          "title": "Ensure Network Time Protocol (NTP) Service Is Configured for Cluster Clock Synchronization",
          "description": "<VulnDiscussion>CockroachDB requires moderate levels of clock synchronization to preserve data consistency. When a node detects that its clock is out of sync with at least half of the other nodes in the cluster by 80% of the maximum offset allowed (500 ms by default), it will spontaneously shut down to avoid consistency anomalies.\nAdministrators must run clock synchronization software (for example, NTP or chrony) on each node and use compatible time sources (e.g. Google Public NTP, Amazon Time Sync Service) that implement leap-second smearing consistently across the cluster.\nCockroachDB does not install or configure NTP automatically, this must be done at the OS or platform level.\n\nRationale:\nAccurate clock synchronization is critical for CockroachDB to ensure:\n• Serializable transactions: Prevents transaction overlap errors.\n• Reliable Raft consensus: Ensures proper election and replication timing.\n• Accurate event logs: Enables consistent auditing and troubleshooting.\n• Automatic fault detection: Prevents unnecessary node shutdowns caused by clock drift.\nWithout NTP, clock skew can cause transaction uncertainty, stale reads, data inconsistencies, or even cluster instability.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.1"
          },
          "fixtext": {
            "+content": "Key requirements:\n• Run one clock sync service (NTP/chrony or provider's built-in time source) on each node.\n• Use compatible, leap-smearing time sources across all nodes.\n1. Install and enable a time sync service (per environment):\n• AWS EC2: Configure each instance to use Amazon Time Sync Service via 169.254.169.123, verify via c hronyc sources -v.\n• GCE: use GCE's internal NTP via metadata.google.internal; optionally Google Public NTP ( time.google.com) for non-GCE nodes.\n• On-prem / DigitalOcean / Azure: install NTP (or chrony) and configure Google Public NTP ( time[1-4].google.com) as the source.\n2. Configure trusted, compatible NTP sources:\nFollow the environment-specific doc:\n• Use:\no Amazon Time Sync on AWS.\no GCP internal NTP on GCE.\no Google Public NTP ( time.google.com, time1-4.google.com) elsewhere.\n• Ensure all nodes are synced either:\no To the same time source, or\no To sources that implement leap second smearing in the same way (Google and Amazon are compatible with each other but not with default NTP pool).\n3. Restart and verify the time service:\nUse your OS commands (for example, systemctl restart chronyd then chronyc sources -v, or timedatectl) to verify:\n• The service is running on all nodes.\n• Nodes are synchronized to the intended sources (e.g., 169.254.169.123 for AWS, time*.google.com for Google NTP).\n4. Validate CockroachDB cluster offset:\nClock offsets, as observed via Prometheus metrics or the DB Console, are well below the configured --max-offset (500 ms by default).\n5. (Optional hardening):\n• Prefer internal/enterprise NTP sources or cloud-provider services (Amazon Time Sync, Google internal NTP).\n• If not using Google/AWS services, configure chrony with client-side leap smearing, identically on all nodes.\n5. (Optional) Harden configuration using authenticated or internal NTP sources for production deployments.",
            "+@fixref": "F-88A90A77r01_fix"
          },
          "fix": {
            "+@id": "F-88A90A77r01_fix"
          },
          "check": {
            "+@system": "C-88A90A77_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Key Requirement:\n• Some clock sync service (NTP, chrony, etc.) is running on each node.\n• All nodes are synchronized to compatible, leap-smearing time sources (e.g., Amazon Time Sync, Google internal/public NTP), or chrony with client-side smearing configured consistently.\n• Clock offsets stay within the configured --max-offset.\n1. Confirm an NTP/chrony service is active (platform/OS-specific):\nUse your OS tools (for example, systemctl status chronyd, timedatectl, etc.) to ensure one clock sync service is enabled and running on each node.\n2. Verify time source selection and leap-second behavior:\nCheck that all nodes use:\n• Amazon Time Sync Service on AWS.\n• Google internal NTP on GCE, or Google Public NTP elsewhere.\n• Or chrony with client-side leap smearing if your time source does not smear server-side, configured the same way on all nodes 3. Inspect CockroachDB's view of clock offsets The --max-offset flag is a startup parameter for the cockroach start command, with a default value of 500 milliseconds (ms).\nTo inspect clock offsets between nodes, use Prometheus metrics such as clock_offset_meannanos or view the Clock Offset graph in the DB Console. These tools provide visibility into clock synchronization and help ensure offsets remain well below the configured --max-offset Expected Results:\nOffsets are well below the configured --max-offset (500 ms by default).\n4. Check logs for clock skew errors:\ngrep -i \"clock\" /var/lib/cockroach/logs/cockroach.log\nExpected Results:\nNo entries indicating that a node is \"more than 400ms away from at least half of the known nodes\" or similar clock-offset errors."
          }
        }
      },
      {
        "+@id": "V-3754BB96",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-3754BB96r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.2",
          "title": "Ensure Each CockroachDB Node Runs as a Dedicated Process on a Separate Host",
          "description": "<VulnDiscussion>For all self-hosted VM or bare-metal production deployments (on-premises or on cloud providers like AWS, GCP, Azure), each CockroachDB node must run as a single, dedicated cockroach start process on its own physical or virtual machine (VM).\nIt is a critical violation of best practices to co-locate multiple CockroachDB node processes on the same host, as this undermines the database's high-availability design and fault tolerance. Additionally, the data directory/storage volume for each node must be unique and not shared across any other node.\n\nRationale:\nCockroachDB's distributed consensus mechanism (Raft) and replication model are fundamentally built on the assumption that each node is an independent failure domain.\n• Single Point of Failure (SPOF): Running multiple nodes on the same VM turns that single host into an SPOF. A simple hardware failure, OS crash, or kernel panic on that host could simultaneously take down multiple data replicas, potentially eliminating the quorum required for the cluster to remain available.\n• Undermined Fault Tolerance: Co-location means data ranges (replicas) are not properly isolated. This defeats the purpose of maintaining multiple replicas across different failure zones.\n• Resource Contention: Sharing CPU, I/O, and network resources among multiple node processes can lead to unpredictable latency, performance issues, and conflicts.\n• Data Corruption Risk: Sharing the same data directory (--store path) between two or more active node processes will lead to locking errors and data corruption.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.2"
          },
          "fixtext": {
            "+content": "If any host is found to be running more than one cockroach start process, or if storage is shared:\n1. Stop Extra CockroachDB Processes:\nOn the overloaded host, identify and stop the excess node processes:\n# Identify all cockroach start processes\nps -ef | grep '[c]ockroach start'\n# Stop via systemd if used (assuming only one service should remain)\nsystemctl stop cockroach.service\n2. Provision Separate VMs and Re-deploy:\nThe nodes that were stopped must be moved to their own dedicated infrastructure:\n• Provision a new VM or host for each node that was sharing a machine.\n• Install CockroachDB on the new host.\n• Configure your virtualization platform (e.g., VMware, Hyper-V) to enforce Anti- Affinity rules. This guarantees that no two CockroachDB VMs are ever scheduled to run on the same underlying physical host. This prevents a single hardware failure from taking down multiple nodes.\n• Create a dedicated, unique data directory and ensure proper ownership before starting the node:\nmkdir /var/lib/cockroach\nuseradd cockroach\nchown -R cockroach /var/lib/cockroach\n3. Start Each Node as a Single Service on Its Own Host:\nUse a single cockroach start per machine, either manually or via systemd. For example (secure on-premises):\ncockroach start \\\n--certs-dir=/var/lib/cockroach/certs \\\n--store=/var/lib/cockroach \\\n--advertise-addr=<node-ip-address>:26257 \\\n--listen-addr=10.0.0.1:26357 \\ # Note: This configuration is discussed\nin a further control\n--sql-addr=192.168.1.1:26257 \\ # Note: This configuration is discussed\nin a further control\n--join=<node1-addr>,<node2-addr>,<node3-addr> \\\n--cache=.25 \\\n--max-sql-memory=.25 \\\n--background\nOr using the systemd service template from the docs, with one instance per host and -- advertise-addr set to that host's address. (Control 1.2) 4. Ensure Storage is Not Shared Verify that the --store directory on the new host points to a local, dedicated disk or partition that is not mounted or accessible by any other CockroachDB node.\n• For each node, allocate its own disk or partition.\n• Start the node with one or more --store flags referencing that host's local storage. Separate --store per disk and warn that you cannot later \" scale down \" stores without decommissioning the node.\nDo not mount the same data directory for multiple nodes, even across different hosts.",
            "+@fixref": "F-3754BB96r01_fix"
          },
          "fix": {
            "+@id": "F-3754BB96r01_fix"
          },
          "check": {
            "+@system": "C-3754BB96_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Perform the following checks on every host intended to run a CockroachDB node:\n1. Verify that only one CockroachDB process is active per host:\n#Check for active \"cockroach start\" processes on the VM:\nps -ef | grep '[c]ockroach start'\nExpected Results:\nAt most one cockroach start process per VM.\n2. Verify Data Directories Are Isolated:\n#Examine the data directory paths used by the node process (e.g., the default\n\"/var/lib/cockroach\" or a custom \"--store\" path):\nls -ld /var/lib/cockroach\n# Also check any other custom store paths\nExpected Results:\n• Each CockroachDB node process may use one or more unique data directories by specifying multiple --store flags (for example, when using multiple disks or SSDs on a single node).\n• Each data directory or disk must be dedicated to a single node process and must not be shared with any other CockroachDB node process or host. Sharing a data directory or disk between node processes is not supported and can lead to data corruption or operational issues 3. Confirm Topology Design:\nFrom your deployment design and inventory, verify that the number of VMs equals the number of CockroachDB nodes. All self-hosted deployment guides state that each node should run on a separate VM or machine Expected Results:\n• The number of dedicated VMs (or physical hosts) must equal the number of CockroachDB nodes deployed.\n• There should be no documented or intentional design to run multiple node processes on a single VM."
          }
        }
      },
      {
        "+@id": "V-D21566C7",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-D21566C7r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.3",
          "title": "Do Not Grant Sudo Privileges to the CockroachDB Process User",
          "description": "<VulnDiscussion>CockroachDB should run under a dedicated, non-root operating system account that has only the permissions required to read and write CockroachDB's data, log, and certificate directories. This dedicated service user (commonly cockroach) must not be granted sudo privileges or be included in any privileged system groups (such as wheel, sudo, or adm).\nAllowing the CockroachDB service account to escalate privileges could enable an attacker who compromises the database process to gain full control of the underlying host. This violates the principle of least privilege and undermines system isolation.\n\nRationale:\nRestricting the CockroachDB service account to minimal privileges ensures that even if the CockroachDB process or a client connection is compromised, the attacker cannot:\n• Escalate to root access.\n• Modify system configurations or kernel parameters.\n• Access sensitive files outside CockroachDB directories.\n• Install or execute arbitrary system commands.\nThis control upholds operating system-level separation between application processes and administrative privileges, reducing the risk of full system compromise.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.3"
          },
          "fixtext": {
            "+content": "1. Create a dedicated non-privileged account for CockroachDB if it does not already exist:\n# Create Cockroach directory\nmkdir -p /var/lib/cockroach\n# Create a Unix user named 'cockroach'\nuseradd cockroach\n# Make the directory owned by 'cockroach'\nchown -R cockroach /var/lib/cockroach\nThe docs use useradd cockroach directly; you may additionally configure shell/flags to fit your distro standards.\n2. Remove any sudo privileges for the CockroachDB user:\nEdit /etc/sudoers or /etc/sudoers.d/* and delete any entries granting CockroachDB ALL privileges.\nFor example, remove lines such as:\n• Edit /etc/sudoers or ``/etc/security/limits.d/cockroach.conf`:\ncockroach ALL=(ALL) NOPASSWD: ALL\n• Log out and back in, or restart the CockroachDB process to apply.\n3. Verify service operation:\nEnsure the CockroachDB systemd unit runs as the cockroach user and group:\ngrep -E 'User=|Group=' /etc/systemd/system/cockroach.service\nEnsure the output:\nUser=cockroach\nGroup=cockroach\nExpected Results:\nUser=cockroach\nGroup=cockroach\nReload systemd and restart CockroachDB if changes are made:\nsudo systemctl daemon-reload\nsudo systemctl restart cockroach",
            "+@fixref": "F-D21566C7r01_fix"
          },
          "fix": {
            "+@id": "F-D21566C7r01_fix"
          },
          "check": {
            "+@system": "C-D21566C7_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Perform the following checks on each CockroachDB node to verify that the process user does not have sudo or root-level privileges:\n# 1. Identify the CockroachDB process user\nps -eo user,comm | grep '[c]ockroach'\n# 2. Verify the user's UID is non-root (should not be 0)\nid -u cockroach\n# Expected: A non-zero value (e.g., 997)\n# Inspect the systemd unit to confirm non-root user\nsudo systemctl cat cockroach | grep -E '^User=|^Group='\n#Expected : User=cockroach ; Group=cockroach\n# 3. Check if the user belongs to privileged groups\nid cockroach\n# Ensure groups like 'sudo', 'wheel', or 'adm' are NOT listed.\n# 4. Verify that the user does not have sudo permissions\nsudo -l -U cockroach\nExpected Results:\n• The CockroachDB process runs under a dedicated non-root user (e.g., cockroach).\n• The user is not a member of any privileged system groups.\n• The sudo -l command returns \"user cockroach is not allowed to run sudo\" or a similar denial message.\nIf the CockroachDB service account can execute commands as root or escalate privileges, this is a finding."
          }
        }
      },
      {
        "+@id": "V-18E80386",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-18E80386r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.4",
          "title": "Ensure Database Servers Are Isolated in a Separate Subnet with Restricted Ingress and Egress",
          "description": "<VulnDiscussion>The core requirement is to deploy CockroachDB nodes on private, non-public networks (subnets, VLANs, or VPC private segments) and to use strict firewall or security group rules to control access to the required ports. This ensures the database is shielded from the public internet and enforces least-privilege access.\nFor enhanced security, it is a best practice to separate the listen address for inter- node communication from the listen address used for client SQL connections.\nThe three critical TCP ports to control are:\n• 26257 (SQL Traffic): Must be accessible only to application servers, and authorized load balancers.\n• 8080 (DB Console): Must be highly restricted, ideally accessible only by administrator networks or via secure tunneling (e.g., SSH).\n• Other Port (e.g. 26357): Must be accessible only by other CockroachDB nodes.\n\nRationale:\nNetwork segmentation limits the blast radius of a compromise and prevents direct exposure of database nodes to the public Internet or untrusted systems.\nBy isolating CockroachDB in a dedicated subnet, you:\n• Reduce the attack surface available to external threats.\n• Prevent lateral movement from compromised web/app servers.\n• Enforce least-privilege access paths between tiers.\n• Support compliance requirements for network zoning and data-plane isolation.\nImproperly exposing CockroachDB to shared or public networks can result in unauthorized access, data exfiltration, or denial-of-service attacks.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.4"
          },
          "fixtext": {
            "+content": "1. Place CockroachDB Nodes in Private Networks:\nEnsure all CockroachDB VMs are located in a private VLAN or non-internet-routable subnet. If a public IP is assigned for management, ensure it is not the primary address used for cluster communication and that its inbound ports are fully locked down.\n2. Tighten Inbound Access:\nConfigure host-based firewalls or network security groups to enforce the principle of least-privilege:\n• For Port 26257/TCP:\no Permit: Inbound traffic only from the IP ranges/security groups associated with:\n▪ The other CockroachDB nodes.\n▪ Load balancers (if applicable).\no Deny: All other traffic by default.\n• For Port 8080/TCP (DB Console):\no Permit: Inbound traffic only from specific, known admin IP ranges (e.g., VPN gateways or jump hosts).\no Highly Recommended: Block direct external access and require administrators to use an SSH tunnel to securely access the DB Console via a jump host.\n• For Other Port (26357/TCP):\no Permit: Inbound traffic only from the IP ranges/security groups associated with:\n▪ The other CockroachDB nodes.\n3. Separate Internal and SQL Ports CockroachDB allows you to separate the listen address for internal inter-node communication from the listen address used for client connections connections.\nThis is the best practice for granular security control.\n• --listen-addr: Defines the address and port used for inter-node (cluster) communication. Restrict this port to only allow traffic from other CockroachDB nodes.\n• --sql-addr: Defines the address and port used for client SQL connections.\nRestrict this port to only allow traffic from application servers or load balancers.\nExample Command:\ncockroach start \\\n--listen-addr=10.0.0.1:26357 \\\n--sql-addr=192.168.1.1:26257 \\\nThis configuration allows you to:\n• Block all external access to 10.0.0.1:26357 (the inter-node port), only allowing traffic from the internal IP range of other nodes.\n• Restrict access to 192.168.1.1:26257 (the SQL port) to application servers and load balancers.\n4. Restrict Outbound Access:\nFollowing general security hardening principles, implement a restrictive egress policy:\n• Allow: Outbound traffic only to necessary services (e.g., NTP, S3/GCS/Azure Blob for backups, monitoring/logging endpoints).\n• Deny: All other outbound traffic by default.",
            "+@fixref": "F-18E80386r01_fix"
          },
          "fix": {
            "+@id": "F-18E80386r01_fix"
          },
          "check": {
            "+@system": "C-18E80386_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Perform the following checks to confirm CockroachDB nodes are deployed in a dedicated subnet or network segment with restrictive security rules.\n1. Identify CockroachDB Node Addresses and Network Placement:\n• Check Node IPs: Use the cockroach node status command to list the addresses the cluster is using for communication.\ncockroach node status \\\n--certs-dir=/path/to/certs \\\n--host=<node-address> \\\n--format=table\nExpected Results:\nConfirm the node addresses are internal/private (e.g., RFC 1918 ranges like 10.x.x.x, 172.16.x.x, or 192.168.x.x) and are consistent with your controlled internal network segments (VLANs or VPC private subnets).\n2. Review Ingress Firewall / Security Group Rules:\nReview the host-level firewalls (e.g., iptables, firewalld) or cloud security groups (AWS, GCP, Azure) applied to the CockroachDB nodes.\nRequired Ingress Sources Port Purpose Finding if (Must be restricted) Only permit ingress from SQL Open to 0.0.0.0/0 or 26257/TCP Application Servers and Load Clients untrusted public networks.\nBalancers.\nOpen to 0.0.0.0/0 or DB Admin IP ranges only or local 8080/TCP accessible from non- Console host (if using an SSH tunnel).\nadmin networks.\nOther Port (e.g. Inter- Other CockroachDB Nodes Open to 0.0.0.0/0 or 26357/TCP) node (for cluster sync). untrusted public networks.\n3. Review Egress Rules:\nReview network perimeter devices (or security groups) controlling outbound traffic from the CockroachDB subnet.\nExpected Results:\nEgress is generally restricted, allowing traffic only to required external services (e.g., NTP servers, object storage for backups, monitoring endpoints)."
          }
        }
      },
      {
        "+@id": "V-70254B74",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-70254B74r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.5",
          "title": "Disable Linux Memory Swapping to Prevent Performance and Stability Issues",
          "description": "<VulnDiscussion>For production CockroachDB deployments on Linux VMs or bare-metal, it is a critical requirement to disable Linux memory swapping or ensure the system has adequate RAM to prevent swap usage. Swapping database memory pages to slow disk I/O introduces severe and unpredictable latency, compromising the database's performance and stability.\nThe core strategy is to:\n• Provision sufficient physical RAM ( minimum 4 GiB RAM per vCPU in production).\n• Configure CockroachDB's memory limits ( --cache and --max-sql-memory) conservatively to prevent the process from exceeding available RAM.\n\nRationale:\nMemory swapping introduces unpredictable I/O latency when data is paged to disk.\nIn CockroachDB, this can lead to:\n• Increased transaction latency or timeouts.\n• Node unresponsiveness or crash loops under memory contention.\n• Delayed Raft consensus and loss of quorum availability.\nDisabling swap guarantees that memory access is always from physical RAM, ensuring consistent performance and preventing severe performance regressions during heavy load.\nThis aligns with Cockroach Labs' production guidance and CIS system hardening principles for high-performance database systems.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.5"
          },
          "fixtext": {
            "+content": "1. Ensure Adequate RAM and Tune Limits:\n• Provisioning: Upgrade hardware or VM resources to meet the 4 GiB RAM per vCPU requirement.\n• Tuning: Adjust the --cache and --max-sql-memory flags in the cockroach start command to adhere to the 80% safety margin.\nExample: On a 16 GiB RAM VM, the limit is 16 GiB * 0.8 = 12.8 GiB.\n• Set --cache=0.25 ( 4 GiB)\n• Set --max-sql-memory=0.25 ( 4 GiB)\n• Total check: (2* 4 GiB) + 4 GiB = 12 GiB. This is safely below 12.8 GiB.\n2. Disable Swap Permanently:\nApply the standard Linux hardening procedure to disable swap on the host:\n• Temporarily Disable Swap: Use the swapoff command to immediately disable all active swap devices:\nsudo swapoff -a\n• Permanently Disable Swap: Edit the /etc/fstab file and comment out or remove the line corresponding to the swap partition or swap file. This prevents the OS from activating swap on reboot.\n# Open /etc/fstab in a text editor (e.g., nano or vi)\nsudo nano /etc/fstab\n# Locate and comment out the swap entry (add a '#' at the start)\n# /dev/mapper/centos-swap swap swap defaults 0 0 <-- This line should be\ncommented\n• Reboot the host to confirm the changes are persistent, then re-run the audit step ( sudo swapon --show).",
            "+@fixref": "F-70254B74r01_fix"
          },
          "fix": {
            "+@id": "F-70254B74r01_fix"
          },
          "check": {
            "+@system": "C-70254B74_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Perform the following commands on each CockroachDB node to verify that swap is disabled:\n# 1. Check current swap status\nsudo swapon --show\n# 2. Confirm kernel swap configuration\ncat /proc/swaps\nExpected Results:\nswapon --show and /proc/swaps produce no output, indicating no active swap devices.\n2. Verify Physical RAM Sizing vs. vCPU:\nCompare the node's RAM to Cockroach Labs' minimum production recommendation:\n• At least 4 GiB RAM per vCPU for production nodes.\n• 2 GiB per vCPU is only acceptable for testing, not production.\n3. Check CockroachDB Memory Configuration Inspect the node's startup configuration (e.g., the systemd service file) for the memory allocation flags.\nFormula Check: The total estimated maximum memory usage for the CockroachDB process should not exceed 80% of system RAM.\n(2 × --max-sql-memory) + --cache <= 80% of system RAM\n• --cache (Pebble block cache): Should be set to 25% or higher of system RAM.\n• --max-sql-memory (SQL temporary data): Should be set to ensure the total formula is met.\nIf the calculated memory usage exceeds 80% of RAM, or if --cache is below 25% of RAM, it is a finding."
          }
        }
      },
      {
        "+@id": "V-B6D25E5A",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-B6D25E5Ar01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.6",
          "title": "Configure Transparent Huge Pages (THP) to madvise to Avoid Increased Memory Usage",
          "description": "<VulnDiscussion>Linux Transparent Huge Pages (THP) is a kernel feature that automatically manages large memory pages to improve performance for some workloads. However, for databases such as CockroachDB, this behavior can cause unpredictable latency spikes, increased memory fragmentation, and excessive CPU overhead due to THP defragmentation.\nCockroach Labs recommends configuring THP to madvise instead of the default always mode. This allows CockroachDB to control when huge pages are used without forcing the kernel to automatically allocate them for all processes.\n\nRationale:\nTHP in always mode may result in:\n• Unwanted latency from page compaction and defragmentation.\n• Increased memory consumption under load.\n• CPU overhead when the kernel continually attempts to merge pages.\nSetting THP to madvise ensures CockroachDB explicitly requests huge pages when beneficial while maintaining predictable memory performance and reducing kernel overhead. This configuration aligns with production best practices for database workloads and CIS Linux performance hardening standards.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.6"
          },
          "fixtext": {
            "+content": "1. Temporarily set THP to madvise (takes effect immediately):\nOn each node (run as root or via sudo):\necho madvise | sudo tee /sys/kernel/mm/transparent_hugepage/enabled\n#Optionally, align the defrag behavior:\necho madvise | sudo tee /sys/kernel/mm/transparent_hugepage/defrag\n2. Persist the setting across reboots Use your distribution's standard method (for example, systemd unit, boot-time script, or vendor-recommended approach) to ensure enabled is set to madvise at boot.\nCockroach Labs notes that the method for permanently changing the THP setting depends on the OS, please refer to the OS documentations.\n3. Verify the configuration after reboot:\ncat /sys/kernel/mm/transparent_hugepage/enabled\nConfirm that madvise remains the active selection.",
            "+@fixref": "F-B6D25E5Ar01_fix"
          },
          "fix": {
            "+@id": "F-B6D25E5Ar01_fix"
          },
          "check": {
            "+@system": "C-B6D25E5A_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Perform the following commands on each CockroachDB node to verify THP configuration:\n# 1. Check the current THP mode\ncat /sys/kernel/mm/transparent_hugepage/enabled\n# 2. Check the defrag setting\ncat /sys/kernel/mm/transparent_hugepage/defrag\nExpected Results:\nThe enabled line should show madvise as the active mode, for example:\nExample output:\nalways madvise [never]\nOr\nmadvise [never]\nThe exact formatting may vary by kernel, but madvise should be the selected (bracketed) option.\nIf [ always ] is selected (for example, [always] madvise never), this deviates from the CockroachDB recommendation and is a finding for new deployments or for existing deployments without a comfortable memory margin."
          }
        }
      },
      {
        "+@id": "V-F19B5ADA",
        "title": "2 System Hardening and Topology",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-F19B5ADAr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "2.7",
          "title": "Set the File Descriptor Limit to at Least 15,000 to Accommodate Cluster Growth",
          "description": "<VulnDiscussion>CockroachDB can use a large number of open file descriptors, often more than is available by default. Therefore, note the following recommendations.\nFor each CockroachDB node:\n• At a minimum, the file descriptors limit must be 1956 (1700 per store plus 256 for networking). If the limit is below this threshold, the node will not start.\n• It is recommended to set the file descriptors limit to unlimited; otherwise, the recommended limit is at least 15000 (10000 per store plus 5000 for networking).\nThis higher limit ensures performance and accommodates cluster growth.\n• When the file descriptors limit is not high enough to allocate the recommended amounts, CockroachDB allocates 10000 per store and the rest for networking; if this would result in networking getting less than 256, CockroachDB instead allocates 256 for networking and evenly splits the rest across stores.\n\nRationale:\nFailing to configure adequate FD limits can lead to critical operational issues:\n• Startup Failures: If the hard limit is below the minimum of 1956, the CockroachDB node will explicitly refuse to start.\n• Operational Instability: Low limits prevent the cluster from accommodating growth in storage (more stores) and network connections, leading to runtime failures under load.\n• Performance Assurance: The recommended limit of $\\ge 15,000$ ensures performance and stability as the cluster scales.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "2.7"
          },
          "fixtext": {
            "+content": "1. Set per-process limit:\nTo adjust the file descriptors limit for a single process on Linux, enable PAM user limits and set the hard limit to the recommendation mentioned above. Note that CockroachDB always uses the hard limit, so it's not technically necessary to adjust the soft limit, although we do so in the steps below.\nFor example, for a node with 1 stores, we would set the hard limit to at least 15000 (10000 per store and 5000 for networking) as follows:\nThe docs distinguish between systemd-managed and non-systemd setups.\n• If you use systemd:\nManually-set limits set using the ulimit command or a configuration file like /etc/limits.conf are ignored for services started by systemd. To limit the number of open file descriptors, add a line like the following to the service definition for the cockroach process:\nLimitNOFILE=15000\nYou may also set LimitNOFILE=INFINITY if you have tested that configuration with a realistic workload.\nReload systemd for the new limit to take effect:\nsudo systemctl daemon-reload\nRestart the CockroachDB service:\nsudo systemctl restart cockroach.service\n• If you do not use systemd:\nEdit /etc/security/limits.conf and append the following lines to the file:\n* soft nofile 15000\n* hard nofile 15000\nYou can replace * with the specific username that runs CockroachDB (for example, cockroach). Then:\nEnsure pam_limits.so is enabled in /etc/pam.d/common-session and common- session-noninteractive:\nsession required pam_limits.so\nReboot or log out / log back in and restart the CockroachDB process, then verify with ulimit -a.\n2. Increase system-wide file descriptor limit:\nYou should also confirm that the file descriptors limit for the entire Linux system is at least 10 times higher than the per-process limit documented above (e.g., at least 150000).\n• If using systemd, add in /etc/systemd/system.conf or a drop-in for the Manager service:\n[Manager]\nLimitNOFILE=150000\nThen:\nsudo systemctl daemon-reload\n• If not using systemd:\ncat /proc/sys/fs/file-max # check current\necho 150000 | sudo tee /proc/sys/fs/file-max\nTo persist, configure your distribution's sysctl settings accordingly.\n3. Verify:\nAfter changes:\ncat /proc/$(pgrep cockroach)/limits | grep \"open files\"\ncat /proc/sys/fs/file-max\nConfirm:\n• Process hard limit for open files ≥ 15000.\n• System-wide file- max ≥ 10× that value.",
            "+@fixref": "F-F19B5ADAr01_fix"
          },
          "fix": {
            "+@id": "F-F19B5ADAr01_fix"
          },
          "check": {
            "+@system": "C-F19B5ADA_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Perform the following checks on each CockroachDB node:\n1. Check CockroachDB's Active Per-Process Hard Limit:\nUse the standard Linux process limits interface:\ncat /proc/$(pgrep cockroach)/limits | grep \"open files\"\nExpected Results:\nThe 'Max open files' Hard limit must be ≥ 15000(or the value you have set, if higher).\n2. Check systemd service LimitNOFILE (if using systemd):\nInspect your unit file (for example, /var/systemd/system/cockroach.service or similar):\nsudo systemctl cat cockroach | grep -i LimitNOFILE\n3. Verify System-Wide FD Limit:\nThe system-wide file descriptor limit should be at least 10× the per- process limit (e.g., ≥ 150000 when using 15000 per process)\ncat /proc/sys/fs/file-max\nExpected Results:\nIf the CockroachDB process hard limit is < 15000, or the system-wide limit is too low to support that many FDs per process, this is a finding."
          }
        }
      },
      {
        "+@id": "V-39E3263D",
        "title": "3 Logging Monitoring and Auditing",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-39E3263Dr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "3.1",
          "title": "Ensure CockroachDB Logging Is Enabled and Logs Are Securely Stored",
          "description": "<VulnDiscussion>CockroachDB's built-in logging must remain enabled to capture database events, and log files should be stored in a secure location with appropriate access restrictions. By default, CockroachDB produces log files for subsystems such as SQL, authentication, and cluster events. Although logging cannot be fully disabled, misconfiguration (e.g., setting --log-dir= empty or invalid) can prevent logs from being retained. Ensuring that logging is active and directed to a secure directory provides an audit trail of activity.\nOnly the CockroachDB service account and authorized administrators should have access.\n\nRationale:\nLogs record authentication attempts, configuration changes, and operational events.\nDisabling or neglecting logging prevents forensic analysis and detection of unauthorized activity. Storing logs on a secure filesystem prevents exposure or tampering. Proper log management supports non-repudiation and accountability.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "3.1"
          },
          "fixtext": {
            "+content": "Do not introduce a separate way to start CockroachDB; modify the existing cockroach.service from Control 1.2.\n1. Create and harden the log directory:\nmkdir -p /var/lib/cockroach/logs\nchown -R cockroach:cockroach /var/lib/cockroach/logs\nchmod 700 /var/lib/cockroach/logs\nThis aligns with the docs' recommendation to use a separate directory from the main store for logs in cloud / IOPS-sensitive deployments.\n2. Add logging configuration to cockroach.service:\nEdit /etc/systemd/system/cockroach.service (the unit defined in 1.2) and update the existing ExecStart to include a secure logging path. For example:\n[Service]\nWorkingDirectory=/var/lib/cockroach\nExecStart=/usr/local/bin/cockroach start \\\n--certs-dir=certs \\\n--store=/var/lib/cockroach \\\n--log-dir=/var/lib/cockroach/logs \\\n--advertise-addr=<node address> \\\n--join=<node1 address>,<node2 address>,<node3 address> \\\n--cache=.25 \\\n--max-sql-memory=.25\nUser=cockroach\n--log-dir=/var/lib/cockroach/logs is an alias for --log='file-defaults:\n{dir: /var/lib/cockroach/logs}'.\nAlternatively, point to a YAML configuration:\nExecStart=/usr/local/bin/cockroach start \\\n--certs-dir=certs \\\n--store=/var/lib/cockroach \\\n--log-config-file=/etc/cockroach/log-config.yaml \\\n...\nEnsure the YAML uses appropriate file-defaults / file-groups (e.g., dir, file- permissions, max-file-size, max-group-size) consistent with or hardening the documented defaults.\nReload and restart:\nsudo systemctl daemon-reload\nsudo systemctl restart cockroach.service\n3. Tighten file permissions (if needed):\nIf files were created with looser modes, enforce restrictive permissions:\nchmod 640 /var/lib/cockroach/logs/*.log\nIf you use a YAML log config, set file-permissions: \"0640\" (or stricter) in file-defaults or specific file-groups.\n4. Validate logging after changes:\n• Perform any test action such as adding rows, simple query.\n• Confirm that new entries appear in the appropriate log files (for example, cockroach.log)",
            "+@fixref": "F-39E3263Dr01_fix"
          },
          "fix": {
            "+@id": "F-39E3263Dr01_fix"
          },
          "check": {
            "+@system": "C-39E3263D_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Prerequisite: Control 1.2 is implemented and CockroachDB is started only via systemd (e.g., cockroach.service).\n1. Verify that CockroachDB logging is enabled and logs are securely stored:\nFor default configurations:\ncockroach debug check-log-config\nFor custom configured during:\nCheck for explicit logging flags on the running process:\nInspect the ExecStart line in the active systemd unit:\nsudo systemctl cat cockroach.service | grep ExecStart\nExpected Results:\nOne of the following is true:\n• The ExecStart line contains --log-dir=<path>, for example:\n--log-dir=/var/lib/cockroach/logs\n( --log-dir=XXX is an alias for --log='file-defaults: {dir: XXX}').\n• Or the ExecStart line contains either:\no --log-config-file=/path/to/log-config.yaml, or\no --log={yaml}.\nIf none of these flags are present, determine the on-disk store directory from the -- store flag (or the default cockroach-data) and infer the logging directory as <store>/logs.\nFor hardened deployments, prefer an explicit log directory separate from the main data store to avoid IOPS contention.\n2. Verify the existence and timeliness of log files:\nUsing the identified logging directory (e.g., /cockroach-data/logs or a custom directory such as /var/lib/cockroach/logs), verify that log files exist and have recent modification times, for example: ls -lh /var/lib/cockroach/logs or ls -lh /cockroach-data/logs ( default)\ntail -n 10 /var/lib/cockroach/logs/cockroach.log\nWith the default logging configuration, expect files such as:\n• cockroach.log (general / DEV and OPS)\n• cockroach-security.log (USER_ADMIN, PRIVILEGES)\n• cockroach-sql-audit.log (SENSITIVE_ACCESS)\n• cockroach-sql-auth.log (SESSIONS) 3. Verify directory ownership and permissions:\nCheck that the log directory and files are owned by the CockroachDB service account and have restrictive permissions:\nls -ld /var/lib/cockroach/logs\nExpected Results:\n• Owner: cockroach\n• Group: cockroach\n• Mode: drwx------ (700) or more restrictive.\nThe service in Control 1.2 should already be configured to run as User=cockroach Expected Results:\n• The systemd service's ExecStart line configures logging either via:\no --log-dir=/var/lib/cockroach/logs (or another secure path), or\no --log-config-file or --log={yaml}, or, if relying on defaults, logs are observed in <store>/logs (for example, /cockroach-data/logs).\n• Log files such as cockroach.log, cockroach-security.log, and cockroach- sql-audit.log exist in the configured logging directory with recent timestamps.\n• The log directory is owned by the CockroachDB service account and has permissions of 700 (or stricter). Log files are owned by the same account and have permissions of 640 (or stricter).\nIf any of these checks fail, this is a finding."
          }
        }
      },
      {
        "+@id": "V-BCB5D945",
        "title": "3 Logging Monitoring and Auditing",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-BCB5D945r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "3.2",
          "title": "Ensure Log Rotation and Retention Settings Are Configured",
          "description": "<VulnDiscussion>CockroachDB log rotation and retention must be configured to prevent uncontrolled growth and to preserve an appropriate history window.\nCockroachDB supports per-group rotation parameters that control both individual file size and total group retention. Each log file group can define:\n• max-file-size - maximum size of an individual log file before a new file is created.\n• max-group-size - total size (or number of files) to retain before older files are purged.\nBy default, CockroachDB rotates each file at ~10 MiB and keeps about 100 MiB (~10 files) per group.\n\nRationale:\nUnbounded log growth poses both reliability and security risks.\nIf log files are not rotated, a busy CockroachDB node could generate extremely large files, potentially exhausting disk space or complicating log review.\nProper log rotation ensures:\n• Predictable and capped disk usage.\n• Easier log management and review.\n• Compliance with audit retention policies.\n• Smaller, segmented logs suitable for archiving or centralization.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "3.2"
          },
          "fixtext": {
            "+content": "1. Use defaults or define explicit rotation settings:\nIf you do not configure a custom logging YAML, CockroachDB applies defaults of max- file-size: 10MiB and max-group-size: 100MiB to most file groups, which are acceptable for many small-to-medium deployments.\nTo explicitly configure rotation and retention for self-hosted nodes, define or edit a logging configuration YAML, for example:\nfile-defaults:\ndir: /var/lib/cockroach/logs\nmax-file-size: 10MiB # default per-file size\nmax-group-size: 500MiB # example: increase retention cluster-wide\nfilter: INFO\nsinks:\nfile-groups:\ndefault:\n# Inherit dir, max-file-size, max-group-size from file-defaults\nchannels: {INFO: [DEV, OPS], WARNING: [HEALTH, STORAGE, SESSIONS,\nSQL_SCHEMA,\nUSER_ADMIN, PRIVILEGES, SENSITIVE_ACCESS, SQL_EXEC, SQL_PERF,\nSQL_INTERNAL_PERF, TELEMETRY, KV_DISTRIBUTION]}\nsql-audit:\nchannels: {INFO: [SENSITIVE_ACCESS]}\nmax-file-size: 20MiB # override if audit volume is high\nmax-group-size: 200MiB\nbuffered-writes: false # typical for security/audit logs\nauditable: true # when using file-based auditing\n• file-defaults is the recommended place to set common dir, max-file-size and max-group-size for all file groups, overriding only where necessary.\n• max-group-size controls total bytes of all log files per group; when exceeded, the oldest files are removed asynchronously.\n2. Start nodes with the configuration:\nStart (or restart) each self-hosted node with the logging configuration:\n• For nodes started directly with cockroach start, apply the logging configuration as follows:\ncockroach start \\\n--certs-dir=/var/lib/cockroach/certs \\\n--store=/var/lib/cockroach \\\n--log-config-file=/etc/cockroach/logging.yaml\nAlternatively, you can override rotation parameters inline with the --log flag, for example:\ncockroach start \\\n--certs-dir=/var/lib/cockroach/certs \\\n--store=/var/lib/cockroach \\\n--log='file-defaults: {dir: /var/lib/cockroach/logs, max-file-size:\n10MiB, max-group-size: 500MiB}'\n```[[cockroach start –\nlogging](https://www.cockroachlabs.com/docs/stable/cockroach-\nstart#logging)]\n• For nodes managed by systemd using cockroachdb.service, add the same -- log or --log-config-file (example: --log-config- file=/etc/cockroach/logging.yaml) flags to the ExecStart line in the service unit and then run:\nsudo systemctl daemon-reload\nsudo systemctl restart securecockroachdb\n3. Adjust retention to meet policy:\n• Set max-group-size (or max-file-count if used) to retain logs for the desired number of days.\n• Estimate daily log volume to determine appropriate total retention (i.e. In high-traffic deployments, It is recommendded increasing file-defaults.max- group-size to 500MiB or 1GiB to extend log retention).\n• If a centralized log management system is used, keep only a small local backlog and forward older logs.\n4. Verify rotation after changes:\nAfter configuration, monitor the log directory over time.\nYou should observe:\n• New files being created when size thresholds are exceeded.\n• Oldest files automatically deleted when the total size limit is reached.\nIf this behavior does not occur,:\n• The YAML syntax (especially indentation and placement under file-defaults and sinks.file-groups).\n• That nodes were actually started with --log or --log-config-file and not with conflicting flags such as --log-dir alone.\n5. Ensure diagnostic bundles can still be collected:\nEnsure that rotation does not interfere with diagnostic collections.\nThe cockroach debug zip command automatically includes rotated log files.\nAs a best practice, avoid setting max-file-size extremely low (for example, a few kilobytes), as this could create thousands of small files and impact performance or manageability.",
            "+@fixref": "F-BCB5D945r01_fix"
          },
          "fix": {
            "+@id": "F-BCB5D945r01_fix"
          },
          "check": {
            "+@system": "C-BCB5D945_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Review the active logging configuration:\n• To view the current logging configuration as applied:\nUse the following command on a node to display the validated logging configuration:\ncockroach debug check-log-config\nif you want to see the default or currently active configuration.*\n• Alternatively, if you start nodes with a static ( – log config) YAML file:\nsudo grep -A10 'file-groups:' /etc/cockroach/logging.yaml\nVerify that each file-groups entry used in production has reasonable values for:\n• max-file-size\n• max-group-size 2. Inspect on-disk rotation behavior:\nCheck the node's log directory (by default inferred from the first store directory as cockroach-data/logs, or as customized with file-defaults.dir).\nls -lh /var/lib/cockroach/logs\ndu -sh /var/lib/cockroach/logs\n3. Check file sizes and count per log group:\nfind /var/lib/cockroach/logs -type f -printf '%p %s\\n' \\\n| sort -k2 -nr | head\n4. Confirm rotation behavior via filenames and timestamps:\nls -ltr /var/lib/cockroach/logs | tail\nExpected Results:\n• EMultiple timestamped files per group (e.g., cockroach.log, cockroach.2025- 10-22T14_10_00Z.log).\n• File sizes are capped approximately at the configured max-file-size.\n• The total size of files per group does not exceed max-group-size (The total size of files per group does not exceed ).\n• No single log file grows indefinitely.\nIf logs exceed expected limits or only a single oversized file exists, rotation is misconfigured - this is a finding."
          }
        }
      },
      {
        "+@id": "V-C5BFC1A3",
        "title": "3 Logging Monitoring and Auditing",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-C5BFC1A3r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "3.3",
          "title": "Ensure Connection Authentication Logging Is Enabled",
          "description": "<VulnDiscussion>CockroachDB can log client connection attempts, successful logins, authentication failures, and session disconnections through the SESSIONS logging channel.\nBy default, this channel's logging is disabled to minimize overhead on all clusters.\nEnabling connection and authentication logging ensures that each login event is recorded with details such as the username, client host/IP address, and timestamp for both connection and disconnection events.\nThese settings are controlled by two cluster parameters:\n• server.auth_log.sql_connections.enabled - logs connection open and close events.\n• server.auth_log.sql_sessions.enabled - logs authentication successes, failures, and session termination.\nBoth parameters must be set to true to enable comprehensive session logging.\n\nRationale:\nTracking who connects to the database, when, and from where is fundamental to security monitoring.\nAuthentication logs provide early visibility into brute-force attempts, misuse of credentials, or anomalous login patterns.\nThey form a crucial audit trail for compliance and incident response.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "3.3"
          },
          "fixtext": {
            "+content": "1. Enable connection and authentication logging:\nUse SQL to enable both cluster settings:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> \\\n-e \"SET CLUSTER SETTING server.auth_log.sql_connections.enabled = true;\"\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> \\\n-e \\\"SET CLUSTER SETTING server.auth_log.sql_sessions.enabled = true;\\\"\nThese settings apply cluster-wide and persist across restarts.\n2. Ensure SESSIONS channel is logged to disk With the default logging configuration, the SESSIONS channel is already mapped to cockroach-sql-auth.log via the sql-auth file group.\nBy default, server.auth_log.sql_connections.enabled and server.auth_log.sql_sessions.enabled are false, so no connection events are logged.\nIf you use a custom logging YAML, verify that SESSIONS is included in at least one file sink, for example:\nsinks:\nfile-groups:\nsql-auth:\nchannels: {INFO: [SESSIONS]}\ndir: /var/lib/cockroach/logs\nmax-file-size: 10MiB\nmax-group-size: 100MiB\nbuffered-writes: false\nformat: crdb-v2\nThis ensures SESSIONS events are persisted to disk. Adjust rotation ( max-file-size, max-group-size) in line with Control 3.2.\n3. Be aware of performance impact:\nCockroach Labs documents that enabling server.auth_log.sql_connections.enabled and server.auth_log.sql_sessions.enabled can impact performance because each event incurs a disk write.\n• Enable both by default on security-sensitive or audited clusters.\n• On extremely high-throughput clusters, consider sampling or tightening HBA rules while monitoring the performance impact.\n4. Integrate with centralized logging (if applicable):\n• Include cockroach-sql-auth.log (or the custom sink that carries SESSIONS) in your log shipping configuration.\n• Use the structured fields ( EventType, User, RemoteAddress, Method, Reason) for alerting on repeated failures or suspicious IP ranges.",
            "+@fixref": "F-C5BFC1A3r01_fix"
          },
          "fix": {
            "+@id": "F-C5BFC1A3r01_fix"
          },
          "check": {
            "+@system": "C-C5BFC1A3_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Verify cluster settings for connection and session authentication logging:\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> \\\n-e \"SHOW CLUSTER SETTING server.auth_log.sql_connections.enabled;\"\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> \\\n-e \"SHOW CLUSTER SETTING server.auth_log.sql_sessions.enabled;\"\nExpected Results:\nserver.auth_log.sql_connections.enabled\n-------------------------------\ntrue\nserver.auth_log.sql_sessions.enabled\n----------------------------\ntrue\nBoth settings must be true for comprehensive logging of connection and authentication events to SESSIONS.\n2. Verify that the SESSIONS log file is present and contains recent events:\nDetermine the logging directory (default <store>/logs, for example /var/lib/cockroach/logs).\nls -lh /var/lib/cockroach/logs/\ntail -n 10 /var/lib/cockroach/logs/cockroach-sql-auth.log\nIn the default configuration, you should see cockroach-sql-auth.log (or rotated variants such as cockroach-sql-auth.2025- …log) tied to the SESSIONS channel.\nExpected Result :\n• JSON event entries with EventType such as client_connection_start, client_connection_end, client_authentication_info, client_authentication_ok, client_authentication_failed, and client_session_end.\n• Fields including User, RemoteAddress, Network, Transport, and timestamps.\nExample (password failure):\n... \"EventType\":\"client_authentication_failed\", ...\n\"User\":\"‹roach›\",\"Detail\":\"‹password authentication failed for user\nroach›\",\"Method\":\"cert -password\"}\n... \"EventType\":\"client_session_end\", ...\n\"RemoteAddress\":\"‹[::1]:52632›\",\"Duration\":...\n3. Verify events via system.eventlog:\nIf server.eventlog.enabled is true (default), notable SQL session events are also written into system.eventlog per tenant.\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=<node> \\\n-e \"SELECT timestamp, (info::JSONB)->>'EventType' AS eventtype, info FROM\nsystem.eventlog WHERE (info::JSONB)->>'EventType' IN\n('client_connection_start', 'client_connection_end',\n'client_authentication_failed', 'client_session_end') ORDER BY timestamp DESC\nLIMIT 20;\"\nPresence of recent records for active clients further confirms that connection and authentication events are being captured.\n4. Verify inclusion in centralized logging:\nIf you ship logs to a SIEM or log aggregator, confirm that messages from the SESSIONS channel (typically from cockroach-sql-auth.log) are collected. How you query this depends on your logging stack."
          }
        }
      },
      {
        "+@id": "V-16FB32BE",
        "title": "3 Logging Monitoring and Auditing",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-16FB32BEr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "3.4",
          "title": "Ensure Database Monitoring and Alerting Are in Place",
          "description": "<VulnDiscussion>A robust monitoring and alerting system must be implemented for CockroachDB to continuously observe the health, performance, and security posture of the database cluster. CockroachDB exposes detailed metrics and events through its internal time- series database and provides a built-in Admin UI (DB Console) for visualization.\nFor comprehensive observability, these metrics should be exported to an external monitoring stack such as Prometheus and Grafana, and alerts should be configured using tools like Alertmanager or equivalent monitoring services.\nKey aspects include:\n• Node and Cluster Health: Live node count, replication status, and under- replicated ranges.\n• Performance Metrics: Throughput, latency, and resource utilization (CPU, memory, disk).\n• Security Events: Frequent authentication failures, unusual query volume, or configuration changes.\n• Backup and Job Status: Ensure scheduled tasks complete successfully.\nPlease find more information here:\nhttps://www.cockroachlabs.com/docs/stable/essential-metrics-self-hosted\n\nRationale:\nContinuous monitoring enables early detection of both operational and security anomalies.\nMetrics and alerts allow administrators to respond proactively to incidents such as node outages, disk exhaustion, performance degradation, or unauthorized activity.\nEffective monitoring also supports compliance frameworks that require audit log management and system availability oversight.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "3.4"
          },
          "fixtext": {
            "+content": "1. Integrate with Prometheus and Grafana:\nConfigure Prometheus to scrape metrics from all CockroachDB nodes (default endpoint:\nhttp://<node>:8080/_status/vars).\nExample Prometheus job configuration:\nscrape_configs:\n- job_name: 'cockroachdb'\nscrape_interval: 10s\nstatic_configs:\n- targets: ['node1:8080', 'node2:8080', 'node3:8080']\nImport the official CockroachDB Grafana dashboard JSON from Cockroach Labs to visualize performance data.\n2. Set Up Alerting:\nDefine alert rules for conditions such as:\n• Node down or unreachable for >1 minute\n• Disk capacity <15%\n• CPU >90% for 10 minutes\n• Query latency above threshold\n• Frequent authentication failures;\nIntegrate alerts with notification channels (email, PagerDuty, Slack, etc.).\n3. Use and Secure the DB Console:\nEnsure the Admin UI is enabled and accessible over TLS.\nRegularly review Metrics and Events pages for anomalies.\n4. Centralize Logs and Correlate with Metrics:\nForward CockroachDB logs and audit data to your SIEM or log management platform.\n5. Establish Routine Checks:\nConduct periodic reviews of dashboards and alerts, perform simulated outage drills, and update rules as new risks or features emerge.",
            "+@fixref": "F-16FB32BEr01_fix"
          },
          "fix": {
            "+@id": "F-16FB32BEr01_fix"
          },
          "check": {
            "+@system": "C-16FB32BE_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Perform the following checks to confirm that monitoring and alerting are properly configured:\n# 1. Verify that Prometheus or an equivalent collector is scraping\nCockroachDB metrics\n# Check the Prometheus configuration for CockroachDB targets:\ngrep \"cockroach\" /etc/prometheus/prometheus.yml\n# or verify metrics endpoint access directly from a node:\ncurl -s http://<node-host>:8080/_status/vars | head\n# 2. Verify Grafana dashboards are deployed and show live data\n# (Log into Grafana at https://<grafana-server>:3000 and confirm CockroachDB\npanels update)\n# The official dashboard JSON is available from Cockroach Labs documentation.\n# 3. Verify alerting rules are configured (Prometheus Alertmanager or\nequivalent)\ngrep \"alert:\" /etc/prometheus/alert.rules.yml\n# Expected rules: node down, high CPU usage, low disk space, replication\nissues, failed backups, etc.\n# 4. Verify DB Console (Admin UI) metrics and event logs\n# Access https://<node>:8080, navigate to:\n# Metrics → confirm charts update.\n# Advanced Debug → Events → confirm node join/restart/configuration events\nappear.\n# 5. Verify process ownership and alert response procedures\n# Interview administrators or review documentation confirming that alerts are\nactively monitored and acted upon.\nExpected Results:\n• Metrics endpoint ( /_status/vars) is reachable and producing data.\n• Grafana dashboards show current activity and historical trends.\n• Alerting rules exist for critical events (node down, high latency, disk <15%).\n• Admin UI displays live metrics and events.\n• A defined process exists for alert response and periodic review.\nIf any of these components are missing or inactive, this is a finding."
          }
        }
      },
      {
        "+@id": "V-EF5BB7CA",
        "title": "4 User Access and Authorization",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-EF5BB7CAr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "4.1",
          "title": "Restrict Access Using Host-Based Authentication",
          "description": "<VulnDiscussion>CockroachDB uses a host-based authentication (HBA) configuration to control which SQL connection attempts are allowed to proceed and which authentication methods they may use, based on the originating IP address and the SQL user. HBA rules are expressed in a PostgreSQL-compatible DSL and stored in the server.host_based_authentication.configuration cluster setting; each rule defines a connection type, database scope (currently always all), user, address (IP/CIDR), and authentication method. By configuring this setting, administrators can explicitly restrict inbound SQL connections to approved network ranges and enforce the desired authentication methods per origin, adding an application-layer access-control boundary on top of network-level controls.\nFor hardened deployments, HBA rules should permit connections only from trusted IP addresses or CIDR ranges and then require strong authentication methods such as TLS client certificates (cert / cert-password / cert-scram-sha-256) or SCRAM-SHA-256 (scram-sha-256), depending on organizational policy. Less restrictive methods like password can be allowed where justified, but unauthenticated or trivially authenticated methods such as trust should be avoided in production, and reject should be used as a catch-all deny rule for unintended origins.\n\nRationale:\nRestrictive HBA configuration reduces the attack surface by ensuring that only connections from explicitly permitted IP addresses or ranges can reach the authentication stage, even if credentials are compromised. Binding each allowed origin to a strong authentication method, such as certificate-based or SCRAM-SHA-256 authentication, limits downgrade opportunities and helps prevent unauthorized network-based access. Because HBA operates at the SQL layer, it provides an additional defense-in-depth control beyond network firewalls and load balancer rules, and can be used to implement patterns such as \"jumpbox-only\" access or IP-scoped user rules.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "4.1"
          },
          "fixtext": {
            "+content": "1. Define explicit whitelisted origins Construct an HBA configuration that lists only authorized IP ranges or hosts. For example:\nSET CLUSTER SETTING server.host_based_authentication.configuration = '\nTYPE DATABASE USER ADDRESS METHOD\nhost all jump_user 203.0.113.10/32 password\nhost all app_user 198.51.100.0/24 password\nhost all all all reject\n';\nThis pattern mirrors the documented approach of whitelisting a jumpbox or application IPs and rejecting all others.\n2. Enforce appropriate authentication methods Adjust the METHOD column so that each allowed origin uses an authentication method consistent with organizational policy (for example, cert or scram-sha-256 for human users, cert/cert-scram-sha-256 for service accounts), following the supported methods listed in the documentation.\n3. Remove or tighten permissive rules:\n• Replace overly broad rules (for example, host all all all password) with IP-scoped entries and a final reject rule.\n• Remove or narrow any rules that grant access from unapproved IP ranges or that permit methods contrary to hardening policy.\n4. Apply configuration changes\n• Set the updated HBA configuration:\nSET CLUSTER SETTING server.host_based_authentication.configuration =\n'<rules>';\n• No node restart is required; HBA changes take effect from the cluster setting, but existing sessions will continue using the authentication method under which they were established.",
            "+@fixref": "F-EF5BB7CAr01_fix"
          },
          "fix": {
            "+@id": "F-EF5BB7CAr01_fix"
          },
          "check": {
            "+@system": "C-EF5BB7CA_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Review HBA configuration for allowed network origins\nSHOW CLUSTER SETTING server.host_based_authentication.configuration;\nExpected Results:\n• Rules explicitly list approved IP addresses or CIDR ranges for client access (for example, a jumpbox /32 or specific app subnets).\n• A final rule (or equivalent configuration) rejects all other addresses using reject or an IP pattern that effectively denies unintended sources.\nFail if:\n• The configuration is empty or effectively equivalent to host all all all password on self-hosted, which is the default and not hardened.\n• Broad patterns such as all without a corresponding reject rule allow unvetted origins that violate organizational policy.\n2. Verify authentication methods for each origin From the same server.host_based_authentication.configuration output, inspect the METHOD column for each rule.\nExpected Results:\n• Strong methods such as cert, cert-password, cert-scram-sha-256, or scram-sha-256 are used where required by policy.\n• reject is used for disallowed origins.\nFail if:\n• Rules intended to be hardened rely on trust (no authentication) or on weaker methods in contexts where strong methods are mandated by policy (for example, where the Benchmark requires SCRAM-only, but password is still allowed).\n3. Verify default-deny behavior Confirm that the effective rule set implements a default-deny stance — for example, by ending with:\nhost all all all reject\nas shown in hardened examples.\nFail if:\nThere is no explicit or effective deny rule for \"all other\" origins, resulting in unintended access from unlisted networks."
          }
        }
      },
      {
        "+@id": "V-3C6F5940",
        "title": "4 User Access and Authorization",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-3C6F5940r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "4.2",
          "title": "Restrict and Secure Password Authentication",
          "description": "<VulnDiscussion>CockroachDB self-hosted clusters support several authentication methods for SQL clients, including passwords, SCRAM-SHA-256, TLS client certificates, and others such as GSSAPI/Kerberos (Enterprise), JWT, LDAP/AD and OIDC. For production, Cockroach Labs recommends using TLS client certificates instead of passwords, and provides first-class support for SCRAM-SHA-256 as a stronger password-based mechanism where passwords are still required. In self-hosted environments, operators can further restrict where and how password authentication is used by configuring the HBA rules ( server.host_based_authentication.configuration) and by managing per-user authentication settings via SQL statements such as CREATE USER and ALTER USER.\nCockroachDB supports SCRAM-SHA-256 for storing and verifying password credentials and provides guidance for enabling SCRAM for new and existing users, including migration from older cleartext/bcrypt formats. Operators can configure the cluster to encrypt new passwords using SCRAM-SHA-256 ( server.user_login.password_encryption = 'scram-sha-256') and allow pre- hashed SCRAM passwords where strict isolation of cleartext credentials is required. For hardened self-hosted deployments, passwords should be minimized and, where necessary, protected with SCRAM-SHA-256 and constrained to specific users and client origins via HBA rules, while the majority of access relies on stronger certificate- based or externally managed authentication mechanisms.\n\nRationale:\nPassword-based authentication is more susceptible to brute-force attacks, credential reuse, and phishing than certificate-based or federated identity mechanisms.\nCockroachDB's support for TLS client certificates and SCRAM-SHA-256 enables operators to reduce these risks by preferring certificate-based authentication and, where passwords remain in use, storing them as strong SCRAM hashes instead of weaker or cleartext formats. Enforcing SCRAM-SHA-256 for password credentials and phasing out legacy cleartext/bcrypt formats hardens the cluster against offline cracking and replay attacks, and aligns with CockroachDB's recommendation to migrate existing users to SCRAM.\nSeparating privileged access paths and favoring certificate-based authentication for highly privileged roles (for example, root with TLS client certificates) provides stronger cryptographic guarantees and reduces reliance on reusable secrets for critical administration tasks. Restricting where password authentication is allowed — via both user configuration and HBA rules — helps ensure that password-based logins are tightly controlled, strongly hashed, and limited in scope, contributing to a defense-in-depth posture for self-hosted clusters.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "4.2"
          },
          "fixtext": {
            "+content": "1. Enable SCRAM-SHA-256 for new passwords Configure the cluster to use SCRAM-SHA-256 for all newly set passwords:\nSET CLUSTER SETTING server.user_login.password_encryption = 'scram-sha-256';\nThis ensures that any CREATE USER ... WITH PASSWORD or ALTER USER ... WITH PASSWORD operations store SCRAM-SHA-256 hashes.\n2. Migrate existing users to SCRAM-SHA-256 If legacy/bcrypt hashes exist and migration is desired:\n• For eventual migration:\nSET CLUSTER SETTING\nserver.user_login.upgrade_bcrypt_stored_passwords_to_scram.enabled = true;\n• For immediate migration, rotate passwords:\nALTER USER {user} WITH PASSWORD '{new_password}';\nThis writes a SCRAM-SHA-256 hash if server.user_login.password_encryption = 'scram-sha-256'.\n3. Optionally implement strict isolation of cleartext credentials For environments that require full separation of cleartext password handling:\n• Enable acceptance of pre-computed SCRAM hashes:\nSET CLUSTER SETTING\nserver.user_login.store_client_pre_hashed_passwords.enabled = true;\n• Configure HBA to allow only scram-sha-256 and/or cert-scram-sha-256, and remove password / cert-password from allowed methods:\nSET CLUSTER SETTING server.host_based_authentication.configuration = '\nTYPE DATABASE USER ADDRESS METHOD\nhost all app_user all scram-sha-256\nhost all all all reject\n';\nGenerate SCRAM hashes externally and use them directly in CREATE USER / ALTER USER statements as documented.\n4. Tighten HBA rules around password usage Replace broad password-based rules with IP-scoped and user-scoped entries that use scram-sha-256 where passwords are allowed and reject for other cases, following the secure HBA examples.",
            "+@fixref": "F-3C6F5940r01_fix"
          },
          "fix": {
            "+@id": "F-3C6F5940r01_fix"
          },
          "check": {
            "+@system": "C-3C6F5940_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Review SQL users and hashed password formats\n• List users:\nSHOW USERS;\nThis shows usernames and role memberships To inspect stored password hashes and infer whether SCRAM-SHA-256 is used:\nSET bytea_output = 'escape';\nSELECT username, \"hashedPassword\"\nFROM system.users\nWHERE \"hashedPassword\" IS NOT NULL;\nSCRAM-SHA-256 hashes appear with the SCRAM-SHA-256$... prefix when decoded from hex Expected Results (for hardened deployments):\n• Most privileged and application accounts either:\no Do not have a password (and use certificate or other non-password auth), or\no Have SCRAM-SHA-256 hashes, not legacy/bcrypt formats.\n• No unexpected users with password hashes exist beyond those intended by policy.\nFail if:\n• Many or all users have legacy non-SCRAM hashes, contrary to cluster policy where SCRAM is required.\n• Users that should rely on certificate-based or external authentication still have local password hashes, against policy.\n2. Verify cluster settings enforce SCRAM-SHA-256 for new/updated passwords Check the password encryption mode:\nSHOW CLUSTER SETTING server.user_login.password_encryption;\nExpected Results:\n• scram-sha-256 for hardened self-hosted deployments.\nOptionally, confirm automatic migration of bcrypt passwords to SCRAM (if enabled by policy):\nSHOW CLUSTER SETTING\nserver.user_login.upgrade_bcrypt_stored_passwords_to_scram.enabled;\nExpected Results (if migration is desired):\n• true in v22.2+ to automatically upgrade legacy bcrypt hashes on first login.\nFail if:\n• server.user_login.password_encryption is not set to scram-sha-256 where policy requires SCRAM.\n• Automatic upgrade settings conflict with the organization's migration strategy.\n3. Review HBA configuration for password-based methods Display HBA:\nSHOW CLUSTER SETTING server.host_based_authentication.configuration;\nInspect METHOD values for rules that allow password-based authentication, such as password or scram-sha-256.\nExpected Results:\n• Password-based methods appear only where explicitly intended (for example, limited user/IP combinations).\n• No trust entries are used in hardened self-hosted production.\nFail if:\n• Broad rules (for example, host all all all password) permit password authentication from unbounded origins against policy.\n• trust or other unauthenticated methods appear in production HBA."
          }
        }
      },
      {
        "+@id": "V-92CC4944",
        "title": "4 User Access and Authorization",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-92CC4944r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "4.3",
          "title": "Secure the root User and Administrative Roles",
          "description": "<VulnDiscussion>The root user in CockroachDB has unrestricted administrative privileges across the entire cluster. Because compromise of root grants full control, its use must be tightly restricted and secured. The root account should authenticate exclusively with TLS client certificates (mTLS), and password authentication must be disabled.\nAccess to root should be limited to hardened administrative hosts (e.g., jump boxes/bastion hosts) via host-based access rules. The root user must not be used for day-to-day operations, routine queries, application access, or automated scripts;\ninstead, operational tasks should be performed using delegated roles with least privilege.\n\nRationale:\nThe root account is a high-value target for attackers. Certificate-based authentication provides significantly stronger protection than passwords because it requires possession of a private key rather than a reusable secret. Preventing password authentication for root, restricting where root can log in from, and enforcing strict operational separation reduces the risk of cluster-wide compromise. Delegating password fallback to a separate emergency user (Control 1.3) preserves recoverability without weakening root security.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "4.3"
          },
          "fixtext": {
            "+content": "1. Enforce certificate-only authentication for root Update the HBA configuration to allow only certificate-based methods:\nSET CLUSTER SETTING server.host_based_authentication.configuration = '\nTYPE DATABASE USER ADDRESS METHOD\nhost all root <jumpbox-cidr> cert\nhost all root all reject\n';\nReplace <jumpbox-cidr> with your actual admin network.\n2. Restrict root usage operationally\n• Replace script usage with least-privilege service accounts.\n• Ensure applications do not use root.\n• Use root only for tasks requiring full administrative privilege.",
            "+@fixref": "F-92CC4944r01_fix"
          },
          "fix": {
            "+@id": "F-92CC4944r01_fix"
          },
          "check": {
            "+@system": "C-92CC4944_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Verify root authentication requires TLS client certificates (mTLS):\nCheck HBA configuration:\nSHOW CLUSTER SETTING server.host_based_authentication.configuration;\nExpected Results:\n• Entries for root use methods such as cert or cert-*.\n• No password-based or unauthenticated method ( password, scram-sha-256, trust) appears for root.\n• root access restricted to specific jump-box IP addresses or CIDR blocks.\n• No wildcard or universal access (e.g., all, 0.0.0.0/0,::/0).\nFail if:\n• Any rule allows root to authenticate using a password.\n• Any rule allows root without certificate authentication.\n• root can connect from any IP or a broad range outside approved admin hosts.\n2. Verify root is not used for routine or application operations If audit logs are enabled:\ncockroach sql --execute=\"\nSELECT timestamp, eventType, reportingID\nFROM system.eventlog\nWHERE info LIKE '%root%';\n\"\nFail if:\n• root is used frequently by applications or scripts.\n• Routine operations are performed using root instead of delegated roles."
          }
        }
      },
      {
        "+@id": "V-18B4D97D",
        "title": "4 User Access and Authorization",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-18B4D97Dr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "4.4",
          "title": "Centralize and Standardize User Management",
          "description": "<VulnDiscussion>Self-hosted CockroachDB clusters should maintain consistent and centralized management of database user identities to ensure secure, controlled, and predictable access. CockroachDB supports several external authentication systems — such as OIDC, GSSAPI/Kerberos, and LDAP — but organizations may also centralize identity management by standardizing how CockroachDB accounts are created, assigned roles, and deprovisioned.\nCentralizing user lifecycle processes reduces the risk of unmanaged or orphaned accounts and ensures that authentication and authorization policies are applied consistently across environments. When possible, administrators should prefer certificate-based authentication or integrate CockroachDB with an external identity provider to unify identity governance.\n\nRationale:\nDecentralized or ad-hoc management of CockroachDB user accounts increases the likelihood of misaligned privileges, stale accounts, and unauthorized access. A centralized user management process — whether implemented operationally or via integration with an external directory — improves governance, auditability, and consistency. Even without full IdP integration, standardizing user provisioning, modification, and deprovisioning across self-hosted clusters strengthens security posture.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "4.4"
          },
          "fixtext": {
            "+content": "1. Establish a centralized user management process Document and enforce:\n• Account creation approval\n• Standardized role/privilege assignment\n• Required justification for role elevation\n• Timely account deprovisioning 2. Remove orphaned or unauthorized accounts\nDROP USER <username>;\nOnly after confirming the user is no longer required and privileges are reassigned as needed.\nEncourage certificate-based authentication for administrators and service accounts.\n3. (Optional) Integrate an external identity provider If suitable for the environment, configure CockroachDB with:\n• OIDC\n• GSSAPI/Kerberos\n• LDAP To unify database identities with organizational IAM.",
            "+@fixref": "F-18B4D97Dr01_fix"
          },
          "fix": {
            "+@id": "F-18B4D97Dr01_fix"
          },
          "check": {
            "+@system": "C-18B4D97D_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Review user lifecycle documentation or practices:\nVerify administrators follow a defined user management workflow including:\n• Approval required for account creation\n• Standard role assignment rules\n• Prompt deprovisioning of users who no longer require access Fail if:\n• No documented or enforced process exists\n• Users remain active after leaving the organization or after their access is no longer needed 2. Review CockroachDB users for orphaned or unused accounts:\nRun:\nSHOW USERS;\nExpected Results:\n• Only current, authorized users exist\n• No stale, unused, or orphaned accounts appear Fail if:\n• Accounts exist that do not correspond to active personnel or services 3. Verify consistent authentication methods Check whether users have local password hashes (meaning they rely on password auth) or no hash (meaning certificate/IdP authentication):\nSELECT username,\n(hashedPassword != '\\x') AS has_password\nFROM system.users;\nInterpretation:\n• has_password = false → user authenticates via certificates or external identity\n• has_password = true → user authenticates using a password Fail if:\n• Authentication methods vary without policy\n• Users authenticate in ways that contradict organizational standards\n• Users expected to use certificates still have password hashes 4. If external identity integration is used, verify configuration:\n(Required only if the organization has deployed external auth.) Confirm that:\n• OIDC configuration is valid\n• Kerberos/GSSAPI or LDAP settings are correct\n• Authentication flows align with organizational identity policies"
          }
        }
      },
      {
        "+@id": "V-1C11CAE6",
        "title": "5 Data Protection",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-1C11CAE6r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "5.1",
          "title": "Ensure Backups Are Encrypted Using an External Method",
          "description": "<VulnDiscussion>In self-hosted CockroachDB deployments, the Encryption-at-Rest (EAR) feature encrypts only the node's local on-disk data, including SSTables, write-ahead logs (WAL), and temporary files. EAR does not encrypt backup files created using the BACKUP statement.\nBy default, backups generated by CockroachDB are written as plaintext objects to the configured backup destination unless an explicit backup encryption mechanism is enabled. As a result, backup data may be exposed if the storage location is accessed by unauthorized users.\nTo protect backup data, organizations must apply separate encryption for all backup files using one or more of the following CockroachDB-supported methods:\n• Cloud Key Management Service (KMS) - based encryption (recommended), using AWS KMS, GCP Cloud KMS, or Azure Key Vault to encrypt a randomly generated data-encryption key for each backup.\n• AES-256 passphrase-based encryption, which encrypts backup manifests and data files using a key derived from a strong passphrase.\n• Storage-provider encryption, where the destination storage system enforces encryption at rest (for example, Amazon S3 server-side encryption, Google Cloud Storage default encryption or CMEK, or Azure Storage Service Encryption).\n\nRationale:\nBackup files typically contain a complete copy of the database, including sensitive or regulated information. Because CockroachDB's EAR feature does not protect backup objects, leaving backups unencrypted exposes organizations to risks such as:\n• Compromise of cloud storage buckets or local filesystems\n• Misconfigured access control lists (ACLs) or IAM policies\n• Exposure through snapshots or removable media\n• Insider access or credential theft Using explicit backup encryption — either at the SQL level (passphrase or KMS) or enforced at the storage layer — ensures that backup data remains confidential even if the backup storage location is compromised. This provides cryptographic separation between live database storage and backup data and aligns with regulatory requirements for encryption of data at rest.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "5.1"
          },
          "fixtext": {
            "+content": "Option 1 - Enable AES-256 Passphrase-Based Backup Encryption Configure backups to use passphrase-based encryption:\nBACKUP DATABASE <db>\nINTO 's3://bucket/path?AWS_ACCESS_KEY_ID=...&AWS_SECRET_ACCESS_KEY=...'\nWITH encryption_passphrase = '<strong-passphrase>';\nUse an external secret manager (e.g., Vault, AWS Secrets Manager) to store the passphrase (not documented in CockroachDB, but strongly recommended operationally).\nOption 2 — Use Cloud KMS - managed encryption (recommended) Use the kms option so that CockroachDB encrypts each backup with a random 256-bit key, which is then encrypted by the cloud KMS.\nExample — AWS KMS:\nBACKUP INTO 's3://{BUCKET NAME}?AWS_ACCESS_KEY_ID={KEY\nID}&AWS_SECRET_ACCESS_KEY={SECRET ACCESS KEY}'\nWITH kms = 'aws:///{key}?AWS_ACCESS_KEY_ID={KEY\nID}&AWS_SECRET_ACCESS_KEY={SECRET ACCESS KEY}&REGION=us-east-1';\nExample — GCP KMS:\nBACKUP INTO 'gs://{BUCKET NAME}?AUTH=specified&CREDENTIALS={ENCODED KEY}'\nWITH kms = 'gs:///projects/{project name}/locations/us-\neast1/keyRings/{key ring name}/cryptoKeys/{key\nname}?AUTH=specified&CREDENTIALS={encoded key}';\nExample — AZURE KMS:\nBACKUP INTO 'azure-blob://{container\nname}?AUTH=specified&AZURE_ACCOUNT_NAME={account\nname}&AZURE_CLIENT_ID={client ID}&AZURE_CLIENT_SECRET={client\nsecret}&AZURE_TENANT_ID={tenant ID}'\nWITH kms = 'azure-kms:///{key}/{key version}?AZURE_TENANT_ID={tenant\nID}&AZURE_CLIENT_ID={client ID}&AZURE_CLIENT_SECRET={client\nsecret}&AZURE_VAULT_NAME={key vault name}';\nThe same kms URI(s) must be supplied for RESTORE and SHOW BACKUP to decrypt the backup.\nOption 3 — Enforce encryption at the storage layer If you do not use SQL-level backup encryption:\n• Configure S3, GCS, or Azure Blob Storage to require server-side encryption for all objects.\n• Ensure bucket/container policies block uploads without encryption.\nCockroachDB will then write plaintext backup objects that are encrypted at rest by the storage provider.",
            "+@fixref": "F-1C11CAE6r01_fix"
          },
          "fix": {
            "+@id": "F-1C11CAE6r01_fix"
          },
          "check": {
            "+@system": "C-1C11CAE6_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Identify recent CockroachDB backups\n• Artifact-level view :\nSHOW BACKUPS IN 's3://bucket/path?AUTH=...';\nSHOW BACKUP FROM '<subdir>'\nIN 's3://bucket/path?AUTH=...';\n• Job-level view :\nSELECT *\nFROM system.jobs\nWHERE job_type = 'BACKUP'\nORDER BY created DESC;\nWarning (unsafe internals, audit-only):\nThis step uses the system.jobs table behind the allow_unsafe_internals gate and is not a supported production API for general use . Treat it strictly as a break-glass operation during periodic audits, run only in a dedicated admin session by authorized operators. Do not expose this query or allow_unsafe_internals = true to application workloads or end users, and end the session (or reset allow_unsafe_internals to off) immediately after the check .\nSET allow_unsafe_internals = false;\n• If you need history beyond the jobs retention window, there is no long-term source in SQL; you need exported logs / observability (e.g., recovery_event in eventlog, log shipping to SIEM) to prove older backups were taken.\n2. Inspect backup job options for encryption For each backup job, inspect its details (for example, using SHOW JOB job_id or equivalent) and verify whether one of the following options is present:\n• encryption_passphrase\n• kms = ...\nIf neither option appears and storage-layer encryption is not being used (see Step 3), record a finding.\n3. Validate destination storage encryption Using cloud or storage tooling outside of CockroachDB, verify encryption at rest is enforced for the backup destination:\n• AWS S3: Confirm server-side encryption (SSE-S3 or SSE-KMS) is enabled.\n• Google Cloud Storage: Confirm default encryption or customer-managed encryption keys (CMEK) are configured.\n• Azure Blob Storage: Confirm Storage Service Encryption is enabled.\nIf backups are written to a local filesystem, verify that filesystem-level or external encryption is enabled using operating system or platform tooling.\nIf encryption is not enforced at the storage layer, record a finding.\n4. Verify secure handling of passphrases and keys Confirm that backup encryption_passphrase or KMS credentials are not stored in plaintext within:\n• Shell scripts\n• Systemd unit files\n• Application source code\n• CI/CD pipelines\n• Logs or configuration files Secrets should be stored in an approved secret-management solution."
          }
        }
      },
      {
        "+@id": "V-102A9449",
        "title": "5 Data Protection",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-102A9449r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "5.2",
          "title": "Periodically Test Backup Recovery Procedures",
          "description": "<VulnDiscussion>Self-hosted CockroachDB deployments must regularly test backup recovery procedures to ensure that backups can be successfully restored, data integrity is preserved, and security controls remain effective during restoration.\nCockroachDB provides multiple backup and restore features, including full and incremental backups, encrypted backups, and backup validation tools. These features allow you to verify that stored backups are restorable without always performing a full restore.\nWhile creating backups (including encrypted backups) is critical, backups that are never tested may be unusable due to corruption, misconfiguration, missing encryption keys, expired credentials, or incorrect recovery procedures. Periodic restore testing validates that:\n• Backup files are complete and readable (for example, via SHOW BACKUP ...\nWITH check_files or RESTORE ... WITH schema_only, verify_backup_table_data).\n• Encryption mechanisms (passphrase or KMS) function correctly during restore when using encrypted backups.\n• Required credentials and permissions to access external/cloud storage are available and functional.\n• Restore procedures are documented, repeatable, and effective.\nRestore testing must be performed in a non-production environment (for example, an empty or dedicated test cluster) to avoid impacting live workloads and to prevent accidental data exposure. schema_only restores and validation restores can be run on such test clusters and then dropped after validation.\n\nRationale:\nBackups that cannot be restored are operationally equivalent to having no backups at all. CockroachDB provides tools to take and restore full and incremental backups, including encrypted backups, and to validate that backups are restorable.\nCommon failure scenarios include:\n• Lost or inaccessible encryption passphrases or KMS keys required to restore encrypted backups.\n• Undocumented or untested recovery steps.\n• Corrupted or incomplete backup artifacts (for example, missing SST files in cloud storage).\nRegular restore testing using CockroachDB's validation and restore mechanisms ensures that:\n• Recovery time objectives (RTOs) defined in the disaster recovery strategy are achievable in practice.\n• Data integrity is preserved end-to-end by confirming that schemas and data can be restored and, where applicable, that table data files are present and uncorrupted.\n• Encryption and access controls remain enforced during recovery when using encrypted backups.\n• Organizations are prepared for real incident response scenarios, including ransomware or other disaster events.\nTesting recovery procedures is a fundamental resilience practice and is aligned with CockroachDB's disaster recovery and data resilience guidance.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "5.2"
          },
          "fixtext": {
            "+content": "1. Define a restore testing schedule Establish a policy requiring restore or backup validation testing at a regular cadence and after key changes, such as:\n• Changes to backup configuration or destinations.\n• Changes to backup encryption (for example, enabling encrypted backups or changing KMS configuration).\n• KMS key rotation or changes to encryption keys.\n• IAM or credential changes affecting access to backup storage or KMS.\n• Major CockroachDB version upgrades that may affect backup/restore compatibility.\n2. Perform restore tests in a non-production environment Restore or validate backups into a dedicated test cluster or isolated environment using documented procedures:\n• Primary DR validation (recommended):\nRun a normal RESTORE (cluster, database, or table, as appropriate) into a real CockroachDB cluster (non-production) and validate that:\no The cluster can be provisioned or scaled for recovery.\no Restored objects are present and accessible.\no Representative application queries run successfully against the restored data.\n• Supplementary / faster validation between full DR tests (optional):\no For schema validation and credential checks, use:\nRESTORE ... WITH schema_only; on an empty or test cluster to validate backup existence, storage/KMS credentials, and schema/version compatibility.\no Where required for additional assurance or compliance, use:\nRESTORE ... WITH schema_only, verify_backup_table_data; to read and checksum table data from backup storage without writing it to disk. This is a validation/debugging tool and does not replace periodic full RESTORE tests into a real cluster.\n• For encrypted backups, ensure restore commands include the correct KMS or passphrase configuration.\nAfter validation, drop the restored database or destroy the test cluster to return the environment to its pre-test state.\n3. Validate data integrity After restoration or validation:\n• Verify that database objects (databases, tables, schemas) are present using standard SQL inspection (for example, SHOW TABLES).\n• Run basic queries to confirm data accessibility when performing a full restore test.\n• Confirm schema and metadata integrity (for example, that table definitions match expectations).\n• Where applicable, rely on verify_backup_table_data to ensure that table data files are present and uncorrupted.\n4. Document and remediate failures If restore testing or backup validation reveals failures:\n• Update documentation and automation (runbooks, scripts, or scheduled jobs) to reflect correct restore and validation procedures.\n• Correct permission, connectivity, or encryption issues (for example, missing KMS permissions or incorrect storage URIs).\n• Re-run restore or validation tests until successful.\n• Maintain records of restore tests and outcomes, including timestamps, scope (cluster/database/table), and any issues discovered and resolved.",
            "+@fixref": "F-102A9449r01_fix"
          },
          "fix": {
            "+@id": "F-102A9449r01_fix"
          },
          "check": {
            "+@system": "C-102A9449_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Verify existence of documented restore procedures Confirm that the organization maintains documented procedures covering:\n• Where CockroachDB backups are stored (for example, S3, GCS, Azure Blob, or other supported cloud storage).\n• How to restore backups, including:\no Use of RESTORE for full-cluster, database, or table restores into a non-production CockroachDB cluster (for example, a dedicated DR/test cluster).\no Steps to provision or scale a recovery cluster (infrastructure, networking, access control) and restore into it, so that DR procedures exercise real-world operational workflows (not just validation options).\n• Use of backup validation options (for testing/debug only), including:\no Use of RESTORE ... WITH schema_only as a fast validation / debugging tool to confirm:\n▪ Storage credentials and IAM are correct.\n▪ Backups exist and are discoverable.\n▪ Basic schema and version compatibility.\no Optional use of RESTORE ... WITH schema_only, verify_backup_table_data only where required for additional assurance or compliance (for example, to checksum table data files in backup storage). Note that this option:\n▪ Must always be used together with schema_only.\n▪ Is primarily a validation aid, and is not a substitute for a full restore into a real CockroachDB cluster.\n• How to restore encrypted backups, including:\no Required kms configuration (AWS KMS, GCP KMS, Azure Key Vault) or\no Required encryption_passphrase usage.\n• Required credentials and permissions to access backup storage and KMS.\n• Validation steps after restore (for example, checking schemas, running queries, or using backup validation options) If no documented restore procedure exists -> Finding 2. Verify restore testing frequency Review operational records, tickets, or logs to confirm that restore tests or backup validation operations are performed on a defined schedule (for example, quarterly, bi- annually, or after major changes).\nEvidence may include:\n• Change records or DR test reports referencing RESTORE or SHOW BACKUP ...\nWITH check_files.\n• Runbooks or automation with execution timestamps for validation restores (for example, schema_only or verify_backup_table_data restores).\n• CI/CD or automation logs showing periodic backup validation or test restores.\n• Monitoring or observability records for backup and restore jobs.\nIf restore testing is ad- hoc or never performed → Finding.\n3. Validate successful restore execution Confirm that at least one recent restore or validation test was successfully completed in a non-production environment, including:\n• Successful access to backup storage and reading of backup files (for example, via SHOW BACKUP ... WITH check_files or a validation restore).\n• Successful cluster, database, or table restore or validation restore, such as:\no RESTORE ... WITH schema_only to validate schema.\no RESTORE ... WITH schema_only, verify_backup_table_data to validate that table data files are present and uncorrupted.\n• Verification that restored objects (schemas and, where applicable, data) are accessible in the test environment.\nFor encrypted backups, confirm that restore tests used the correct encryption configuration:\n• WITH kms = 'kms://...' (or equivalent) for KMS-encrypted backups, or\n• WITH encryption_passphrase = '...' for passphrase-encrypted backups.\nFailure to restore or validate encrypted backups → Finding.\n4. Validate post-restore security controls After restore testing, verify that:\n• Restored data remains protected according to the organization's encryption-at- rest configuration (for example, if encryption at rest is enabled on the test cluster, it remains enabled after restore).\n• Access controls (SQL users/roles and network controls) are correctly applied in the test environment and do not expose restored data beyond intended scope.\n• Temporary credentials or secrets used during testing (for example, temporary cloud storage keys or KMS credentials) are revoked or rotated according to organizational policy.\nIf security controls are bypassed or weakened during restore → Finding."
          }
        }
      },
      {
        "+@id": "V-15AAE31E",
        "title": "5 Data Protection",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-15AAE31Er01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "5.3",
          "title": "Control Key Data Usage for Multi-Region Clusters with Data Localization Restrictions",
          "description": "<VulnDiscussion>In self-hosted CockroachDB multi-region clusters, organizations must ensure that Personally Identifiable Information (PII) or other regulated data is not used as part of primary key constraints or secondary indexes.\nIn CockroachDB, primary keys define the primary index, and primary key columns are implicitly included in all secondary indexes. When a column is included in a primary key or index, its values are:\n1. Stored in index keyspace entries as part of the primary or secondary index.\n2. Implicitly included in secondary indexes when part of the primary key.\n3. Distributed and replicated across the cluster for query routing and fault tolerance, including across regions in multi-region deployments.\nAs a result, placing PII fields (such as names, email addresses, phone numbers, national identifiers, or other sensitive attributes) into primary keys or secondary indexes can cause unintentional cross-region data replication, potentially conflicting with data localization, residency, or sovereignty requirements.\nCockroachDB's data domiciling and multi-region features (for example, REGIONAL BY ROW, super regions, and placement controls) are designed to help keep data within specific geographic boundaries, but indexed columns are still part of the distributed key- value and index structures.\nTo comply with data localization restrictions, organizations should ensure that PII fields are stored only as non-indexed columns where possible, and that primary keys and indexes use non-sensitive surrogate identifiers. CockroachDB documentation recommends using UUIDs or composite keys with well-distributed, non-sensitive columns as primary keys, and warns about the implications of primary key design on data distribution.\n\nRationale:\nCockroachDB's distributed architecture prioritizes availability and low-latency access by automatically sharding and replicating data, including index structures, across nodes and regions. While this behavior is desirable for performance and resilience, it has implications for sensitive data:\n• Primary key values are embedded in the primary index and implicitly included in all secondary indexes.\n• Secondary indexes store indexed column values and primary key columns, which are distributed across the cluster.\n• In multi-region deployments, index data participates in the same replication and placement mechanisms as other data, and may be present in multiple regions for availability and latency.\nIf PII is used in keys or indexes, it may:\n• Be stored or replicated in regions where it is not intended to reside, complicating compliance with GDPR, CCPA, or other data residency regulations.\n• Be exposed to administrators or systems in other jurisdictions that have access to those regions.\n• Make it harder to enforce strict data localization using features like super regions or placement restrictions, because indexed values are part of the distributed keyspace.\nBy using surrogate keys (for example, UUIDs) and restricting PII to non-indexed columns, organizations can:\n1. Follow CockroachDB's primary key best practices (for example, using UUIDs for single-column primary keys).\n2. Maintain CockroachDB's performance and availability benefits.\n3. Better align with data domiciling and data sovereignty requirements by limiting where sensitive values appear in the distributed keyspace.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "5.3"
          },
          "fixtext": {
            "+content": "1. Replace PII-based keys with surrogate keys Refactor schemas so that primary keys use surrogate identifiers instead of PII.\nCockroachDB recommends:\n• Using UUID-typed columns with default values generated by gen_random_uuid() for single-column primary keys.\n• Using composite primary keys where the leading column is a well-distributed, non-sensitive identifier.\nEnsure PII columns are stored as regular, non-indexed fields where possible.\nSchema changes may require:\n• Creating a new surrogate key column.\n• Updating application code to reference the surrogate key.\n• Using ALTER TABLE ... ALTER PRIMARY KEY to change the primary key definition (not explicitly covered in the provided sources, but implied by CockroachD's support for primary key changes).\n2. Remove or redesign PII-based secondary indexes Where indexing on PII is not strictly required:\n• Drop secondary indexes containing PII columns using DROP INDEX.\n• Replace them with indexes on non-sensitive identifiers (for example, surrogate keys) that can still support required query patterns.\nIf indexing on PII is required for business reasons:\n• Document the justification and obtain compliance or legal approval.\n• Where possible, restrict table locality and data placement using multi-region features (for example, REGIONAL BY ROW plus super regions or placement controls) so that PII-bearing indexes are domiciled within the required geographic boundary.\n3. Validate data locality after changes After schema changes:\n• Verify table locality settings using SHOW CREATE TABLE and database region configuration ( ALTER DATABASE ... PRIMARY REGION, ADD REGION, ADD SUPER REGION).\n• Confirm that PII columns are not part of primary keys or secondary indexes by re-running SHOW CREATE TABLE and SHOW INDEXES FROM <table_name>.\n• Re-test application queries to ensure functionality and performance are preserved, adjusting query patterns to use surrogate keys where necessary.\n• For data domiciling scenarios, verify that regional placement (for example, REGIONAL BY ROW with crdb_region, super regions) still enforces the intended residency boundaries.",
            "+@fixref": "F-15AAE31Er01_fix"
          },
          "fix": {
            "+@id": "F-15AAE31Er01_fix"
          },
          "check": {
            "+@system": "C-15AAE31E_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Identify multi-region configuration Confirm that the CockroachDB cluster is configured for multi-region deployment.\nEvidence may include:\n• Databases configured with PRIMARY REGION and additional REGION entries.\n• Use of table locality settings such as LOCALITY REGIONAL BY TABLE, LOCALITY REGIONAL BY ROW, or LOCALITY GLOBAL.\n• Use of super regions or placement controls for data domiciling.\nIf the cluster is single-region only and not using multi-region abstractions, this control may be marked Not Applicable.\n2. Identify tables containing PII Review database schemas, data classification inventories, or application documentation to identify tables containing PII or regulated data. CockroachDB does not automatically classify PII, so this step relies on organizational data classification.\nCommon examples include columns such as:\n• Name, email address, phone number.\n• National ID numbers.\n• Customer identifiers tied to real individuals.\n• Account numbers associated with personal data.\nIf no inventory exists, auditors may need to sample schemas and consult application owners.\n3. Review primary key definitions Inspect table definitions to identify whether PII fields are included in primary keys.\nUse:\nSHOW CREATE TABLE <table_name>;\nReview the PRIMARY KEY clause in the CREATE TABLE statement.\nIf any PII column appears in the PRIMARY KEY definition -> Finding.\n4. Review secondary indexes List indexes for tables containing PII:\nSHOW INDEXES FROM <table_name>;\nThe SHOW INDEX output lists each index and its columns, including implicitly included primary key columns.\nFinding if:\n• Any PII column is included as an indexed column in a secondary index (that is, appears with s toring = false and implicit = false for that index).\n• PII appears as part of the primary key and therefore is implicitly included in all secondary indexes ( implicit = true for that column in secondary indexes).\n5. Validate use of surrogate identifiers Confirm that:\n• Primary keys use non-sensitive surrogate identifiers (for example, UUIDs generated with gen_random_uuid() or other non-PII identifiers), consistent with CockroachDB primary key best practices.\n• PII columns are stored as non-indexed attributes unless explicitly required for lawful and documented reasons (for example, regulatory or business requirements that have been reviewed and approved).\n• Where PII must be indexed, table locality and data domiciling configurations (for example, REGIONAL BY ROW plus super regions) are used to constrain where that data is stored and replicated"
          }
        }
      },
      {
        "+@id": "V-789AFD9B",
        "title": "6 CockroachDB Settings",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-789AFD9Br01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "6.1",
          "title": "Ensure Certificate File Permissions and Validity",
          "description": "<VulnDiscussion>While Control 1.3 ensures the cluster operates with TLS enabled, this control focuses on the security and maintenance of the certificate files residing on the node's file system. Critical security artifacts, specifically private keys ( node.key, client.root.key), must be protected with strict OS-level file permissions to prevent unauthorized local users from reading the keys. Additionally, certificates must be actively monitored for validity periods to prevent outages caused by expiration.\n\nRationale:\nIf a private key file on the disk is improperly secured and world-readable (e.g., chmod 644 or 777), a local attacker on the host system can copy the key, leading to a direct compromise of the private key. This compromises the confidentiality of the system, potentially allowing the attacker to decrypt captured network traffic. Enforcing strict OS- level file permissions (0600) ensures that only the dedicated database user can read the keys, significantly reducing the likelihood of a private key compromise. Furthermore, failing to monitor certificate expiration leads to sudden cluster unavailability when the certificates become invalid and rotation is neglected.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "6.1"
          },
          "fixtext": {
            "+content": "1. Restrict Private Key Permissions:\nIf keys are found to be too open, lock them down immediately.\nchmod 600 /var/lib/cockroach/certs/*.key\n2. Correct Ownership:\nEnsure the directory and files are owned by the specific CockroachDB user.\nchown -R cockroach:cockroach /var/lib/cockroach/certs\n3. Renew Expiring Certificates:\nIf openssl indicates the certificate is expired or near expiration:\n• Generate new certificates on your secure CA workstation (as defined in Control 1.3 Remediation).\n• Upload them to /var/lib/cockroach/certs on the node.\n• Reload the certificates by sending a SIGHUP to the process:\npkill -SIGHUP cockroach",
            "+@fixref": "F-789AFD9Br01_fix"
          },
          "fix": {
            "+@id": "F-789AFD9Br01_fix"
          },
          "check": {
            "+@system": "C-789AFD9B_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Verify Private Key Permissions:\nRun the following command to check permissions for all .key files in the certificate directory.:\nls -l /var/lib/cockroach/certs/*.key\nExpected Results:\n• Permissions must be -rw------- (600) or -r-------- (400).\n• Owner must be the database user (e.g., cockroach).\nFindings: Mark as failed if permissions allow Group or Others to read/write (e.g., -rw- r--r--).\n2. Verify Certificate Permissions:\nRun the following command to check permissions for.crt files:\nls -l /var/lib/cockroach/certs/*.crt\nExpected Results:\n• Permissions should be -rw-r--r-- (644).\n• Owner must be the database user (e.g., cockroach).\n3. Verify Certificate Expiration:\nCheck the expiration date of the current node certificate.\nopenssl x509 -in /var/lib/cockroach/certs/node.crt -noout -enddate\nExpected Results:\n• The output date must be in the future."
          }
        }
      },
      {
        "+@id": "V-FAC2F08B",
        "title": "6 CockroachDB Settings",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-FAC2F08Br01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "6.2",
          "title": "Ensure local connections via UNIX domain sockets are configured correctly",
          "description": "<VulnDiscussion>UNIX domain sockets provide secure local inter-process communication for database connections on the same host. When combined with certificate-based authentication (see section 6.0), UNIX domain sockets offer the most secure mechanism for local database access. For matching UNIX usernames, certificate-less authentication via UNIX domain sockets can provide secure local access without requiring certificate management, though certificate-based authentication is still recommended for maximum security.\nUNIX domain sockets eliminate network-based attack vectors and provide file system- level access controls. The socket file location, permissions, and authentication methods should be properly configured to prevent unauthorized local access.\n\nRationale:\nUNIX domain sockets provide several security advantages for local connections:\n• No network exposure or attack surface\n• File system-level access controls\n• Better performance than TCP/IP for local connections\n• Integration with UNIX user authentication\n• Reduced complexity for local administrative tasks</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "6.2"
          },
          "fixtext": {
            "+content": "Prerequisites: Complete certificate infrastructure setup as described in section 6.0.\n1. Configure UNIX Domain Socket Location\n# Start CockroachDB with custom socket directory\ncockroach start \\\n--certs-dir=certs \\\n--store=node1 \\\n--listen-addr=localhost:26257 \\\n--socket-dir=/var/run/cockroach \\\n--http-addr=localhost:8080\n# Ensure socket directory has appropriate permissions\nmkdir -p /var/run/cockroach\nchown cockroach:cockroach /var/run/cockroach\nchmod 755 /var/run/cockroach\n2. Create Client Certificates for Local Users\n# Create database user\ncockroach sql --certs-dir=certs --host=/tmp --execute=\"CREATE USER\nlocaluser;\"\n# Create client certificate for local user\ncockroach cert create-client localuser --certs-dir=certs --ca-key=ca-\nkey/ca.key\n# Create user-specific certificate directory\nmkdir -p /home/localuser/.cockroach-certs\ncp certs/ca.crt /home/localuser/.cockroach-certs/\ncp certs/client.localuser.crt /home/localuser/.cockroach-certs/\ncp certs/client.localuser.key /home/localuser/.cockroach-certs/\n# Set appropriate permissions\nchown -R localuser:localuser /home/localuser/.cockroach-certs\nchmod 700 /home/localuser/.cockroach-certs\nchmod 600 /home/localuser/.cockroach-certs/client.localuser.key\nchmod 644 /home/localuser/.cockroach-certs/*.crt\n3. Test Local Authentication\n# Test as root user\ncockroach sql --certs-dir=certs --host=/tmp --user=root\n# Test as regular user\nsu - localuser\ncockroach sql --certs-dir=/home/localuser/.cockroach-certs --host=/tmp --\nuser=localuser\n4. Security Hardening\n# Remove world permissions from socket if needed (depends on use case)\n# Note: This may prevent legitimate access by other users\nchmod 660 /tmp/.s.PGSQL.26257\n# For production, consider dedicated socket directory\nmkdir -p /var/lib/cockroach/sockets\nchown cockroach:cockroach /var/lib/cockroach/sockets\nchmod 750 /var/lib/cockroach/sockets",
            "+@fixref": "F-FAC2F08Br01_fix"
          },
          "fix": {
            "+@id": "F-FAC2F08Br01_fix"
          },
          "check": {
            "+@system": "C-FAC2F08B_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Prerequisites: Ensure certificate infrastructure is properly configured (see section 6.0).\nVerify that UNIX domain socket connections work with certificate authentication:\n# Test local connection via UNIX domain socket with certificates\ncockroach sql --certs-dir=certs --host=/tmp --user=root\nroot@localhost:26257/defaultdb>\nTest UNIX domain socket path configuration:\n# Check default socket location\nls -la /tmp/.s.PGSQL.26257\nsrwxrwxrwx 1 cockroach cockroach 0 [date] /tmp/.s.PGSQL.26257\n# Test connection with explicit socket directory\ncockroach sql --certs-dir=certs --host=/var/run/cockroach --user=root\nroot@localhost:26257/defaultdb>\nVerify that insecure connections are rejected:\n# Attempt insecure connection via UNIX socket\ncockroach sql --insecure --host=/tmp --user=root\nERROR: server is not running in insecure mode, must use certificates\nVerify socket file permissions and ownership:\n# Check socket file permissions\nls -la /tmp/.s.PGSQL.*\nsrwxrwxrwx 1 cockroach cockroach 0 [date] /tmp/.s.PGSQL.26257\n# Check socket directory permissions\nls -ld /tmp\ndrwxrwxrwt 10 root root 4096 [date] /tmp\nTest user-specific certificate authentication:\n# Test with user-specific certificates\nsu - user1\ncockroach sql --certs-dir=/home/user1/certs --host=/tmp --user=user1\nuser1@localhost:26257/defaultdb>"
          }
        }
      },
      {
        "+@id": "V-FCB459A3",
        "title": "6 CockroachDB Settings",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-FCB459A3r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "6.3",
          "title": "Ensure remote connections via TCP/IP sockets are configured correctly",
          "description": "<VulnDiscussion>TCP/IP connections enable remote access to CockroachDB clusters across networks.\nCockroachDB supports multiple authentication methods for remote connections, with certificate-based authentication being the most secure option. All TCP/IP connections must use TLS encryption to protect data in transit.\nRemote connection security depends on proper network configuration, authentication method selection, and TLS settings. Certificate-based authentication is strongly recommended for production environments as it provides mutual TLS authentication and eliminates password-based attack vectors.\nSupported authentication methods for remote connections include:\n• Certificate-based authentication: Uses client certificates signed by the cluster's CA (recommended)\n• Password authentication: Username and password over TLS\n• GSSAPI/Kerberos: For enterprise environments with existing Kerberos infrastructure\n• OIDC/JWT: For integration with identity providers\n\nRationale:\nSecure remote connections are essential for:\n• Distributed cluster administration\n• Application connectivity from remote hosts\n• Multi-tier architecture security\n• Compliance with data protection requirements\n• Prevention of network-based attacks</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "6.3"
          },
          "fixtext": {
            "+content": "Prerequisites: Complete certificate infrastructure setup as described in section 6.0.\n1. Configure Server for Remote Access\n# Start CockroachDB listening on appropriate network interfaces\ncockroach start \\\n--certs-dir=certs \\\n--store=node1 \\\n--listen-addr=0.0.0.0:26257 \\\n--advertise-addr=database-server:26257 \\\n--http-addr=0.0.0.0:8080 \\\n--join=database-server:26257\n# For multi-node clusters, configure each node\ncockroach start \\\n--certs-dir=certs \\\n--store=node2 \\\n--listen-addr=0.0.0.0:26257 \\\n--advertise-addr=db2.example.com:26257 \\\n--http-addr=0.0.0.0:8080 \\\n--join=db1.example.com:26257,db2.example.com:26257,db3.example.com:26257\n2. Create Remote Users and Certificates\n-- Create users for remote access\nCREATE USER appuser;\nCREATE USER backupuser;\nCREATE USER monitoruser;\n-- For password-based authentication, set passwords\nALTER USER appuser WITH PASSWORD 'strong_app_password';\nALTER USER monitoruser WITH PASSWORD 'strong_monitor_password';\n-- Grant appropriate privileges\nGRANT ALL ON DATABASE myapp TO appuser;\nGRANT SELECT ON DATABASE myapp TO monitoruser;\nGRANT ALL ON DATABASE *.* TO backupuser;\n3. Generate and Distribute Client Certificates\n# Create client certificates for remote users\ncockroach cert create-client appuser --certs-dir=certs --ca-key=ca-key/ca.key\ncockroach cert create-client backupuser --certs-dir=certs --ca-key=ca-\nkey/ca.key\ncockroach cert create-client monitoruser --certs-dir=certs --ca-key=ca-\nkey/ca.key\n# Securely distribute certificates to remote hosts\n# Application server\nscp certs/ca.crt certs/client.appuser.* appserver:/var/lib/cockroach/certs/\nssh appserver \"chown -R app:app /var/lib/cockroach/certs && chmod 600\n/var/lib/cockroach/certs/client.appuser.key\"\n# Backup server\nscp certs/ca.crt certs/client.backupuser.* backupserver:/opt/backup/certs/\nssh backupserver \"chown -R backup:backup /opt/backup/certs && chmod 600\n/opt/backup/certs/client.backupuser.key\"\n# Monitoring server\nscp certs/ca.crt certs/client.monitoruser.*\nmonitor:/var/lib/monitoring/certs/\nssh monitor \"chown -R monitor:monitor /var/lib/monitoring/certs && chmod 600\n/var/lib/monitoring/certs/client.monitoruser.key\"\n4. Network Security Configuration\n# Configure firewall to restrict access (example with iptables)\n# Allow only specific hosts to connect to CockroachDB port\niptables -A INPUT -p tcp --dport 26257 -s 10.0.1.100 -j ACCEPT # app server\niptables -A INPUT -p tcp --dport 26257 -s 10.0.1.200 -j ACCEPT # backup\nserver\niptables -A INPUT -p tcp --dport 26257 -s 10.0.1.300 -j ACCEPT # monitor\nserver\niptables -A INPUT -p tcp --dport 26257 -j DROP # deny all others\n# For cloud environments, use security groups or network policies\n# Example AWS security group rule:\n# Port: 26257, Protocol: TCP, Source: sg-app-servers\n# Port: 26257, Protocol: TCP, Source: sg-backup-servers\n5. Test Remote Connectivity\n# Test from application server\nssh appserver\ncockroach sql --certs-dir=/var/lib/cockroach/certs --host=database-\nserver:26257 --user=appuser\n# Test from backup server\nssh backupserver\ncockroach sql --certs-dir=/opt/backup/certs --host=database-server:26257 --\nuser=backupuser\n# Test connection pooling and load balancing\ncockroach sql --certs-dir=certs --\nhost=db1.example.com:26257,db2.example.com:26257,db3.example.com:26257 --\nuser=appuser",
            "+@fixref": "F-FCB459A3r01_fix"
          },
          "fix": {
            "+@id": "F-FCB459A3r01_fix"
          },
          "check": {
            "+@system": "C-FCB459A3_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "Prerequisites: Ensure certificate infrastructure is properly configured (see section 6.0).\nTest certificate-based authentication for remote connections:\n# Test from remote host using certificates\ncockroach sql --certs-dir=certs --host=database-server:26257 --user=root\nroot@database-server:26257/defaultdb>\nVerify TLS encryption is enforced:\n# Attempt insecure connection (should fail)\ncockroach sql --insecure --host=database-server:26257 --user=root\nERROR: server is not running in insecure mode, must use certificates\n# Test secure connection\ncockroach sql --certs-dir=certs --host=database-server:26257 --user=root\nroot@database-server:26257/defaultdb>\nTest password-based authentication over TLS:\n# First, set password for user (if using password auth)\ncockroach sql --certs-dir=certs --host=database-server:26257 --execute=\"ALTER\nUSER appuser WITH PASSWORD 'secure_password';\"\n# Test password authentication from remote host\ncockroach sql --host=database-server:26257 --user=appuser\nEnter password:\nappuser@database-server:26257/defaultdb>\nVerify connection from multiple remote locations:\n# Test from application server\ncockroach sql --certs-dir=app-certs --host=db1.example.com:26257 --\nuser=appuser\n# Test from backup server\ncockroach sql --certs-dir=backup-certs --host=db1.example.com:26257 --\nuser=backupuser\n# Test from monitoring system\ncockroach sql --certs-dir=monitor-certs --host=db1.example.com:26257 --\nuser=monitoruser\nCheck TLS configuration and cipher suites:\n# Verify TLS version and cipher suites (requires openssl)\necho | openssl s_client -connect database-server:26257 -tls1_2 2>/dev/null |\ngrep -E \"(Protocol|Cipher)\""
          }
        }
      },
      {
        "+@id": "V-685B7C9D",
        "title": "6 CockroachDB Settings",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-685B7C9Dr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "6.4",
          "title": "Ensure Security Audit Logging Is Enabled",
          "description": "<VulnDiscussion>CockroachDB provides comprehensive audit logging capabilities to track security- relevant events including user authentication, privilege changes, and access to sensitive data. Role-based audit logging allows administrators to configure which users, roles, or statements should be logged, while sensitive access logging can track access to specific tables or databases containing sensitive information.\nAudit logging is essential for compliance, security monitoring, incident response, and detecting unauthorized access or privilege escalation attempts. CockroachDB supports multiple logging channels including SESSIONS, USER_ADMIN, PRIVILEGES, and SENSITIVE_ACCESS that can be configured to capture different types of security events.\nThe sql.log.user_audit cluster setting enables role-based audit logging, allowing administrators to specify which users or roles should have their SQL statements logged.\nAdditionally, table-level audit logging can be enabled using EXPERIMENTAL_AUDIT to track all access (read and write) to sensitive tables.\n\nRationale:\nSecurity audit logging provides several critical functions:\n• Accountability: Establishes a record of who performed what actions and when, supporting non-repudiation requirements\n• Compliance: Many regulatory frameworks (PCI-DSS, HIPAA, SOX, GDPR) require audit trails for sensitive data access\n• Threat Detection: Enables detection of suspicious patterns, unauthorized access attempts, or privilege escalation\n• Incident Response: Provides forensic evidence for security incidents and data breaches\n• Operational Monitoring: Helps identify misconfigurations, policy violations, or operational issues Without proper audit logging, organizations cannot effectively monitor database activity, detect security incidents, or demonstrate compliance with regulatory requirements.\nLogging should capture authentication events, privilege changes, and access to sensitive data as a minimum baseline for Level 1 security.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "6.4"
          },
          "fixtext": {
            "+content": "1. Enable role-based SQL audit logging::\nEnable role-based SQL audit logging Configure sql.log.user_audit with appropriate rules. The setting uses a table-like syntax where each row specifies a user/role and a statement filter ( ALL or NONE).\nExample - audit all statements for all users:\n-- Example - audit all statements for all users:\nSET CLUSTER SETTING sql.log.user_audit = 'ALL ALL';;\n-- Example - audit all statements for specific roles (one per line):\nSET CLUSTER SETTING sql.log.user_audit = '\nadmin ALL\nroot ALL\n';\n(Adjust syntax to match your CockroachDB version's documented examples.) 2. Configure security logging channels in an auditable sink:\nEnsure your logging configuration includes a security file group with the recommended channels and auditable: true:\nsinks:\nfile-groups:\nsecurity:\nchannels: [SESSIONS, USER_ADMIN, PRIVILEGES, SENSITIVE_ACCESS]\nauditable: true\nApply the configuration according to your deployment method (for example, via log- config at node start or the appropriate cluster setting, depending on version).\n3. Enable connection/authentication logging:\nTurn on SQL client connection logging to the SESSIONS channel:\nSET CLUSTER SETTING server.auth_log.sql_connections.enabled = true;\n4. (Optional) Optimize audit evaluation performance:\nIf you use complex sql.log.user_audit configurations, consider enabling reduced-config evaluation to compute the audit configuration once per session:\nSET CLUSTER SETTING sql.log.user_audit.reduced_config.enabled = true;",
            "+@fixref": "F-685B7C9Dr01_fix"
          },
          "fix": {
            "+@id": "F-685B7C9Dr01_fix"
          },
          "check": {
            "+@system": "C-685B7C9D_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Verify role-based audit logging is configured:\nSHOW CLUSTER SETTING sql.log.user_audit;\n• The documented syntax for sql.log.user_audit is table-like text, where each line is:\nUSER_OR_ROLE STATEMENT_FILTER\nThis setting must contain one or more audit rules in the documented table-like syntax (user/role and statement filter).\nExpected Results (conceptual):\n• sql.log.user_audit is non-empty and includes appropriate entries for privileged users/roles (for example, admin, root, or an equivalent privileged role), or a global rule (for example, ALL).\no Explicit entries for privileged users/roles (for example, admin ALL, root ALL, or other high-privilege roles).\n2. Check logging channel configuration:\nReview the logging configuration to ensure a file sink (for example, security) includes the security-relevant channels and is marked auditable. A typical configuration is:\nsinks:\nfile-groups:\nsecurity:\nchannels: [SESSIONS, USER_ADMIN, PRIVILEGES, SENSITIVE_ACCESS]\nauditable: true\nThis writes combined output to a cockroach-security.log file in the configured logging directory and enforces non-repudiable logging via auditable: true.\nExpected Results:\nA sink exists that includes SESSIONS, USER_ADMIN, PRIVILEGES, and SENSITIVE_ACCESS, and auditable: true is set for that sink.\nFail if:\n• These channels are not present in any sink, or\n• No security-relevant sink is configured as auditable.\n3. Verify security event logging is active:\nFor connection events, confirm the cluster setting is enabled:\nSHOW CLUSTER SETTING server.auth_log.sql_connections.enabled;\nExpected Results:\n• server.auth_log.sql_connections.enabled is true, and\n• The SESSIONS channel is included in an active sink.\nFail if:\n• server.auth_log.sql_connections.enabled is false or\n• SESSIONS is not routed to any sink.\n4. Test audit log generation:\nAs a privileged user covered by sql.log.user_audit, execute a simple statement (for example, a SELECT or CREATE USER) and verify that an audit event is written to the configured sink (for example, cockroach-security.log).\nExpected Results:\n• The test statement appears in the audit logs with timestamp, user, and statement text.\nFail if:\n• No corresponding audit entry is observed in the configured logs."
          }
        }
      },
      {
        "+@id": "V-411A9867",
        "title": "6 CockroachDB Settings",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-411A9867r01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "6.5",
          "title": "Ensure OCSP Certificate Revocation Checking Is Enabled",
          "description": "<VulnDiscussion>Online Certificate Status Protocol (OCSP) is a mechanism for checking whether a certificate has been revoked by its Certificate Authority (CA) before it expires.\nCockroachDB supports OCSP checking through the security.ocsp.mode cluster setting, which can be configured to verify certificate revocation status when validating client and node certificates.\nWhen enabled, CockroachDB will query the OCSP responder specified in the certificate (or a configured OCSP server) to verify that certificates have not been revoked. This provides defense-in-depth for certificate-based authentication by ensuring that compromised or revoked certificates cannot be used to authenticate to the cluster, even if they are still within their validity period.\nOCSP checking is particularly important in environments where certificates may need to be revoked before expiration due to:\n• Compromised private keys\n• Certificate misuse or unauthorized access\n• Organizational policy changes\n• Employee termination or role changes\n\nRationale:\nCertificate revocation checking addresses a critical gap in certificate-based authentication: a certificate that is valid (not expired) but has been revoked should not be accepted for authentication. Without OCSP checking, a compromised certificate could continue to be used until it naturally expires, which could be months or years.\nKey security benefits of OCSP checking include:\n• Revocation Detection: Immediately detects and rejects certificates that have been revoked by the CA\n• Compromise Response: Enables rapid response to certificate compromises by revoking affected certificates\n• Compliance: Meets requirements for certificate validation in regulated environments\n• Defense-in-Depth: Adds an additional layer of security beyond basic certificate validation The security.ocsp.mode setting supports three modes:\n• off: OCSP checking is disabled (default)\n• lax: OCSP checking is performed, but certificate validation succeeds even if OCSP check fails (e.g., OCSP server unavailable)\n• strict: OCSP checking is required, and certificate validation fails if OCSP check fails or cannot be performed For production environments, strict mode is recommended to ensure that revoked certificates are always rejected, though lax mode may be appropriate in environments where OCSP server availability cannot be guaranteed.</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "6.5"
          },
          "fixtext": {
            "+content": "1. Enable OCSP checking:\nConfigure OCSP mode based on organizational requirements:\n-- For production with reliable OCSP server: use strict mode\nSET CLUSTER SETTING security.ocsp.mode = 'strict';\n-- For environments where OCSP server availability cannot be guaranteed: use\nlax mode\nSET CLUSTER SETTING security.ocsp.mode = 'lax';\nMode Selection:\n• strict: Recommended for production. Certificates will be rejected if OCSP check fails or cannot be performed. Requires reliable OCSP server availability.\n• lax: Use when OCSP server availability cannot be guaranteed. Certificates are validated even if OCSP check fails, but revoked certificates are still rejected when OCSP check succeeds.\n2. Configure OCSP cache and timeout (if needed):\nOptimize OCSP performance and reliability:\n-- Configure OCSP query timeout (default: 3s)\nSET CLUSTER SETTING security.ocsp.timeout = '5s';\nRecommended Values:\n• Timeout: 3s to 10s (prevent blocking certificate validation) 3. Ensure certificates include OCSP responder URLs:\nWhen generating certificates, ensure they include OCSP responder information:\n## When creating certificates, include OCSP responder URL in certificate\nextensions\n# This is typically handled by the CA or certificate generation tool\ncockroach cert create-client \\\nclient_name \\\n--certs-dir=certs \\\n--ca-key=ca-key/ca.key \\\n--ocsp-url=https://ocsp.example.com\n4. Certificate Validation\n# Verify certificate chain\nopenssl verify -CAfile certs/ca.crt certs/node.crt\n# Check certificate expiration dates\nfor cert in certs/*.crt; do\necho \"Certificate: $cert\"\nopenssl x509 -in \"$cert\" -noout -dates\necho\ndone\nNote: OCSP responder URLs can be specified in certificates or configured separately depending on the certificate generation method used.\n5. Verify OCSP server availability:\nEnsure OCSP responder is accessible and functioning:\n# Test OCSP responder accessibility\ncurl -I https://ocsp.example.com\n# Test OCSP query\nopenssl ocsp -issuer certs/ca.crt -cert certs/client.crt \\\n-url https://ocsp.example.com\n6. Monitor OCSP checking:\nAfter enabling OCSP, monitor for:\n• OCSP query failures or timeouts\n• Certificate validation failures due to OCSP issues\n• Performance impact from OCSP checking\n-- Monitor certificate validation (may require log analysis)\n-- Check for OCSP-related errors in logs\n7. Document OCSP configuration:\nMaintain documentation of:\n• OCSP mode selection and rationale\n• OCSP server locations and availability requirements\n• Cache and timeout settings\n• Procedures for certificate revocation\n• Monitoring and alerting for OCSP failures",
            "+@fixref": "F-411A9867r01_fix"
          },
          "fix": {
            "+@id": "F-411A9867r01_fix"
          },
          "check": {
            "+@system": "C-411A9867_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Verify OCSP mode is configured:\nSHOW CLUSTER SETTING security.ocsp.mode;\nExpected Results:\n• Should be set to strict or lax (not off)\n• strict is recommended for production environments with reliable OCSP server availability\n• lax may be acceptable if OCSP server availability cannot be guaranteed Fail if:\n• The setting is off (disabled)\n• The setting is not configured (defaults to off) 2. Verify OCSP server configuration (if applicable):\nCheck if OCSP servers are configured for certificate validation:\n-- Check cluster settings related to OCSP\nSHOW CLUSTER SETTING security.ocsp.timeout;\nExpected Results:\n• OCSP timeout settings should be configured appropriately if OCSP is enabled\n• Timeout should be reasonable (e.g., 3s to 10s) to avoid blocking certificate validation Note: Some OCSP configuration may be specified in certificates themselves (OCSP responder URLs) rather than cluster settings.\n3. Test OCSP checking behavior:\nTo verify OCSP checking is working:\n# Check certificate for OCSP responder URL\nopenssl x509 -in certs/client.crt -noout -ocsp_uri\n# Test OCSP query (if OCSP responder is accessible)\nopenssl ocsp -issuer certs/ca.crt -cert certs/client.crt \\\n-url $(openssl x509 -in certs/client.crt -noout -ocsp_uri)\nExpected Results:\n• Certificates should include OCSP responder URLs (if OCSP is to be used)\n• OCSP queries should return valid responses when certificates are not revoked 4. Review certificate validation logs:\nCheck logs for OCSP-related messages:\n# Check for OCSP validation in logs\ngrep -i \"ocsp\" /var/lib/cockroach/logs/cockroach.log | tail -20\nExpected Results:\n• Logs should show OCSP checking activity when certificates are validated\n• No errors indicating OCSP server unavailability (if in strict mode)"
          }
        }
      },
      {
        "+@id": "V-09EC9BED",
        "title": "6 CockroachDB Settings",
        "description": "<GroupDescription></GroupDescription>",
        "Rule": {
          "+@id": "SV-09EC9BEDr01_rule",
          "+@weight": "10.0",
          "+@severity": "medium",
          "version": "6.6",
          "title": "Ensure SQL Session Idle Timeouts Are Configured for SQL Roles",
          "description": "<VulnDiscussion>CockroachDB allows administrators to define role-level SQL session idle timeouts using the idle_in_session_timeout session variable. This setting ensures that any session authenticated under a given SQL role is automatically terminated after a defined period of inactivity.\nConfiguring idle session timeouts for SQL roles provides a more granular control model than cluster-wide settings. It ensures that different classes of users — such as read-only roles, application service accounts, or privileged administrators — are governed by timeout values that reflect their risk level and organizational policy. This control is particularly important for organizations subject to compliance frameworks that mandate automatic termination of inactive sessions (e.g., SOC 2, NIST 800-53 AC-12, PCI DSS 8.2.8). Using per-role idle session timeouts helps enforce consistent and auditable policy compliance.\n\nRationale:\nWithout enforced role-level idle timeout settings, SQL sessions may remain active indefinitely, creating several risks:\nSecurity Risks\n• Abandoned Session Exposure: Idle but authenticated sessions can be abused if an attacker gains access to the client environment or connection path.\n• Privilege Abuse: Privileged roles without idle timeouts increase the likelihood of misuse or lateral movement.\n• Session Hijacking: Long-lived sessions expand the window of opportunity for an attacker to intercept and reuse session credentials.\nCompliance Risks Many standards require automatic termination of inactive sessions. For example:\n• SOC 2 (CC6.x): Requires controls on session management.\n• NIST 800-53 AC-12: Requires automatic session termination after inactivity.\n• PCI DSS 8.2.8: Requires session timeouts of 15 minutes or less unless justified.\nFailure to configure role-specific idle timeouts may lead to audit findings and non- compliance.\nOperational Benefits\n• Reduces buildup of idle or abandoned sessions\n• Provides predictable resource usage\n• Enforces security baselines consistently across user groups</VulnDiscussion>",
          "reference": {
            "dc:title": "CIS CockroachDB v25.x Benchmark",
            "dc:publisher": "CIS",
            "dc:type": "Benchmark",
            "dc:subject": "CIS CockroachDB v25.x Benchmark",
            "dc:identifier": "6.6"
          },
          "fixtext": {
            "+content": "1. Set idle timeout for a SQL role:\nSet a reasonable timeout for idle connections:\nALTER ROLE <role_name>\nSET idle_in_session_timeout = '<duration>';\n-- Example - set 30-minute idle timeout for a read-only role:\nALTER ROLE table_reader\nSET idle_in_session_timeout = '30m';\nRecommended Values\n• High-security environments: 10m-15m\n• Standard enterprise roles: 15m-30m\n• Administrative roles: 15m-30m (stricter values recommended)\n• Application roles: 30m-1h (with documented justification) Values should reflect:\n• Organizational security policies\n• Compliance requirements\n• Justified business or application needs 2. Verify timeout configuration after applying settings:\nSHOW DEFAULT SESSION VARIABLES FOR ROLE <role_name>;\nExpected Results:\n• idle_in_session_timeout reflects the newly configured value.\nNote on Session Variable Precedence: idle_in_session_timeout will use the newly configured default, but CockroachDB applies session variables according to a precedence hierarchy. Even if you set idle_in_session_timeout globally (for example with ALTER ROLE ALL SET idle_in_session_timeout = { value } or at a role level), a user can:\n• Override it in their current session with SET idle_in_session_timeout = { value }, and\n• Have it overridden by a more specific ALTER ROLE { role_name } IN DATABASE { db_name } SET idle_in_session_timeout = { value } setting, which takes precedence over broader role- or cluster-level defaults.\n3. Document configuration:\nMaintain documentation that includes:\n• Assigned timeout values per role\n• Rationale supporting each timeout (e.g., compliance mapping)\n• Exceptions or deviations\n• Review process and periodic re-validation",
            "+@fixref": "F-09EC9BEDr01_fix"
          },
          "fix": {
            "+@id": "F-09EC9BEDr01_fix"
          },
          "check": {
            "+@system": "C-09EC9BED_chk",
            "check-content-ref": {
              "+@href": "CIS_CockroachDB_v25_x_Benchmark.xml",
              "+@name": "M"
            },
            "check-content": "1. Identify the SQL role and view its idle session timeout configuration:\nIdentify the SQL role and view its idle session timeout configuration\nSHOW DEFAULT SESSION VARIABLES FOR ROLE <role_name>;\n(Note: This statement only returns session variables that have been explicitly changed from their defaults. If no variables have been changed for the role, it will return no results.) Expected Results:\n• idle_in_session_timeout explicitly appears in the results table.\n• The value is non-zero and aligned with organizational policy (e.g., 15m, 30m, 1h).\nFail if:\n• The statement returns no results (meaning idle_in_session_timeout is unset/default).\n• The timeout is explicitly set to 0 (disabled).\n• The timeout exceeds the organization's session inactivity limits without a documented business justification."
          }
        }
      }
    ]
  }
}
