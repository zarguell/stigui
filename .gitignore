.direnv
data/stigs/*
data/stigs.zip
!data/stigs/.gitkeep

# The full DISA library lives here, committed like upstream
# (see scripts/ to regenerate from a fresh DISA download).

# CIS Benchmark source PDFs. CIS's license restricts redistribution of
# the documents themselves, so only the converted STIG-format output in
# public/data/stigs/ is committed. Drop downloaded PDFs here.
data/cis/
scripts/cis/.venv/
scripts/cis/.cache/
__pycache__/

result
node_modules
out
.next
*.tsbuildinfo
.direnv
.DS_Store

# env files
.env*

# playwright
test-results/

# Generated at deploy time for LLM agents (not committed)
public/llms.txt
public/markdown/
